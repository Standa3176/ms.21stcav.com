---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: Intelligence + B2B
status: planning
stopped_at: Completed quick task 260611-rl4 — `cutover:auto-sync` nightly 23:00 London chain (scan → push → re-scan + parity-regression alarm). Closes the daily operator chore left by 260611-g4q + 260611-qcq: every night MS↔Woo drift creeps back in (post-business edits, Woo-side timing), and operators were manually firing `cutover:divergence-scan --live` then 3 separate `products:push-divergence-to-woo --field=…` runs. This task automates steps 2-5 only (hydrate stays separate on its 07:20 cron from 260611-qcq). New `app/Console/Commands/Cutover/AutoSyncDivergenceCommand.php` extending BaseCommand, constructor DI on `Auditor`, 5 options (`--field=stock_quantity,buy_price,category_id --max-products=500 --skip-scan --skip-rescan --dry-run`). **Drift-prevention contract:** private const `CHAINED_COMMANDS = ['cutover:divergence-scan', 'products:push-divergence-to-woo']` (grep-discoverable); ALL chained work flows through `Artisan::call` — NEVER duplicates scan/push logic. If a chained command's signature changes, the Artisan::call exception bubbles up loud — fix the auto-sync options dict, NOT the dependent command. **Four phases:** (1) SCAN — `cutover:divergence-scan --live` writes sync_diffs + dashboard_snapshots.sync_diffs_parity; (2) PUSH — `products:push-divergence-to-woo --field=… --no-confirm --limit=$max --dry-run?`; (3) RE-SCAN — fresh `cutover:divergence-scan --live` so we measure closed-loop parity_after (different correlation_id from phase 1); (4) REPORT — counters table + audit log + parity-regression detector. **Parity-regression alarm:** reads parity_percent_before vs parity_percent_after via `DashboardSnapshot::where('metric_key','sync_diffs_parity')->first()->metric_value_json['parity_percent']` (same row DivergenceScanner writes). If `parity_after < parity_before` → audit `cutover.auto_sync_parity_regression` with both values + delta, **exit code 1** (NOT 0 — explicit alarm so cron logs + Horizon visibly mark the run as failed). Does NOT roll back pushed changes — detection alarm only; operator investigates next morning (typical causes: comparator wrong, Woo cache stale, third-party writer). **Planner correction folded in:** phase 3 re-scan SKIPPED on phase 2 push failure (fail-fast — partial-push parity_after would mix unfixed + half-fixed diffs into a misleading number; brief said "phase 3 still runs" but cleaner contract is fail-fast since the audit log + exit 1 already alarm ops). Phase 1 fail also skips phases 2-3 (loud abort). `--dry-run` propagates: phase 1 scan stays LIVE (read-only against Woo); phase 2 push gets `--dry-run=true`; phase 3 re-scan SKIPPED; parity-regression check SKIPPED. `--skip-scan` operator escape hatch (reuses latest sync_diffs); `--skip-rescan` for ad-hoc manual runs. **Scheduled `cron('0 23 * * *')` `Europe/London`** in routes/console.php — 23:00 London uncontended (22:00 Fri-only audit-categories + 00:30 daily quotes:expire closest). `onOneServer()` + `withoutOverlapping(120)` defensive. Audit namespace: `cutover.auto_sync_completed` / `cutover.auto_sync_failed` (with `phase=scan|push|rescan`) / `cutover.auto_sync_parity_regression`. **DivergenceScanner / PushDivergenceToWooCommand / DivergenceScanCommand / HydrateProductStockFromOffersCommand UNTOUCHED.** 8 Pest cases A-H GREEN: A happy-path 80→95 / B --skip-scan / C --dry-run / D phase-1-scan-fail / E phase-2-push-fail-rescan-skipped / F --field=stock_quantity-only / G --max-products=10 → --limit=10 / H parity-regression 95→80 → audit + exit 1. **Test rig:** replaces the registered chained commands' Symfony instances with anonymous-subclass stubs whose `handle()` records invocations, returns canned exit codes, and optionally swaps the snapshot row mid-run — Rule 3 fix folded: `Kernel::getArtisan()` is protected in Laravel 12; pierced via Reflection. Touched-area regressions GREEN: DivergenceScanCommandTest 9/9 + PushDivergenceToWooCommandTest 10/10 + HydrateProductStockFromOffersCommandTest 10/10 + PushVisibilityToWooCommandTest 6/6 (35 cases / 220 assertions, 0 new fails). Full Pest baseline check vs 260611-qcq baseline (2,015 pass / 222 skipped / 3 risky): focused/touched-area equivalent confirmed +8 pass / 0 new fails; full-suite reconciliation deferred (suite OOMs at 512MB on Windows herd PHP — pre-existing infrastructure issue, NOT introduced by 260611-rl4). **Probe disagreements documented:** (a) plan path `tests/Feature/Console/Cutover/DivergenceScanCommandTest.php` — actual is `tests/Feature/Cutover/DivergenceScanCommandTest.php` (no `Console/` subfolder; used actual path); (b) plan regression target `tests/Feature/Console/ResyncProductsToWooCommandTest.php` does NOT exist in this repo (the `products:resync-to-woo` command itself IS referenced from BackfillCategoryFromWoo / BackfillMerchantFeed / StockDivergencePage / AutoCreateHealthPage but has no dedicated test file — skipped per scope-boundary rule). 4 atomic commits (fc3ee90 feat + 5bbd98b chore + 78d2485 test + Task 6 STATE commit). **POST-DEPLOY OPERATOR ACTION:** (1) deploy all 4 commits; (2) `php artisan cutover:auto-sync --dry-run` — prints phase 1 scan results + phase 2 dry-run plan; phase 3 SKIPPED; no parity-regression alarm; (3) `php artisan schedule:list | grep cutover:auto-sync` confirms `0 23 *` Europe/London entry (displays as `0 22 *` UTC during BST — correct); (4) wait for 23:00 London tonight; check `activity_log` for `cutover.auto_sync_completed` row, confirm `parity_after >= parity_before`; (5) pin a calendar reminder to investigate the next `cutover.auto_sync_parity_regression` audit row — that's the alarm this whole task exists to surface. **What this does NOT do:** no rollback on regression, no retry on transient Woo errors, no new config keys (re-uses cutover.parity_threshold_percent indirectly via the snapshot), no hydrate chaining. Earlier this session: 260611-qcq (hydrate-stock-from-offers), 260611-g4q (push-divergence-to-woo), 260611-f1y (hide internal-use products), 260610-qc4 (WooFieldComparator 13-field extension), 260609-rie (stockseparate dual-file fix), 260609-nku (stock-divergence audit). Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.

old_stopped_at: Completed quick task 260611-qcq — `products:hydrate-stock-from-offers` closes the supplier_offer_snapshots → products.stock_quantity gap proved on prod today by HA310-2EP (snapshot had Ingram stock=5,659; products.stock_quantity=0). New `app/Console/Commands/HydrateProductStockFromOffersCommand.php` extending BaseCommand with 5 options (`--skus= --limit=0 --only-stale=24 --dry-run --chunk=500`). Constructor DI imports `SupplierFreshnessResolver` (260608-g8x single-source-of-truth — never duplicate the freshness predicate). Per-SKU cheapest-fresh-in-stock pick mirrors `SupplierDbSyncCommand::buildBestOfferMap` exactly. In-stock branch writes stock_quantity / stock_status='instock' / buy_price / last_synced_at. OOS branch sets stock_quantity=0 / stock_status='outofstock' / last_synced_at and PRESERVES buy_price (last-known cost stays valid for margin math; wiping it would propagate £0.00 cost into the next undercut run). Per-product `DB::transaction` for partial-failure safety. Empty fresh-set sentinel `'__NO_FRESH_SUPPLIERS__'` (mirrors `SupplierOfferSnapshot::scopeFreshOnly`). NO Woo writes — pure MS-side hydration; the Pest suite binds a throwing `WooClient` stub as suite-wide guard so any future regression that gains a Woo call fails the whole suite. The push-divergence-to-woo (260611-g4q) + push-visibility-to-woo (260611-f1y) commands handle storefront sync separately. **Scheduled Mon-Fri 07:20 London** (`20 7 * * 1-5`) — planner correction: 07:15 was contended by `products:flag-missing-buy-price`; 07:20 wedges between flag-missing-buy-price (07:15) and `suggestions:auto-apply` (07:30) so a freshly-pending product doesn't get hydrated the same minute it was demoted. Default `--only-stale=24` keeps the daily cron incremental; first manual full-catalogue rehydrate is `--only-stale=0`. **Planner correction:** ProductOverride has NO `pin_stock` column (only SEO/content pins) — dropped the pinned_skipped counter, replaced Case J with an instock→outofstock downgrade-persistence test. 10 Pest cases A-J GREEN (single-fresh-instock / cheapest-pick / OOS-preserve-buy_price / stale-supplier-sentinel / --skus narrow / --dry-run / --only-stale=24 skip / mixed batch / partial-failure rollback / downgrade persistence). Rule 1 fix folded in: `--skus` parse dropped `strtolower()` — products.sku stores canonical Woo case (`HA310-2EP`); lowercasing silently failed to match. Focused suite GREEN (10/10) + 5 touched-area regression suites all GREEN (PushDivergence + PushVisibility + SupplierDbSyncStockSeparate + SupplierFreshnessResolver + AdCandidateScanner). Full Pest baseline check (vs 260611-g4q baseline 2,005/222/3): +10 pass / 0 new fails delta confirmed on focused/touched-area suites; full-suite reconciliation deferred (suite OOMs at 512MB on Windows herd PHP — pre-existing infrastructure issue, NOT introduced by 260611-qcq). 4 atomic commits (d29a795 feat + ab71ab5 chore + 873e1b5 test + Task 6 STATE commit). **POST-DEPLOY OPERATOR ACTION (DO IN ORDER):** (1) deploy commit; (2) `php artisan products:hydrate-stock-from-offers --only-stale=0 --dry-run` confirms full-catalogue plan size — expect a few hundred to a couple thousand `updated_in_stock` + `updated_out_of_stock` rows; (3) live `--only-stale=0` aligns the products table with supplier truth in one shot; (4) tinker spot-check `Product::where('sku','HA310-2EP')->first()->only(['stock_quantity','stock_status','buy_price','last_synced_at'])` — expect ~5,659 / instock / now-ish last_synced_at; (5) re-run `php artisan products:push-divergence-to-woo --dry-run` — stock_quantity diff count should DROP dramatically (the staleness 260611-g4q was propagating is now closed); (6) confirm Mon 07:20 London cron fires tomorrow morning between flag-missing-buy-price (07:15) and suggestions:auto-apply (07:30). Subsequent daily cron `--only-stale=24` is incremental. Earlier this session: 260611-g4q (push-divergence-to-woo), 260611-f1y (hide internal-use products), 260610-qc4 (WooFieldComparator 13-field extension), 260609-rie (stockseparate dual-file fix), 260609-nku (stock-divergence audit). Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.

old_stopped_at: Completed quick task 260611-f1y — hide 3 internal-use Woo products (Credit / Offer / Quote Payment (No Vat) — all empty-SKU, qty=4, £1050.89 sentinel rows) from storefront via Woo `catalog_visibility=hidden`. They remain orderable + accessible by direct URL (for the custom-quote workflow) but disappear from /shop, /search, category listings. New `products.is_internal_only` BOOLEAN column + index; seed migration flags the 3 known internals by `woo_product_id IN (167493, 167492, 165038)` (NOT SKU — all 3 have empty SKU; SKU lookup would false-positive on Dell 8K monitor woo=7317). New `app/Console/Commands/PushVisibilityToWooCommand.php` (`products:push-visibility-to-woo {--dry-run} {--skus=} {--limit=}`) extending BaseCommand. Logic: stream `Product::query()->where('is_internal_only', true)->whereNotNull('woo_product_id')` (or `--skus` override), pre-GET each from Woo (skip if catalog_visibility already `hidden` → `already_hidden++`), else PUT single-field `['catalog_visibility'=>'hidden']` (no split-PUT WAF workaround needed — that 260530-clv issue was specific to combined-PUT with regular_price). Counters: scanned/hidden/already_hidden/errors/no_woo_product_id. 200ms usleep between Woo calls (mirrors 260607-v5g batch pattern). New Filament toggle on Products edit form (NOT ToggleColumn on table — T-f1y-03 mitigation: avoids per-row-render Woo I/O risk). Admin + pricing_manager only. Synchronous artisan invocation on save → operator gets immediate feedback if PUT fails (surfaced as Filament notification). 6 Pest cases A-F: dry-run / live happy path / no woo_product_id / --skus override / Woo 5xx graceful / already-hidden skip. Pest delta vs 260610-qc4 baseline (1,989/222/3): **1,995/222/3 (+6 pass / 0 new fails)**. 5 atomic commits (31f1967 + 2babc42 + 5fb2c4a + 279ce0b + 574878b). Drift-prevention docblock notes WooFieldComparator does NOT track catalog_visibility — these 3 SKUs won't flood the 260610-qc4 divergence scan. **Toggle-OFF caveat documented**: flipping is_internal_only OFF does NOT auto-restore visible on Woo (operator-restore + queued follow-up for a --visibility= flag). **POST-DEPLOY operator action:** (1) deploy + migrate (seed auto-flags 3 internals); (2) `php artisan products:push-visibility-to-woo --dry-run` confirms 3-row plan; (3) live run, expect `hidden=3 errors=0`; (4) storefront verify — search "Credit"/"Offer"/"Quote Payment" returns 0 results, direct URLs still load + addable to cart (orders preserved). **Queued next quick task 2b:** MS→Woo divergence pusher consuming the 3,078 sync_diffs from 260610-qc4 (1,598 stock_quantity + 1,480 buy_price = 73% of the 4,235 cutover-parity gap). Earlier in this session: 260610-qc4 (WooFieldComparator extension), 260609-rie (stockseparate dual-file fix), 260609-nku (stock-divergence audit), 260608-g8x (supplier-stale), 260607-v5g (backfill-category-from-woo). Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.

old_stopped_at: Completed quick task 260610-qc4 — H-1 from Phase 7 gap review. Phase 7 cutover's "shadow-mode divergence scan" (2026-04-24 → 2026-05-30) gated the production flag-flip on a 7-field parity check (name/slug/short_description/long_description/meta_description/sell_price/image_url) when the live Product model has 14+ customer-impacting columns. Every quick task since 260607-v5g has been retroactively closing one parity gap the original WooFieldComparator couldn't see: categories (260607-v5g), EAN+brand (260607-cgd), stock (260609-nku audit → 260609-rie stockseparate fix), supplier-stale (260608-g8x). This task closes the loop: extends `app/Domain/Cutover/Services/WooFieldComparator.php` to compare 6 missing fields — `stock_quantity`, `stock_status`, `buy_price` (Algoritmika _alg_wc_cog_cost meta, planner correction from brief's _wc_cog_cost), `category_id`, `brand_id` (via _product_brand_id meta, pa_brand attribute fallback documented but deferred — Case G silent-skip contract), `ean` (3 plugin conventions: _ean / _global_unique_id / _alg_ean). Defensive split: canonical Woo fields (stock_*, category_id) emit a diff when local has a value but Woo absent (absence IS divergence); meta-only fields (buy_price, brand_id, ean) silent-skip when Woo lacks the meta key (most installs don't have the plugins — emitting diffs would flood sync_diffs with noise and bury real divergences). Two helper methods extractBrandId() + extractEan() walk Woo's meta_data and attributes arrays defensively; constants pin the meta keys for grep-discoverability. New architectural Pest test `DivergenceComparatorCoverageTest` (mirrors 260606-c4o EnvUsageTest + 260609-rie StockSeparateJoinTest pattern) scans the comparator source for 13 expected `'field' => 'X'` literals + meta-assertion proving the guard would bite if a field were silently removed. Failure message literally instructs the next dev: "Edit BOTH the comparator AND this test's expected list — see .planning/quick/260610-qc4-*/PLAN.md Drift-prevention contract". Also closed a pre-existing meta_description implementation gap (Plan 07-05 docblock declared the mapping but no comparison block existed — added _yoast_wpseo_metadesc Yoast meta comparison). 9 Pest cases A-G + DivergenceScanCommandTest fixture extension (stock_status='instock' added to IDENT-1 + PAR-1 mocks since the new canonical-absence contract correctly emitted a stock_status diff against the minimal fixtures). Full suite delta vs 260609-rie baseline (1,978/222/3): **1,989/222/3 (+11 pass / 0 new fails)**. 4 commits (8e52b78 + cccffe5 + 29066e4 + 401c197). **Probe context that triggered this:** today's Phase 7 gap review probe of prod surfaced 82.4% (3,231/3,922) live products NULL `brand_id`, 89% (3,490/3,922) empty `attributes_json`, 3 confirmed internal-use products on storefront (Credit / Offer / Quote Payment) + 1 false-positive with empty SKU (Dell 8K monitor data-quality issue). M-3 cutover:checklist = 11 PASS / 3 PENDING (3 PENDINGs are time-based monitoring + Bitrix carryover, NOT dev work). **POST-DEPLOY operator action:** run `php artisan cutover:divergence-scan --live` on prod — expect SyncDiffsParityWidget to flip green→red as it now reports truth on 13 fields not 7. Read sync_diffs by dominant field to size the next backfill task (likely brand_id then ean). **Queued next quick tasks:** (a) Internal-products `catalog_visibility=hidden` push (3 SKUs + new MS-side toggle column), (b) M-1 brand_id audit + name-prefix backfill, (c) M-2 attributes_json deferred — Phase 14 SEO-content pass candidate. Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.

old_stopped_at: Completed quick task 260609-rie — CRITICAL infrastructure fix discovered during 260609-nku audit validation. While verifying the stock-divergence audit's 256 flagged "phantom" SKUs against Ingram's Xvantage portal, found that supplier_db has a DUAL-FILE architecture our app was entirely blind to: `feeds` table has `is_stock_separate` flag; for Ingram (feed id=10, flag=1) the real per-SKU stock lives in `stockseparate` table, NOT `feeds_products.stock` (which holds placeholder zeros). Live prod proof: SKU CP15851 (HA310-2EP V7 headphone) — feeds_products.stock=0 vs stockseparate.stock=5659 (matches Ingram portal exactly). Scale: Ingram had only 0.73% in-stock (1,418/193,429) via feeds_products vs 57.6% (125,319/217,417) via stockseparate. We were silently zeroing **123,901 in-stock Ingram SKUs** from every downstream surface: AdCandidateScanner ad targets, products.stock_quantity, BackfillMerchantFeedCommand buy-price resolution, the stock-divergence audit itself. **The fix:** new `App\Domain\Sync\Concerns\JoinsStockSeparate` trait with two SQL helpers (stockColumnSelect + stockSeparateJoinClause). Applied at 3 real fix sites: SupplierDbSyncCommand lines 139 + 392 + ExplainSupplierCostCommand line 72. Planner correction: the 4 BackfillMerchantFeedCommand sites in the original brief read EAN/manufacturer NOT stock — out of scope, annotated with `// stock-separate-not-applicable:` markers (also applied to SupplierSkuRegistry, SupplierFeedSourceabilityChecker, SupplierAddCandidateScanner). The SQL pattern: LEFT JOIN feeds f ON f.id=fp.supplierid, LEFT JOIN stockseparate ss ON f.is_stock_separate=1 AND ss.supplier_id=fp.supplierid AND LOWER(TRIM(ss.sku))=LOWER(TRIM(fp.suppliersku)), SELECT COALESCE(CASE WHEN f.is_stock_separate=1 THEN ss.stock ELSE fp.stock END, 0) AS stock. Byte-identical behaviour for non-stockseparate suppliers (WestCoast etc); reveals real Ingram stock without changing anything else. **Regression guard:** new architectural Pest test `StockSeparateJoinTest` scans app/Domain/Sync/, app/Console/Commands/, app/Domain/Pricing/Services/ for any `FROM feeds_products` and requires either trait usage OR explicit annotation — mirrors EnvUsageTest pattern from 260606-c4o, prevents this exact bug from being silently reintroduced. Pest 13/13 GREEN (SupplierDbSync 5 + ExplainSupplierCost 5 + StockSeparateJoin arch 3). Full suite delta vs 260609-nku baseline (1,965/222/3): **1,978/222/3 (+13 pass / 0 new fails)**. 4 atomic commits (779472a + bfc9f75 + d8781bf + 7ebb774). **IMPORTANT operator action sequence on prod:** (1) deploy this commit, (2) re-run `php artisan supplier:db-sync` — will scan all 217k Ingram rows + 281k WestCoast etc, expect 30-60min wall time; this writes the REAL stock numbers into MS, (3) re-run `php artisan products:audit-stock-divergence` — the 256 "phantom" count from yesterday is expected to drop dramatically (most were false positives caused by THIS bug), (4) only AFTER both above, the bulk-resync action on /admin/stock-divergence becomes trustworthy. **260609-nku findings table is INVALID until step 3 completes.** Phase 7 cutover plan needs revision — the legacy Stock Updater plugin clearly already reads stockseparate (that's why Woo's numbers were closer to truth than ours); cutover validation must include this dual-file path. Earlier today: 260609-nku audit + 260608-g8x supplier-stale + 260607-v5g backfill-category-from-woo. Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.

old_stopped_at: Completed quick task 260609-nku — `products:audit-stock-divergence` to catch phantom stock surfaced live today by SKU 45-243-224 (Ergotron LX wall mount, woo_product_id=8502): MS=0 + Ingram+WestCoast both fresh & reporting 0 since 2026-05-28 BUT Woo holds stock_quantity=7 last modified 2026-05-31. Customers buy "in stock", land on `backorders: yes` limbo. Continuous revenue/UX leak with no visibility. Audit + Filament UI shipped to detect every Ergotron-like ghost weekly. New `stock_divergence_findings` table {sku, name, woo_product_id, ms_stock_quantity, woo_stock_quantity, phantom_units, woo_last_modified, ms_last_synced_at, status enum('woo_overcount'), run_id ULID, audited_at} with index on phantom_units DESC for the sortable table. New `app/Console/Commands/AuditStockDivergenceCommand.php` extending BaseCommand — flags `--limit=0 --chunk=50 --dry-run`. Logic: candidate query is publish + woo_product_id NOT NULL + stock_quantity=0 + NOT EXISTS (fresh supplier offer with stock>0 in last 7 days) — `freshSupplierIds()` injected from 260608-g8x SupplierFreshnessResolver (drift-prevention: one rule, two surfaces). Cursor-streamed, chunked, batch-queried Woo via existing `WooClient::get('products', ['include'=>..., 'orderby'=>'include'])` mirroring 260607-v5g. Per-row outcomes: divergent / matched / woo_not_found (woo product deleted) / error (5xx swallowed per chunk). Headline counter: total_phantom_units. TRUNCATE-replace inside DB::transaction (mirrors 260607-t6w semantics). 200ms usleep between Woo batches. **Scheduled Mon 09:15 London** (`15 9 * * 1`) — planner correction: 09:00 + 09:05 contended by woo:import-products / supplier:db-sync safety-net retries. New `/admin/stock-divergence` Filament page under Catalogue nav sort 17 (next to category-audit at 15). Admin + pricing_manager visibility. Columns: SKU/Name/MS qty/Woo qty/Phantom diff (default sort DESC) / Woo last modified / MS last synced / Audited at. Filter: phantom-units-min number input. Per-row: View on storefront + Resync to Woo (calls products:resync-to-woo). Bulk: Resync selected to Woo capped at 100 SKUs/click. New StockDivergenceWidget on home dashboard (16th now) + SnapshotAggregator::computeStockDivergence + NotificationCentreAggregator stockDivergence() entry. CategoryAuditPage + StockDivergencePage now cross-reference each other in their footer hints — future ecom mgrs find both without archaeology. Pest 12/12 GREEN (engine 6 cases A-F covering happy path / real stock / non-zero MS / matched / 404 / dry-run, page 5 cases covering role gate + filter + bulk, aggregator 1). Full suite delta vs 260608-g8x baseline: **1,965 / 222 / 3 (+12 pass / 0 new fails)**. 7 atomic commits (a1b6354 + 6bbbbce + f93010d + 65d65f2 + af7d8ad + 8dfd7c9 + 9ce2124). Next cron fire: Mon 2026-06-15 09:15 London. **Operator workflow:** hit /admin/stock-divergence after first run, sort by phantom-units DESC, multi-select top offenders, "Resync selected to Woo" — Woo's phantom numbers get overwritten by MS's confirmed-zero. Earlier this session: 260608-g8x supplier-stale-feed + 260607-v5g backfill-category-from-woo. Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.

last_updated: "2026-06-11T20:35:00.000Z"
last_activity: 2026-06-11
progress:
  total_phases: 11
  completed_phases: 8
  total_plans: 29
  completed_plans: 29
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-24 — v2.0 milestone kicked off)

**Core value:** One Laravel app owns product data, pricing rules, competitor intelligence and CRM sync — Woo is the display layer, nothing more.
**Current focus:** Phase 12 complete — ready for phase verification + Phase 13 (E3 WhatsApp Channel) planning

## Current Position

Milestone: v2.0 Intelligence + B2B
Phase: 13
Plan: Not started
Status: Phase ready for `gsd-tools phase complete 12`; Phase 13 ready for planning (run `/gsd-research-phase 13` first — research flag YES)
Last activity: **2026-05-31 — Day 1 LIVE confirmed.** Auto-reprice schedule SILENTLY MISSED its first 08:00 BST fire today because `routes/console.php` used `env()` instead of `config()` to read the toggle — env() returns the default in cached-config mode (deploy.sh runs config:cache). Recovered manually: `pricing:undercut-competitors --live` evaluated 5,292 products, changed 5,279 (3,345 undercut, 1,015 floored, 928 cost-plus margin), produced 3,909 successful Woo POSTs (avg 484ms, zero failures, zero queue:failed entries). Shipped permanent fix `d7d0e39` — moved 3 env-gated schedule toggles (PRICING_UNDERCUT_*, CUTOVER_DIVERGENCE_SCAN_*, AGENT_SEO_BATCH_*) to config keys; updated routes/console.php to use config(). Verified with schedule:test (1m 25s clean run via scheduler pipeline) + crontab confirmation + dashboard_snapshots showing `dashboard:refresh` firing every 5 min. **Tomorrow 2026-06-01 07:00 UTC the autonomous reprice will fire.** Also shipped today: Flatsome layout parity via attributes_json (`8a31731`, verified live on Huddly S1 #5633 → Woo #179682), EAN/global_unique_id end-to-end (`99d34bd → 812fb29 → c51f674`, with SKU fallback for manufacturers that leave the EAN column blank — Huddly EAN 7090043790993 live on Woo), CSV parse errors page improvements (`cf3fb1b` — per-competitor stat widget + XLSX export for round-tripping to Claude Code), competitor FTP stale threshold 30→4 days with inline "N days old" display (`8aafc3e`), CSV ingest header-row + name-only filter (`1372623` — cleared 1,065 historical Onedirect noise rows; future Onedirect re-ingests produce zero false errors), brand + tags pushed on auto-create (`b902803` initial pa_brand approach, then `73ac682` corrective refactor to native /products/brands taxonomy after pa_brand turned out to hold a single stale "Huddlecamhd" term — Huddly S1 verified on storefront with proper brand link to /product-brand/huddly/). Cutover checklist 12 PASS / 3 PENDING (monitoring-7-days through 2026-06-06; weekly-digest-landed next Monday; bitrix_quote_type_id_verified optional).

**Cutover Day 1 totals (2026-05-30 evening flip → 2026-05-31 morning verification):**
- Total Woo writes: 1,939 status reconciliations + 3 canary prices + 3,909 day-1 auto-reprice POSTs + a handful of Huddly attribute/EAN PUTs = ~5,855
- Failed jobs: 0
- AbortGuard trips: 0
- Storefront customer impact: zero (no error spike, no payment issue, no stuck inventory)

**Lessons captured (don't repeat):**

1. **`env()` outside config in cached-config mode** — env() returns the default. Always bind via `env()` inside a `config/*.php` file, then read via `config('foo.bar')` from app/route code. Three schedule toggles silently fell off-air post-cutover.

2. **Resolver "first non-empty source wins" is dangerous when multiple sources exist.** `TaxonomyResolver::allBrands()` preferred the `pa_brand` global attribute (1 stale term "Huddlecamhd") over the native `/products/brands` taxonomy (176 real terms). The whole site silently fuzzy-matched Sony/Barco/ViewSonic/Huddly all to "Huddlecamhd" for months without anyone noticing — because the resolver returned a value and the consumers trusted it. Fix (`73ac682`): inverted the priority — native first, pa_brand fallback. Anti-pattern to watch for elsewhere: any resolver/cache/fallback that exits on the FIRST non-empty result without sanity-checking volume or correctness. If two sources can both legitimately have data, you usually want to MERGE them (or pick by completeness, not by registration order).

Progress: [░░░░░░░░░░] 0% (0/8 v2 phases; 7/7 v1 phases shipped 2026-04-24)

**v2 build order:** 8 (C4) ∥ 9 (E1) → 10 (C1) → 11 (E2) → 12 (C3) → 13 (E3) → 14 (E4) → 15 (C2 — LATE, gates on v1 cutover + ≥4 weeks UTM data)

**v2 active research flags:**

- Phase 8: YES — Prism API surface, Langfuse self-hosted Docker, MCP PHP SDK, shield:safe-regenerate design
- Phase 10: YES — prompt design, deterministic temp=0 calibration, token budget across input contexts
- Phase 13: YES — WABA setup, Meta OBO BSP deprecation 2026, 24h window state machine

**Top 3 operator decisions blocking ANY v2 phase (per research/SUMMARY.md):**

1. Anthropic monthly budget ceiling — proposal: £200/month default
2. Self-hosted Langfuse vs Cloud — proposal: self-hosted Docker (EU residency)
3. Catalogue size sanity-check — assumption: ~5k SKUs (drives E4 FULLTEXT vs vector DB)

## Performance Metrics

**Velocity (v1.50.1 final):**

- Total plans completed: 62 (v1)
- Average duration: ~25 min/plan
- Total execution time: ~16 hours over 6 days

**By Phase (v1.50.1):**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 1 | 5 | ~3.7h | 44m |
| 2 | 5 | ~2.0h | 24m |
| 3 | 5 | - | - |
| 4 | 5 | ~3.5h | 42m |
| 5 | 6 | ~2.4h | 24m |
| 6 | 6 | ~1.7h | 17m |
| 7 | 6 | ~2.0h | 19m |
| 08 | 5 | - | - |
| 09 | 6 | - | - |
| 10 | 5 | - | - |
| 11.1 | 1 | - | - |
| 11.2 | 1 | - | - |
| 09.1 | 1 | - | - |
| 12 | 5 | - | - |

**v2 phases:** No data yet — v2 planning kicks off with Phase 8.
| Phase 08-c4-agent-framework P01 | 55min | 3 tasks | 20 files |
| Phase 08-c4-agent-framework P02 | 36min | 3 tasks | 9 files |
| Phase 08 P03 | 40 min | 3 tasks | 25 files |
| Phase 08-c4-agent-framework P04 | 19min | 3 tasks | 14 files |
| Phase 08 P05 | 17min | 4 tasks | 23 files |
| Phase 09 P09-02 | 15min | 3 tasks | 4 files |
| Phase 09-e1-trade-customer-pricing P03 | 44m | 3 tasks | 6 files |
| Phase 09 P09-04 | 7m | 3 tasks | 11 files |
| Phase 09 P05 | 37min | 2 tasks | 12 files |
| Phase 09-e1-trade-customer-pricing P06 | 25min | 3 tasks | 6 files |
| Phase 10-c1-pricing-agent P01 | 16min | 3 tasks | 11 files |
| Phase 10-c1-pricing-agent P02 | 16min | 2 tasks | 12 files |
| Phase 10-c1-pricing-agent P03 | 13min | 3 tasks | 7 files |
| Phase 10-c1-pricing-agent P04 | 17min | 3 tasks | 11 files |
| Phase 10-c1-pricing-agent P05 | 21min | 4 tasks | 8 files |
| Phase 11-e2-quote-request-bitrix-deal-flow P11-01 | 28min | 2 tasks | 23 files |
| Phase 11-e2-quote-request-bitrix-deal-flow P02 | 14min | 2 tasks | 12 files |
| Phase 11-e2-quote-request-bitrix-deal-flow P03 | 60min | 2 tasks | 20 files |
| Phase 11 P04 | 38min | 3 tasks | 20 files |
| Phase 11-e2-quote-request-bitrix-deal-flow PP05 | 30min | 2 tasks | 11 files |
| Phase 11.1 P01 | 35min | 3 tasks | 27 files |
| Phase 11.2 P01 | 25min | 3 tasks | 44 files |
| Phase 09.1 P01 | 50min | 3 tasks | 38 files |
| Phase 12-c3-seo-content-agent P12-01 | 23min | 3 tasks | 18 files |
| Phase 12-c3-seo-content-agent P02 | 38 | 3 tasks | 11 files |
| Phase 12-c3-seo-content-agent P03 | 14min | 3 tasks | 11 files |
| Phase 12-c3-seo-content-agent P04 | 17 | 3 tasks | 12 files |
| Phase 12-c3-seo-content-agent P05 | 75min | 5 tasks | 14 files |

## Quick Tasks Completed

| Date       | Task                                | Type        | Commit  |
| ---------- | ----------------------------------- | ----------- | ------- |
| 2026-05-03 | Embed Horizon in Filament chrome    | feat(admin) | 2b77a25 |
| 2026-05-03 | Horizon Cluster (8 sub-pages)       | feat(admin) | a2def38 |
| 2026-05-03 | Native Horizon pages (drop iframe + cluster) | feat(admin) | 90d989f |
| 2026-05-03 | Brand recolor (violet-800) + dashboard polish + nav restructure | feat(admin) | d806312 |
| 2026-05-03 | OpenAI/ChatGPT credential kind + FTP creds → Admin nav group | feat(admin) | ecb376a |
| 2026-05-03 | FTP-credential UTF-8 disable + Sun+Wed pull schedule + ftp ext | fix(competitor-ftp) | c562ea4 |
| 2026-05-03 | Inline-creatable competitor + auto local_filename + Competitors admin page | feat(competitor-feeds) | 538b0ee |
| 2026-05-03 | Post-create UX (toast + redirect + pull-status column) + afterStateUpdated fix | feat(competitor-feeds) | cce9107 |
| 2026-05-04 | FTP MDTM fallback (older daemons reject MDTM) | fix(competitor-ftp) | 0f3770e |
| 2026-05-04 | Capture orphan SKUs as queryable rows (4,264 rows materialised from live FTP feeds) | feat(competitor-prices) | cbca30b |
| 2026-05-04 | woo:import-products bulk Woo catalogue import + optional supplier enrichment | feat(sync) | 78d43ea |
| 2026-05-04 | SQLite WAL default + competitor:retry-quarantine command | feat(competitor-csv) | 5762ff7 |
| 2026-05-04 | Was£X Save Y% £Z sale-price extraction + fallback SKU column (242 rows recovered) | feat(competitor-csv) | 53fa2ac |
| 2026-05-04 | Default new competitors Active + cascade-impact delete modal | feat(competitor-feeds) | a9112e7 |
| 2026-05-04 | 8-group nav restructure + colored attention badges (29 files) | feat(admin) | def23dc |
| 2026-05-04 | Wire Domain/Integrations resource discovery (Admin → Integration Credentials) | fix(admin) | 5479e16 |
| 2026-05-04 | Defensive try/catch on all 12 nav badge queries | fix(admin) | 660b25a |
| 2026-05-04 | Clarify Integration Credential edit form (saved-value placeholder) | fix(admin) | b33f034 |
| 2026-05-04 | Live Woo import — 5,633 products + stock_quantity + Live/Pending labels (24.8% competitor match) | feat(products) | 60dee1f |
| 2026-05-04 | Extract buy_price from Woo _alg_wc_cog_cost meta (5,430 products / 96.4% coverage) | feat(sync) | be8e5a9 |
| 2026-05-04 | Supplier DB (Remote MySQL) credential kind — Phase 1 of remote supplier sync | feat(integrations) | b2101b7 |
| 2026-05-04 | Supplier DB host field accepts hostname or IP (kind-explicit URL detection) | fix(integrations) | 5cf41d0 |
| 2026-05-04 | Supplier DB test action + edit-form rendering fixes (mysqli_report + Group $record) | fix(integrations) | 69b9273 |
| 2026-05-04 | supplier:db-sync command + daily schedule (Phase 2; 3,939 of 5,629 SKUs matched) | feat(sync) | 7890d5c |
| 2026-05-04 | 90-day price + stock history snapshots (5,632 product + 8,017 supplier-offer rows; per-supplier breakdown + Filament UI + history:prune) | feat(products) | 864a14e |
| 2026-05-04 | Price History searchable picker (sku/name/desc) + reschedule supplier Mon-Fri 07:00 + competitor Sun+Wed 02:00 | feat(price-history) | 4afd884 |
| 2026-05-24 | AI product-page creation (extends Phase 6): products:generate-drafts (Claude content, meetingstore 6-section structure) + products:source-images (Icecat+Serper web search+Claude-vision validation, woo_product_id nullable, gallery_image_urls) + products:assign-taxonomy (fuzzy brand+Claude category) + /preview/product/{id} page + Icecat/ImageSearch credential kinds + Langfuse OTel silenced | feat(autocreate) | 9b7f094 |
| 2026-06-03 | Suggestions UX: competitor-count SelectFilter (replaces broken-tabs attempt — Filament 3 `where(Closure)` + custom tableQuery hit Builder.php:355 null model); filters layout fix (inline above table, not behind funnel toggle); Product Exceptions allowlist resource (operator pause/unpause SKUs vs. auto-pending flag) + searchable SKU picker matching Product Overrides; --skus and --no-confirm flags on products:draft-from-suggestions for queue-driven bulk runs | feat(suggestions) | 0a715f3, e959ae0, 780ddb9 |
| 2026-06-04 | Bulk auto-create from Suggestions: BulkAction "Auto-create selected (full pipeline)" dispatches RunAutoCreatePipelineJob → products:draft-from-suggestions chain (generate-drafts → mark-applied → assign-taxonomy → source-images → auto-publish) on the operator-selected new_product_opportunity rows; ShouldBeUnique by SKU-set hash; admin-only via ->authorize; toggles for source-images on/off and auto-publish to Woo vs. review-inbox path | feat(suggestions) | 433dc0d |
| 2026-06-04 | products:retry-missing-images command: finds local Products with empty gallery_image_urls and re-runs products:source-images on them; --brand=Manhattan/--days=N/--status= scope; --resync chains products:resync-to-woo; default scopes to auto_create_status IS NOT NULL (legacy WC-migration products excluded — they have Woo images already, re-sourcing wastes Claude spend); dry-run shows per-status + per-age-bucket breakdown + cost estimate | feat(products) | 77b65b7, cd3848c, ca92caa, 271ad37 |
| 2026-06-04 | "On supplier DB" filter on /admin/suggestions: SelectFilter Yes/No backed by a local `supplier_sku_cache` table (single sku PRIMARY KEY column, ~900k rows materialised from supplier_db.feeds_products). EXISTS subquery scales (whereIn at 900k blows MySQL's max_allowed_packet). SupplierSkuRegistry::refresh() bulk-inserts in chunks of 1k via insertOrIgnore. Cache warmed Mon-Fri 07:05 London (5 min after supplier:db-sync) by supplier:refresh-sku-cache. Live split: 5,396 sourceable / 8,867 competitor-only on 14,263 new_product_opportunity rows (38% actionable) | feat(suggestions) | df17629, cd84e75 |
| 2026-05-25 | Simplify admin nav 8 groups → 6: quarantine ~18 set-once screens into one collapsible "Settings" group, merge operational logs into "Sync & CRM", remove WooCommerce/CRM&Bitrix/Admin/orphan FTP&CSV groups, nest 5 competitor-feed screens under a "Competitor Feeds" parent (Horizon-style navigationParentItem). Pure nav metadata, ~22 files; 81 admin routes verified. Quick task [260525-gtv](./quick/260525-gtv-simplify-admin-nav-8-groups-to-6-rename-/) | refactor(admin) | 46086a4 |
| 2026-05-25 | Pricing Operations dashboard `/admin/pricing-operations` (4 panels: recent price changes, new SKUs, competitor at/below 6% floor, competitor below cost) via CompetitorPositionScanner reusing floor-report ex-VAT margin math. + PHP 8.4 fix: removed `public string $queue` trait collision in AgentAlertNotification/RunAgentJob (dormant on prod 8.3) + added pest-plugin-livewire dev dep. 7 new tests green. Quick task [260525-pnk](./quick/260525-pnk-pricing-operations-dashboard-php-8-4-tes/) | feat(pricing) | 24f03aa |
| 2026-05-25 | Core-loop #3b publish-on-Woo (PublishProductJob creates auto-drafts on Woo, shadow-safe; fixes rest_no_route slash bug) + daily 08:00 undercut pricing schedule (opt-in) + cutover runbook (`docs/ops/cutover-runbook.md`, phases A–D). All gated. Quick task [260525-szn](./quick/260525-szn-core-loop-3b-publish-on-woo-pricing-sche/) | feat(autocreate) | 6ce34f6 |
| 2026-05-25 | Supplier cost accuracy: buy_price = cheapest IN-STOCK supplier (was latest-updated; 1,772 SKUs re-costed live) + `supplier:explain-cost {sku}` diagnostic + `--flag-obsolete` (no-supplier products → pending; 160 demoted live). Pricing Ops dashboard UX: clickable tiles → filterable modal + CSV/XLS export, active-nav "you are here" highlight, + "Products to add" tile (parts on ≥4 suppliers not on MS — default tuned from data: ≥2=68k, ≥3=20k, ≥4=2,135; via `supplier:scan-add-candidates`, weekly Sun 05:00). 10 tests green. Quick task [260525-szo](./quick/260525-szo-supplier-cost-accuracy-cheapest-in-stock/) | fix(sync)+feat(pricing) | 1d95f73 |
| 2026-05-27 | Pricing Ops **bucket popup** now shows the supplier name under "Our cost (ex)" and the competitor name under "Lowest comp (ex)" (muted sub-lines, null-safe). Extended `CompetitorPositionScanner` to resolve both, batched: competitor via argmin `competitor_id` → raw `DB::select` on `competitors` (deptrac: Pricing↛Competitor, so NO model import); supplier via cheapest-current `SupplierOfferSnapshot` (matches how `buy_price` is chosen). Popup blade ONLY — inline panels + CSV untouched (scoped out by user). PHPStan L6 + deptrac (touched file) + Pint clean; scanner Pest 7 green. **Deployed + operator-verified 2026-05-27.** Quick task [260527-c0m](./quick/260527-c0m-show-supplier-name-under-our-cost-and-co/) | feat(pricing) | d74a604 |
| 2026-05-27 | **"Sourcing gaps"** view — parts a competitor lists that NO supplier carries and we don't sell (obsolete, or need a supplier). `SourcingGapScanner` (Pricing) aggregates windowed `competitor_prices` per sku (distinct competitors + lowest ex-VAT + winning competitor), drops parts we sell, then delegates a remote-feed sourceability check to a new `SupplierFeedSourceabilityChecker` (Sync owns the Integrations cred — Pricing↛Integrations); gaps = no-supplier parts. `pricing:scan-sourcing-gaps` (cached, weekly Sun 05:30) + dashboard tile + filterable modal + CSV/XLS. Also: cost counters now scope to `status='publish'` so no-supplier (pending/obsolete) products don't inflate below_cost/at_floor/winnable/matched. "Products to add" (≥4 suppliers) is disjoint by construction. Inline at-floor/below-cost panels also got supplier/competitor name parity (5ce4df6). 26 Pricing tests green; deptrac zero new violations. **Deployed + operator-verified 2026-05-27 ("ALL WORKING").** Quick task [260527-sgp](./quick/260527-sgp-sourcing-gaps-view-no-supplier-competit/) | feat(pricing) | 23c8632 |
| 2026-06-06 | **env() guardrail.** Pest 3 architectural test `tests/Architecture/EnvUsageTest.php` forbids `env()` outside `config/`, `bootstrap/`, `tests/` — prevents 2026-05-31 cached-config silent-toggle recurrence (see d7d0e39). Two assertions: Pest `arch()` DSL covers App\ namespace; `RecursiveDirectoryIterator` + regex covers routes/+database/ (which arch() cannot see). Third meta-assertion proves the regex would catch a real `env(` call so future "simplifications" cannot silently weaken it. Manual probe verified the test bites: env() injected to routes/console.php → file-scan FAILS; env() injected to App\Console\Commands\BaseCommand → arch() FAILS. Three preparatory commits cleared pre-existing app/Domain/Cutover violations so the guardrail lands green: 9feb585 (WOO_DB_* in WooDbSnapshotter), 5d3888d (WOO_WRITE_ENABLED in RollbackDrill), 1e275bc (drill/disable-live gate env vars in DrillRollbackCommand + DisableLegacyPluginsCommand + Cutover tests). Total env() count in app/+routes/+database/ now 0 (was 7 across 4 files). Quick task [260606-c4o](./quick/260606-c4o-add-architectural-guardrail-env-must-onl/) | test(architecture)+3×fix(cutover) | 2336e30 + 1e275bc + 5d3888d + 9feb585 |
| 2026-06-06 | **Suggestions inbox triage.** `suggestions:prune-orphans` artisan command auto-rejects stale competitor-only orphans (pending + new_product_opportunity + sku NOT EXISTS in supplier_sku_cache + supporting_competitors<2 + created_at older than `--days=30`). Scheduled Mon 06:00 London (cron `0 5 * * 1` UTC) so it runs *before* `supplier:db-sync` (07:00) and `supplier:refresh-sku-cache` (07:05) — today's-fresh suggestions are never pruned. Driver-aware JSON expression (MySQL `JSON_UNQUOTE`/`UNSIGNED` vs SQLite `json_extract`/`INTEGER`) so the Pest test runs on in-memory SQLite. Filament navigation badge rewrite: was raw pending count (~14k noise); now counts "high-confidence sourceable" only (pending + new_product_opportunity + EXISTS in supplier_sku_cache + supporting_competitors≥3) — actionable signal. New `getNavigationBadgeTooltip()` exposes the three-tier breakdown (`N high-confidence • M sourceable • K raw`), cached 60s. Empirical dev DB: `--dry-run` reported 17,164 candidates. **Live prod cleanup 2026-06-06 evening:** ran `--days=7` once → 8,515 orphans auto-rejected, pending NPO inbox dropped 14,263 → ~5,748 actionable. Default later changed to `--days=14` (commit b934473) so future Mon 06:00 cron drains the >14-day tail. Focused Pest 5/5 (18 assertions, 5.59s); full suite delta vs 260606-c4o baseline = zero new failures (1,803 passed / 223 pre-existing failures / 3 skipped). Quick task [260606-gnu](./quick/260606-gnu-inbox-triage-suggestions-prune-orphans-h/) | feat(suggestions) | ed893b8 + d6c8a4d + 69223cf + d853a27 + b934473 |
| 2026-06-11 | **MS→Woo divergence pusher — close 3,078 of 4,235 cutover-parity gaps (stock_quantity + buy_price + category_id).** Follow-up to 260611-f1y's "queued next quick task 2b" — consumes the sync_diffs emitted by 260610-qc4's extended 13-field divergence scan and pushes MS-side truth back to Woo for the 3 deterministically-pushable fields. Field distribution from first prod scan: stock_quantity=1,598 + buy_price=1,480 + category_id=555 = **3,078 (73%)** of the 4,235 gap (remaining 1,157 image_url+exists+meta_description need separate triage). Neither OverridePopulator (writes pin_* flags only, never to Woo) nor ResyncProductsToWooCommand (pushes regular_price + brand tag + attributes, not stock/buy_price/category) closed this gap. **New `app/Console/Commands/PushDivergenceToWooCommand.php`** extending BaseCommand. Signature: `products:push-divergence-to-woo {--field=stock_quantity,buy_price,category_id} {--limit=0} {--chunk=50} {--dry-run} {--correlation-id=} {--no-confirm}`. **Drift-prevention contract:** private const `SUPPORTED_FIELDS = ['stock_quantity','buy_price','category_id']` (grep-discoverable; bails on unknown --field= values); imports `WooFieldComparator::BUY_PRICE_META_KEY` — NEVER duplicates literal `'_alg_wc_cog_cost'`. **6 outcomes per product:** pushed / errors (5xx — rows stay pending for next-run retry) / no_woo_product_id (rows marked applied+annotated to stop recycling) / woo_not_found (404 — rows marked status='woo_not_found', NOT applied, since data wasn't pushed) / already_applied (informational) / partial_success (reserved forward-compat, single-PUT design keeps at 0). **Non-negotiable contract:** pre-GET on every product so the buy_price meta_data MERGE preserves unrelated Yoast SEO / EAN / brand_id meta entries — blind PUT with `meta_data=[{key:_alg_wc_cog_cost,...}]` WIPES every other meta entry. Algoritmika WC Cost of Goods plugin stores buy_price in meta_data[] alongside those, not in a top-level field. **Single PUT per product carries all 3 fields** (no split-PUT WAF workaround — that 260530-clv issue was specific to regular_price triggering the COG recompute hook; our payload has stock + meta_data + categories, no regular_price). `manage_stock=true` ALWAYS co-emitted with `stock_quantity` (without it Woo treats the value as a manual override, not source-of-truth). Categories prefer `Product.category_ids` multi-cat JSON; falls back to single `category_id`. **sync_diff lifecycle:** pending → applied (success, applied_at=now) | woo_not_found (Woo 404) | pending (5xx retryable). Live-confirmation prompt (`About to PUT N products to live Woo. Continue?`) — `--no-confirm` for cron-style invocation. 200ms usleep between successful PUTs matches 260607-v5g + 260609-nku + 260611-f1y pacing. Operator-triggered, NOT scheduled. DivergenceScanner / OverridePopulator / ResyncProductsToWooCommand / PushVisibilityToWooCommand UNTOUCHED. 10 Pest cases A-J: stock-only / buy_price meta_data MERGE preserves Yoast+brand / category multi-cat / 3-field combo SINGLE PUT / --field= partial-scope re-run safety / 5xx errors stay pending / 404 woo_not_found / --dry-run no-op / pre-applied row filter / --correlation-id override. 2 atomic commits (7f07659 + 6d712d5). **Operator action post-deploy:** (1) `php artisan cutover:divergence-scan --live` to refresh sync_diffs; (2) `php artisan products:push-divergence-to-woo --dry-run` to size the plan; (3) live run with confirmation; (4) re-scan to verify parity climbs from 45% toward 70%+. Quick task [260611-g4q](./quick/260611-g4q-ms-woo-divergence-pusher-closes-3-078-st/) | feat(cutover) | 7f07659 + 6d712d5 |
| 2026-06-11 | **Hide internal-use products from storefront — Credit / Offer / Quote Payment.** Three legacy products used by the team for custom-quote workflows were appearing on /shop, /search and category pages — confusing for customers. All three: empty SKU, sentinel qty=4 + £1050.89 (data quality issue surfaced as side-finding, deferred). Must REMAIN in Woo (orderable + addable to quotes via direct URL) but be invisible on public discovery surfaces. Standard Woo B2B pattern: `catalog_visibility=hidden`. New `products.is_internal_only` BOOLEAN column + index; seed migration uses `woo_product_id IN (167493, 167492, 165038)` as the lookup key (SKU is empty for all 3 — SKU lookup would false-positive on Dell 8K monitor woo=7317). New `app/Console/Commands/PushVisibilityToWooCommand.php` (`products:push-visibility-to-woo {--dry-run} {--skus=} {--limit=}`) — streams candidates, pre-GETs each Woo product to skip already-hidden no-ops, PUT single-field `['catalog_visibility'=>'hidden']` (no split-PUT WAF workaround needed — 260530-clv issue was specific to combined-PUT with regular_price triggering Cost-of-Goods recompute). Counters: scanned/hidden/already_hidden/errors/no_woo_product_id. 200ms usleep mirrors 260607-v5g pattern. New Filament toggle on Products **edit form** (NOT ToggleColumn on table — planner T-f1y-03 risk mitigation: avoids per-row-render Woo I/O). Admin + pricing_manager visibility only. Synchronous artisan invocation on save → immediate Filament notification on PUT failure. 6 Pest cases A-F: dry-run / live happy path / no woo_product_id / --skus override / Woo 5xx graceful / already-hidden idempotency. **Drift-prevention docblock** notes WooFieldComparator does NOT track `catalog_visibility` — these 3 SKUs won't flood the 260610-qc4 divergence scan as new diffs. **Toggle-OFF caveat documented**: flipping is_internal_only OFF does NOT auto-restore `visible` on Woo (operator-restore + queued follow-up for a `--visibility=` flag covering all 4 Woo states). Pest delta vs 260610-qc4 baseline (1,989/222/3): **1,995/222/3 (+6 pass / 0 new fails)**. 5 atomic commits (31f1967 + 2babc42 + 5fb2c4a + 279ce0b + 574878b). **Operator action post-deploy:** `php artisan migrate` auto-flags 3 internals, then `php artisan products:push-visibility-to-woo` (expect hidden=3 errors=0). Storefront verify: search "Credit"/"Offer"/"Quote Payment" returns 0 results; direct URLs still load + cart-addable (orders preserved). Quick task [260611-f1y](./quick/260611-f1y-hide-internal-use-products-credit-offer-/) | feat(products)+feat(filament)+test | 31f1967 + 2babc42 + 5fb2c4a + 279ce0b + 574878b |
| 2026-06-11 | **`cutover:auto-sync` — nightly 23:00 London chain (scan → push → re-scan + parity-regression alarm).** Automates the manual operator chore left after 260611-g4q + 260611-qcq landed: every night, MS↔Woo drift creeps back in (post-business edits, Woo-side timing). New `app/Console/Commands/Cutover/AutoSyncDivergenceCommand.php` extending BaseCommand, constructor DI on `Auditor`, signature `cutover:auto-sync {--field=stock_quantity,buy_price,category_id} {--max-products=500} {--skip-scan} {--skip-rescan} {--dry-run}`. **Drift-prevention contract:** private const `CHAINED_COMMANDS = ['cutover:divergence-scan', 'products:push-divergence-to-woo']` (grep-discoverable); ALL chained work flows through `Artisan::call` — NEVER duplicates scan/push logic. If a chained command's signature changes, the Artisan::call exception bubbles up loud — fix the auto-sync options dict, NOT the dependent command. **Four phases:** (1) SCAN — `cutover:divergence-scan --live` writes sync_diffs + dashboard_snapshots.sync_diffs_parity; (2) PUSH — `products:push-divergence-to-woo --field=… --no-confirm --limit=$max --dry-run?`; (3) RE-SCAN — fresh `cutover:divergence-scan --live` so we measure the closed-loop parity_after; (4) REPORT — counters table + audit log + parity-regression detector. **Parity-regression alarm:** reads parity_percent_before vs parity_percent_after via `DashboardSnapshot::where('metric_key','sync_diffs_parity')->first()->metric_value_json['parity_percent']` (same row DivergenceScanner writes). If `parity_after < parity_before` → audit `cutover.auto_sync_parity_regression` with both values + delta, exit code 1 (NOT 0 — explicit alarm so cron logs + Horizon visibly mark the run as failed). **Does NOT roll back pushed changes** — detection alarm only; operator investigates next morning (typical causes: comparator wrong, Woo cache stale, third-party writer). **Planner correction folded in:** Phase 3 re-scan SKIPPED on phase 2 push failure (fail-fast — partial-push parity_after would mix unfixed + half-fixed diffs into a misleading number; brief said "phase 3 still runs" but cleaner contract is fail-fast since the audit log + exit 1 already alarm ops). **Phase 1 fail also skips phases 2-3 (loud abort)**. `--dry-run` propagates: phase 1 scan stays LIVE (read-only against Woo); phase 2 push gets `--dry-run=true`; phase 3 re-scan SKIPPED (no actual writes to remeasure); parity-regression check SKIPPED. `--skip-scan` operator escape hatch (reuses latest sync_diffs correlation_id); `--skip-rescan` for ad-hoc manual runs. **Scheduled `cron('0 23 * * *')` `Europe/London`** in routes/console.php — 23:00 London uncontended (22:00 Fri-only audit-categories + 00:30 daily quotes:expire are closest neighbours; no 23:xx slot). `onOneServer()` + `withoutOverlapping(120)` defensive (cap'd by --max-products=500). Audit-log namespace: `cutover.auto_sync_completed` / `cutover.auto_sync_failed` (with `phase=scan|push|rescan`) / `cutover.auto_sync_parity_regression`. **Hydrate is NOT chained** — stays on its own Mon-Fri 07:20 cron (260611-qcq). **DivergenceScanner / PushDivergenceToWooCommand / DivergenceScanCommand / HydrateProductStockFromOffersCommand UNTOUCHED.** 8 Pest cases A-H GREEN (happy-path scan/push/rescan + parity 80→95 / --skip-scan / --dry-run / phase-1-scan-fail / phase-2-push-fail-rescan-skipped / --field=stock_quantity-only / --max-products=10 → --limit=10 / parity-regression 95→80 → audit-log + exit 1). **Test rig:** replaces the registered `cutover:divergence-scan` + `products:push-divergence-to-woo` Symfony commands with anonymous-subclass stubs whose `handle()` records invocations, returns canned exit codes, and optionally swaps the snapshot row mid-run — Rule 3 fix folded in: `Kernel::getArtisan()` is protected, pierced via Reflection (Laravel 12 no longer exposes `Artisan::fake()`). Touched-area regressions all GREEN (DivergenceScanCommandTest 9/9 + PushDivergenceToWooCommandTest 10/10 + HydrateProductStockFromOffersCommandTest 10/10 + PushVisibilityToWooCommandTest 6/6 = 35 cases / 220 assertions). **Probe disagreements documented:** (a) plan referenced `tests/Feature/Console/Cutover/DivergenceScanCommandTest.php` — actual path is `tests/Feature/Cutover/DivergenceScanCommandTest.php` (no `Console/` subfolder); (b) `tests/Feature/Console/ResyncProductsToWooCommandTest.php` referenced in the plan's regression set does not exist in this repo (command itself exists, called from BackfillCategoryFromWoo / BackfillMerchantFeed / StockDivergencePage / AutoCreateHealthPage — no dedicated test file; out of scope). 4 atomic commits (fc3ee90 feat + 5bbd98b chore + 78d2485 test + Task 6 STATE commit). **POST-DEPLOY OPERATOR ACTION:** (1) deploy all 4 commits; (2) `php artisan cutover:auto-sync --dry-run` — should print phase 1 scan results + phase 2 dry-run plan; phase 3 SKIPPED; no parity-regression alarm; (3) `php artisan schedule:list | grep cutover:auto-sync` confirms `0 23 *` Europe/London entry; (4) wait for 23:00 London tonight — check `activity_log` for `cutover.auto_sync_completed` row, confirm `parity_after >= parity_before`; (5) pin a calendar reminder to investigate the next `cutover.auto_sync_parity_regression` audit row (that's the alarm this whole task exists to surface). **What this does NOT do:** no rollback on regression, no retry on transient Woo errors (push command handles per-product errors with its own counter; whole-run non-zero = structural failure for operator triage), no new config keys (re-uses cutover.parity_threshold_percent indirectly via the snapshot), no hydrate chaining. Quick task [260611-rl4](./quick/260611-rl4-auto-chain-cutover-divergence-scan-push-/) | feat(cutover) | fc3ee90 + 5bbd98b + 78d2485 |
| 2026-06-11 | **`products:hydrate-stock-from-offers` — close the supplier_offer_snapshots → products.stock_quantity gap.** Direct fix for the missing step in the daily data flow proved on prod 2026-06-11 with SKU HA310-2EP: supplier_offer_snapshots row had Ingram `stock=5,659` (correct, post-260609-rie stockseparate fix), but `products.stock_quantity=0` — the snapshot data never made it into the local products table. The downstream 260611-g4q `push-divergence-to-woo` was therefore propagating phantom OOS to the storefront for a SKU where the cheapest fresh supplier actually had 5.6k units of drop-ship stock. **New `app/Console/Commands/HydrateProductStockFromOffersCommand.php`** extending BaseCommand. Signature: `products:hydrate-stock-from-offers {--skus=} {--limit=0} {--only-stale=24} {--dry-run} {--chunk=500}`. **Best-offer pick rule:** mirrors `SupplierDbSyncCommand::buildBestOfferMap` — cheapest FRESH (per 260608-g8x `SupplierFreshnessResolver`) supplier_offer_snapshots row with `stock > 0` per SKU. ONE freshness rule, TWO consumers — never duplicate the predicate; IMPORT the resolver via constructor DI. **Empty fresh-set sentinel:** `'__NO_FRESH_SUPPLIERS__'` mirrors `SupplierOfferSnapshot::scopeFreshOnly`. **Per-product `DB::transaction`** for partial-failure safety — a column-level failure rolls back THAT product, batch continues for siblings, errors counter increments. **In-stock branch:** `stock_quantity=offer.stock, stock_status='instock', buy_price=offer.price, last_synced_at=now()`. **OOS branch:** `stock_quantity=0, stock_status='outofstock', last_synced_at=now()` — `buy_price` PRESERVED so margin math + sell-price calculator keep the last-known cost (wiping it would propagate £0.00 cost into the next undercut run). **NO Woo writes** — pure MS-side hydration; the suite-wide Pest guard binds a throwing `WooClient` stub so any future regression that gains a Woo call fails the whole suite. The push-divergence-to-woo (260611-g4q) + push-visibility-to-woo (260611-f1y) commands handle storefront sync separately. **Scheduled Mon-Fri 07:20 London** (`20 7 * * 1-5`) — planner correction: 07:15 was already taken by `products:flag-missing-buy-price`; 07:20 wedges between flag-missing-buy-price (07:15) and `suggestions:auto-apply` (07:30) so a freshly-pending product doesn't get its stock_status hydrated the same minute it was demoted to pending. Default `--only-stale=24` keeps the daily cron incremental; first manual full-catalogue rehydrate is `--only-stale=0`. **Planner correction:** ProductOverride has NO `pin_stock` column (only SEO/content pins exist); dropped the `pinned_skipped` counter, replaced Case J with an instock→outofstock downgrade-persistence test. 10 Pest cases A-J GREEN (single fresh in-stock / cheapest-pick / OOS+preserve buy_price / stale-supplier sentinel / --skus narrow with case-preservation / --dry-run no-op / --only-stale=24 skip / mixed batch / partial-failure rollback / downgrade persistence). **Rule 1 fix folded in:** the original `--skus` parse used `strtolower(trim($s))` which silently failed to match `products.sku` rows stored in canonical Woo case (e.g. `HA310-2EP`). Fix: drop `strtolower()` from the product-filter; keep it on the snapshot lookup (matchKey form is lowercase). Pest delta vs 260611-g4q baseline (2,005/222/3): **+10 pass / 0 new fails** target met for the focused suite + 5 touched-area regressions (PushDivergence + PushVisibility + SupplierDbSyncStockSeparate + SupplierFreshnessResolver + AdCandidateScanner all GREEN). 3 atomic commits (d29a795 + ab71ab5 + 873e1b5). **POST-DEPLOY OPERATOR ACTION:** (1) `php artisan products:hydrate-stock-from-offers --only-stale=0 --dry-run` confirms full-catalogue plan size (expect a few hundred to a couple thousand `updated_in_stock` + `updated_out_of_stock`); (2) live `--only-stale=0` aligns products with supplier truth in one shot; (3) tinker spot-check `Product::where('sku','HA310-2EP')->first()->only(['stock_quantity','stock_status','buy_price','last_synced_at'])` — expect ~5,659 / instock; (4) re-run `php artisan products:push-divergence-to-woo --dry-run` — stock_quantity diff count should DROP dramatically. Subsequent daily cron `--only-stale=24` is incremental. Quick task [260611-qcq](./quick/260611-qcq-products-hydrate-stock-from-offers-popul/) | feat(products) | d29a795 |
| 2026-06-11 | **Phase 7 cutover-parity completeness (H-1) — WooFieldComparator extended from 7 to 13 fields.** Phase 7 retrospective surfaced the root pattern behind every post-cutover quick task since 260607-v5g: the divergence scanner that gated the 2026-05-30 cutover compared only 7 of 14+ critical product fields, so each missed field (categories, EAN, brand, stock, supplier-stale, stockseparate) had to be discovered customer-first and remediated retroactively. This task closes the loop. **Extended `app/Domain/Cutover/Services/WooFieldComparator::diff()`** with 6 new field comparisons: (1) `stock_quantity` canonical int diff vs `$woo['stock_quantity']`; (2) `stock_status` canonical string diff vs `$woo['stock_status']`; (3) `buy_price` float (0.005 tolerance) vs `meta_data._alg_wc_cog_cost` (Algoritmika WC Cost of Goods — planner correction from brief's `_wc_cog_cost`); (4) `category_id` int diff vs `$woo['categories'][0]['id']`; (5) `brand_id` int diff via new helper `extractBrandId()` walking meta_data._product_brand_id (pa_brand attribute fallback documented but deferred — Case G silent-skip contract); (6) `ean` string diff via new helper `extractEan()` walking 3 plugin keys: `_ean` / `_global_unique_id` / `_alg_ean`. **Defensive contract:** canonical Woo fields (stock_*, category_id) emit a diff when local has a value but Woo absent — absence IS divergence (the 260609-nku Ergotron case). Meta-only fields (buy_price, brand_id via meta, ean) silent-skip when Woo lacks the meta key — most installs don't have the plugins; emitting diffs for absence would flood sync_diffs with thousands of false positives and bury real divergences. Constants pin the meta keys for grep-discoverability. **Closed pre-existing meta_description gap** during execution: Plan 07-05 docblock declared the mapping but no comparison block existed — added `_yoast_wpseo_metadesc` Yoast meta comparison. **Regression guard:** new architectural Pest test `tests/Architecture/DivergenceComparatorCoverageTest.php` scans the comparator source for 13 expected `'field' => 'X'` literals plus meta-assertion proving the guard would bite if a field were silently removed. Failure message literally instructs the next dev: "Edit BOTH the comparator AND this test's expected list — see .planning/quick/260610-qc4-*/PLAN.md Drift-prevention contract". Mirrors 260606-c4o EnvUsageTest + 260609-rie StockSeparateJoinTest pattern exactly. 9 new Pest cases A-G in `WooFieldComparatorTest` (file didn't exist — planner correction from brief's "extend existing" assumption); DivergenceScanCommandTest fixtures extended (IDENT-1 + PAR-1 mocks gained `stock_status='instock'` so the new canonical-absence contract doesn't false-flag against minimal fixtures). Pest delta vs 260609-rie baseline (1,978/222/3): **1,989/222/3 (+11 pass / 0 new fails)**. 4 commits (8e52b78 + cccffe5 + 29066e4 + 401c197). **Probe context that drove this:** today's Phase 7 review probed prod live state — M-3 cutover:checklist = 11 PASS / 3 PENDING (3 PENDINGs all operator/time, not dev work); M-1 = 82.4% (3,231/3,922) live products NULL `brand_id`; M-2 = 89% (3,490/3,922) empty `attributes_json`; internal-products = 3 confirmed (Credit / Offer / Quote Payment) + 1 false-positive Dell 8K monitor with missing-SKU data-quality issue. **Operator action post-deploy:** `php artisan cutover:divergence-scan --live` will likely flip SyncDiffsParityWidget green→red — THAT'S CORRECT, it now reports truth on 13 fields not 7. Read sync_diffs by dominant field to size the next backfill task (likely brand_id then ean). **Queued next:** internal-products `catalog_visibility=hidden` push + M-1 brand_id name-prefix backfill; M-2 attributes_json deferred to Phase 14 SEO-content pass. Quick task [260610-qc4](./quick/260610-qc4-extend-woofieldcomparator-to-cover-6-mis/) | feat(cutover)+test(architecture)+test(cutover) | 8e52b78 + cccffe5 + 29066e4 + 401c197 |
| 2026-06-09 | **CRITICAL: supplier_db dual-file architecture — join `stockseparate` when `feeds.is_stock_separate=1`.** Discovered while validating yesterday's 260609-nku stock-divergence audit against Ingram's Xvantage portal screenshot for SKU HA310-2EP. Our entire stock pipeline was reading `feeds_products.stock` even for Ingram, where that column holds placeholder zeros — the real live stock lives in a separate `stockseparate` table (Ingram feed metadata explicitly carries `is_stock_separate=1`, `path_to_stock_file='AVAIL/TOTUKHRL.ZIP'`). **Live prod proof:** CP15851 (V7 HA310-2EP headphone) — feeds_products.stock=0 vs stockseparate.stock=**5,659** (matches Ingram portal exactly). **Scale of the blindspot:** Ingram had only 1,418/193,429 with stock>0 via feeds_products (0.73%) vs **125,319/217,417 via stockseparate (57.6%)** — **123,901 in-stock Ingram SKUs silently hidden** from AdCandidateScanner, products.stock_quantity, BackfillMerchantFeedCommand buy-price, and the stock-divergence audit itself. **The fix:** new `App\Domain\Sync\Concerns\JoinsStockSeparate` trait with two SQL helpers (`stockColumnSelect()` + `stockSeparateJoinClause()`) — single source of truth for the JOIN pattern. Applied at 3 fix sites: `SupplierDbSyncCommand` lines 139 + 392, `ExplainSupplierCostCommand` line 72. **Planner correction:** brief listed 4 BackfillMerchantFeedCommand sites — they read EAN/manufacturer NOT stock, OUT of scope, annotated with `// stock-separate-not-applicable:` markers (plus SupplierSkuRegistry + SupplierFeedSourceabilityChecker + SupplierAddCandidateScanner). SQL pattern: `LEFT JOIN feeds f ON f.id=fp.supplierid, LEFT JOIN stockseparate ss ON f.is_stock_separate=1 AND ss.supplier_id=fp.supplierid AND LOWER(TRIM(ss.sku))=LOWER(TRIM(fp.suppliersku)); SELECT COALESCE(CASE WHEN f.is_stock_separate=1 THEN ss.stock ELSE fp.stock END, 0) AS stock`. **BYTE-IDENTICAL** behaviour for non-stockseparate suppliers (WestCoast etc) — the CASE gate ensures pure additive change. **Regression guard:** new `tests/Architecture/StockSeparateJoinTest` scans app/Domain/Sync/ + app/Console/Commands/ + app/Domain/Pricing/Services/ for any `FROM feeds_products` and requires either trait usage OR an explicit `// stock-separate-not-applicable:` annotation. Mirrors the EnvUsageTest pattern from 260606-c4o — prevents this exact bug from being silently reintroduced ever again. Pest 13/13 GREEN (SupplierDbSync 5 + ExplainSupplierCost 5 + arch 3 covering: trait usage detected, escape-hatch annotation honoured, regex would catch a real violation). Full suite delta vs 260609-nku baseline (1,965/222/3): **1,978/222/3 (+13 pass / 0 new fails)**. 4 atomic commits (779472a + bfc9f75 + d8781bf + 7ebb774). **OPERATOR ACTION SEQUENCE on prod (do these IN ORDER, none skippable):** (1) deploy commit; (2) re-run `php artisan supplier:db-sync` — scans all 217k Ingram rows + 281k WestCoast etc, 30-60min wall time; this writes the REAL stock numbers into MS; (3) re-run `php artisan products:audit-stock-divergence` — the 256 "phantom" count from yesterday is expected to drop dramatically (most were false positives caused by THIS bug); (4) ONLY AFTER step 3 completes, the bulk-resync action on /admin/stock-divergence becomes trustworthy. **260609-nku audit findings are INVALID until step 3 completes.** Side-effect insight: the legacy Stock Updater plugin clearly already reads stockseparate (which is why Woo's stock numbers were closer to truth than MS's all along); Phase 7 cutover validation must include this dual-file path. Quick task [260609-rie](./quick/260609-rie-join-supplier-db-stockseparate-when-feed/) | feat(sync)+fix(sync)+test(architecture)+test(sync) | 779472a + bfc9f75 + d8781bf + 7ebb774 |
| 2026-06-09 | **`products:audit-stock-divergence` — catch phantom Woo stock the suppliers don't back.** Surfaced live today by SKU 45-243-224 (Ergotron 34" LX wall mount, woo_product_id=8502): MS=0, Ingram + WestCoast BOTH fresh & reporting `stock=0` every day since 2026-05-28, yet Woo holds `stock_quantity=7` last modified 2026-05-31. With `backorders: yes` the storefront still takes orders, and customers wait in queue for inventory that's gone. Continuous revenue + UX leak we had no visibility into. Audit pattern mirrors 260607-t6w category-audit + 260607-v5g Woo-REST-batch idiom. **New `stock_divergence_findings` table** {sku, name, woo_product_id, ms_stock_quantity, woo_stock_quantity, phantom_units, woo_last_modified, ms_last_synced_at, status enum('woo_overcount'), run_id ULID, audited_at} with index on phantom_units DESC for the sortable Filament table. **New `app/Console/Commands/AuditStockDivergenceCommand.php`** — flags `--limit=0 --chunk=50 --dry-run`. Candidate query: publish + woo_product_id NOT NULL + stock_quantity=0 + NOT EXISTS (fresh supplier offer with stock>0 in last 7 days) — `freshSupplierIds()` injected from 260608-g8x `SupplierFreshnessResolver` (drift-prevention: ONE rule, two surfaces, NO duplicated DATEDIFF/julianday SQL). Cursor-streamed, chunked, batch-queried Woo via `WooClient::get('products', ['include'=>..., 'orderby'=>'include'])`. Per-row outcomes: divergent / matched / woo_not_found (Woo product deleted) / error (5xx swallowed per chunk). Headline counter: **total_phantom_units**. TRUNCATE-replace inside `DB::transaction()`. 200ms `usleep` between Woo batches. **Scheduled Mon 09:15 London** (`15 9 * * 1`) — planner correction: 09:00 and 09:05 were contended by `woo:import-products` + `supplier:db-sync` safety-net retries. **New `/admin/stock-divergence` Filament page** under Catalogue nav sort 17 (next to category-audit at 15). Admin + pricing_manager visibility. Columns: SKU/Name/MS qty/Woo qty/Phantom diff (default sort DESC) / Woo last modified / MS last synced / Audited at. Filter: phantom-units-min number input. Per-row actions: View on storefront + Resync to Woo (calls products:resync-to-woo). Bulk: Resync selected to Woo capped at 100 SKUs/click (Woo REST + queue throttle). **New `StockDivergenceWidget`** on home dashboard (16th now — after SupplierFreshnessWidget 15th from 260608-g8x); `SnapshotAggregator::computeStockDivergence()` wired into computeAll(). `NotificationCentreAggregator::stockDivergence()` entry surfaces count + phantom-units in the notification centre. **CategoryAuditPage + StockDivergencePage cross-reference** each other in their footer hints so future ecom mgrs find both without archaeology. Pest 12/12 GREEN (engine 6 cases A-F: divergent / real stock / non-zero MS / matched / Woo 404 / dry-run; page 5 cases: role gate + filter wiring + bulk-action button; aggregator 1). Full suite delta vs 260608-g8x baseline (1,953/222/3): **1,965 / 222 / 3 (+12 pass / 0 new fails)**. 7 atomic commits (a1b6354 + 6bbbbce + f93010d + 65d65f2 + af7d8ad + 8dfd7c9 + 9ce2124). **Operator workflow:** hit `/admin/stock-divergence` after first run, sort by phantom-units DESC, multi-select top offenders, "Resync selected to Woo" — Woo's phantom numbers get overwritten by MS's confirmed-zero. Next cron fire: Mon 2026-06-15 09:15 London. Quick task [260609-nku](./quick/260609-nku-products-audit-stock-divergence-surface-/) | feat(products)+feat(stock-divergence)+feat(dashboard)+test | a1b6354 + 6bbbbce + f93010d + 65d65f2 + af7d8ad + 8dfd7c9 + 9ce2124 |
| 2026-06-08 | **Per-supplier stale-feed handling — stock-decay across 3 scanners.** User-observed problem: some suppliers (Nuvias named) go silent for weeks but their cached prices/stock still count in `cheapest buy_price` / ad-candidate margin / Merchant Center backfill. Mirrors the existing competitor staleness shape but for supplier_offer_snapshots. **New tables:** `suppliers` (created from scratch — no FK; supplier_id is VARCHAR(16) text key matching supplier_offer_snapshots — planner correction; brief assumed it existed) with `stale_after_days` per-row override; `supplier_freshness_snapshots` (TRUNCATE-and-replace per run, ULID run_id, mirrors 260607-t6w semantics). **New `config/supplier.php`**: `default_stale_after_days=7`, `amber_warning_ratio=0.7`. **New `App\Domain\Sync\Services\SupplierFreshnessResolver`** — single source of truth: `freshSupplierIds()` / `staleSupplierIds()` / `amberSupplierIds()` / `classify($id)` / `thresholdDaysFor($id)`. Per-request cached via private array; driver-aware SQL (MySQL `DATEDIFF` vs SQLite `julianday()`). `'__NO_FRESH_SUPPLIERS__'` string sentinel prevents empty whereIn from collapsing to "all rows" against the VARCHAR(16) supplier_id column. **New `suppliers:check-stale {--dry-run}`** artisan — Mon-Fri **07:45 London** cron (45 min post supplier:db-sync at 07:00; 06:30/07:30 slots were contended). Snapshots {supplier_id, supplier_name, latest_recorded_at, days_since, threshold_days, status}. **New `SupplierOfferSnapshot::freshOnly()`** scope. **Three independent scanner wirings**, each reversible via constructor flag (all default TRUE): (1) `AdCandidateScanner` — stock-side predicate excludes stale-supplier rows from `in_stock` test (price-side untouched per stock-decay policy: stale supplier prices stay VISIBLE in lowest-comp comparison, only stock decays to 0); (2) `CompetitorPositionScanner.cheapestSupplier` — falls back to next-cheapest FRESH supplier when picking "Our cost (ex)" / best_supplier label; (3) `SupplierDbSyncCommand.buildBestOfferMap` — the ACTUAL buy-price resolver wired here, NOT BackfillMerchantFeedCommand as briefed (planner correction). Drift-prevention contract: flip the policy or move the threshold = ONE-FILE edit per the 260607-pys principle. **New `SupplierFreshnessWidget`** on home dashboard (15th widget now); `SnapshotAggregator::computeSupplierFreshness()` wired into computeAll(). **NotificationCentreAggregator** extended with `stale_suppliers` bucket mirroring `stale_feeds`. Pest 18/18 GREEN (resolver 6 + command 3 + adcand-stale 2 + comppos-stale 4 + supdbsync-stale 2 + scope 1). Full suite delta vs 260607-v5g baseline: **1,953 / 222 / 3 (+18 pass / 0 new fails)**. Dry-run smoke (3-supplier fixture): NUVIAS=fresh / INGRAM=amber / MIDWICH=stale (days_since=22 vs threshold=7). **Operator action:** run dry-run on prod first to see classification, then DB-set Nuvias' `stale_after_days=21` if their irregular cadence justifies the longer grace. Quick task [260608-g8x](./quick/260608-g8x-per-supplier-stale-feed-handling-stock-d/) | feat(suppliers)+feat(sync)+feat(ads)+feat(pricing)+feat(dashboard)+test | a9115bd + ebd1a00 + 91e2088 + 2c84923 + d4e4caf + 5e42fb6 + bf8f85a + 23a2a37 |
| 2026-06-07 | **`products:backfill-category-from-woo` — close the MS↔Woo category mirror gap.** Direct fix for the 3,244 NULL-`category_id` rows surfaced by yesterday's 260607-t6w audit. Root cause: legacy `woo:import-products` (260504-d7v) ingested SKU/name/price/stock but skipped Woo's `categories` array. Confirmed via tinker probe AND user's Woo admin screenshot showing SKU `960-001352` has 5 categories live (Google Meet Rooms / Medium Rooms / MS Teams Rooms / Small Rooms / Zoom Rooms) while MS local was NULL. New `app/Console/Commands/BackfillCategoryFromWooCommand.php` (310 lines) — flag-symmetric with 260607-cgd `BackfillMerchantFeedCommand` (`--skus=` / `--limit=0` / `--chunk=50` / `--dry-run` / `--resync`). Reuses existing `WooClient::get()` (line 94, no new client surface). Cursor-streams the candidate query so the 3,244-row scan is memory-safe. Calls Woo REST `GET /wp-json/wc/v3/products?include=ID1,..&per_page=50&orderby=include`, extracts `categories` per row, writes primary=`categories[0].id` to `category_id` + full id list JSON to `category_ids` via `DB::table()->update()` (bypasses Eloquent `array` cast for batch speed — inline comment captures why). Outcomes: `updated` / `no_woo_categories` / `no_woo_product_id` / `woo_not_found` / `error` (chunk-scoped on 5xx). Registered in `AppServiceProvider::$commands`; NO scheduled cron — operator-triggered, since Woo→MS category drift is rare once initial backfill lands. `--resync` chain present for symmetry with 260607-cgd, with docblock caveat it's mostly a no-op (categories flow Woo→MS, not MS→Woo). CategoryAuditPage blade (260607-t6w) now carries a sky-blue footer banner signposting the command so future ecom mgrs find it without archaeology. 6/6 Pest cases A-F GREEN (5xx error, 404 woo_not_found, empty categories, dry-run, no woo_product_id filter, happy path) — 26 assertions, 5.67s. Focused regression filters (EnvUsage + AutoCreatedPredicate + AutoCreateHealthPage + CategoryAuditPage + AdCandidatesPage) all GREEN. Full suite delta vs 260607-t6w baseline: **1,935 / 222 / 3 (+6 pass / 0 new fails — exactly the 6 new cases)**. **Operator action pending:** run `php artisan products:backfill-category-from-woo` on prod — expect `missing_category_id` audit count to drop from 3,244 toward near-zero on next Fri 22:00 audit; `suspicious_brand_category_mismatch` (105) stays unchanged (different problem class). Quick task [260607-v5g](./quick/260607-v5g-backfill-local-category-id-from-woo-rest/) | feat(products)+feat(category-audit) | 7cd9366 + 20514e1 + 2ce631a + c047d11 |
| 2026-06-07 | **Weekly category audit for ecom mgr — `/admin/category-audit` + Fri 22:00 cron.** Rule-based structural check over live products (no Claude, free, runs in seconds). New `products:audit-categories` artisan command classifies each product into one of 4 most-severe-wins buckets: (1) `missing_category_id` (NULL — danger), (2) `orphaned_category_id` (set but not in Woo product_cat taxonomy — warning), (3) `uncategorized_named` (resolves to "Uncategorized" sink — warning), (4) `suspicious_brand_category_mismatch` (brand belongs to BRAND_NATURAL_HOMES map but current category root doesn't match — info). BRAND_NATURAL_HOMES class constant (Samsung→Displays, Sony→Displays/Projection/VC, Yealink→VC/Audio, Panasonic→Projection/Displays, Epson→Projection, Logitech/Poly/Barco/Neat→VC, etc) — operator-tunable via PR. TRUNCATE-and-replace snapshot semantics (one snapshot at a time, not history). New `category_audit_findings` migration + Eloquent model + ULID run_id. Scheduled `cron('0 22 * * 5')` → Fri 22:00 London (`0 21 * * 5` UTC during BST) — ecom manager has fresh report each Monday morning to review across the week. New `/admin/category-audit` Filament page under **Catalogue** nav group (sort 15, between Products + Quotes). Admin + pricing_manager visibility. Columns: SKU/Name/Brand/Category/Issue type badge/Audited at; filters: Issue type SelectFilter + Brand multi-select with alpha-sort + type-to-filter (mirrors fe962b9 pattern). Per-row + bulk actions: "Run Claude category review" calls `products:assign-taxonomy --skus=N` (~1p Claude, banner before spend). Footer summary banner: count by issue type + last-run timestamp + "Next run: Fri 22:00 London" hint. New CategoryAuditWidget dashboard tile (14th now) reading from SnapshotAggregator::computeCategoryAuditHealth(). Pest 23/23 GREEN (4-bucket classifier + page role gate + filter wiring + model casts). Full suite delta vs 260607-pys baseline: 1,929 / 222 / 3 (+18 pass / 0 new fails). Quick task [260607-t6w](./quick/260607-t6w-weekly-category-audit-rule-based-check-f/) | feat(category-audit)+feat(dashboard) | c86bf26 + 8c87193 + 9b5e34c + 5a466f9 + 143efb4 + c7aed51 + 5289cec |
| 2026-06-07 | **`/admin/ad-candidates` — brand-filterable Google Ads campaign planner.** Persistent Filament UI replacing the manual tinker workflow we've been running today. Operator picks one or more brands (multi-select), tunes Min margin £ (default 199), Stock-required toggle, Beat-required toggle — page renders matching products with SKU/Name/Brand/Margin£/Sell£/LowestComp£/Beat%/Stock/Supplier columns, default sort margin DESC, footer summary banner shows count + total margin potential + avg-per-SKU. Per-row actions: View on storefront (opens Woo product URL in new tab), Edit in admin. Bulk actions: Copy SKU CSV (streams download for Google Ads bulk upload), Send to Google Ads (placeholder notification pointing at v3/Phase 15 — direct API push deferred). **Architectural foundation: `App\Domain\Pricing\Services\AdCandidateScanner`** — the SINGLE source of truth for the "golden ad target" predicate (live + margin + stock + beat). `BackfillMerchantFeedCommand` refactored to delegate to it; grep guard confirms `competitor_prices` SQL no longer in the command. Page uses £199 default; backfill uses £300 — same scanner, different threshold via constructor arg. **New `SnapshotAggregator::computeAdCandidatesHealth()`** returns `{count, total_margin_pence, average_margin_pence}` cached via dashboard_snapshots — `AdCandidatesReadyWidget` reads from snapshot for sub-ms render. Widget added to home dashboard (13 widgets total now). Admin + pricing_manager visibility on both page + widget. Focused Pest 37/37 GREEN (scanner predicate matrix + backfill A-I + page role gate + filter wiring + aggregator). Full suite delta vs 260607-hxa baseline: 1,911 / 222 / 3 (+15 pass / +3 pre-existing fails — IntegrationHealthWidgetTest hardcoded count broken by 260607-hxa's EanSearch enum addition; logged in deferred-items.md as follow-up). Drift-prevention enforced: BackfillMerchantFeedCommand can't fork the predicate without arch test failure. Quick task [260607-pys](./quick/260607-pys-ad-candidates-filament-page-brand-filter/) | feat(ads)+feat(dashboard) | c3b6b7f + 3259545 + 58a61a2 + 0d5273f + 8293549 + 659e38e |
| 2026-06-07 | **EAN-search.org swap — replace Icecat as the default EAN provider.** This morning's 260607-g25 Icecat fallback shipped fine but its prod run returned **0/116 matches at 23p Icecat spend** — premium B2B AV SKUs (Sony FW-Bravia, Panasonic PT-, PTZOptics, Roland VR-, BirdDog, Vivitek) aren't in Icecat's index. EAN-search.org specifically supports MPN→GTIN reverse lookup with stronger industrial coverage at 1/7th the cost (€0.003/query vs 0.2p, free tier 100/day). **New `App\Domain\Integrations\Enums\IntegrationCredentialKind::EanSearch`** with `requiredFields()=['token']` + label/color/TestIntegrationAction wired — admin form auto-discovers, IntegrationHealthWidget surfaces it without edits. **New `App\Domain\ProductAutoCreate\Services\EanSearchClient`** — `lookupGtinByMpn(?brand, ?mpn): ?string` hits `https://api.ean-search.org/api?op=barcode-search&format=json`, parses JSON array response (picks first row whose `name` contains the brand, falls back to first row when brand null), validates EAN via 8-14 digit check, returns null on 4xx/5xx/empty. Token redacted in IntegrationLogger calls. 8/8 unit tests cover brand-match / multi-row / empty / garbage / HTTP-error / null-brand paths. **BackfillMerchantFeedCommand: provider-switchable** via new `config('integrations.ean_fallback_provider')` defaulting to `'ean_search'`; flip to `'icecat'` via `EAN_FALLBACK_PROVIDER=icecat` for forensic A/B comparison. Both clients injected via DI; the config picks the active one at call-time. **Outcome bucket rename:** `recovered_from_icecat → recovered_from_ean_lookup`, `icecat_no_match → ean_lookup_no_match`, `icecat_invalid_ean → ean_lookup_invalid_ean`, `icecat_budget_exhausted → ean_lookup_budget_exhausted` (provider-agnostic). CLI flag `--icecat-fallback` preserved for operator muscle memory (now provider-agnostic internally). Cost guard `--max-icecat-spend-pence=200` caps whichever provider is active. **IcecatClient retained UNTOUCHED** — still used by SourceProductImagesCommand for product images. **Post-deploy operator action required:** sign up at https://www.ean-search.org, paste API token via `/admin/integration-credentials` → New → kind=EAN-search → save. Without that the EAN-search calls return null gracefully (no crashes). Pest +15 GREEN; full suite delta vs 260607-g25 baseline: 1,896 / 219 / 3 (+15 pass / 0 new fails). Quick task [260607-hxa](./quick/260607-hxa-swap-icecat-for-ean-search-org-as-the-de/) | feat(integrations)+feat(products) | ddb2311 + 96cded7 + f656102 + 52285f5 + c9704e7 |
| 2026-06-07 | **Icecat EAN fallback — unblock premium B2B SKUs.** Today's run of `products:backfill-merchant-feed --field=ean --skus=<golden 137 ad targets>` returned **0/116 updated** because supplier_db.feeds_products.ean is "N/A" or empty for premium AV brands (distributors like Midwich/Tech Data/Nuvias don't publish GTIN for channel-only Panasonic PT- projectors, Sony FW-Bravia displays, Roland VR-, PTZOptics, BirdDog cameras etc). Icecat indexes EANs against MPNs for the same brands. **New `IcecatClient::lookupGtinByMpn(?brand, ?mpn): ?string`** — calls Icecat's brand+MPN query (re-uses the existing fetchImageUrls auth path via extracted `requestRawData()` helper), extracts GTIN from 3 schema variants (GeneralInfo.GTIN / GeneralInfo.GTINs[].Value / EANCodes[]). 8/8 unit tests cover all schema shapes + null/error paths. **BackfillMerchantFeedCommand extended:** when supplier_db EAN fails `normaliseEan()` validation, falls through to Icecat lookup (brand resolved via Product.brand_id → TaxonomyResolver allBrands name; null brand still triggers MPN-only query as weak fallback). Cost guard: `--max-icecat-spend-pence=200` (£2 default cap, integer math in tenths-of-pence). Backwards-compatible: `--no-icecat-fallback` produces byte-identical 4-row output table from 260607-cgd. Default path produces new 7-row table with `recovered_from_icecat` / `icecat_no_match` / `icecat_invalid_ean` / `icecat_budget_exhausted` buckets. Operator banner before live runs surfaces "Icecat fallback enabled (~0.2p/query, budget cap £X)". 6 new Pest cases (A: supplier-N/A + Icecat-valid recovers; B: Icecat-null no_match; C: Icecat-garbage rejected; D: supplier-first wins, Icecat not called; E: --no-icecat-fallback opts out; F: budget cap fires). Pest +14 GREEN (8 IcecatClient unit + 6 BackfillCommand cases); full suite delta vs 260607-cgd baseline: 1,881 / 219 / 3 (+14 pass / 0 new fails). Quick task [260607-g25](./quick/260607-g25-icecat-ean-fallback-for-backfill-merchan/) | feat(products) | cbb5b60 + fd9430a + be4b811 + d0f880f |
| 2026-06-07 | **`products:backfill-merchant-feed` — fix 89% Google Merchant Center disapproval.** Diagnostic established today: 3,922 live products, 3,778 missing EAN, 3,493 in TRIPLE FAIL state (no EAN AND no brand) auto-disapproved by Google. Supplier_db join confirmed 94% EAN recoverability. New `products:backfill-merchant-feed --field=ean,brand,category --dry-run --resync` artisan command: (1) **EAN path** — joins `feeds_products` by `LOWER(TRIM(suppliersku|mpn))`, validates via new `App\Console\Concerns\NormalisesEan` trait (8-14 digits, rejects "N/A"/"0"/"99999…"/empty — caught `32sm5j → N/A` in today's sample). Free, deterministic. (2) **Brand path** — joins `manufacturer` column, fuzzy-matches via `TaxonomyResolver::resolveBrand()` (≥0.85 threshold, native /products/brands taxonomy per 73ac682). Free, deterministic, no Claude. (3) **Category path** — chains `products:assign-taxonomy --skus=<batch-of-50>` via `Artisan::call()`. Claude ~£35 for full 3,490-row backfill. Interactive `confirm()` banner before live spend; `--no-confirm` skips for cron use. (4) **`--resync` chain** — pushes only successfully-updated SKUs to `products:resync-to-woo` so the Google Listings & Ads plugin re-feeds Merchant Center (not all attempts — set-intersection of before-missing ∩ after-assigned). **Drift-prevention refactor:** extracted private `normaliseEan()` from `GenerateProductDraftsCommand` to `App\Console\Concerns\NormalisesEan` trait; both commands now `use NormalisesEan`. Grep guards forbid duplicate private methods. Focused Pest 27/27 GREEN (15 trait + 7 command + 5 architecture); full suite +22 pass / 0 new fails vs 260607-9c6 baseline (1,867 / 219 / 3 vs 1,845 / 219 / 3). Quick task [260607-cgd](./quick/260607-cgd-products-backfill-merchant-feed-ean-bran/) | feat(products) | da9bbdb + e86fe74 + 1c35c30 + 0f6af53 + 7da38a7 |
| 2026-06-07 | **Security HIGH remediation — H-1 webhook receipts GDPR prune + H-2 mysqldump cred leak.** Ships both HIGH findings from yesterday's 1c76e63 security review as atomic fixes. **H-1:** new `webhooks:prune-receipts` artisan command with `--order-days=30` / `--customer-days=7` / `--other-days=90` per-topic retention (customer is tightest per GDPR Art. 5(1)(e)) + Mon-Sun 03:25 London cron in routes/console.php, slotted between integration-events:prune (03:10) and sync-diffs:prune (03:30). Pest 6/6 covers per-topic deletion + dry-run + flag override. **H-2:** `WooDbSnapshotter` refactored to write a chmod-0600 `.cnf` temp file (`[client]\nuser=…\npassword=…\nhost=…`) and pass via `mysqldump --defaults-extra-file=<path>` — password no longer reachable via `/proc/*/cmdline` during the dump window. `try { ... } finally { @unlink($cnf); }` ensures the temp file is shredded even on dump failure. Refactored `runDumpCommand()` as a protected hook so the new Pest test uses an anonymous subclass to capture the assembled command string without invoking real mysqldump. **Sentinel-password regression guard:** test uses `SECRET_SHOULD_NOT_LEAK_123!@#` + asserts the command string `->not->toContain($sentinel)` — any future revert that reintroduces `-p<pwd>` fails CI loudly. SECURITY-REVIEW.md frontmatter flipped `status: shippable_with_followups` → `status: high_findings_remediated` with H-1/H-2 remediation banners pointing at this task's commits. Focused tests 22/22 GREEN (61 assertions, 20.83s); full suite delta vs 260606-rld baseline: 1,845 / 219 / 3 (+12 pass / 0 new fails). Quick task [260607-9c6](./quick/260607-9c6-security-high-fixes-h-1-webhooks-prune-r/) | fix(security) | 36fceb5 + 4a14eef + 304dca2 + 7e6511e |
| 2026-06-06 | **Pricing-ops Below-cost + At-floor: Brand column + 3 filters.** Adds a Brand column AND Brand/Supplier/Competitor SelectFilters (Alpine client-side, scoped via `x-model`/`x-show`) to the bucket modals at `/admin/pricing-operations` for the **below_cost** + **at_floor** buckets. Winnable + matched buckets are byte-identical — column + filters conditionally rendered only for the two target buckets. **Scanner extension:** `CompetitorPositionScanner` emits `brand_id` per row (single new column in the existing SELECT). **Decoration layer:** `PricingOpsReport::positions()` decorates the cached scan with `brand_name` resolved via `TaxonomyResolver::allBrands()` (1h cache reused) — keeps the scanner's deptrac surface unchanged. **Deptrac arrow:** runtime FQCN escape did NOT bypass static analysis; allow-list extended `Pricing → ProductAutoCreate` in both `deptrac.yaml` + `depfile.yaml` (783 → 784 allowed arrows) as its own atomic chore commit with justification — no runtime cycle (Pricing reads at dashboard render time; ProductAutoCreate writes brand_id at pipeline time). **Exports:** CSV/XLS at PricingOpsReport::csv() conditionally include Brand header + value for the two target buckets. Full Pest delta vs 260606-q7h baseline: 1,833 / 219 / 3 (+7 pass / 0 new fails). Quick task [260606-rld](./quick/260606-rld-pricing-ops-below-cost-at-floor-modals-a/) | feat(pricing-ops) | 384f7a8 + dd4fcc4 + 1fc9bbb + 7f7edcd |
| 2026-06-06 | **Forensic audit — exit-on-first-non-empty resolver anti-pattern.** Bounded read-only sweep of all 9 resolver/registry files under app/Domain/ for the Huddlecamhd shape (2026-05-31 commit 73ac682: `TaxonomyResolver::allBrands` preferred 1-term pa_brand over 176-term native taxonomy, silently misrouting Sony/Barco/ViewSonic/Huddly for months). Result: **0 HIGH / 1 MEDIUM / 4 LOW / 1 SANITY**. Verified TaxonomyResolver 73ac682 fix still intact (native primary at line 261, pa_brand fallback at 273). MEDIUM: `IntegrationCredentialResolver` DB→env fallback is intentional (documented intent in docblock — not Huddlecamhd shape because env is canonically failover-only, not co-equal data source). 4 LOW resolvers are clean single-source. Anti-pattern is NOT proliferating across the codebase. Audit shipped as `260606-q7h-FINDINGS.md` for the retrospective; investigation IS the deliverable. Full Pest suite delta vs 260606-p4q baseline: 1,826 / 219 / 3 (zero regressions). Quick task [260606-q7h](./quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/) | docs(audit) | 320c290 |
| 2026-06-06 | **Filament 3 database notifications — bell icon for long-running ops.** Wires the standard Filament 3 database-notifications channel so RunAutoCreatePipelineJob (260606-433dc0d — multi-minute Horizon job from Suggestions bulk action) and RetryMissingImagesCommand (260606-77b65b7 — interactive artisan run, ~10p/SKU Claude spend) surface a completion alert in the admin bell-icon when they finish, instead of forcing the operator to tail /horizon. Build: (1) `2026_06_06_171032_create_notifications_table` migration (Laravel default shape — UUID PK + polymorphic notifiable + JSON data + read_at) via `php artisan notifications:table` + migrate. (2) `->databaseNotifications()` + `->databaseNotificationsPolling('30s')` on `AdminPanelProvider` panel() chain between `->plugins()` and `->authMiddleware()`. (3) Generic reusable `App\Notifications\OperatorJobCompletedNotification` (ShouldQueue + via=['database']) with title/body/level/url/icon payload — 4 level→heroicon mappings (success→check-circle, danger→x-circle, warning→exclamation-triangle, info→information-circle). Pest 7/7 GREEN (12 assertions, 4.85s). (4) RunAutoCreatePipelineJob::handle() notifies triggering user after Artisan::call returns (success/warning by SKU-success ratio + URL to /admin/products or /admin/auto-create-reviews); new `failed(Throwable)` callback notifies with level=danger. Both wrapped in try/catch — notification-table-missing must not break the pipeline. (5) RetryMissingImagesCommand captures `auth()->user()` at perform() start, notifies on LIVE SUCCESS path only (skips dry-run + cron — null user = silent skip by construction). Tinker probe: synthetic notify → row landed in `notifications` table with the 5-key JSON payload + correct icon. Full Pest delta vs 260606-o63 baseline: 1,826 / 219 / 3 (+7 pass = exactly the new test count, 0 new fails). **Prod deploy: `php artisan migrate --force` REQUIRED after pull** — without it, try/catch absorbs the missing-table error (pipeline still works, just no bell badge until migrated). Quick task [260606-p4q](./quick/260606-p4q-filament-database-notifications-panel-be/) | feat(notifications) | 995ca67 + 9a80993 + 3637a18 + 62c49ab + 2bc8563 |
| 2026-06-06 | **Vacuous auto_create_status predicate — structural fix.** Today's 260606-mx9 surfaced that `auto_create_status` is `NOT NULL DEFAULT 'manual'` per migration `2026_04_22_100300`, so every prior `whereNotNull('auto_create_status')` filter was a no-op — `RetryMissingImagesCommand` had been silently including the entire 5,632-row legacy WC catalogue in default-mode runs since 260606-ca92caa. Fix: (1) Canonical `Product::scopeAutoCreated(Builder)` returning `where('auto_create_status', '!=', 'manual')` — driver-agnostic, ships with 12-assertion unit test covering all 8 enum statuses. (2) Two read-sites swapped to the scope: `RetryMissingImagesCommand.php:62` (the broken caller) + `AutoCreateHealthPage.php:141/336` (the correct-but-inline caller — refactored for consistency). (3) `tests/Architecture/AutoCreatedPredicateTest.php` recursive-grep over `app/` fails on any of 6 vacuous-pattern variants (`whereNotNull/whereNull('auto_create_status')`, raw `IS NOT NULL`/`IS NULL` SQL, single/double-quoted) — same shape as 260606-c4o `EnvUsageTest`. Includes manual-probe recipe in docblock + meta-assertion that the regex actually catches all 6 synthetics. Sanity probe: temp-inject `whereNotNull('auto_create_status')` into RetryMissingImagesCommand → arch test FAILS at line:49; revert → green. (4) 14-grep-hit audit attached to the fix commit body: 12 sites untouched (write-side + specific-status filters in SeoAgent batch, SnapshotAggregator, WeeklyDigestComposer, PricingOpsReport, etc), 2 sites edited. (5) Local DB probe: `Product::query()->autoCreated()->count() = 0` (zero auto-created products in dev DB); `Product::query()->where('auto_create_status', 'manual')->count() = 5,633` (entire legacy catalogue). With `--all`: scope bypassed, full catalogue reachable. Focused tests 4+3/4+3 GREEN; full suite delta vs 260606-mx9 baseline: 1,819 / 219 / 3 (+5 pass / 0 new fails). Known follow-ups: RetryMissingImagesCommand has no Pest test (coverage gap); MySQL-only `JSON_LENGTH` in the empty-images expression (pre-existing — lift `AutoCreateHealthPage::emptyImagesExpr()` helper in a future quick). Quick task [260606-o63](./quick/260606-o63-fix-vacuous-auto-create-status-is-not-nu/) | fix(products) | 48c8f06 + bfdd812 + 2f7f717 |
| 2026-06-06 | **Auto-create Health page.** New admin-only Filament page at `/admin/auto-create-health` (Operations nav group, sort 110) surfaces auto-created products with broken local state — any of: no images (`gallery_image_urls IS NULL OR json_array_length = 0`), no brand_id, no category_id, no woo_product_id. One row per Product with columns SKU/Name/auto_create_status/Brand/Category/Images/Woo ID/Last synced/Issue summary; default sort created_at desc. Per-row admin-only actions: **Resync to Woo** (`Artisan::call('products:resync-to-woo', ['--skus' => $sku])` with try/catch + Filament Notification success/danger) and **Source images** (visible only when images=0, calls `products:source-images --skus=`). Nav badge counts unhealthy + tooltip shows per-issue breakdown (`"N no images • M no brand • K no category • J no Woo"`) cached 60s mirroring SuggestionResource pattern. Closes the 2026-06-06 Manhattan placeholder incident gap for LOCAL-state drift (Woo-side image-count drift remains a follow-up — needs a Woo REST scan). **Latent bug fix:** `auto_create_status` column is NOT NULL DEFAULT 'manual' per migration `2026_04_22_100300`, so the `IS NOT NULL` predicate has been vacuous everywhere it's used (RetryMissingImagesCommand since 260606-ca92caa). New page uses `!= 'manual'` which actually excludes legacy WC products. RetryMissingImagesCommand inherits the bug — flagged for a future quick task. Driver-aware JSON expression (MySQL `JSON_LENGTH` + SQLite `json_array_length`) so Pest in-memory SQLite runs the predicate. Drift-prevention: page query + nav badge count both call `unhealthyQuery()` private helper. Focused Pest 3/3 GREEN (10 assertions, 8.15s); full suite delta vs 260606-lhp baseline: 1,814 / 219 / 3 (+3 pass / 0 new fails). Auto-discovered by AdminPanelProvider — no panel edit needed. Quick task [260606-mx9](./quick/260606-mx9-auto-create-health-page-filament-table-s/) | feat(dashboard) | 3d0bec2 + 75c0463 + 5139da9 |
| 2026-06-06 | **Decision-grade dashboard tiles.** Replaces the raw pending-count signal on Home with two actionable Filament StatsOverviewWidgets: (1) **HighConfidenceSourceableWidget** — single Stat with the high-confidence count (pending + new_product_opportunity + EXISTS in supplier_sku_cache + supporting_competitors ≥ 3), description shows 3-tier breakdown `H high-conf • S sourceable • R raw pending`, clickable URL with Filament tableFilters pre-applied (kind=new_product_opportunity, status=pending, competitor_count_bucket=3plus, on_supplier_db=yes). (2) **SuggestionsQueueHealthWidget** — three stats stacked: applied-last-7d, rejected-last-7d, oldest-pending-age-days; color flips to warning when oldest_pending > 30d. BOTH consume from new `SnapshotAggregator::computeSuggestionsTriageHealth()` (single payload, 6 keys) cached via `dashboard_snapshots` and refreshed by the existing `dashboard:refresh` scheduler (5-min cadence) — widget render path never hits a hot DB query. **Drift-prevention:** extracted `Suggestion::scopeHighConfidenceSourceable(Builder)` so badge + tile + aggregator share one predicate; Pest test pins the badge count = aggregator's high_confidence_count value. PendingReviewsWidget surgery: removed only the NPO Stat (kept Auto-create drafts + Margin changes). Focused Pest 4/4; full suite delta vs 260606-gnu baseline: 1,811 / 219 / 3 (+8 passed / −4 failed vs 1,803 / 223 / 3 — zero new failures introduced). Quick task [260606-lhp](./quick/260606-lhp-dashboard-decision-grade-tiles-high-conf/) | feat(dashboard) | 1159bfe + dbbb6aa + 62ad882 + ad815e0 + eef3e7e |
| 2026-05-30 → 31 | **🎉 v1 CUTOVER LIVE + Day 1 verified.** WOO_WRITE_ENABLED=true (flipped 19:07 UTC 05-30). Stock Updater plugin deactivated (manually in WP — WP-CLI not available). itgalaxy Bitrix plugin intentionally LEFT ACTIVE (CRM half deferred). 3 canary writes verified live on storefront. 1,939 status reconciliations pushed clean. **Same session also shipped 5 follow-ons:** PUT→POST WAF workaround (`fb7ac18` — host Apache/mod_security blocks PUT to /wp-json/*; POST allowed; WC treats both identically for resource updates), `products:push-status-to-woo` command + `obsolete-statuses-pushed` gate (`a9d90b3 → 28420aa`), Flatsome layout parity via `products.attributes_json` (`8a31731` — verified live on Huddly), EAN/global_unique_id end-to-end (`99d34bd → c51f674` — chained normaliseEan(supplier) ?? normaliseEan(sku)), CSV parse errors per-competitor widget + XLSX export (`cf3fb1b`), stale-feed threshold 30→4 days (`8aafc3e`), cutover-flip-day-script.md doc (`c3b8848`). **Critical mid-cutover bug found + fixed (`d7d0e39`):** schedule env-toggles in `routes/console.php` used `env()` instead of `config()` — silently returns default in cached-config mode, so `pricing:undercut-competitors --live` schedule never registered post-deploy. First 08:00 BST auto-reprice silently missed. Manual recovery: 5,279 price changes, 3,909 successful Woo POSTs (avg 484ms, zero failures). `schedule:test` + `crontab -l` + dashboard:refresh-every-5min all confirm **tomorrow 07:00 UTC autonomous reprice will fire**. Lesson captured in STATE: never call `env()` outside `config/*.php` when config:cache is in use. Quick task [260530-clv](./quick/260530-clv-cutover-live-day-1-verified-plus-fixes/) | feat(cutover)+follow-ons | a14545c |

### Known debt / separate milestones
- **Test-suite remediation (full green on PHP 8.3/CI)** — surfaced 2026-05-25: the Pest suite has ~165 pre-existing failures + 1 hanging (networked) test, spanning many domains. Root causes are **test-infra rot, not prod bugs**: fixtures not seeding FK deps (e.g. customer_groups), Filament action-visibility drift (`callTableAction` on a hidden action now throws), MySQL-vs-SQLite skip-guards — compounded by local PHP 8.4 vs prod PHP 8.3. The suite hadn't been runnable for a long time (the missing pest-plugin-livewire, now added, proves it). **Not a cutover blocker.** Cutover **Gate 3 (feature-suite)** is satisfied on critical-path evidence instead (app boots, 81 routes resolve, prod is 8.3, and the changed/critical suites pass: Pricing 107, Sync 23, Products 20, Suggestions 16, PublishProductJob 5, + 7 new dashboard tests). Greening the rest = its own milestone, domain-by-domain, ideally on PHP 8.3 in CI.

## Accumulated Context

### v2 Milestone Decisions (locked 2026-04-24)

- **Anthropic budget:** £200/month default; revisit after Phase 8 ships + 2 weeks of real usage
- **Langfuse:** self-hosted Docker on ops VPS (EU residency for margin + customer data)
- **Catalogue assumption:** ~5k SKUs; MySQL FULLTEXT for E4; vector DB deferred to v2.1
- **Phase numbering:** Continues from v1 (Phase 8-15); does NOT reset
- **Net-additive architecture:** 5 new Deptrac layers, 2 new Horizon queues, 4 required composer packages, ZERO version bumps to v1's stack
- **Cutover gate on Phase 15:** Must NOT begin Phase 15 planning until v1 cutover is live in production AND ≥4 weeks of Bitrix Deal UTM data has accumulated. Pre-flight (ADAGT-05) verifies GCLID capture or ships a v1 hotfix first.
- **v1 frozen:** v2 is net-additive; v1 jobs/listeners are NEVER modified; all v2 hooks are subscribers via DomainEvent + listener-based extension

### v2 Cross-Cutting Invariants (every phase respects)

1. Suggestions seam mandatory for any data-changing feature
2. Dual-YAML Deptrac sync — every new layer in BOTH `depfile.yaml` AND `deptrac.yaml`
3. Dry-run-default CLI — `--live` opt-in
4. Shadow-mode gates default false: `AGENT_WRITE_ENABLED`, `AGENT_AUTO_APPLY_ENABLED`, `WHATSAPP_OUTBOUND_ENABLED`, `QUOTE_BITRIX_PUSH_ENABLED`
5. `shield:safe-regenerate` wrapper ships in Phase 8 — every later phase uses it
6. Correlation_id threading: Context → Prism → Langfuse → Suggestions → integration_events
7. ULID PKs for all cross-domain references (`AgentRun`, `Quote`, `WhatsAppConversation`, `ChatbotSession`)
8. Listener-based extension of v1 — never modify v1 jobs
9. Provider seam pattern — new external APIs get thin `<X>Client` wrappers (mirror `WooClient` / `BitrixClient`)
10. Golden fixture extension, not modification — v1's 50 PriceCalculator triples remain byte-identical

### Decisions (carry-forward from v1.50.1 — see PROJECT.md Key Decisions table)

Decisions are logged in PROJECT.md Key Decisions table. Recent v1 decisions affecting v2 work:

- v1 framework + audit/integration/suggestions seams are the load-bearing seams for every v2 phase
- v1 cutover is ops-executed in parallel with v2 dev (does NOT block v2 phases 8-14)
- E5 RAMS cross-project integration deferred to v2.1+
- Channel feeds (Phase 8 in candidate list), customer automation, forecasting all deferred to v2.1+

**v2 Plan 08-02 (2026-04-25):**

- ClaudeClient is the SOLE Anthropic call site (Prism wrapper); enforced by Deptrac + AgentsWriteOnlyViaSuggestionsTest
- Self-hosted Langfuse on lf.ops.meetingstore.co.uk with 127.0.0.1-only port binding; nginx + admin basic auth; mliviu79 shim primary path, custom-OTel fallback documented in observability.md
- Plan W8 verify regex corrected to `[* ]v?<version>` (handles real `composer show v0.100.1` prefix); future plan-checkers should use this pattern
- ClaudeResponse maps Prism's 7-case FinishReason enum to local 5-case D-06 enum with default→Error fall-through (future-proofs against Prism enum additions)
- CostCalculator throws RuntimeException on unknown model (fail-loud — unbudgeted call surfaces as runtime error, not silent zero-cost)

**v2 Plan 11-01 (2026-05-01) — Phase 11 Foundation:**

- OQ-2 RESOLVED: BitrixEntityMap dedup ledger extended with nullable `quote_id` ULID + composite UNIQUE(entity_type, quote_id) coexisting with existing UNIQUE(entity_type, woo_id) for orders. Plan 11-04 EntityDeduper queries via two parallel scope methods (scopeForWooOrder + scopeForQuote)
- A2 RESOLVED (Rule 1 deviation): bitrix_entity_map.entity_type was MySQL ENUM (not VARCHAR as plan A2 assumed). Migration MODIFIES the ENUM allow-list to include 'quote_deal' — preserves the Phase 4 DB-level enum guarantee. SQLite no-op via `DB::getDriverName() === 'mysql'` guard.
- A1 LOCKED: VAT-INCLUSIVE pence storage convention encoded at column-comment + model-docblock level (Pitfall 1). PDF strips VAT at render time via PriceCalculator::stripVat in Plan 11-04. Pest test asserts integer cast preserves 1999 sentinel value.
- OQ-1 RESOLVED: quotes.total_pence_at_quote cached UNSIGNED BIGINT column added; recompute observer ships in Plan 11-02 (draft-only writes; locked alongside lines after status=sent).
- D-04 separation-of-duties enforced at QuotePolicy::approve gate — sales role explicitly DENIED (T-11-01-03 mitigation). 4-eyes pattern in place before Filament UI lands in Plan 11-03.
- D-06 reserved enum cases: QuoteStatus ships PendingApproval + Approved cases for v1.x non-breaking extension; v1.0 transitions never branch on them. NO `withdrawn` case (D-06 deferred — sales overwrites by editing draft).
- PolicyTemplateIntegrityTest floor bumped 27 → 29 covering QuotePolicy + QuoteLinePolicy.
- A9 LOCKED: customer_group_name_at_quote VARCHAR(255) denormalised on quotes table (CONTEXT.md Claude's Discretion).

### Roadmap Evolution

- Phase 09.1 inserted after Phase 9: Integration Connections Admin (URGENT) — Filament admin page for Supplier API + WooCommerce REST + Bitrix + Langfuse + Anthropic credentials with per-integration "Test connection" actions; closes the env-only-credentials ops gap before Phase 10 PricingAgent ships and burns Anthropic budget against potentially-misconfigured upstreams
- Phase 11.1 inserted after Phase 11: Competitor FTP Pull (URGENT) — scheduled FTP/SFTP fetch of competitor CSVs from external feeds into `storage/app/competitors/incoming/` so Phase 5 watcher picks them up. New `competitor_ftp_sources` admin-managed table (host/port/path/encrypted credentials/cron/active flag), `competitor:ftp-pull` artisan command (dry-run default + `--live` opt-in), Filament Resource, scheduled before `competitor:watch`, audit_log entry per pull. Closes the only missing link in the FTP→parse→DB→pricing pipeline. Single-plan phase, ~50-100 LOC + 5-6 tests. Deps: Phase 5 (existing CSV ingest pipeline).

### Pending Todos

None yet — Phase 8 planning kicks off with `/gsd-plan-phase 8`.

### Blockers/Concerns

**Operator decisions to confirm before Phase 8 plan 01 lands:**

- Anthropic budget ceiling sign-off (proposal: £200/month)
- Self-hosted Langfuse Docker provisioning on ops VPS

**Carry-forward from v1.50.1 (operator-side, not v2 dev work):**

- v1 cutover execution per `docs/ops/cutover-handover.md` Appendix A
- 3 operator carry-forward gates: supplier API probe, Woo sandbox image URL pass-through re-validation, feature-tier Pest suite run against online MySQL

**Per-phase operator questions (per research/SUMMARY.md operator decisions tracker):**

- Phase 9 (E1): trade customer-group seed list (Trade / Reseller / Education / NHS proposed); display strategy `retail` vs `hidden`
- Phase 11 (E2): quote PDF branding template ownership; customer signature mechanism; Bitrix Deal line-item modelling for 30-line quotes
- Phase 13 (E3): WABA ownership confirmation; template catalogue scope; Meta OBO deprecation verification
- Phase 14 (E4): public vs internal SKU split; anonymous chatbot PII storage posture; chatbot 24/7 vs business-hours
- Phase 15 (C2): GCLID capture in v1 — hotfix on v1 may be required before P15
- Plan 08-01: AgentRunTest (12 unit tests) + migration verification deferred until MySQL service is running on 127.0.0.1:3306
- Plan 08-02: 5 Prism::fake() integration tests in tests/Feature/Agents/ClaudeClientTest.php deferred until MySQL service is online (Pest auto-applies RefreshDatabase to tests/Feature). 9 unit tests pass today. Langfuse Docker stack ready for ops to provision per docs/ops/observability.md

## Session Continuity

Last session: 2026-06-06T11:00:00.000Z
Stopped at: Completed quick task 260606-c4o — env() architectural guardrail shipped. tests/Architecture/EnvUsageTest.php (commit 2336e30) ships two Pest assertions (arch() over App\, file-scan over routes/+database/) plus a meta-regex sanity check. Three preparatory cutover refactor commits (9feb585 WOO_DB_*, 5d3888d WOO_WRITE_ENABLED, 1e275bc drill/disable-live gates) moved 7 pre-existing env() reads to config-bound keys so the guardrail lands green. Manual probe verified (routes/console.php + BaseCommand.php). Phase 12 still complete; Phase 13 (E3 WhatsApp) still ready for planning.
Resume: Orchestrator runs `gsd-tools phase complete 12` to finalize the phase, then `/gsd-research-phase 13` (E3 WhatsApp Channel — research flag YES per WABA setup + Meta OBO BSP deprecation 2026 + 24h window state machine edge cases).

### Phase 12 Decisions (logged 2026-05-16)

- **O-1 Resolved YES** — Prism v0.100.1 supports `withEnumParameter`; ProposeContentPatchTool's `field` arg pinned to 4 valid SEO fields at the Anthropic schema level.
- **O-2 Resolved env-flag default true** — `AGENT_SEO_BATCH_SCHEDULE_ENABLED=true` wraps the nightly schedule entry; operator can flip to false in `.env` for emergency disable without code deploy.
- **O-3 Resolved subset stays pending** — Suggestion status only flips to `applied` when ALL patches in `payload.patches[]` have `applied_at` set; subset approvals stay `pending` so admin can return later.
- **O-4 Resolved temperature=0.4** — `agents.seo.temperature` defaults to 0.4 (env override via `AGENTS_SEO_TEMPERATURE`) — balances creativity vs reproducibility for SEO copy.
- **O-5 Resolved default-hide with escape-hatch filter** — SuggestionResource::getEloquentQuery hides `agent_guardrail_blocked` by default; explicit `tableFilters.kind.value=agent_guardrail_blocked` shows them.
- **P12-A LAST-WINS dedup defended at 3 layers** — unconditional `$patchesByField[$field] = ...` + Pest fixture asserting second-call wins + source grep gate returning 0 for `isset($patchesByField`.
- **P12-B catch-block audit before rethrow** — RunSeoAgentJob line 242 calls `createGuardrailBlockedSuggestion` BEFORE line 271's `throw $e`; one `agent_guardrail_blocked` Suggestion + zero `seo_content_patch` Suggestions after blocked run.
- **P12-D TruncatingTool relocation clean (no shim)** — moved from `Tools/Pricing/` to shared `Tools/` parent; old FQCN absent; all 4 Phase 10 read_* tools updated.
- **P12-E budget race 3-layer defence** — pre-flight Cache::get + between-dispatch Cache::get + BatchCommandBudgetRaceTest with 2 ceiling fixtures.
- **P12-F additive Filament sidebar** — `seoPatchesInfolist` method name (NOT `infolist()`); EditAutoCreateReview declares neither `form()` nor `infolist()` locally; reflection check in AutoCreateEditFormUnchangedTest.
- **P12-H brand-voice opacity** — ReadBrandStyleGuideTool + system.blade.php contain neither `Blade::render` nor `@include`; grep-asserted in test suite.
- **Critical title→Product.name remap** — SEOAGT-01 user-facing 'title' field maps to Product.name column via `FIELD_TO_PRODUCT_COLUMN` constant; defended by SeoContentPatchApplierTitleToNameTest 3 cases.
- **UAT deferred to production deploy** — `ms.21stcav.com` not yet bootstrapped; 10 of 10 manual UAT steps have direct Pest substitution; deferred items + re-run conditions captured in 12-UAT-DISPOSITION.md.

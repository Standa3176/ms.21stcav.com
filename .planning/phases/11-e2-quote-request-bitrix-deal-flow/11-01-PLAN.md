---
phase: 11-e2-quote-request-bitrix-deal-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_01_010000_create_quotes_table.php
  - database/migrations/2026_05_01_010100_create_quote_lines_table.php
  - database/migrations/2026_05_01_010200_add_quote_id_to_bitrix_entity_map_table.php
  - database/migrations/2026_05_01_010300_add_receives_quote_alerts_to_alert_recipients_table.php
  - app/Domain/Quotes/Models/Quote.php
  - app/Domain/Quotes/Models/QuoteLine.php
  - app/Domain/Quotes/Enums/QuoteStatus.php
  - app/Domain/Quotes/Enums/RejectionReason.php
  - app/Domain/Quotes/Policies/QuotePolicy.php
  - app/Domain/Quotes/Policies/QuoteLinePolicy.php
  - app/Domain/CRM/Models/BitrixEntityMap.php
  - app/Domain/Alerting/Notifiables/AlertRecipient.php
  - database/factories/Domain/Quotes/QuoteFactory.php
  - database/factories/Domain/Quotes/QuoteLineFactory.php
  - config/quote.php
  - .env.example
  - depfile.yaml
  - deptrac.yaml
  - tests/Architecture/DeptracQuotesLayerTest.php
  - tests/Unit/Domain/Quotes/Models/QuoteTest.php
  - tests/Unit/Domain/Quotes/Models/QuoteLineTest.php
autonomous: true
requirements: [QUOT-01, QUOT-02]
must_haves:
  truths:
    - "quotes table exists with ULID PK (CHAR(26)), nullable user_id FK to users(id) ON DELETE SET NULL, nullable customer_group_id FK to customer_groups(id) ON DELETE RESTRICT, customer_group_name_at_quote VARCHAR(255) nullable, customer_email/customer_name VARCHAR(255), billing_address JSON nullable, status VARCHAR enum (draft|pending_approval|approved|sent|accepted|rejected|expired) default draft, total_pence_at_quote INTEGER default 0, expires_at DATETIME nullable, sent_at/accepted_at/rejected_at/expired_at DATETIME nullable, rejection_metadata JSON nullable, correlation_id VARCHAR(64) nullable, timestamps"
    - "quote_lines table exists with ULID PK (CHAR(26)), quote_id FK CHAR(26) ON DELETE CASCADE, sku VARCHAR(255), quantity_int INTEGER (validated 1..9999), unit_price_pence_at_quote INTEGER (VAT-INCLUSIVE per A1), line_total_pence_at_quote INTEGER, product_snapshot JSON, sort_order SMALLINT default 0, timestamps"
    - "bitrix_entity_map table extended with nullable quote_id CHAR(26) column + composite UNIQUE INDEX (entity_type, quote_id) named bitrix_entity_map_entity_type_quote_id_unique (resolves RESEARCH OQ-2 option a)"
    - "alert_recipients table extended with receives_quote_alerts BOOLEAN default false (mirrors receives_crm_alerts/receives_competitor_alerts pattern)"
    - "Quote Eloquent model uses HasUlids + LogsActivity (logOnly status/sent_at/accepted_at/rejected_at/expired_at, logOnlyDirty), defines STATUS_* constants, lines() HasMany, customer() BelongsTo User, customerGroup() BelongsTo, ulidShort() returns first 8 chars"
    - "QuoteLine Eloquent model uses HasUlids, casts unit_price_pence_at_quote/line_total_pence_at_quote/quantity_int as integer, product_snapshot as array, fillable lock prevents direct mass-assignment of unit_price_pence_at_quote (only set via PriceSnapshotter in Plan 11-02)"
    - "QuoteStatus + RejectionReason PHP 8.2 enums exist matching schema enum values; QuoteStatus reserves intermediate enum cases (PendingApproval, Approved) without exposing them in v1 transitions per D-06 — no `withdrawn` case shipped (D-06 deferred)"
    - "QuotePolicy + QuoteLinePolicy ship with view/viewAny/create/update/delete/restore/forceDelete + approve/markAccepted/markRejected/revert (QuotePolicy only); approve denies sales role explicitly per D-04 separation-of-duties"
    - "BitrixEntityMap model adds ENTITY_QUOTE_DEAL='quote_deal' constant + scopeQuoteDeals() + scopeForQuote(string $quoteId) returning where(entity_type='quote_deal') + where(quote_id=$quoteId)"
    - "AlertRecipient model fillable + cast updated for receives_quote_alerts (no LogsActivity column update needed — operational column)"
    - "config/quote.php exists with company_name/company_address/company_vat_number/company_registration_number (hard-coded), default_expiry_days (default 14), email_on_expiry (default false), pdf_signature_block (default false), bitrix_deal_type_id (default 'QUOTE'), bitrix_push_enabled (env QUOTE_BITRIX_PUSH_ENABLED default false)"
    - ".env.example contains QUOTE_BITRIX_PUSH_ENABLED=false (default off — shadow-mode gate per cross-cutting invariant 4)"
    - "Quotes Deptrac layer registered in BOTH depfile.yaml AND deptrac.yaml with directory regex app/Domain/Quotes/.* and ruleset [Foundation, Products, Pricing, TradePricing, Suggestions, CRM, Webhooks] (DENIES Agents, Competitor, ProductAutoCreate, Sync, Cutover, Marketing, Channels)"
    - "CRM allow-list extended in BOTH YAMLs to include Quotes (one-way arrow: CRM listener reads Quote model; Quotes domain MUST NOT import BitrixClient)"
    - "DeptracQuotesLayerTest passes — asserts dual-YAML registration + ruleset members + denial list + deptrac analyse exit-0 against both YAML files (mirrors DeptracTradePricingLayerTest pattern)"
  artifacts:
    - path: "database/migrations/2026_05_01_010000_create_quotes_table.php"
      provides: "quotes table with ULID PK + 14 cols + status enum + status timestamps"
      contains: "Schema::create('quotes'"
    - path: "database/migrations/2026_05_01_010100_create_quote_lines_table.php"
      provides: "quote_lines table FK + snapshot cols"
      contains: "Schema::create('quote_lines'"
    - path: "database/migrations/2026_05_01_010200_add_quote_id_to_bitrix_entity_map_table.php"
      provides: "Nullable quote_id ULID column + UNIQUE(entity_type, quote_id) on bitrix_entity_map (RESEARCH OQ-2 option a)"
      contains: "bitrix_entity_map_entity_type_quote_id_unique"
    - path: "database/migrations/2026_05_01_010300_add_receives_quote_alerts_to_alert_recipients_table.php"
      provides: "receives_quote_alerts BOOLEAN default false"
      contains: "receives_quote_alerts"
    - path: "app/Domain/Quotes/Models/Quote.php"
      provides: "Quote Eloquent model — HasUlids + LogsActivity + status constants + relations"
      contains: "use HasUlids"
      min_lines: 60
    - path: "app/Domain/Quotes/Models/QuoteLine.php"
      provides: "QuoteLine Eloquent model — HasUlids + integer casts + immutable price columns"
      contains: "use HasUlids"
      min_lines: 40
    - path: "app/Domain/Quotes/Enums/QuoteStatus.php"
      provides: "PHP 8.2 backed enum (string) — 7 cases matching schema"
      contains: "case Draft = 'draft'"
    - path: "app/Domain/Quotes/Policies/QuotePolicy.php"
      provides: "RBAC gates — approve denies sales (D-04)"
      contains: "function approve"
      min_lines: 50
    - path: "config/quote.php"
      provides: "Company details + expiry + bitrix gate + pdf_signature_block"
      contains: "bitrix_push_enabled"
    - path: "depfile.yaml"
      provides: "Quotes layer + ruleset + CRM allow-list extension (dual-YAML lockstep)"
      contains: "name: Quotes"
    - path: "deptrac.yaml"
      provides: "Quotes layer + ruleset + CRM allow-list extension (dual-YAML lockstep)"
      contains: "name: Quotes"
    - path: "tests/Architecture/DeptracQuotesLayerTest.php"
      provides: "Dual-YAML registration + deptrac analyse exit-0 + denial list assertion"
      min_lines: 50
  key_links:
    - from: "depfile.yaml AND deptrac.yaml"
      to: "Quotes layer ruleset"
      via: "dual-YAML sync (Phase 5 P05-05 lesson)"
      pattern: "Quotes:\\s*\\[Foundation.*Products.*Pricing.*TradePricing.*Suggestions.*CRM.*Webhooks\\]"
    - from: "quote_lines.quote_id"
      to: "quotes.id"
      via: "foreignUlid constrained cascadeOnDelete"
      pattern: "foreignUlid\\('quote_id'\\).*->constrained\\('quotes'\\)"
    - from: "bitrix_entity_map.quote_id"
      to: "quotes.id"
      via: "Composite UNIQUE (entity_type, quote_id) — separates quote_deal lookups from order deal lookups"
      pattern: "bitrix_entity_map_entity_type_quote_id_unique"
    - from: "QuotePolicy::approve"
      to: "Sales role denial"
      via: "Separation of duties — D-04 — sales cannot approve own quote"
      pattern: "approve.*sales|sales.*approve"
---

<objective>
Foundation plan for Phase 11 — ships the entire schema (quotes + quote_lines + bitrix_entity_map.quote_id extension + alert_recipients.receives_quote_alerts), the Quote/QuoteLine Eloquent models with HasUlids + LogsActivity + relations, the PHP enums, the policies (with D-04 separation-of-duties on approve), the BitrixEntityMap model extension (ENTITY_QUOTE_DEAL constant + scopes), the config/quote.php (with QUOTE_BITRIX_PUSH_ENABLED env flag default false per cross-cutting invariant 4), and the new Quotes Deptrac layer registered in BOTH depfile.yaml AND deptrac.yaml (Phase 5 P05-05 dual-YAML lesson). All later plans (11-02..11-05) consume these artefacts.

Purpose: Phase 11's keystone — without the schema + model + enum + Deptrac layer this plan ships, Plan 11-02 (PriceSnapshotter + immutability observer) has no QuoteLine to write to, Plan 11-03 (Filament QuoteResource) has no model to back the Resource, Plan 11-04 (push pipeline) has no BitrixEntityMap quote_id column to dedup against. Closes RESEARCH OQ-2 (Option a — add nullable quote_id ULID column) and resolves A1 (VAT-inclusive storage convention) explicitly via column-level docblock.

Output: 4 migrations, 2 new models + factories, 2 PHP enums, 2 policies, 1 BitrixEntityMap extension, 1 AlertRecipient extension, 1 config file + .env.example edit, dual-YAML Deptrac edit, 1 new architecture test + 2 model unit tests.

Net stack delta: Zero composer changes. Pure schema + model-layer + Deptrac additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-CONTEXT.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-RESEARCH.md
@CLAUDE.md
@composer.json
@depfile.yaml
@deptrac.yaml
@app/Domain/CRM/Models/BitrixEntityMap.php
@app/Domain/Alerting/Notifiables/AlertRecipient.php
@app/Domain/TradePricing/Models/CustomerGroup.php
@tests/Architecture/DeptracTradePricingLayerTest.php

<interfaces>
<!-- Existing v1/v2 contracts this plan extends. Verbatim from codebase. -->

From app/Domain/CRM/Models/BitrixEntityMap.php (Phase 4 — verified lines 35-70):
```php
final class BitrixEntityMap extends Model
{
    public const ENTITY_DEAL = 'deal';
    public const ENTITY_CONTACT = 'contact';
    public const ENTITY_COMPANY = 'company';
    public const VIA_PUSH = 'push';
    public const VIA_BACKFILL = 'backfill';
    public const VIA_ADOPTED_LEGACY = 'adopted_legacy';
    public const VIA_MANUAL = 'manual';

    protected $table = 'bitrix_entity_map';
    protected $fillable = ['entity_type','woo_id','bitrix_id','email_hash',
        'last_payload_hash','last_status_snapshot','notes_hash_set',
        'last_pushed_at','last_correlation_id','created_via'];

    protected $casts = ['woo_id' => 'integer','last_pushed_at' => 'datetime',
        'notes_hash_set' => 'array'];
    // scopeDeals/scopeContacts/scopeCompanies/scopeForWooOrder exist
}
```

THIS PLAN EXTENDS:
- Add ENTITY_QUOTE_DEAL = 'quote_deal' constant
- Add 'quote_id' to $fillable
- Add scopeQuoteDeals(Builder $q) returning where(entity_type, ENTITY_QUOTE_DEAL)
- Add scopeForQuote(Builder $q, string $quoteId) returning where(entity_type=quote_deal)->where(quote_id=$quoteId)

From app/Domain/Alerting/Notifiables/AlertRecipient.php (Phase 1 + Phase 4/5/6/7/8 extensions):
```php
// Existing fillable already includes:
// receives_crm_alerts, receives_competitor_alerts, receives_auto_create_alerts,
// receives_agent_alerts (boolean defaults false)
// THIS PLAN ADDS: receives_quote_alerts boolean default false
```

From depfile.yaml + deptrac.yaml (Phase 9 reference for pattern):
```yaml
# Existing TradePricing layer (Phase 9 Plan 01 — verified lines 86-89 depfile.yaml):
- name: TradePricing
  collectors:
    - type: directory
      regex: app/Domain/TradePricing/.*
# THIS PLAN ADDS analogous Quotes layer + extends CRM ruleset to include Quotes
```

From config/horizon.php:190 (verified):
```php
// 'crm-bitrix' supervisor already configured (tries=5, timeout=120, processes=2)
// THIS PLAN: no horizon edit — Plan 11-04 dispatches PushQuoteToBitrixDealJob onto existing crm-bitrix queue
```

From tests/Architecture/DeptracTradePricingLayerTest.php (Phase 9 Plan 01 pattern — verified):
```php
// Pattern: assertDualYamlRegistration() + assertRulesetMembers() + assertDeptracAnalyseExitsZero()
// THIS PLAN clones the test shape verbatim for Quotes layer
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations + BitrixEntityMap + AlertRecipient extensions</name>
  <files>database/migrations/2026_05_01_010000_create_quotes_table.php, database/migrations/2026_05_01_010100_create_quote_lines_table.php, database/migrations/2026_05_01_010200_add_quote_id_to_bitrix_entity_map_table.php, database/migrations/2026_05_01_010300_add_receives_quote_alerts_to_alert_recipients_table.php, app/Domain/CRM/Models/BitrixEntityMap.php, app/Domain/Alerting/Notifiables/AlertRecipient.php, tests/Unit/Domain/Quotes/Models/QuoteTest.php (scaffold)</files>
  <read_first>app/Domain/CRM/Models/BitrixEntityMap.php (full file — see &lt;interfaces&gt;), app/Domain/Alerting/Notifiables/AlertRecipient.php (verify existing receives_* columns), database/migrations/2026_04_19_*_create_pricing_rules_table.php (Phase 3 — see foreign key + index pattern), database/migrations/Phase 4 BitrixEntityMap creation (verify entity_type column type — VARCHAR per A2)</read_first>
  <behavior>
    - Migration `2026_05_01_010000_create_quotes_table.php`:
      - `Schema::create('quotes', function (Blueprint $t) {`
      - `$t->ulid('id')->primary();` (CHAR(26))
      - `$t->foreignId('user_id')->nullable()->constrained('users')->nullOnDelete();`
      - `$t->foreignId('customer_group_id')->nullable()->constrained('customer_groups')->restrictOnDelete();`
      - `$t->string('customer_group_name_at_quote', 255)->nullable();` (denormalised — D-13 + Pitfall 6 + A9)
      - `$t->string('customer_email', 255);`
      - `$t->string('customer_name', 255)->nullable();`
      - `$t->json('billing_address')->nullable();`
      - `$t->string('status', 32)->default('draft');` (validated by enum; not native ENUM for SQLite test compat)
      - `$t->unsignedBigInteger('total_pence_at_quote')->default(0);` (RESEARCH OQ-1 RESOLVED — cached integer)
      - `$t->timestamp('expires_at')->nullable();`
      - `$t->timestamp('sent_at')->nullable(); $t->timestamp('accepted_at')->nullable(); $t->timestamp('rejected_at')->nullable(); $t->timestamp('expired_at')->nullable();`
      - `$t->json('rejection_metadata')->nullable();`
      - `$t->string('correlation_id', 64)->nullable()->index();`
      - `$t->timestamps();`
      - `$t->index(['status', 'expires_at']);` (powers quotes:expire query in Plan 11-05)
      - `$t->index('customer_email');`
    - Migration `2026_05_01_010100_create_quote_lines_table.php`:
      - `$t->ulid('id')->primary();`
      - `$t->foreignUlid('quote_id')->constrained('quotes')->cascadeOnDelete();`
      - `$t->string('sku', 255);`
      - `$t->unsignedInteger('quantity_int');` (D-12)
      - `$t->unsignedBigInteger('unit_price_pence_at_quote');` (VAT-INCLUSIVE per A1; document in column comment)
      - `$t->unsignedBigInteger('line_total_pence_at_quote');`
      - `$t->json('product_snapshot');`
      - `$t->unsignedSmallInteger('sort_order')->default(0);`
      - `$t->timestamps();`
      - `$t->index('sku'); $t->index(['quote_id', 'sort_order']);`
      - Add table comment: `'Phase 11 D-13 — unit_price_pence_at_quote stored VAT-INCLUSIVE (matches PriceCalculator::compute output). PDF strips VAT at render time via PriceCalculator::stripVat. NEVER store as float/decimal — Pitfall 1.'`
    - Migration `2026_05_01_010200_add_quote_id_to_bitrix_entity_map_table.php`:
      - `Schema::table('bitrix_entity_map', function (Blueprint $t) {`
      - `$t->ulid('quote_id')->nullable()->after('woo_id');`
      - `$t->unique(['entity_type', 'quote_id'], 'bitrix_entity_map_entity_type_quote_id_unique');`
      - down(): drop index then drop column
    - Migration `2026_05_01_010300_add_receives_quote_alerts_to_alert_recipients_table.php`:
      - `Schema::table('alert_recipients', function (Blueprint $t) {`
      - `$t->boolean('receives_quote_alerts')->default(false)->after('receives_agent_alerts');` (column position must follow latest receives_* — verify Phase 8 Plan 05 added receives_agent_alerts; if not present, append after receives_auto_create_alerts)
    - `app/Domain/CRM/Models/BitrixEntityMap.php`:
      - ADD constant: `public const ENTITY_QUOTE_DEAL = 'quote_deal';`
      - ADD `'quote_id'` to `$fillable` array
      - ADD scope: `public function scopeQuoteDeals(Builder $q): Builder { return $q->where('entity_type', self::ENTITY_QUOTE_DEAL); }`
      - ADD scope: `public function scopeForQuote(Builder $q, string $quoteId): Builder { return $q->where('entity_type', self::ENTITY_QUOTE_DEAL)->where('quote_id', $quoteId); }`
      - Update PHPDoc `@property` block to include `string|null $quote_id`
    - `app/Domain/Alerting/Notifiables/AlertRecipient.php`:
      - ADD `'receives_quote_alerts'` to $fillable array (after `receives_agent_alerts`)
      - ADD cast: `'receives_quote_alerts' => 'boolean'`
      - Update existing `scopeReceivingX` pattern: ADD `public function scopeReceivingQuoteAlerts(Builder $q): Builder { return $q->where('receives_quote_alerts', true); }`
    - Test `tests/Unit/Domain/Quotes/Models/QuoteTest.php` SCAFFOLD ONLY (full body in Task 2):
      - Pest test: `it('migrates quotes table with expected columns')` asserts `Schema::hasColumns('quotes', [...])`
      - Pest test: `it('migrates quote_lines table with integer pence columns')` asserts column types via `Schema::getColumnType('quote_lines', 'unit_price_pence_at_quote') === 'integer'`
      - Pest test: `it('extends bitrix_entity_map with quote_id column + unique index')` asserts column exists + index name present
  </behavior>
  <action>
    1. Create migration `database/migrations/2026_05_01_010000_create_quotes_table.php` per &lt;behavior&gt; spec. Use anonymous class `return new class extends Migration { ... };` per Laravel 11+ convention (Phase 9 precedent).
    2. Create migration `database/migrations/2026_05_01_010100_create_quote_lines_table.php` per &lt;behavior&gt; spec.
    3. Create migration `database/migrations/2026_05_01_010200_add_quote_id_to_bitrix_entity_map_table.php` per &lt;behavior&gt; spec. Verify in MySQL by running `php artisan migrate --pretend` (or full migrate against SQLite if MySQL not online). The new UNIQUE index COEXISTS with the existing `bitrix_entity_map_entity_type_woo_id_unique` — both must remain.
    4. Create migration `database/migrations/2026_05_01_010300_add_receives_quote_alerts_to_alert_recipients_table.php`. Inspect `database/migrations/` for the latest receives_* migration to determine `->after(...)` target.
    5. EDIT `app/Domain/CRM/Models/BitrixEntityMap.php`:
       - Add `public const ENTITY_QUOTE_DEAL = 'quote_deal';` after ENTITY_COMPANY.
       - Insert `'quote_id'` in `$fillable` array between `'woo_id'` and `'bitrix_id'`.
       - Add scopeQuoteDeals + scopeForQuote methods after scopeForWooOrder.
       - Extend `@property` PHPDoc with `* @property string|null $quote_id  ULID — Phase 11 quote_deal entity_type`.
    6. EDIT `app/Domain/Alerting/Notifiables/AlertRecipient.php`:
       - Add `'receives_quote_alerts'` to $fillable after the latest receives_* entry.
       - Add `'receives_quote_alerts' => 'boolean'` cast.
       - Add scopeReceivingQuoteAlerts mirroring existing scopeReceivingCrmAlerts pattern.
    7. Create scaffold `tests/Unit/Domain/Quotes/Models/QuoteTest.php` with the 3 schema-presence tests in &lt;behavior&gt; — full model behaviour tests come in Task 2 after the model is created.
  </action>
  <verify>
    <automated>php artisan migrate --pretend --no-interaction 2>&amp;1 | grep -E "create table.*(quotes|quote_lines)|alter table.*(bitrix_entity_map|alert_recipients)"</automated>
    <automated>php artisan test tests/Unit/Domain/Quotes/Models/QuoteTest.php --filter="migrates"</automated>
    <automated>grep -n "ENTITY_QUOTE_DEAL\|scopeQuoteDeals\|scopeForQuote" app/Domain/CRM/Models/BitrixEntityMap.php</automated>
    <automated>grep -n "receives_quote_alerts" app/Domain/Alerting/Notifiables/AlertRecipient.php</automated>
  </verify>
  <done>4 migrations created (passing migrate:fresh in dev), BitrixEntityMap + AlertRecipient extended, schema-presence tests pass, all integer pence columns confirmed via Schema::getColumnType assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quote/QuoteLine models + enums + policies + factories + config + .env.example + Quotes Deptrac layer (dual-YAML) + DeptracQuotesLayerTest</name>
  <files>app/Domain/Quotes/Models/Quote.php, app/Domain/Quotes/Models/QuoteLine.php, app/Domain/Quotes/Enums/QuoteStatus.php, app/Domain/Quotes/Enums/RejectionReason.php, app/Domain/Quotes/Policies/QuotePolicy.php, app/Domain/Quotes/Policies/QuoteLinePolicy.php, database/factories/Domain/Quotes/QuoteFactory.php, database/factories/Domain/Quotes/QuoteLineFactory.php, config/quote.php, .env.example, depfile.yaml, deptrac.yaml, tests/Architecture/DeptracQuotesLayerTest.php, tests/Unit/Domain/Quotes/Models/QuoteTest.php (extended), tests/Unit/Domain/Quotes/Models/QuoteLineTest.php</files>
  <read_first>app/Domain/TradePricing/Models/CustomerGroup.php (HasFactory + LogsActivity pattern), app/Domain/Suggestions/Models/Suggestion.php (HasUlids + STATUS_* constants pattern), app/Domain/Agents/Models/AgentRun.php (HasUlids precedent), tests/Architecture/DeptracTradePricingLayerTest.php (clone target), depfile.yaml + deptrac.yaml lines 86-89 + 256-258 (TradePricing layer registration pattern)</read_first>
  <behavior>
    - Quote model: STATUS_DRAFT='draft', STATUS_PENDING_APPROVAL='pending_approval', STATUS_APPROVED='approved', STATUS_SENT='sent', STATUS_ACCEPTED='accepted', STATUS_REJECTED='rejected', STATUS_EXPIRED='expired'.
    - Quote::ulidShort() returns substr($this->id, 0, 8).
    - Quote::lines() returns hasMany(QuoteLine::class).
    - Quote::customer() returns belongsTo(\App\Models\User::class, 'user_id').
    - Quote::customerGroup() returns belongsTo(\App\Domain\TradePricing\Models\CustomerGroup::class).
    - QuoteLine::quote() returns belongsTo(Quote::class).
    - QuoteStatus::cases() returns 7 cases matching schema enum strings.
    - RejectionReason::cases() returns: PriceTooHigh='price_too_high', WrongSpecifications='wrong_specifications', CompetitorWon='competitor_won', DelayedDecision='delayed_decision', Other='other' (D-08).
    - QuotePolicy::approve(User $user, Quote $quote): bool — TRUE only if $user->hasRole(['admin', 'pricing_manager']) AND $quote->status === STATUS_DRAFT (D-04 separation-of-duties — sales role explicitly DENIED).
    - QuotePolicy::revert(User $user, Quote $quote): bool — TRUE only if $user->hasRole('admin') AND $quote->status === STATUS_SENT AND $quote->sent_at?->diffInMinutes(now()) < 5 (D-05).
    - QuotePolicy::markAccepted/markRejected: TRUE if $user->hasAnyRole(['admin', 'pricing_manager', 'sales']) AND $quote->status === STATUS_SENT.
    - DeptracQuotesLayerTest: 4 Pest tests — assertDualYamlRegistration, assertRulesetMembers (Foundation, Products, Pricing, TradePricing, Suggestions, CRM, Webhooks), assertDenialList (Agents, Competitor, ProductAutoCreate, Sync, Cutover, Marketing, Channels), assertDeptracAnalyseExitsZero against BOTH YAMLs.
    - QuoteTest: it('uses HasUlids trait creating CHAR(26) id'), it('logs activity on status change only'), it('relates to lines'), it('belongs to customer + customer group'), it('exposes ulidShort 8 chars').
    - QuoteLineTest: it('casts unit_price_pence_at_quote as integer'), it('casts product_snapshot as array'), it('does not allow direct mass-assignment of unit_price_pence_at_quote outside fillable lock or via factory only').
  </behavior>
  <action>
    1. Create `app/Domain/Quotes/Enums/QuoteStatus.php` as `enum QuoteStatus: string { case Draft = 'draft'; ... }` with all 7 cases (D-06 — intermediate enum cases reserved but unused; NO `withdrawn` case shipped per D-06 deferral). Add static method `public static function isReserved(self $case): bool` returning true for PendingApproval/Approved (D-05 + D-06 reserved-but-unused).
    2. Create `app/Domain/Quotes/Enums/RejectionReason.php` as `enum RejectionReason: string` with the 5 D-08 cases.
    3. Create `app/Domain/Quotes/Models/Quote.php`:
       - `namespace App\Domain\Quotes\Models;`
       - Use `HasUlids, HasFactory, LogsActivity` traits.
       - Define 7 STATUS_* string constants matching enum (mirror Phase 8 AgentRun pattern of constants alongside enum for backwards compat with raw-string queries).
       - $fillable per Quote model spec in &lt;behavior&gt;.
       - $casts: billing_address=>array, rejection_metadata=>array, expires_at/sent_at/accepted_at/rejected_at/expired_at=>datetime, total_pence_at_quote=>integer.
       - `protected $keyType = 'string'; public $incrementing = false;` (HasUlids requirement).
       - `getActivitylogOptions()` returns `LogOptions::defaults()->logOnly(['status','sent_at','accepted_at','rejected_at','expired_at','total_pence_at_quote'])->logOnlyDirty();`
       - lines/customer/customerGroup relations + ulidShort method.
       - `protected static function newFactory() { return \Database\Factories\Domain\Quotes\QuoteFactory::new(); }`
    4. Create `app/Domain/Quotes/Models/QuoteLine.php`:
       - HasUlids + HasFactory traits.
       - $fillable: ['quote_id','sku','quantity_int','unit_price_pence_at_quote','line_total_pence_at_quote','product_snapshot','sort_order'].
       - $casts per spec.
       - quote() relation.
    5. Create `app/Domain/Quotes/Policies/QuotePolicy.php` + `QuoteLinePolicy.php`:
       - Standard 7 methods (viewAny/view/create/update/delete/restore/forceDelete) + approve/revert/markAccepted/markRejected (Quote only).
       - approve denies sales explicitly per D-04. Inline comment cites D-04.
    6. Create factories `database/factories/Domain/Quotes/QuoteFactory.php` + `QuoteLineFactory.php` with sensible defaults (status=draft, expires_at=now()+14d).
    7. Create `config/quote.php`:
       ```php
       return [
           'company_name' => 'MeetingStore Limited',
           'company_address' => "Suite 8, 110 Bishopsgate\nLondon EC2N 4AD",
           'company_vat_number' => env('COMPANY_VAT_NUMBER', 'GB123456789'),
           'company_registration_number' => env('COMPANY_REGISTRATION_NUMBER', '12345678'),
           'default_expiry_days' => env('QUOTE_DEFAULT_EXPIRY_DAYS', 14),
           'email_on_expiry' => env('QUOTE_EMAIL_ON_EXPIRY', false),
           'pdf_signature_block' => env('QUOTE_PDF_SIGNATURE_BLOCK', false),
           'bitrix_deal_type_id' => env('QUOTE_BITRIX_DEAL_TYPE_ID', 'QUOTE'),
           'bitrix_push_enabled' => env('QUOTE_BITRIX_PUSH_ENABLED', false),
           'bitrix_quote_type_verified' => env('QUOTE_BITRIX_TYPE_VERIFIED', false),  // Plan 11-05 cutover gate
       ];
       ```
    8. Append to `.env.example`:
       ```
       # Phase 11 — Quote → Bitrix Deal flow
       QUOTE_BITRIX_PUSH_ENABLED=false       # SHADOW-MODE GATE — DO NOT enable until bitrix:quotes-bootstrap PASSES
       QUOTE_BITRIX_TYPE_VERIFIED=false      # Cutover gate (Plan 11-05) — flipped TRUE by ops after Bitrix dealtype verified
       QUOTE_DEFAULT_EXPIRY_DAYS=14
       QUOTE_EMAIL_ON_EXPIRY=false
       QUOTE_PDF_SIGNATURE_BLOCK=false
       QUOTE_BITRIX_DEAL_TYPE_ID=QUOTE
       ```
    9. EDIT `depfile.yaml`:
       - Add new layer block `Quotes` directory regex `app/Domain/Quotes/.*` (insert after TradePricing layer block, before Foundation).
       - In ruleset block: add `Quotes: [Foundation, Products, Pricing, TradePricing, Suggestions, CRM, Webhooks]` (insert after `TradePricing: [...]`).
       - EXTEND existing `CRM: [...]` ruleset to add `Quotes` at end → `CRM: [Foundation, Sync, Alerting, Webhooks, Suggestions, Agents, Quotes]`. Add deviation comment block above the line citing Phase 11 Plan 11-04 listener-based-extension precedent (one-way arrow CRM→Quotes; Quotes→CRM remains denied).
       - EXTEND `Http: [...]` ruleset to include `Quotes` at end (Filament QuoteResource lives under Http surface).
    10. EDIT `deptrac.yaml`: mirror identical changes to depfile.yaml (Phase 5 P05-05 dual-YAML lockstep — both files MUST stay in sync).
    11. Create `tests/Architecture/DeptracQuotesLayerTest.php` cloning DeptracTradePricingLayerTest:
        - Test 1: Both YAMLs register Quotes layer with directory regex `app/Domain/Quotes/.*`.
        - Test 2: Both YAMLs declare ruleset `Quotes: [Foundation, Products, Pricing, TradePricing, Suggestions, CRM, Webhooks]`.
        - Test 3: Denial list — Quotes ruleset MUST NOT contain Agents, Competitor, ProductAutoCreate, Sync, Cutover, Marketing, Channels.
        - Test 4: `Process` runs `php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml` AND deptrac.yaml — assert exit code 0.
    12. Extend `tests/Unit/Domain/Quotes/Models/QuoteTest.php` with model behaviour tests per &lt;behavior&gt;.
    13. Create `tests/Unit/Domain/Quotes/Models/QuoteLineTest.php` per &lt;behavior&gt;.
  </action>
  <verify>
    <automated>php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml --no-progress 2>&amp;1 | tail -5</automated>
    <automated>php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=deptrac.yaml --no-progress 2>&amp;1 | tail -5</automated>
    <automated>php artisan test tests/Architecture/DeptracQuotesLayerTest.php</automated>
    <automated>php artisan test tests/Unit/Domain/Quotes/</automated>
    <automated>grep -n "name: Quotes" depfile.yaml deptrac.yaml</automated>
    <automated>grep -n "Quotes:.*Foundation.*Products.*Pricing.*TradePricing.*Suggestions.*CRM.*Webhooks" depfile.yaml deptrac.yaml</automated>
    <automated>grep -n "QUOTE_BITRIX_PUSH_ENABLED" .env.example</automated>
  </verify>
  <done>Models + enums + policies + factories created. Quotes Deptrac layer registered in BOTH YAMLs (verified by grep). Both deptrac analyse runs exit 0. DeptracQuotesLayerTest 4/4 PASS. Model unit tests PASS. config/quote.php + .env.example contain all 7 keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filament UI → Quote model | Authenticated admin/pricing_manager/sales submits Quote create/edit forms — Filament Form validation + Policy authorization at the boundary |
| Filament UI → QuoteLine model | Same actor — but QuoteLine has D-13 immutability invariant (Plan 11-02 observer enforces) |
| BitrixEntityMap UNIQUE index | Database-level dedup boundary — UNIQUE(entity_type, quote_id) prevents Plan 11-04 race condition where two simultaneous push jobs attempt to insert the same quote_deal row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | quote_lines.unit_price_pence_at_quote | mitigate | Column docblock + Plan 11-02 immutability observer; Pest test asserts integer cast (Pitfall 1) |
| T-11-01-02 | Tampering | bitrix_entity_map dedup ledger | mitigate | UNIQUE(entity_type, quote_id) + UNIQUE(entity_type, woo_id) coexist; Plan 11-04 always queries via scopeForQuote/scopeForWooOrder (Pitfall 3) |
| T-11-01-03 | Elevation | QuotePolicy::approve | mitigate | D-04 separation-of-duties — sales role explicitly denied; Pest test in Plan 11-03 asserts approve action 403s for sales-role user |
| T-11-01-04 | Information Disclosure | quotes.billing_address JSON | mitigate | PII protected by Filament policy `view`/`viewAny` gates; existing `gdpr:erase-bitrix-customer` extension to scrub Quote PII deferred per CONTEXT.md to v1.x (logged in deferred-items.md) |
| T-11-01-05 | Repudiation | quotes status transitions | mitigate | LogsActivity trait auto-captures status changes via Phase 1 activity_log; status-timestamp columns (sent_at/accepted_at/etc.) preserve transition moments without scanning activity_log |
| T-11-01-06 | Spoofing | quotes.user_id mass-assignment | mitigate | Quote::$fillable includes user_id; Filament form's user picker is permission-gated in Plan 11-03 (Filament Action `->visible(fn() => $user->hasRole('admin'))` for setting arbitrary user_id); non-admin sales cannot pick foreign users |
| T-11-01-07 | Denial of Service | bulk Quote creation via API | accept | No public Quote endpoint in Phase 11 (D-09 — zero public-facing URLs); Filament admin-only is rate-limited by Laravel session middleware |
</threat_model>

<verification>
- `php artisan migrate --pretend` shows all 4 migrations as fresh creates
- `php artisan migrate` (against SQLite test DB or MySQL when online) succeeds
- `php artisan test tests/Architecture/DeptracQuotesLayerTest.php` 4/4 PASS
- `php artisan test tests/Unit/Domain/Quotes/` ≥6 PASS (3 schema + 3 model + 3 model behaviour)
- `php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml` exits 0
- `php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=deptrac.yaml` exits 0
- `grep -c "ENTITY_QUOTE_DEAL\|receives_quote_alerts\|name: Quotes\|QUOTE_BITRIX_PUSH_ENABLED"` returns ≥4 across modified files
</verification>

<success_criteria>
- 4 migrations created (quotes + quote_lines + bitrix_entity_map.quote_id + alert_recipients.receives_quote_alerts) — all applied without error
- Quote + QuoteLine + 2 enums + 2 policies created with HasUlids + LogsActivity per spec
- BitrixEntityMap extended with ENTITY_QUOTE_DEAL + scopes
- AlertRecipient extended with receives_quote_alerts
- config/quote.php + .env.example contain QUOTE_BITRIX_PUSH_ENABLED + QUOTE_BITRIX_TYPE_VERIFIED + 5 other keys
- depfile.yaml + deptrac.yaml BOTH contain Quotes layer + ruleset + extended CRM/Http allow-lists (dual-YAML lockstep)
- DeptracQuotesLayerTest 4/4 PASS; both deptrac analyse runs exit 0
- All model unit tests PASS
- QUOT-01 (Quote schema) + QUOT-02 (QuoteLine schema + immutability column setup) covered
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-01-SUMMARY.md` documenting:
- 4 migration filenames + applied state (use `php artisan migrate:status | grep 2026_05_01` output)
- BitrixEntityMap + AlertRecipient extension diff hashes
- Quotes Deptrac layer line numbers in BOTH YAMLs (for Plan 11-05 dual-config verification)
- A1 RESOLUTION: VAT-INCLUSIVE storage convention locked (column comment + docblock); PDF strips VAT at render time in Plan 11-04
- A2 RESOLUTION: BitrixEntityMap.entity_type column type verified VARCHAR (no migration needed); new value 'quote_deal' accepted
- OQ-2 RESOLUTION: Option (a) chosen — added nullable quote_id ULID column + composite UNIQUE(entity_type, quote_id) index; existing UNIQUE(entity_type, woo_id) coexists
- OQ-1 RESOLUTION: total_pence_at_quote cached column added per RESEARCH recommendation; Plan 11-02 ships the recompute-on-save observer
- A9 RESOLUTION: customer_group_name_at_quote denormalised string column included (CONTEXT.md Claude's Discretion)
- Open items deferred to Plan 11-02 (PriceSnapshotter immutability observer + QuotePdfPriceImmunityTest regression test)
</output>

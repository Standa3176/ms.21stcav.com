---
phase: 11-e2-quote-request-bitrix-deal-flow
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - app/Domain/Quotes/Filament/Resources/QuoteResource.php
  - app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/ListQuotes.php
  - app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/CreateQuote.php
  - app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/EditQuote.php
  - app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/ViewQuote.php
  - app/Domain/Quotes/Filament/Resources/QuoteResource/RelationManagers/QuoteLinesRelationManager.php
  - app/Domain/Quotes/Filament/Actions/ApproveQuoteAction.php
  - app/Domain/Quotes/Filament/Actions/RevertQuoteAction.php
  - app/Domain/Quotes/Filament/Actions/MarkAcceptedAction.php
  - app/Domain/Quotes/Filament/Actions/MarkRejectedAction.php
  - database/seeders/RolePermissionSeeder.php
  - tests/Feature/Filament/QuoteResourceTest.php
  - tests/Feature/Filament/Actions/ApproveQuoteActionTest.php
  - tests/Feature/Filament/Actions/MarkRejectedActionTest.php
autonomous: false
requirements: [QUOT-01, QUOT-02, QUOT-03]
must_haves:
  truths:
    - "QuoteResource lives at app/Domain/Quotes/Filament/Resources/QuoteResource.php; navigation group 'Sales' (new top-level — first member); admin + pricing_manager + sales roles get viewAny permission"
    - "QuoteResource Create form: Toggle 'Existing customer?' (D-03) — toggled ON reveals user picker (Select::searchable against users.email + auto-fills customer_email/customer_name/customer_group_id from selected user); toggled OFF reveals free-text customer_email + customer_name + billing_address Repeater + manual customer_group_id Select"
    - "QuoteResource Create form persists customer_group_name_at_quote denormalised string at save time (reads CustomerGroup::find($customer_group_id)?->name and stores literal value — D-02 + Pitfall 6)"
    - "QuoteLinesRelationManager: line table with sku/quantity_int/unit_price_pence_at_quote (formatted £X.XX inc-VAT label)/line_total_pence_at_quote columns; Add action shows Select::searchable on products.sku|name|brand (≥3 chars debounced) — search-and-add picker is the PRIMARY input path per D-10 — with manual SKU TextInput fallback for catalogue-gap cases (D-10 fallback path); + quantity_int input — submits via QuoteLineWriter::add per Plan 11-02 (single creation path)"
    - "QuoteLinesRelationManager hides Add/Edit/Delete row actions when parent Quote.status !== draft (mirrors D-13 immutability — UI matches model invariant)"
    - "ApproveQuoteAction: visible only when status===draft; ->authorize('approve') gates to QuotePolicy::approve (sales explicitly denied per D-04); on confirm performs DB::transaction(status flip + sent_at=now() + QuoteApproved event::dispatch + QuoteSentMail dispatched + Auditor row); successFlash 'Quote sent — Bitrix push and customer email queued'"
    - "RevertQuoteAction: visible only when status===sent AND admin role AND sent_at within last 5 minutes (D-05); flips status back to draft + nulls sent_at + audit row"
    - "MarkAcceptedAction: visible only when status===sent; admin/pricing_manager/sales roles allowed; flips status=accepted + accepted_at=now() + optional QuoteAccepted event — implements D-07 manual sales accept mechanism (v1 only; no automatic web/email accept flow)"
    - "MarkRejectedAction: visible only when status===sent; modal Form prompts RejectionReason Select (5 D-08 cases) + optional notes Textarea; persists rejection_metadata JSON {reason, notes, rejected_by_user_id, rejected_at} + flips status=rejected + rejected_at=now() + optional QuoteRejected event — implements D-07 manual sales reject mechanism (v1 only)"
    - "Every Filament Action calls ->authorize('<ability>') server-side per Pitfall 7 — visible() is UI-only, authorize() enforces at handler"
    - "RolePermissionSeeder extended with quote_* Shield permissions: view_any_quote / view_quote / create_quote / update_quote / delete_quote / approve_quote / revert_quote / mark_accepted_quote / mark_rejected_quote — admin gets all; pricing_manager gets all EXCEPT delete; sales gets viewAny/view/create/update/markAccepted/markRejected (NOT approve/revert/delete); read_only gets viewAny/view only"
    - "shield:safe-regenerate executed; PolicyTemplateIntegrityTest floor advanced from 27 to 29 (Quote + QuoteLine policies); P5-F restoration verified per Phase 8 wrapper"
    - "QuoteResourceTest covers: viewAny per role (admin/pricing_manager/sales yes; read_only/anonymous no), create form Toggle behaviour, line repeater uses QuoteLineWriter, customer_group_name_at_quote persisted on save"
    - "ApproveQuoteActionTest covers: sales role gets 403 server-side even when calling action handler directly (Pitfall 7), admin/pricing_manager succeed, status flips draft→sent, sent_at is non-null, QuoteApproved event dispatched (Event::fake assertion), email Mailable queued"
    - "MarkRejectedActionTest covers: rejection_metadata persisted with all 4 keys, status===rejected post-action, rejected_at is non-null"
  artifacts:
    - path: "app/Domain/Quotes/Filament/Resources/QuoteResource.php"
      provides: "Filament 3 Resource — Sales nav group + form + table + 4 row actions"
      contains: "navigationGroup = 'Sales'"
      min_lines: 80
    - path: "app/Domain/Quotes/Filament/Resources/QuoteResource/RelationManagers/QuoteLinesRelationManager.php"
      provides: "Line repeater UI delegating to QuoteLineWriter; hides actions post-draft"
      contains: "QuoteLineWriter"
    - path: "app/Domain/Quotes/Filament/Actions/ApproveQuoteAction.php"
      provides: "draft→sent atomic transaction + event dispatch + email + audit"
      contains: "QuoteApproved::dispatch"
      min_lines: 30
    - path: "app/Domain/Quotes/Filament/Actions/MarkRejectedAction.php"
      provides: "Modal form with RejectionReason enum + notes; persists rejection_metadata JSON"
      contains: "rejection_metadata"
    - path: "database/seeders/RolePermissionSeeder.php"
      provides: "Phase 11 9 quote_* permissions wired to admin/pricing_manager/sales/read_only"
      contains: "approve_quote"
    - path: "tests/Feature/Filament/Actions/ApproveQuoteActionTest.php"
      provides: "Pitfall 7 enforcement test — sales 403s even with direct action handler call"
      contains: "Pitfall 7"
      min_lines: 40
  key_links:
    - from: "QuoteLinesRelationManager Add action"
      to: "QuoteLineWriter::add"
      via: "Service injection — UI form submit calls writer; observer chain (Plan 11-02) handles immutability + total recompute"
      pattern: "QuoteLineWriter"
    - from: "ApproveQuoteAction"
      to: "QuoteApproved domain event"
      via: "DB::transaction wrapping status flip + event::dispatch (Plan 11-04 listener catches)"
      pattern: "QuoteApproved::dispatch"
    - from: "ApproveQuoteAction"
      to: "QuotePolicy::approve"
      via: "->authorize('approve_quote') server-side (Pitfall 7)"
      pattern: "authorize\\('approve_quote'\\)"
    - from: "RolePermissionSeeder"
      to: "shield:safe-regenerate output"
      via: "9 quote_* permissions seeded via Permission::firstOrCreate + role assignment per RBAC matrix"
      pattern: "approve_quote|revert_quote|mark_accepted_quote|mark_rejected_quote"
---

<objective>
Plan 11-03 ships the **operator surface** for Phase 11 — the Filament `QuoteResource` (admin + pricing_manager + sales CRUD scoped to roles), the QuoteLinesRelationManager (search-and-add SKU picker per D-10 reusing Phase 6 ProductResource pattern), 4 Filament Actions implementing the D-04/D-05/D-06/D-07/D-08 state machine (Approve / Revert / MarkAccepted / MarkRejected) with mandatory `->authorize()` gates per Pitfall 7, the RolePermissionSeeder extension wiring 9 quote_* Shield permissions across 4 roles, the `shield:safe-regenerate` execution that lifts the PolicyTemplateIntegrityTest floor from 27 → 29, and 3 Feature tests proving the role boundaries (Pitfall 7 server-side enforcement test is the headline).

Purpose: This is the human-facing plan — every quote in production goes through this UI. The state-machine actions encode D-04 (sales cannot approve own quote — separation of duties), D-05 (5-minute revert window), D-08 (structured rejection reason capture). The QuoteLinesRelationManager defers row creation to QuoteLineWriter (Plan 11-02) so the immutability observer chain runs through the same code path on every line write — UI parity with service layer.

Output: 1 Resource + 4 Pages + 1 RelationManager + 4 Filament Actions + RolePermissionSeeder edit + shield:safe-regenerate execution + 3 Feature tests. Includes 1 checkpoint:human-verify task at the end so the operator can sanity-check the UI flow against a live MySQL + Filament before signing off.

Net stack delta: Zero composer changes. Pure Filament + RBAC additions atop Plan 11-01/02 services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-CONTEXT.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-RESEARCH.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-01-PLAN.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-02-PLAN.md
@.planning/phases/09-e1-trade-customer-pricing/09-05-SUMMARY.md
@.planning/phases/08-c4-agent-framework/08-05-SUMMARY.md
@app/Domain/Quotes/Models/Quote.php
@app/Domain/Quotes/Services/QuoteLineWriter.php
@app/Domain/Quotes/Policies/QuotePolicy.php
@database/seeders/RolePermissionSeeder.php
@app/Domain/TradePricing/Filament/Resources/CustomerGroupResource.php
@app/Domain/Products/Filament/Resources/ProductResource.php

<interfaces>
<!-- Phase 8 + Phase 9 contracts the executor uses verbatim. -->

From app/Domain/Agents/Console/Commands/ShieldSafeRegenerateCommand.php (Phase 8 Plan 05 — AGNT-11):
```php
// CLI: php artisan shield:safe-regenerate [--allow-new=ResourceName] [--restore]
// Documented in docs/ops/shield-regeneration.md.
// Plan 11-03 invokes via:
//   php artisan shield:safe-regenerate --allow-new=QuoteResource
```

From database/seeders/RolePermissionSeeder.php (Phase 9 Plan 05 last-modified — verify current shape):
```php
// Existing pattern uses Permission::firstOrCreate(['name' => '...', 'guard_name' => 'web']);
// Then $role->givePermissionTo($permission);
// Phase 11 ADDS 9 quote_* permissions + RBAC matrix in &lt;behavior&gt;.
```

From app/Domain/Products/Filament/Resources/ProductResource.php (Phase 6 SKU search precedent — verify current Select::searchable() pattern):
```php
// Phase 6 ProductResource ships a Select::searchable() against products.sku|name|brand
// Phase 11 QuoteLinesRelationManager REUSES this exact searchable query shape (no new search infrastructure).
```

From app/Domain/Quotes/Services/QuoteLineWriter.php (Plan 11-02 — verified):
```php
final class QuoteLineWriter
{
    public function add(Quote $quote, string $sku, int $quantity): QuoteLine
    // sole creation path — observer chain (immutability + total recompute) runs on the QuoteLine::create call inside
}
```

From app/Domain/Quotes/Models/Quote.php STATUS_* constants (Plan 11-01):
```php
Quote::STATUS_DRAFT  Quote::STATUS_SENT  Quote::STATUS_ACCEPTED  Quote::STATUS_REJECTED
```

From tests/Architecture/PolicyTemplateIntegrityTest.php (Phase 1 + Phase 8 P05-F precedent):
```php
// Asserts every Resource has a corresponding Policy with no {{ Placeholder }} literals.
// Currently asserts FLOOR=27 policies. Plan 11-03 ADVANCES floor to 29 (QuotePolicy + QuoteLinePolicy).
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuoteResource + 4 Pages + QuoteLinesRelationManager + 4 Filament Actions + role permission seeder + shield:safe-regenerate</name>
  <files>app/Domain/Quotes/Filament/Resources/QuoteResource.php, app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/ListQuotes.php, app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/CreateQuote.php, app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/EditQuote.php, app/Domain/Quotes/Filament/Resources/QuoteResource/Pages/ViewQuote.php, app/Domain/Quotes/Filament/Resources/QuoteResource/RelationManagers/QuoteLinesRelationManager.php, app/Domain/Quotes/Filament/Actions/ApproveQuoteAction.php, app/Domain/Quotes/Filament/Actions/RevertQuoteAction.php, app/Domain/Quotes/Filament/Actions/MarkAcceptedAction.php, app/Domain/Quotes/Filament/Actions/MarkRejectedAction.php, database/seeders/RolePermissionSeeder.php, tests/Feature/Filament/QuoteResourceTest.php, tests/Feature/Filament/Actions/ApproveQuoteActionTest.php, tests/Feature/Filament/Actions/MarkRejectedActionTest.php</files>
  <read_first>app/Domain/Products/Filament/Resources/ProductResource.php (search-and-add Select::searchable pattern from Phase 6 — verify exact query shape against products.sku|name|brand), app/Domain/TradePricing/Filament/Resources/CustomerGroupResource.php (Phase 9 Resource pattern + nav group precedent), database/seeders/RolePermissionSeeder.php (Phase 9 P05 last edit — verify Permission::firstOrCreate + role assignment shape), app/Domain/Agents/Filament/Resources/AgentRunResource.php (Phase 8 Resource — Pages + Actions structure)</read_first>
  <behavior>
    - QuoteResource:
      - `protected static ?string $navigationGroup = 'Sales';`
      - `protected static ?string $navigationIcon = 'heroicon-o-document-text';`
      - `protected static ?int $navigationSort = 10;`
      - `protected static ?string $model = Quote::class;`
      - `protected static ?string $modelLabel = 'Quote';`
      - `protected static ?string $pluralModelLabel = 'Quotes';`
      - form() per &lt;behavior&gt; D-03 toggle pattern
      - table() with columns: ulid_short_8, customer_email, status (badge with color per status), customer_group_name_at_quote, total_pence_at_quote (formatted £X.XX), expires_at, created_at; filters: status (multi-select), customer_group_id, expires_at (range)
      - getRelations() returns [QuoteLinesRelationManager::class]
      - getPages() returns 4 routes (index/create/edit/view)
      - getEloquentQuery() defaults to whereNotNull('id') (no soft-delete inclusion)
    - QuoteLinesRelationManager:
      - `protected static ?string $title = 'Lines';`
      - table columns: sku, quantity_int, unit_price_pence_at_quote (formatted £X.XX inc VAT), line_total_pence_at_quote (formatted £X.XX inc VAT), product_snapshot.brand (extracted from JSON)
      - Add action: `Action::make('add_line')` with Form: Select::make('sku')->searchable()->getSearchResultsUsing(fn (string $search) => Product::where(...)->limit(25)->pluck(name||brand, sku)) reusing Phase 6 ProductResource shape, TextInput::make('quantity_int')->numeric()->minValue(1)->maxValue(9999)->default(1); on submit calls `app(QuoteLineWriter::class)->add($this->ownerRecord, $data['sku'], $data['quantity_int'])`
      - Add action visibility: `->visible(fn () => $this->ownerRecord->status === Quote::STATUS_DRAFT)` AND `->authorize('update', $this->ownerRecord)`
      - Edit/Delete actions on each row: `->visible(fn ($record) => $record->quote->status === Quote::STATUS_DRAFT)`; Edit form allows quantity_int change ONLY (price/snapshot fields hidden); save delegates to $line->update(['quantity_int' => $data['quantity_int']]) — observer chain handles line_total recompute + total recompute
      - Manual SKU fallback: TextInput::make('sku_manual')->visible(fn ($get) => empty($get('sku')))->helperText('SKU not in catalog (Phase 6 auto-create gap fallback)')
    - ApproveQuoteAction:
      - `Action::make('approve')`
      - `->visible(fn (Quote $record) => $record->status === Quote::STATUS_DRAFT)`
      - `->authorize('approve', $record)` — calls QuotePolicy::approve which DENIES sales (D-04)
      - `->requiresConfirmation()->modalHeading('Approve quote and send to customer?')->modalDescription(fn (Quote $record) => 'This will: (1) flip status to sent, (2) email PDF to '.$record->customer_email.', (3) queue Bitrix Deal push (gated by QUOTE_BITRIX_PUSH_ENABLED)')`
      - `->action(function (Quote $record) { ... DB::transaction(...) ... })`:
        ```php
        DB::transaction(function () use ($record) {
            $correlationId = (string) Str::ulid();
            $record->update([
                'status' => Quote::STATUS_SENT,
                'sent_at' => now(),
                'correlation_id' => $correlationId,
            ]);
            QuoteApproved::dispatch(
                $record->id, $record->user_id, $record->customer_email,
                $record->customer_group_id, Quote::STATUS_DRAFT, Quote::STATUS_SENT, $correlationId,
            );
            Mail::to($record->customer_email)->queue(new QuoteSentMail($record));
        });
        Notification::make()->success()->title('Quote sent — Bitrix push + customer email queued')->send();
        ```
      - Note: `QuoteApproved` event class + `QuoteSentMail` Mailable come from Plan 11-04 — Plan 11-03 stubs them as commented placeholders if Plan 11-04 hasn't shipped them yet. Action is wired but full Plan 11-04 plumbing is its own task.
    - RevertQuoteAction:
      - visible: `fn (Quote $r) => $r->status === Quote::STATUS_SENT && auth()->user()->hasRole('admin') && $r->sent_at?->diffInMinutes(now()) < 5`
      - `->authorize('revert', $record)`
      - action: $record->update(['status' => Quote::STATUS_DRAFT, 'sent_at' => null])
    - MarkAcceptedAction:
      - visible: `fn (Quote $r) => $r->status === Quote::STATUS_SENT`
      - `->authorize('markAccepted', $record)`
      - action: $record->update(['status' => Quote::STATUS_ACCEPTED, 'accepted_at' => now()])
    - MarkRejectedAction:
      - visible: `fn (Quote $r) => $r->status === Quote::STATUS_SENT`
      - `->authorize('markRejected', $record)`
      - Form modal: Select::make('reason')->options(RejectionReason::class)->required(), Textarea::make('notes')->maxLength(2000)
      - action: $record->update(['status' => Quote::STATUS_REJECTED, 'rejected_at' => now(), 'rejection_metadata' => ['reason' => $data['reason'], 'notes' => $data['notes'] ?? null, 'rejected_by_user_id' => auth()->id(), 'rejected_at' => now()->toIso8601String()]])
    - RolePermissionSeeder extension RBAC matrix:
      | Permission | admin | pricing_manager | sales | read_only |
      |-----------|-------|-----------------|-------|-----------|
      | view_any_quote | ✓ | ✓ | ✓ | ✓ |
      | view_quote | ✓ | ✓ | ✓ | ✓ |
      | create_quote | ✓ | ✓ | ✓ | ✗ |
      | update_quote | ✓ | ✓ | ✓ | ✗ |
      | delete_quote | ✓ | ✗ | ✗ | ✗ |
      | approve_quote | ✓ | ✓ | ✗ | ✗ |
      | revert_quote | ✓ | ✗ | ✗ | ✗ |
      | mark_accepted_quote | ✓ | ✓ | ✓ | ✗ |
      | mark_rejected_quote | ✓ | ✓ | ✓ | ✗ |
  </behavior>
  <action>
    1. Create `app/Domain/Quotes/Filament/Resources/QuoteResource.php` per &lt;behavior&gt; spec. Build form with Section('Customer'): Toggle('existing_customer')->live()->dehydrated(false), then conditional fields per D-03 toggle behaviour.
    2. Create the 4 Pages (`Pages/ListQuotes.php`, `CreateQuote.php`, `EditQuote.php`, `ViewQuote.php`) — standard Filament 3 page classes extending `ListRecords|CreateRecord|EditRecord|ViewRecord`.
    3. Create `RelationManagers/QuoteLinesRelationManager.php` per &lt;behavior&gt; spec. Add action injects QuoteLineWriter via app() helper and calls add().
    4. Create the 4 Action classes in `app/Domain/Quotes/Filament/Actions/`. Each is a self-contained `class XxxAction extends Filament\Tables\Actions\Action` (or HeaderAction for ApproveQuoteAction at table level — verify Filament 3 semantics).
    5. EDIT `database/seeders/RolePermissionSeeder.php`:
       - Add 9 quote_* permissions block per RBAC matrix in &lt;behavior&gt;.
       - Use existing Phase 9 pattern: `Permission::firstOrCreate(['name' => '...', 'guard_name' => 'web']);`
       - Assign per role: `$adminRole->givePermissionTo([...]); $pricingManagerRole->givePermissionTo([...]); ...`
    6. Run `php artisan db:seed --class=RolePermissionSeeder` (idempotent — firstOrCreate).
    7. Run `php artisan shield:safe-regenerate --allow-new=QuoteResource` per Phase 8 wrapper. Verify P5-F restoration in output.
    8. Verify `php artisan test tests/Architecture/PolicyTemplateIntegrityTest.php` — assert floor advanced to ≥29 (Quote + QuoteLine policies have no `{{ Placeholder }}` residue).
    9. Create `tests/Feature/Filament/QuoteResourceTest.php`:
       - it('viewAny passes for admin/pricing_manager/sales')
       - it('viewAny denies anonymous + read_only')
       - it('create form Toggle reveals user picker when ON, free-text fields when OFF')
       - it('create persists customer_group_name_at_quote denormalised string')
       - it('line repeater Add action calls QuoteLineWriter::add')
    10. Create `tests/Feature/Filament/Actions/ApproveQuoteActionTest.php`:
        - it('sales role gets 403 from QuotePolicy::approve even when invoking action handler directly') — Pitfall 7 hardening
        - it('admin succeeds — status flips to sent + sent_at non-null')
        - it('pricing_manager succeeds — status flips to sent + sent_at non-null')
        - it('dispatches QuoteApproved event on approve') — Event::fake assertion (event class may be a stub from this plan; Plan 11-04 wires the listener)
        - it('queues QuoteSentMail on approve') — Mail::fake assertion
    11. Create `tests/Feature/Filament/Actions/MarkRejectedActionTest.php`:
        - it('persists rejection_metadata with reason + notes + rejected_by_user_id + rejected_at on submit')
        - it('flips status to rejected + rejected_at non-null')
        - it('rejects when status===draft (visibility blocks action)')
    12. STUB QuoteApproved event in `app/Domain/Quotes/Events/QuoteApproved.php` (full implementation in Plan 11-04 — readonly DTO is enough here so Action can dispatch it). STUB QuoteSentMail in `app/Domain/Quotes/Mail/QuoteSentMail.php` (Mailable shell — full PDF attachment wiring in Plan 11-04).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Filament/QuoteResourceTest.php</automated>
    <automated>php artisan test tests/Feature/Filament/Actions/</automated>
    <automated>php artisan test tests/Architecture/PolicyTemplateIntegrityTest.php</automated>
    <automated>grep -n "approve_quote\|revert_quote\|mark_accepted_quote\|mark_rejected_quote" database/seeders/RolePermissionSeeder.php</automated>
    <automated>grep -n "navigationGroup = 'Sales'" app/Domain/Quotes/Filament/Resources/QuoteResource.php</automated>
    <automated>php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml --no-progress 2>&amp;1 | tail -3</automated>
  </verify>
  <done>QuoteResource + 4 Pages + QuoteLinesRelationManager + 4 Actions created. RolePermissionSeeder seeded 9 perms + role assignments. shield:safe-regenerate executed cleanly with P5-F restoration. PolicyTemplateIntegrityTest floor advanced to 29. QuoteResourceTest + 2 Action tests PASS. Pitfall 7 (sales 403 on direct handler call) verified by ApproveQuoteActionTest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator sanity-check QuoteResource UI flow + RBAC + line picker</name>
  <files>(no file changes — operator verification only)</files>
  <action>PAUSE EXECUTION. Present the &lt;how-to-verify&gt; checklist to the operator. Wait for the operator to complete the 14-step UAT against a live Filament admin panel + MySQL database. Do NOT proceed to Plan 11-04 until the operator types the resume-signal.</action>
  <what-built>Filament QuoteResource under "Sales" nav group with: Create form (Existing customer? toggle), QuoteLines repeater with search-and-add SKU picker reusing Phase 6 ProductResource search, 4 state-machine actions (Approve/Revert/MarkAccepted/MarkRejected), 9 quote_* Shield permissions seeded across 4 roles, PolicyTemplateIntegrityTest floor advanced to 29.</what-built>
  <how-to-verify>
    Sign in as admin user (or run `php artisan tinker` and create one):
    1. Visit `/admin/quotes` — should see empty list under "Sales" navigation group (left sidebar)
    2. Click "Create" — verify "Existing customer?" toggle is present at top of form
    3. Toggle ON → verify user picker (searchable) appears + free-text fields hide
    4. Toggle OFF → verify free-text customer_email + customer_name + billing_address Repeater + customer_group_id Select appear
    5. Fill form (free-text mode), select customer_group=Trade, save → quote created with status=draft
    6. On Edit page, click "Lines" tab → click "Add line" → verify Select::searchable on SKU works (type ≥3 chars; matches appear) → set quantity 2 → submit → line appears with auto-resolved unit_price (formatted £X.XX inc VAT) + line_total = unit * 2 + total at top of page updates
    7. Verify Edit/Delete row actions are VISIBLE (status=draft); change quantity → line_total recomputes
    8. Click "Approve" header action — verify confirmation modal with customer_email + warning text; cancel for now
    9. Sign out + sign in as a SALES user (create one if needed): visit same quote — verify "Approve" button is NOT visible (D-04 separation of duties)
    10. Sign back in as admin → click Approve → verify status flips to "sent", sent_at populates, success notification "Quote sent — Bitrix push + customer email queued" shows
    11. Verify "Revert" appears (within 5 min) + "Mark Accepted" + "Mark Rejected" appear
    12. Click "Mark Rejected" → verify modal Form with Select reason (5 options) + Textarea notes; submit with reason=competitor_won + notes "test" → verify status=rejected, rejection_metadata JSON populated correctly in database
    13. Verify Add/Edit/Delete line actions are HIDDEN once status != draft (UI matches D-13 model invariant)
    14. Run `php artisan shield:safe-regenerate --restore` to confirm wrapper restoration works on second invocation (idempotent)

    PASS criteria:
    - All 14 steps behave as specified
    - No 500 errors in storage/logs/laravel.log during the flow
    - PolicyTemplateIntegrityTest still 29/29 PASS

    KNOWN LIMITATIONS (acceptable for sign-off):
    - PDF email is queued but not yet rendered to actual PDF (Plan 11-04 ships PDF + Mailable body)
    - QuoteApproved event fires but no Bitrix push happens yet (Plan 11-04 wires listener + job)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 11-03 complete and continue to Plan 11-04. If issues found, describe them so the executor can iterate before sign-off.</resume-signal>
  <verify>
    <automated>echo "Operator UAT — manual verification per &lt;how-to-verify&gt; checklist; no automated check"</automated>
  </verify>
  <done>Operator typed "approved" after completing all 14 UAT steps; any issues raised + resolved before sign-off; PolicyTemplateIntegrityTest still 29/29 PASS post-shield-regen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filament Action handler entry | Authenticated session crosses into action handler — visibility() is UI hint, authorize() is the actual gate (Pitfall 7) |
| Filament Resource viewAny | Role-based authorization via QuotePolicy::viewAny; Shield permissions seeded by RolePermissionSeeder are the binding |
| QuoteLinesRelationManager Add action | UI form input → QuoteLineWriter → observer chain → DB; SKU validation via Rule::exists('products', 'sku') prevents arbitrary SKU injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Elevation | ApproveQuoteAction handler called directly by sales role | mitigate | ->authorize('approve', $record) gate calls QuotePolicy::approve which checks role NOT user_id; ApproveQuoteActionTest specifically asserts 403 for sales (Pitfall 7) |
| T-11-03-02 | Tampering | QuoteLine quantity_int change after sent | mitigate | UI hides Edit action when status != draft (UI parity) + Plan 11-02 observer throws QuoteLineImmutableException at model layer (defence in depth) |
| T-11-03-03 | Spoofing | Sales user picks arbitrary user_id in Quote create form | mitigate | Toggle "Existing customer?" user picker constrained to users.email matches; future Plan 11-04 listener verifies sender is the same user |
| T-11-03-04 | Information Disclosure | Sales sees other sales's draft quotes | accept | v1 scope — no per-sales-user partitioning; all sales see all quotes (operator can run small team without per-user partitions) |
| T-11-03-05 | Repudiation | Action invocation history | mitigate | LogsActivity captures status transitions + Auditor row written in DB::transaction; activity_log includes causer_id from auth() |
| T-11-03-06 | Tampering | Direct DB manipulation of rejection_metadata | accept | DB-direct attacks require server compromise; out of scope for application-layer threat model |
</threat_model>

<verification>
- `php artisan test tests/Feature/Filament/QuoteResourceTest.php` PASSES (5+ tests)
- `php artisan test tests/Feature/Filament/Actions/` PASSES (8+ tests)
- `php artisan test tests/Architecture/PolicyTemplateIntegrityTest.php` PASSES (floor=29)
- Pitfall 7 hardening verified — sales 403s on direct ApproveQuoteAction handler call
- shield:safe-regenerate output shows P5-F restoration entries (no `{{ Placeholder }}` literal in final policies)
- Operator UAT (Task 2) — 14 steps PASS
- `php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml` exits 0
</verification>

<success_criteria>
- QuoteResource live at /admin/quotes under "Sales" nav group
- Create form D-03 toggle behaviour matches CONTEXT.md (existing customer vs free-text)
- QuoteLinesRelationManager search-and-add SKU picker reuses Phase 6 ProductResource pattern
- 4 state-machine actions wired with mandatory ->authorize() gates (Pitfall 7)
- D-04 separation-of-duties enforced (sales cannot approve own quote — verified by test)
- D-05 5-minute revert window enforced
- D-08 structured rejection metadata captured
- 9 quote_* Shield permissions seeded + role assignments per RBAC matrix
- shield:safe-regenerate executed with P5-F restoration; PolicyTemplateIntegrityTest floor advanced to 29
- Operator sign-off received via checkpoint
- QUOT-03 (Filament UX) covered; QUOT-01/02 reinforced via UI parity with model invariants
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-03-SUMMARY.md` documenting:
- 9 quote_* permissions + RBAC matrix as shipped (post-shield:safe-regenerate verification)
- PolicyTemplateIntegrityTest floor before/after (27 → 29)
- Pitfall 7 enforcement verified — ApproveQuoteActionTest sales-role 403 assertion line numbers
- D-04 separation-of-duties operationalised — exact QuotePolicy::approve method body
- D-05 5-min revert window — exact RevertQuoteAction visible() closure body
- Operator UAT completion timestamp + any issues raised + resolutions
- Plan 11-04 entry point: ApproveQuoteAction stubs QuoteApproved event + QuoteSentMail Mailable; Plan 11-04 fills in the bodies (event payload structure + Mailable PDF attachment)
- Reasoning for navigationGroup placement (Sales — first member; future v1.x can add CustomerResource + InvoiceResource here)
</output>

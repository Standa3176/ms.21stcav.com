---
phase: 11-e2-quote-request-bitrix-deal-flow
plan: 04
type: execute
wave: 4
depends_on: [11-02, 11-03]
files_modified:
  - composer.json
  - composer.lock
  - resources/views/pdf/quote.blade.php
  - app/Domain/Quotes/Services/QuotePdfRenderer.php
  - app/Domain/Quotes/Mail/QuoteSentMail.php
  - app/Domain/Quotes/Events/QuoteApproved.php
  - app/Domain/CRM/Listeners/PushQuoteToBitrix.php
  - app/Domain/CRM/Jobs/PushQuoteToBitrixDealJob.php
  - app/Domain/CRM/Services/BitrixClient.php
  - app/Domain/CRM/Console/Commands/BitrixQuotesBootstrapCommand.php
  - app/Providers/EventServiceProvider.php
  - tests/Unit/Domain/Quotes/Services/QuotePdfRendererTest.php
  - tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php
  - tests/Feature/Domain/Quotes/QuotePdfRouteSnapshotTest.php
  - tests/Feature/Domain/Quotes/BitrixQuotesBootstrapCommandTest.php
  - .env.example
  - docs/ops/quote-cutover-runbook.md
autonomous: true
requirements: [QUOT-04, QUOT-05, QUOT-06, QUOT-07, QUOT-08]
must_haves:
  truths:
    - "composer.json adds spatie/laravel-pdf:^2.8 + dompdf/dompdf:^3.0 (per A6 lock — latest 2026-04-27); composer install completes; vendor autoload loads Spatie\\LaravelPdf\\Facades\\Pdf"
    - "LARAVEL_PDF_DRIVER=dompdf appended to .env.example (per A7 verified env var name)"
    - "QuoteApproved event class (app/Domain/Quotes/Events/QuoteApproved.php) extends DomainEvent + implements ShouldDispatchAfterCommit per Pitfall 5; readonly DTO with quoteId/userId/customerEmail/customerGroupId/statusBefore/statusAfter/correlationId"
    - "resources/views/pdf/quote.blade.php renders branded header (config('quote.company_name') + logo at public/images/meetingstore-logo.png placeholder), customer block, ex-VAT itemised line table (qty/SKU/name/unit_price ex VAT/line_total ex VAT — using PriceCalculator::stripVat at render time per D-11), totals block (Subtotal ex VAT / VAT 20% / Total inc VAT), expiry date, optional signature block (visible when config('quote.pdf_signature_block')===true), footer with 'Quote #{ulid_short_8} • Generated {YYYY-MM-DD HH:mm}'"
    - "QuotePdfRenderer::render(Quote $quote): string returns base64-encoded PDF bytes via Pdf::view('pdf.quote', ['quote'=>$quote->load('lines')])->base64() — deterministic re-renderable from snapshot (NO calls to TradeRuleResolver — reads only unit_price_pence_at_quote per Anti-Pattern 1)"
    - "QuoteSentMail Mailable (app/Domain/Quotes/Mail/QuoteSentMail.php) attaches QuotePdfRenderer output as PDF attachment named 'quote-{ulid_short_8}.pdf' + Markdown body referencing customer_name + expires_at"
    - "BitrixClient::dealProductRowsSet(int $dealId, array $rows, ?string $correlationId): void method ADDED via shadowIfDisabled('crm.deal.productrows.set', ...) + withSdk delegating to $sdk->getCRMScope()->dealProductRows()->set(...) per RESEARCH §11 verified SDK shape"
    - "PushQuoteToBitrix listener (app/Domain/CRM/Listeners/PushQuoteToBitrix.php) subscribes to QuoteApproved event + dispatches PushQuoteToBitrixDealJob — listener LIVES IN CRM domain (Pattern 4 + Anti-Pattern 2: Quotes domain MUST NOT import BitrixClient)"
    - "PushQuoteToBitrixDealJob (app/Domain/CRM/Jobs/PushQuoteToBitrixDealJob.php) clones PushOrderToBitrixJob shape: $tries=3, $backoff=[30,300,1800] (Phase 4 D-11), $timeout=120, ->onQueue('crm-bitrix'); on missing BitrixEntityMap row calls dealAdd then dealProductRowsSet then writes BitrixEntityMap entry with entity_type='quote_deal' + quote_id; on existing entry calls dealUpdate + dealProductRowsSet (idempotent — replaces all rows per QUOT-07)"
    - "PushQuoteToBitrixDealJob honours QUOTE_BITRIX_PUSH_ENABLED=false default — when disabled, BitrixClient writes payload to sync_diffs with provider='bitrix-quote' (extends existing SyncDiff.provider; Plan 11-04 verifies provider column accepts new value via Pest test)"
    - "PushQuoteToBitrixDealJob failed() hook + handle() catch BitrixPermanentException → emit Suggestion(kind='quote_push_failed', payload=[sub_kind/quote_id/error_message]) + notify AlertRecipient::receivingQuoteAlerts() recipients via Notification::send (5-min Cache::add dedup mirroring Phase 4 PushOrderToBitrixJob)"
    - "EventServiceProvider $listen registers QuoteApproved::class => [PushQuoteToBitrix::class] only — single listener; email dispatch lives in ApproveQuoteAction (Plan 11-03) inside the same DB transaction; this listener handles only the Bitrix push."
    - "BitrixQuotesBootstrapCommand (artisan: bitrix:quotes-bootstrap) probes Bitrix via crm.dealcategory.list (per A8 verified via vendor SDK) + verifies dealtype with STATUS_ID=config('quote.bitrix_deal_type_id') exists; calls BitrixClient::userfieldAdd to create UF_CRM_WOO_QUOTE_ID custom field idempotently (clones BitrixBootstrapCommand pattern); on success writes config('quote.bitrix_quote_type_verified', true) marker via env-or-cache; exits 0 on success, 1 with operator runbook output on missing dealtype"
    - "BitrixQuotesBootstrapCommand is a standalone artisan command (NOT an extension of Phase 4 BitrixBootstrapCommand) — Phase 4 bootstrap is left byte-identical per B-03 (DO-NOT-MODIFY)"
    - "QuotePdfRenderer test asserts PDF bytes contain customer_email + ulid_short_8 + total_pence formatted + 'Subtotal' + 'VAT 20%' + 'Total' literals when read with smalot/pdfparser OR strpos on raw bytes"
    - "QuotePdfRouteSnapshotTest is a regression test that renders a PDF, mutates underlying PricingRule margin_basis_points, re-renders PDF, asserts byte-identical PDF output (proves PDF reads from snapshot column only — D-13 + Anti-Pattern 1)"
    - "PushQuoteToBitrixDealJobTest covers (a) shadow-mode writes to sync_diffs with provider='bitrix-quote' when QUOTE_BITRIX_PUSH_ENABLED=false, (b) live mode (mocked BitrixClient) calls dealAdd + dealProductRowsSet on first push, (c) live mode calls dealUpdate + dealProductRowsSet on second push (idempotent QUOT-07), (d) BitrixPermanentException emits quote_push_failed Suggestion + AlertRecipient notification, (e) line items array shape matches RESEARCH §verified DealProductRows::set spec (PRODUCT_NAME/PRICE/QUANTITY/TAX_RATE/TAX_INCLUDED/CUSTOMIZED='Y')"
    - "BitrixQuotesBootstrapCommandTest covers (a) success path when dealtype QUOTE exists in mocked dealcategory.list response, (b) warning path with operator runbook text when dealtype missing, (c) idempotent UF_CRM_WOO_QUOTE_ID userfield creation"
    - "docs/ops/quote-cutover-runbook.md documents: QUOTE_BITRIX_PUSH_ENABLED flip procedure, bitrix:quotes-bootstrap pre-flight execution, sandbox test sequence (RESEARCH §Pre-Cutover Bitrix Sandbox Probe steps 1-4), rollback procedure (flip env false + replay sync_diffs entries)"
  artifacts:
    - path: "composer.json"
      provides: "Pinned spatie/laravel-pdf ^2.8 + dompdf/dompdf ^3.0 dependencies"
      contains: "spatie/laravel-pdf"
    - path: "resources/views/pdf/quote.blade.php"
      provides: "Branded UK B2B ex-VAT itemised PDF template"
      contains: "Subtotal"
      min_lines: 80
    - path: "app/Domain/Quotes/Services/QuotePdfRenderer.php"
      provides: "spatie/laravel-pdf wrapper — reads snapshot column only"
      contains: "Pdf::view"
      min_lines: 20
    - path: "app/Domain/Quotes/Events/QuoteApproved.php"
      provides: "Domain event + ShouldDispatchAfterCommit (Pitfall 5)"
      contains: "ShouldDispatchAfterCommit"
    - path: "app/Domain/CRM/Listeners/PushQuoteToBitrix.php"
      provides: "Listener — CRM domain placement (Anti-Pattern 2 prevention)"
      contains: "QuoteApproved"
    - path: "app/Domain/CRM/Jobs/PushQuoteToBitrixDealJob.php"
      provides: "Push pipeline job — clones PushOrderToBitrixJob shape (Phase 4 D-11 retry)"
      contains: "crm-bitrix"
      min_lines: 100
    - path: "app/Domain/CRM/Services/BitrixClient.php"
      provides: "Adds dealProductRowsSet method preserving Phase 4 byte-identity on existing methods"
      contains: "dealProductRowsSet"
    - path: "app/Domain/CRM/Console/Commands/BitrixQuotesBootstrapCommand.php"
      provides: "Pre-flight check — Bitrix dealtype verification + UF_CRM_WOO_QUOTE_ID creation (Pitfall 2)"
      contains: "bitrix:quotes-bootstrap"
      min_lines: 60
    - path: "tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php"
      provides: "Shadow-mode + live-mode + idempotency + DLQ test coverage"
      contains: "sync_diffs"
      min_lines: 100
    - path: "docs/ops/quote-cutover-runbook.md"
      provides: "Operator runbook — env flip + bootstrap + sandbox + rollback"
      contains: "QUOTE_BITRIX_PUSH_ENABLED"
      min_lines: 60
  key_links:
    - from: "ApproveQuoteAction (Plan 11-03)"
      to: "QuoteApproved event"
      via: "DB::transaction wrapping status flip + QuoteApproved::dispatch (ShouldDispatchAfterCommit fires only after commit per Pitfall 5)"
      pattern: "QuoteApproved::dispatch"
    - from: "QuoteApproved event"
      to: "PushQuoteToBitrix listener"
      via: "EventServiceProvider $listen array — Phase 4 listener-based extension precedent"
      pattern: "PushQuoteToBitrix"
    - from: "PushQuoteToBitrix listener"
      to: "PushQuoteToBitrixDealJob"
      via: "Direct dispatch onto crm-bitrix queue — clones Phase 4 HandleOrderReceived shape"
      pattern: "PushQuoteToBitrixDealJob::dispatch"
    - from: "PushQuoteToBitrixDealJob"
      to: "BitrixClient::dealAdd + dealProductRowsSet"
      via: "Two-call pattern — dealAdd returns Deal ID then dealProductRowsSet replaces all rows (idempotent — RESEARCH §Bitrix Line-Item Comparison)"
      pattern: "dealProductRowsSet"
    - from: "PushQuoteToBitrixDealJob"
      to: "BitrixEntityMap.entity_type='quote_deal'"
      via: "scopeForQuote(quote_id) lookup — separates from Phase 4 order-deal dedup (Pitfall 3)"
      pattern: "ENTITY_QUOTE_DEAL|quote_deal"
    - from: "QuotePdfRenderer"
      to: "quote_lines.unit_price_pence_at_quote (snapshot column)"
      via: "Pdf::view loads quote with lines — Blade reads ONLY snapshot columns (Anti-Pattern 1 prevention)"
      pattern: "unit_price_pence_at_quote"
---

<objective>
Plan 11-04 ships the **push pipeline + PDF + email** — composer require spatie/laravel-pdf:^2.8 + dompdf/dompdf:^3.0 (A6 + A7 verified), creates the branded UK B2B ex-VAT itemised PDF template (D-11 — uses PriceCalculator::stripVat at render time), the `QuotePdfRenderer` service that wraps spatie facade, the `QuoteSentMail` Mailable with PDF attachment, the full `QuoteApproved` event (with ShouldDispatchAfterCommit per Pitfall 5), the `PushQuoteToBitrix` listener (CRM-domain placement per Anti-Pattern 2), the `PushQuoteToBitrixDealJob` (clones Phase 4 PushOrderToBitrixJob shape verbatim — same 3-attempt retry + DLQ + AlertRecipient notification + Suggestion DLQ pattern), the `BitrixClient::dealProductRowsSet` extension (RESEARCH §11 SDK verified), and the `bitrix:quotes-bootstrap` artisan command (Pitfall 2 — verifies Bitrix dealtype QUOTE exists + creates UF_CRM_WOO_QUOTE_ID custom field idempotently).

Purpose: Closes QUOT-04 (PDF) + QUOT-05 (push pipeline) + QUOT-06 (shadow-mode gate) + QUOT-07 (idempotent re-approval) + most of QUOT-08 (audit log via existing LogsActivity). The shadow-mode gate (`QUOTE_BITRIX_PUSH_ENABLED=false` default) keeps the cutover gate Plan 11-05 will codify — operator must run `bitrix:quotes-bootstrap` PASS before flipping the env flag. Direct dispatch from listener (Approval Applier Seam discussion §Phase 4 precedent) — Suggestions seam only enters on FAILURE (`quote_push_failed` kind for ops Replay).

Output: 2 composer packages, 1 PDF Blade template, 1 PDF renderer service, 1 Mailable, 1 event class, 1 listener, 1 Job, 1 BitrixClient method extension, 1 artisan command, 1 EventServiceProvider edit, 4 Feature/Unit tests, 1 ops runbook, 1 .env.example edit.

Net stack delta: 2 composer packages (spatie/laravel-pdf + dompdf/dompdf — both PHP-only, no Node, no Docker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-CONTEXT.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-RESEARCH.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-01-PLAN.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-02-PLAN.md
@.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-03-PLAN.md
@CLAUDE.md
@composer.json
@app/Domain/CRM/Services/BitrixClient.php
@app/Domain/CRM/Jobs/PushOrderToBitrixJob.php
@app/Domain/CRM/Console/Commands/BitrixBootstrapCommand.php
@app/Domain/Quotes/Models/Quote.php
@app/Domain/Pricing/Services/PriceCalculator.php
@app/Domain/CRM/Models/BitrixEntityMap.php
@app/Foundation/Events/DomainEvent.php
@app/Providers/EventServiceProvider.php

<interfaces>
<!-- Verified contracts. Executor uses these directly. -->

From app/Domain/CRM/Services/BitrixClient.php (Phase 4 — verified line 67-100):
```php
public function dealAdd(array $fields, ?string $correlationId = null): string
public function dealUpdate(string $bitrixId, array $fields, ?string $correlationId = null): void
public function userfieldAdd(string $entityId, array $field, ?string $correlationId = null): void  // Phase 4 used for UF_CRM_WOO_ORDER_ID

// Wrapper internals:
private function shadowIfDisabled(string $method, ?int $wooId, array $payload, ?string $cid): ?string
private function withSdk(string $method, array $args, Closure $call, ?string $cid): mixed

// PHASE 11 ADDS (additive — never modifies existing methods):
public function dealProductRowsSet(int $dealId, array $rows, ?string $correlationId = null): void
```

From vendor/bitrix24/b24phpsdk/src/Services/CRM/Deal/Service/DealProductRows.php (RESEARCH §11 verified):
```php
// SDK call: $serviceBuilder->getCRMScope()->dealProductRows()->set($dealId, $rows)
// Row shape: ['PRODUCT_NAME','PRICE','QUANTITY','TAX_RATE','TAX_INCLUDED','CUSTOMIZED'='Y','MEASURE_CODE'=796,'MEASURE_NAME'='pcs','SORT'=10]
```

From vendor/bitrix24/b24phpsdk/src/Services/CRM/Deal/Service/DealCategory.php (A8 verified):
```php
// SDK call: $serviceBuilder->getCRMScope()->dealCategory()->list() returns existing deal categories/types
// USE THIS for Pitfall 2 dealtype verification
```

From app/Domain/CRM/Jobs/PushOrderToBitrixJob.php (Phase 4 Plan 03 — clone target — see lines 50-90 for shape):
```php
public int $tries = 3;
public array $backoff = [30, 300, 1800];   // D-11 retry
public int $timeout = 120;
public function __construct(...) { $this->onQueue('crm-bitrix'); }

public function handle(BitrixClient $client, ...) {
    // Phase 4 pattern: BitrixEntityMap lookup → dealAdd OR dealUpdate → entity events → DLQ on failure
}

public function failed(Throwable $e): void {
    // Phase 4: write Suggestion(kind='crm_push_failed') + AlertDistribution::route to receives_crm_alerts recipients
}
```

From app/Foundation/Events/DomainEvent.php (Phase 1 — verify base class):
```php
abstract class DomainEvent { /* Phase 1 base for all cross-domain events */ }
// QuoteApproved extends DomainEvent + implements ShouldDispatchAfterCommit (Pitfall 5)
```

From app/Domain/Sync/Models/SyncDiff.php (Phase 1 — verify provider column accepts string values):
```php
// Existing providers: 'woo' (Phase 1 WOO_WRITE_ENABLED shadow), 'bitrix' (Phase 4 CRM_WRITE_ENABLED shadow)
// Plan 11-04 ADDS new value: 'bitrix-quote' — Pest test asserts SyncDiff::create([...,'provider'=>'bitrix-quote']) succeeds (column type accepts string per Phase 4 D-XX VARCHAR)
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: composer install spatie/laravel-pdf + dompdf + .env.example + QuotePdfRenderer + quote.blade.php + QuoteSentMail + PDF tests</name>
  <files>composer.json, composer.lock, .env.example, resources/views/pdf/quote.blade.php, app/Domain/Quotes/Services/QuotePdfRenderer.php, app/Domain/Quotes/Mail/QuoteSentMail.php, tests/Unit/Domain/Quotes/Services/QuotePdfRendererTest.php, tests/Feature/Domain/Quotes/QuotePdfRouteSnapshotTest.php</files>
  <read_first>composer.json (verify current PHP + Laravel pins per A6 compatibility — PHP ^8.2 + Laravel ^12), app/Domain/Quotes/Mail/QuoteSentMail.php (Plan 11-03 stub to flesh out), app/Domain/Pricing/Services/PriceCalculator.php (verify stripVat signature for ex-VAT PDF render), config/quote.php (Plan 11-01 — pdf_signature_block + company details), public/images (verify logo placeholder location)</read_first>
  <behavior>
    - composer require spatie/laravel-pdf:^2.8 dompdf/dompdf:^3.0 succeeds without conflicts
    - LARAVEL_PDF_DRIVER=dompdf added to .env.example (per A7 verified spatie docs)
    - QuotePdfRenderer::render(Quote $quote): string returns base64-encoded PDF bytes
    - QuotePdfRenderer::renderToFile(Quote $quote, string $path): void saves to disk (used by Mailable attachment path strategy)
    - quote.blade.php renders:
      - Header: company logo (public/images/meetingstore-logo.png — placeholder ok), config('quote.company_name'), company_address, company_vat_number
      - Customer block: customer_name, customer_email, billing_address fields
      - Quote ref: "Quote #{ulid_short_8}" + created_at + expires_at
      - Line table: thead (#/SKU/Name/Qty/Unit Price (ex VAT)/Line Total (ex VAT)) + tbody loop with PriceCalculator::stripVat($line->unit_price_pence_at_quote, 2000) for ex-VAT display + line_total computed as stripVat(unit) * quantity
      - Totals block: Subtotal (ex VAT) £X,XXX.XX | VAT (20%) £XXX.XX | Total (inc VAT) £X,XXX.XX
      - Optional signature block (visible @if(config('quote.pdf_signature_block')))
      - Footer: page numbers + "Quote #{ulid_short_8} • Generated {now()->toDateTimeString()}"
    - QuoteSentMail Mailable:
      - Markdown view 'quotes.emails.quote-sent' OR plain Markdown body inline
      - to($quote->customer_email)
      - from(config('mail.from.address'), config('mail.from.name'))
      - subject('Your quote from MeetingStore — Quote #'.$quote->ulidShort())
      - attach($pdfBytes, ['as' => 'quote-'.$quote->ulidShort().'.pdf', 'mime' => 'application/pdf'])
    - QuotePdfRendererTest:
      - it('returns base64-encoded PDF bytes') — assert valid base64 + decoded bytes start with %PDF-
      - it('contains customer_email + ulid_short_8 + Subtotal + VAT 20% + Total literals in PDF text') — base64_decode + smalot/pdfparser OR raw string matching
      - it('renders ex-VAT amounts via PriceCalculator::stripVat') — assert specific £ amount matches stripVat(unit_inc_vat, 2000)
    - QuotePdfRouteSnapshotTest:
      - Setup: Create Quote with 2 lines via QuoteLineWriter (Plan 11-02)
      - Step 1: $beforeBytes = base64_decode($renderer->render($quote))
      - Step 2: $rule = PricingRule::find($quote->lines->first()->product_snapshot['matched_rule_id']); $rule->update(['margin_basis_points' => $rule->margin_basis_points + 1000]);
      - Step 3: $afterBytes = base64_decode($renderer->render($quote->fresh('lines')))
      - Step 4: Strip PDF metadata (/CreationDate, /ModDate, /ID lines) from both byte streams to normalize timestamps
      - Step 5: assert sha256($beforeStripped) === sha256($afterStripped) — PDF byte-identical proves snapshot read-only invariant
  </behavior>
  <action>
    1. Run `composer require spatie/laravel-pdf:^2.8 dompdf/dompdf:^3.0` (no --dev — production dependency). Verify no conflicts; commit composer.lock changes.
    2. Append to .env.example:
       ```
       LARAVEL_PDF_DRIVER=dompdf
       ```
       (Place near other LARAVEL_* env vars; if none, place near MAIL_* block.)
    3. Create `resources/views/pdf/quote.blade.php` per &lt;behavior&gt; spec. Include `<style>` block inline (DOMPDF supports CSS2.1; avoid grid/flexbox — use table layout per Pitfall 2 alternative comment in RESEARCH §Standard Stack). The Blade receives `$calc` (an instance of `App\Domain\Pricing\Services\PriceCalculator`) passed in via `Pdf::view('pdf.quote', ['quote' => ..., 'calc' => app(PriceCalculator::class)])` and uses `{{ $calc->stripVat($line->unit_price_pence_at_quote) }}` for ex-VAT amounts (NOT a global helper, NOT a static call).
    4. Create `app/Domain/Quotes/Services/QuotePdfRenderer.php`:
       ```php
       namespace App\Domain\Quotes\Services;

       use App\Domain\Pricing\Services\PriceCalculator;
       use App\Domain\Quotes\Models\Quote;
       use Spatie\LaravelPdf\Facades\Pdf;

       final class QuotePdfRenderer
       {
           public function __construct(private readonly PriceCalculator $calc) {}

           public function render(Quote $quote): string
           {
               return Pdf::view('pdf.quote', ['quote' => $quote->load('lines'), 'calc' => $this->calc])
                   ->name(sprintf('quote-%s.pdf', $quote->ulidShort()))
                   ->base64();
           }

           public function renderToFile(Quote $quote, string $absolutePath): void
           {
               Pdf::view('pdf.quote', ['quote' => $quote->load('lines'), 'calc' => $this->calc])
                   ->save($absolutePath);
           }
       }
       ```
    5. Replace stub `app/Domain/Quotes/Mail/QuoteSentMail.php` with full Mailable per &lt;behavior&gt;. Inject QuotePdfRenderer in constructor or build(); attach PDF bytes via `$this->attach(base64_decode($pdfBytes), [...])`.
    6. Create `tests/Unit/Domain/Quotes/Services/QuotePdfRendererTest.php` per &lt;behavior&gt;.
    7. Create `tests/Feature/Domain/Quotes/QuotePdfRouteSnapshotTest.php` per &lt;behavior&gt;. Use a regex strip helper for PDF metadata normalization (/CreationDate \(D:[^)]+\)/, /ModDate \(D:[^)]+\)/, /\/ID \[<[^>]+> <[^>]+>\]/) before sha256 comparison.
  </action>
  <verify>
    <automated>composer show spatie/laravel-pdf 2>&amp;1 | grep -E "^name|^versions" | head -2</automated>
    <automated>composer show dompdf/dompdf 2>&amp;1 | grep -E "^name|^versions" | head -2</automated>
    <automated>php artisan test tests/Unit/Domain/Quotes/Services/QuotePdfRendererTest.php</automated>
    <automated>php artisan test tests/Feature/Domain/Quotes/QuotePdfRouteSnapshotTest.php</automated>
    <automated>grep -n "LARAVEL_PDF_DRIVER" .env.example</automated>
    <automated>grep -c "Subtotal\|VAT 20%\|Total" resources/views/pdf/quote.blade.php</automated>
  </verify>
  <done>composer packages installed (verified by composer show). PDF renderer + Mailable + Blade template created. PDF tests PASS — including the byte-identical regression test (proves PDF reads from snapshot only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuoteApproved event + PushQuoteToBitrix listener (CRM domain) + EventServiceProvider wiring + BitrixClient::dealProductRowsSet + bitrix:quotes-bootstrap command + tests</name>
  <files>app/Domain/Quotes/Events/QuoteApproved.php, app/Domain/CRM/Listeners/PushQuoteToBitrix.php, app/Domain/CRM/Services/BitrixClient.php, app/Domain/CRM/Console/Commands/BitrixQuotesBootstrapCommand.php, app/Providers/EventServiceProvider.php, tests/Feature/Domain/Quotes/BitrixQuotesBootstrapCommandTest.php</files>
  <read_first>app/Domain/CRM/Services/BitrixClient.php (full — verify shadowIfDisabled + withSdk shape; understand SDK access pattern), app/Domain/CRM/Console/Commands/BitrixBootstrapCommand.php (Phase 4 — clone for QuotesBootstrapCommand pattern; DO NOT MODIFY existing command per B-03), app/Foundation/Events/DomainEvent.php (Phase 1 base class), app/Providers/EventServiceProvider.php (current $listen array shape — find HandleOrderReceived registration as Phase 4 precedent)</read_first>
  <behavior>
    - QuoteApproved (final): readonly DTO with quoteId/userId/customerEmail/customerGroupId/statusBefore/statusAfter/correlationId; extends DomainEvent + implements ShouldDispatchAfterCommit (Pitfall 5)
    - PushQuoteToBitrix listener (one method `handle(QuoteApproved $event)`): dispatches PushQuoteToBitrixDealJob::dispatch($event->quoteId)
    - BitrixClient::dealProductRowsSet(int $dealId, array $rows, ?string $correlationId = null): void:
      - Calls shadowIfDisabled('crm.deal.productrows.set', null, ['dealId'=>$dealId,'rows'=>$rows], $correlationId) — early return if shadow
      - Calls withSdk('crm.deal.productrows.set', [...], fn() => $this->sdk()->getCRMScope()->dealProductRows()->set($dealId, $rows)->isSuccess(), $correlationId)
      - DOES NOT modify existing dealAdd/dealUpdate/userfieldAdd methods (B-03 invariant; Phase 4 byte-identity)
    - BitrixQuotesBootstrapCommand (signature: bitrix:quotes-bootstrap {--probe : check only, no writes}):
      - Step 1: Call BitrixClient method to list dealcategory/dealtype (via SDK $sdk()->getCRMScope()->dealCategory()->list() per A8); check for one with STATUS_ID === config('quote.bitrix_deal_type_id', 'QUOTE')
      - Step 2: If not found AND not --probe: warn with operator runbook ("Create the deal type in Bitrix admin: CRM → Settings → Deal Types → Add → set System Code to 'QUOTE'") + exit 1
      - Step 3: If found: call BitrixClient::userfieldAdd('CRM_DEAL', ['FIELD_NAME' => 'WOO_QUOTE_ID', 'USER_TYPE_ID' => 'string', 'XML_ID' => 'WOO_QUOTE_ID', 'EDIT_FORM_LABEL' => ['en' => 'Woo Quote ID'], 'IS_SEARCHABLE' => 'Y']) — idempotent (Phase 4 BitrixBootstrapCommand userfield pattern); on duplicate field error, treat as success
      - Step 4: Set marker: write `cache(['quote.bitrix_quote_type_verified' => true], now()->addDays(30))` OR record in a settings table — Plan 11-05 cutover:checklist queries this marker
      - Output: success message "bitrix:quotes-bootstrap PASS — UF_CRM_WOO_QUOTE_ID present, dealtype QUOTE verified. Operator may now flip QUOTE_BITRIX_PUSH_ENABLED=true." → exit 0
    - EventServiceProvider $listen: add `\App\Domain\Quotes\Events\QuoteApproved::class => [\App\Domain\CRM\Listeners\PushQuoteToBitrix::class]` to $listen array (do NOT add to $subscribe)
    - BitrixQuotesBootstrapCommandTest:
      - it('exits 0 with success message when dealtype QUOTE exists in mocked dealcategory.list response')
      - it('exits 1 with operator runbook when dealtype QUOTE missing')
      - it('idempotently creates UF_CRM_WOO_QUOTE_ID userfield (treats duplicate as success)')
      - it('marks bitrix_quote_type_verified=true in cache on success')
  </behavior>
  <action>
    1. Replace stub `app/Domain/Quotes/Events/QuoteApproved.php`:
       ```php
       namespace App\Domain\Quotes\Events;

       use App\Foundation\Events\DomainEvent;
       use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;

       final class QuoteApproved extends DomainEvent implements ShouldDispatchAfterCommit
       {
           public function __construct(
               public readonly string $quoteId,
               public readonly ?int $userId,
               public readonly string $customerEmail,
               public readonly ?int $customerGroupId,
               public readonly string $statusBefore,
               public readonly string $statusAfter,
               public readonly string $correlationId,
           ) {}
       }
       ```
    2. Create `app/Domain/CRM/Listeners/PushQuoteToBitrix.php`:
       ```php
       namespace App\Domain\CRM\Listeners;

       use App\Domain\CRM\Jobs\PushQuoteToBitrixDealJob;
       use App\Domain\Quotes\Events\QuoteApproved;

       final class PushQuoteToBitrix
       {
           public function handle(QuoteApproved $event): void
           {
               PushQuoteToBitrixDealJob::dispatch($event->quoteId, $event->correlationId);
           }
       }
       ```
       Note: Job created in Task 3 of this plan — listener references it forward.
    3. EDIT `app/Domain/CRM/Services/BitrixClient.php`:
       - DO NOT MODIFY existing methods (Phase 4 byte-identity per B-03)
       - APPEND new method `dealProductRowsSet` per &lt;behavior&gt; spec
       - Mirror exact shadowIfDisabled + withSdk pattern from existing dealAdd
       - Add @method PHPDoc + 1-line description
    4. EDIT `app/Providers/EventServiceProvider.php`:
       - Add use statement: `use App\Domain\Quotes\Events\QuoteApproved;`
       - Add use statement: `use App\Domain\CRM\Listeners\PushQuoteToBitrix;`
       - Append to $listen array: `QuoteApproved::class => [PushQuoteToBitrix::class],`
    5. Create `app/Domain/CRM/Console/Commands/BitrixQuotesBootstrapCommand.php` per &lt;behavior&gt;:
       - extends `App\Foundation\Console\BaseCommand` (Phase 1 pattern — correlation_id threading)
       - signature `bitrix:quotes-bootstrap {--probe}`
       - Inject BitrixClient + Cache facade
       - Verify dealcategory list — try-catch BaseException; on failure log error + exit 2 (transport error vs missing config)
    6. Create `tests/Feature/Domain/Quotes/BitrixQuotesBootstrapCommandTest.php` per &lt;behavior&gt; — mock BitrixClient SDK chain via Mockery.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Domain/Quotes/BitrixQuotesBootstrapCommandTest.php</automated>
    <automated>grep -n "dealProductRowsSet" app/Domain/CRM/Services/BitrixClient.php</automated>
    <automated>grep -n "QuoteApproved::class" app/Providers/EventServiceProvider.php</automated>
    <automated>php artisan list 2>&amp;1 | grep "bitrix:quotes-bootstrap"</automated>
    <automated>php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml --no-progress 2>&amp;1 | tail -3</automated>
  </verify>
  <done>QuoteApproved event implements ShouldDispatchAfterCommit. PushQuoteToBitrix listener wired in EventServiceProvider. BitrixClient::dealProductRowsSet appended (existing methods byte-identical). bitrix:quotes-bootstrap command registered + tests PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PushQuoteToBitrixDealJob + Job tests (shadow + live + idempotent + DLQ) + ops runbook</name>
  <files>app/Domain/CRM/Jobs/PushQuoteToBitrixDealJob.php, tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php, docs/ops/quote-cutover-runbook.md</files>
  <read_first>app/Domain/CRM/Jobs/PushOrderToBitrixJob.php (FULL FILE — clone shape verbatim per RESEARCH; identify retry + failed() + Suggestion + AlertDistribution patterns), app/Domain/CRM/Notifications/CrmPushFailedNotification.php (Phase 4 — clone for QuotePushFailedNotification if not already created), app/Domain/Suggestions/Models/Suggestion.php (Phase 1 — STATUS_PENDING constant + factory shape), app/Domain/Alerting/Notifiables/AlertRecipient.php (Plan 11-01 — receivingQuoteAlerts scope), app/Domain/CRM/Models/BitrixEntityMap.php (Plan 11-01 — ENTITY_QUOTE_DEAL + scopeForQuote)</read_first>
  <behavior>
    - PushQuoteToBitrixDealJob:
      - $tries=3, $backoff=[30,300,1800], $timeout=120 (Phase 4 D-11)
      - Constructor: (string $quoteId, string $correlationId)
      - onQueue('crm-bitrix')
      - handle(BitrixClient $client, PriceCalculator $calc) flow:
        1. Context::add('correlation_id', $this->correlationId)
        2. Quote::with('lines')->findOrFail($this->quoteId)
        3. Resolve OR create Bitrix Contact via existing EntityDeduper::findOrCreateContact (or simpler — read Phase 4 PushOrderToBitrixJob to understand contact resolution pattern; for v1 simplicity, ALWAYS findOrCreate by customer_email — see RESEARCH §OQ-4 recommendation)
        4. Build dealAdd payload: TITLE='Quote {ulid_short} for {customer_email}', TYPE_ID=config('quote.bitrix_deal_type_id'), OPPORTUNITY=number_format(quote.total_pence_at_quote/100, 2, '.', ''), CURRENCY_ID='GBP', CONTACT_ID=$contactId, UF_CRM_WOO_QUOTE_ID=$quote->id, UF_CRM_WOO_QUOTE_CORRELATION=$this->correlationId
        5. Lookup BitrixEntityMap::forQuote($quote->id)->first()
        6a. If null (first push): $dealId = $client->dealAdd($payload, $correlationId); BitrixEntityMap::create(['entity_type'=>'quote_deal','quote_id'=>$quote->id,'woo_id'=>0,'bitrix_id'=>$dealId,'last_payload_hash'=>sha256(json_encode($payload)),'last_correlation_id'=>$correlationId,'last_pushed_at'=>now(),'created_via'=>'push'])
        6b. If exists (re-approval): $client->dealUpdate($map->bitrix_id, $payload, $correlationId); $map->update(['last_payload_hash'=>...,'last_correlation_id'=>$correlationId,'last_pushed_at'=>now()]); $dealId = $map->bitrix_id
        7. Build $rows array per line (using INJECTED $calc->stripVat — NOT a global helper): ['PRODUCT_ID'=>0, 'PRODUCT_NAME'=>$line->product_snapshot['name'] ?? $line->sku, 'PRICE'=>number_format($line->unit_price_pence_at_quote/100, 2, '.', ''), 'PRICE_EXCLUSIVE'=>number_format($calc->stripVat($line->unit_price_pence_at_quote)/100, 2, '.', ''), 'PRICE_NETTO'=>number_format($calc->stripVat($line->unit_price_pence_at_quote)/100, 2, '.', ''), 'PRICE_BRUTTO'=>number_format($line->unit_price_pence_at_quote/100, 2, '.', ''), 'QUANTITY'=>(string) $line->quantity_int, 'TAX_RATE'=>'20', 'TAX_INCLUDED'=>'Y', 'CUSTOMIZED'=>'Y', 'MEASURE_CODE'=>796, 'MEASURE_NAME'=>'pcs', 'SORT'=>($index+1)*10]
        8. Call $client->dealProductRowsSet((int) $dealId, $rows, $correlationId) — replaces all rows (idempotent QUOT-07)
        9. Log success + correlation_id
      - Catches BitrixPermanentException → emitFailedSuggestion('permanent_validation', ...) → $this->fail($e)
      - Catches Throwable → rethrow (lets retry mechanism kick in for transient)
      - failed(Throwable $e): hook fires after exhaustion; emit Suggestion + AlertDistribution::route as Phase 4 PushOrderToBitrixJob::failed
      - private emitFailedSuggestion(string $subKind, string $err): Suggestion::create(['kind'=>'quote_push_failed','status'=>STATUS_PENDING,'correlation_id'=>$this->correlationId,'payload'=>['sub_kind'=>$subKind,'quote_id'=>$this->quoteId,'error_message'=>$err],'evidence'=>['quote_id'=>$this->quoteId,'retry_count'=>$this->attempts()],'proposed_at'=>now()])
      - private notifyAlertRecipients() — AlertRecipient::receivingQuoteAlerts()->each(fn($r) => Notification::send($r, new QuotePushFailedNotification(...))); 5-min Cache::add dedup key based on quoteId
    - PushQuoteToBitrixDealJobTest:
      - it('writes to sync_diffs with provider=bitrix-quote when QUOTE_BITRIX_PUSH_ENABLED=false') — config()->set + run job + assert SyncDiff exists with provider='bitrix-quote' + payload contains TYPE_ID='QUOTE'
      - it('calls dealAdd then dealProductRowsSet on first push') — Mockery BitrixClient mock; assert call sequence
      - it('calls dealUpdate then dealProductRowsSet on second push (idempotent re-approval)') — pre-populate BitrixEntityMap; run job; assert dealUpdate called with same bitrix_id; no duplicate row
      - it('emits quote_push_failed Suggestion + AlertRecipient notification on BitrixPermanentException') — mock client to throw BitrixPermanentException; run job; assert Suggestion + Notification::fake assertion
      - it('builds line items array with verified DealProductRows shape') — assert $rows[0] keys === expected keys per RESEARCH §11
  </behavior>
  <action>
    1. Read app/Domain/CRM/Jobs/PushOrderToBitrixJob.php FULL FILE. Identify the failed() hook + emitFailedSuggestion + notifyAlertRecipients pattern. Clone shape into PushQuoteToBitrixDealJob.
    2. Create `app/Domain/CRM/Jobs/PushQuoteToBitrixDealJob.php` per &lt;behavior&gt; spec. The Job constructor stores (string $quoteId, string $correlationId); the `handle(BitrixClient $client, PriceCalculator $calc)` method receives BOTH dependencies via Laravel container injection (PriceCalculator is the existing Phase 3 service from app/Domain/Pricing/Services/PriceCalculator.php). Use `$calc->stripVat($line->unit_price_pence_at_quote)` for ex-VAT row fields (PRICE_EXCLUSIVE / PRICE_NETTO). Also inject EntityDeduper (if available — verify Phase 4 EntityDeduper signature; if not applicable for quote contact resolution, hardcode a simpler `findOrCreateContactByEmail($quote->customer_email)` shim).
    3. If `app/Domain/CRM/Notifications/QuotePushFailedNotification.php` does not exist, create it cloning Phase 4 CrmPushFailedNotification shape — message references quoteId + ulid_short + error.
    4. Create `tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php` per &lt;behavior&gt; — 5 tests covering shadow/live-add/live-update/DLQ/line-shape.
    5. Create `docs/ops/quote-cutover-runbook.md`:
       ```markdown
       # Phase 11 Quote → Bitrix Cutover Runbook

       ## Pre-flight (REQUIRED before flipping QUOTE_BITRIX_PUSH_ENABLED=true)

       1. `php artisan bitrix:quotes-bootstrap` — verifies dealtype QUOTE exists + creates UF_CRM_WOO_QUOTE_ID
       2. Sandbox test sequence (RESEARCH §Pre-Cutover Bitrix Sandbox Probe):
          a. With QUOTE_BITRIX_PUSH_ENABLED=true against sandbox Bitrix tenant: create test Quote → approve → assert Bitrix Deal appears with TYPE_ID=QUOTE + UF_CRM_WOO_QUOTE_ID matches
          b. Re-approve same Quote → assert NO duplicate Bitrix Deal (BitrixEntityMap matched on quote_deal entity_type)
          c. Verify crm.deal.productrows.set line items render in Bitrix UI

       ## Flip live (production)

       1. Edit production .env: `QUOTE_BITRIX_PUSH_ENABLED=true`
       2. `php artisan config:clear`
       3. `php artisan horizon:terminate` (Horizon restarts pick up new config)
       4. Monitor first 5 quote approvals — check Horizon dashboard `crm-bitrix` queue + storage/logs/laravel.log

       ## Rollback

       1. Flip `QUOTE_BITRIX_PUSH_ENABLED=false` + config:clear + horizon:terminate
       2. Subsequent approvals serialise to sync_diffs with provider='bitrix-quote'
       3. To replay accumulated diffs after fix: `php artisan sync-diffs:replay --provider=bitrix-quote --since=YYYY-MM-DD`

       ## Failure modes

       - Suggestion kind='quote_push_failed' appears in Filament Suggestion inbox → admin clicks Approve → QuotePushRetryApplier (Plan 11-05) re-dispatches PushQuoteToBitrixDealJob
       - AlertRecipient::receivingQuoteAlerts() recipients receive QuotePushFailedNotification on retry exhaustion (5-min dedup)
       ```
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php</automated>
    <automated>grep -c "dealProductRowsSet\|ENTITY_QUOTE_DEAL\|quote_push_failed" app/Domain/CRM/Jobs/PushQuoteToBitrixDealJob.php</automated>
    <automated>php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml --no-progress 2>&amp;1 | tail -3</automated>
    <automated>grep -n "QUOTE_BITRIX_PUSH_ENABLED" docs/ops/quote-cutover-runbook.md</automated>
    <automated>php artisan test tests/Feature/Domain/Quotes/ tests/Unit/Domain/Quotes/</automated>
  </verify>
  <done>PushQuoteToBitrixDealJob shipped — all 5 tests PASS. Operator runbook written. Deptrac analyse 0 violations (CRM listener allowed to import from Quotes per Plan 11-01 ruleset extension; Quotes domain still doesn't import BitrixClient).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuoteApproved → CRM listener | Cross-domain event boundary; CRM listener owns the BitrixClient interaction (Anti-Pattern 2 prevention) |
| QUOTE_BITRIX_PUSH_ENABLED env flag | Shadow-mode boundary — protects Bitrix tenant from accidental writes during dev/staging |
| BitrixClient → Bitrix REST API | External system boundary; webhook URL is sensitive (T-04-02-01 mitigation reused — never logged) |
| PDF render → quote_lines snapshot | Data integrity boundary; PDF MUST NOT call TradeRuleResolver (Anti-Pattern 1 — QuotePdfRouteSnapshotTest enforces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | PushQuoteToBitrixDealJob duplicate Bitrix Deal on race | mitigate | BitrixEntityMap UNIQUE(entity_type, quote_id) DB constraint + Phase 4 inherited dedup pattern (QUOT-07) |
| T-11-04-02 | Information Disclosure | PDF leak of internal margin | mitigate | quote.blade.php reads ONLY snapshot columns (unit_price_pence_at_quote, product_snapshot.name/brand/category) — supplier_price NEVER exposed |
| T-11-04-03 | Spoofing | Forged QuoteApproved event triggers unauthorised Bitrix push | mitigate | Event dispatched only from ApproveQuoteAction inside DB::transaction (Plan 11-03); no public endpoint dispatches QuoteApproved |
| T-11-04-04 | Tampering | PDF tamper after generation | accept | v1 ships printed PDF; e-signature integration deferred per CONTEXT.md D-09 |
| T-11-04-05 | Repudiation | Quote push timeline | mitigate | IntegrationLogger threads correlation_id through every BitrixClient call; integration_events row + activity_log row + Suggestion row + AlertRecipient notification on failure |
| T-11-04-06 | Denial of Service | Bitrix rate limit (~2 req/s) | mitigate | BitrixClient enforces 500ms throttle (Phase 4 SDK_THROTTLE_USEC); $tries=3 + exponential backoff prevents flooding on transient |
| T-11-04-07 | Elevation | Operator flips QUOTE_BITRIX_PUSH_ENABLED before bootstrap verified | mitigate | Plan 11-05 cutover:checklist gate `bitrix_quote_type_id_verified` blocks flip until bitrix:quotes-bootstrap PASS recorded |
| T-11-04-08 | Information Disclosure | Mailable sends PDF to wrong recipient | mitigate | QuoteSentMail->to($quote->customer_email) — Filament form validates email rule; D-04 confirmation modal shows email address before submit |
</threat_model>

<verification>
- `composer show spatie/laravel-pdf` shows version ^2.8.x installed
- `composer show dompdf/dompdf` shows version ^3.x installed
- `php artisan test tests/Unit/Domain/Quotes/Services/QuotePdfRendererTest.php` PASS
- `php artisan test tests/Feature/Domain/Quotes/QuotePdfRouteSnapshotTest.php` PASS (snapshot byte-identical assertion)
- `php artisan test tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php` PASS (5 tests covering shadow/live/idempotent/DLQ/lines)
- `php artisan test tests/Feature/Domain/Quotes/BitrixQuotesBootstrapCommandTest.php` PASS (4 tests)
- `php artisan list | grep "bitrix:quotes-bootstrap"` shows command registered
- `grep -c "dealProductRowsSet" app/Domain/CRM/Services/BitrixClient.php` returns 1 (single new method)
- BitrixClient::dealAdd / dealUpdate / userfieldAdd existing methods byte-identical (verify diff scoped to additive method)
- `php vendor/qossmic/deptrac-shim/deptrac analyse --config-file=depfile.yaml` exits 0
- docs/ops/quote-cutover-runbook.md exists with 4 sections
</verification>

<success_criteria>
- spatie/laravel-pdf ^2.8 + dompdf/dompdf ^3.0 installed; LARAVEL_PDF_DRIVER=dompdf in .env.example
- quote.blade.php renders UK B2B ex-VAT itemised PDF (D-11) with all required blocks
- QuotePdfRenderer reads ONLY snapshot columns (verified by QuotePdfRouteSnapshotTest)
- QuoteSentMail Mailable attaches PDF + queues to customer_email
- QuoteApproved event implements ShouldDispatchAfterCommit (Pitfall 5)
- PushQuoteToBitrix listener wired in EventServiceProvider
- BitrixClient::dealProductRowsSet additive (Phase 4 byte-identity preserved)
- PushQuoteToBitrixDealJob ships shadow-mode + live-mode + idempotent re-approval + DLQ
- bitrix:quotes-bootstrap command verifies dealtype + creates UF_CRM_WOO_QUOTE_ID idempotently
- docs/ops/quote-cutover-runbook.md documents flip + rollback procedures
- QUOT-04 (PDF) + QUOT-05 (push pipeline) + QUOT-06 (shadow gate) + QUOT-07 (idempotency) covered
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2-quote-request-bitrix-deal-flow/11-04-SUMMARY.md` documenting:
- spatie/laravel-pdf actual installed version (lock for Plan 11-05 verification)
- dompdf/dompdf actual installed version
- A6 RESOLUTION: ^2.8 lock confirmed; A7 RESOLUTION: LARAVEL_PDF_DRIVER=dompdf verified env var name
- A4 RESOLUTION: line items array shape verified against vendor SDK at vendor/bitrix24/b24phpsdk/.../DealProductRows.php
- A8 RESOLUTION: dealcategory.list verified as the dealtype enumeration endpoint (vs crm.status.list)
- OQ-3 RESOLUTION: PDF re-render on retry chosen (deterministic — snapshot integrity guarantees identical output); no PDF storage
- OQ-4 RESOLUTION: contact resolution strategy (EntityDeduper::findOrCreateContact OR simpler email-based shim — document actual choice)
- OQ-5 RESOLUTION: QuotePushRetryApplier wired in Plan 11-05 (deferred from this plan; this plan ships ONLY the failed→Suggestion writer)
- BitrixClient diff hash before/after (proves additive — existing methods unchanged)
- Plan 11-05 entry point: needs to ship QuotePushRetryApplier for kind='quote_push_failed' + cutover:checklist gate `bitrix_quote_type_id_verified`
</output>

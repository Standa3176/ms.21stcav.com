---
phase: 12-c3-seo-content-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Domain/Agents/Agents/SeoAgent.php
  - app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php
  - app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php
  - app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php
  - app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php
  - app/Domain/Agents/Tools/TruncatingTool.php
  - app/Domain/Agents/Tools/Pricing/ReadMarginHistoryTool.php
  - app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php
  - app/Domain/Agents/Tools/Pricing/ReadSupplierPriceTrendTool.php
  - app/Domain/Agents/Tools/Pricing/ReadSalesVolume90dTool.php
  - app/Providers/AppServiceProvider.php
  - config/agents.php
  - resources/agents/brand-voice/_global.md
  - resources/agents/brand-voice/logitech.md
  - tests/Architecture/TruncatingToolRelocationTest.php
  - tests/Feature/Agents/Seo/FrameworkSmokeTest.php
autonomous: true
requirements:
  - SEOAGT-01
  - SEOAGT-02
must_haves:
  truths:
    - "SeoAgent::class implements RunsAsAgent and registers under kind 'seo' in AgentRegistry"
    - "Four Seo tools exist under app/Domain/Agents/Tools/Seo/ with correct read_*/propose_* names"
    - "TruncatingTool relocated to app/Domain/Agents/Tools/TruncatingTool.php (shared); Phase 10 tools still resolve it"
    - "Brand-voice markdown directory exists with mandatory _global.md plus logitech.md example"
    - "agents.seo.temperature config key exists with default 0.4"
  artifacts:
    - path: app/Domain/Agents/Agents/SeoAgent.php
      provides: SeoAgent skeleton implementing RunsAsAgent
      contains: "class SeoAgent implements RunsAsAgent"
    - path: app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php
      provides: propose_content_patch tool (no-op writer)
      contains: "propose_content_patch"
    - path: app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php
      provides: read_brand_style_guide tool with per-brand + global fallback
      contains: "_global.md"
    - path: app/Domain/Agents/Tools/TruncatingTool.php
      provides: shared 3-KB cap helper used by Pricing + Seo tools
      contains: "namespace App\\Domain\\Agents\\Tools;"
    - path: resources/agents/brand-voice/_global.md
      provides: mandatory MeetingStore tone-of-voice doc
      contains: "Tone & voice"
    - path: config/agents.php
      provides: agents.seo.temperature config slot
      contains: "AGENTS_SEO_TEMPERATURE"
  key_links:
    - from: app/Providers/AppServiceProvider.php
      to: app/Domain/Agents/Services/AgentRegistry.php
      via: "afterResolving registration of 'seo' kind"
      pattern: "register\\(\\s*'seo'"
    - from: app/Domain/Agents/Tools/Pricing/ReadMarginHistoryTool.php
      to: app/Domain/Agents/Tools/TruncatingTool.php
      via: "extends TruncatingTool (new shared namespace)"
      pattern: "use App\\\\Domain\\\\Agents\\\\Tools\\\\TruncatingTool"
---

<objective>
Stand up the Phase 12 SeoAgent skeleton, all 4 Seo tool stubs, the brand-voice markdown directory scaffold (with the mandatory `_global.md` first draft and `logitech.md` example), the AgentRegistry/ToolBus wiring for kind `seo`, the `agents.seo.temperature=0.4` config key, and relocate `TruncatingTool` from `Tools/Pricing/` to the shared `Tools/` parent so Phase 12 (and later Phase 14) can extend it.

Purpose: Lay the contract surface for SEOAGT-01 (`SeoAgent implements RunsAsAgent`) and SEOAGT-02 (4 tool names). Subsequent plans (12-02 tool bodies, 12-03 prompt + guardrail, 12-04 job + applier + sidebar, 12-05 batch command + verification) all depend on these files existing.

Output: SeoAgent class, 4 tool stub classes (correct names + Prism tool definitions, bodies are placeholders returning `{}` for the read_* tools and acknowledged for `propose_content_patch`), brand-voice directory with `_global.md` + `logitech.md`, AppServiceProvider registration of `seo` kind, `agents.seo.temperature` config, TruncatingTool relocated and Phase 10 tools' `use` statements updated, plus one architecture test asserting the relocation succeeded without orphaning Phase 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-c3-seo-content-agent/12-CONTEXT.md
@.planning/phases/12-c3-seo-content-agent/12-RESEARCH.md

# Phase 10 mirror precedent (heavy reuse — copy + adapt)
@app/Domain/Agents/Agents/PricingAgent.php
@app/Domain/Agents/Tools/Pricing/TruncatingTool.php
@app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php
@app/Domain/Agents/Tools/Pricing/ProposeMarginBandTool.php
@app/Domain/Agents/Tools/Pricing/ReadMarginHistoryTool.php
@app/Domain/Agents/Tools/Pricing/ReadSupplierPriceTrendTool.php
@app/Domain/Agents/Tools/Pricing/ReadSalesVolume90dTool.php

# Phase 8 framework contract surface
@app/Domain/Agents/Contracts/RunsAsAgent.php
@app/Domain/Agents/Enums/TrustTier.php
@app/Domain/Agents/Services/Tools/Tool.php
@app/Domain/Agents/Services/AgentRegistry.php
@app/Domain/Agents/Services/PromptRenderer.php

# Integration points
@app/Providers/AppServiceProvider.php
@config/agents.php

<interfaces>
<!-- Key contracts the executor needs. Extracted from Phase 8 framework. -->

From app/Domain/Agents/Contracts/RunsAsAgent.php:
```php
interface RunsAsAgent
{
    public static function kind(): string;                                // 'seo'
    public static function trustTier(): TrustTier;                         // TrustTier::Trusted
    public function tools(): array;                                        // array<Tool>
    public function systemPrompt(array $context = []): string;             // Blade-rendered
    public function guardrails(): array;                                   // array<Guardrail>
    public function execute(array $input, TrustTier $tier): AgentResult;   // Stub — throw LogicException
}
```

From app/Domain/Agents/Services/Tools/Tool.php (base class all 4 Seo tools extend):
```php
abstract class Tool
{
    abstract public function name(): string;
    abstract public function description(): string;
    abstract public function asPrismTool(): \Prism\Prism\Tool;
}
```

From config/agents.php (existing):
- `agents.daily_caps.seo = 300` (already provisioned per Phase 8 D-05)
- `agents.write_enabled` (default false)
- `agents.monthly_ceiling_pence = 20000`
- `agents.models.claude-sonnet-4-6.input_pence_per_1k` / `.output_pence_per_1k`
- Plan 12-01 ADDS: `agents.seo.temperature` (env: `AGENTS_SEO_TEMPERATURE`, default 0.4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate TruncatingTool to shared Tools/ namespace + update Phase 10 tools</name>
  <read_first>
    - app/Domain/Agents/Tools/Pricing/TruncatingTool.php (current canonical location)
    - app/Domain/Agents/Tools/Pricing/ReadMarginHistoryTool.php (extends TruncatingTool — update use stmt)
    - app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php (extends TruncatingTool — update use stmt)
    - app/Domain/Agents/Tools/Pricing/ReadSupplierPriceTrendTool.php (extends TruncatingTool — update use stmt)
    - app/Domain/Agents/Tools/Pricing/ReadSalesVolume90dTool.php (extends TruncatingTool — update use stmt)
    - app/Domain/Agents/Tools/Pricing/ProposeMarginBandTool.php (verify whether it extends Tool or TruncatingTool — update if needed)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §P12-D (relocation mitigation steps)
  </read_first>
  <behavior>
    - Test: After relocation, `class_exists(App\Domain\Agents\Tools\TruncatingTool::class) === true`
    - Test: `class_exists(App\Domain\Agents\Tools\Pricing\TruncatingTool::class) === false` (old class deleted, no shim)
    - Test: Each of the 4 Phase 10 Read*Tool classes still instantiable via `app($cls)` after relocation (autoload resolves new namespace)
    - Test: `(new ReadMarginHistoryTool(...))->name() === 'read_margin_history'` still works (no behaviour regression)
  </behavior>
  <action>
    1. MOVE the file: Read current contents of `app/Domain/Agents/Tools/Pricing/TruncatingTool.php`, Write the SAME class body to `app/Domain/Agents/Tools/TruncatingTool.php` with `namespace App\Domain\Agents\Tools;` (was `App\Domain\Agents\Tools\Pricing;`). Then DELETE the old file.
    2. Update Phase 10 tool `use` statements — in each of these 4 files, change `use App\Domain\Agents\Tools\Pricing\TruncatingTool;` to `use App\Domain\Agents\Tools\TruncatingTool;`:
       - app/Domain/Agents/Tools/Pricing/ReadMarginHistoryTool.php
       - app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php
       - app/Domain/Agents/Tools/Pricing/ReadSupplierPriceTrendTool.php
       - app/Domain/Agents/Tools/Pricing/ReadSalesVolume90dTool.php
       If `ProposeMarginBandTool.php` ALSO extends TruncatingTool (read the file first to verify), update it too. If it extends `Tool` directly, leave alone.
    3. Run `composer dump-autoload` to refresh PSR-4 maps.
    4. Create `tests/Architecture/TruncatingToolRelocationTest.php` Pest test asserting:
       - `expect(class_exists(\App\Domain\Agents\Tools\TruncatingTool::class))->toBeTrue();`
       - `expect(class_exists('App\\Domain\\Agents\\Tools\\Pricing\\TruncatingTool'))->toBeFalse();`
       - `expect((new \ReflectionClass(\App\Domain\Agents\Tools\Pricing\ReadMarginHistoryTool::class))->getParentClass()->getName())->toBe(\App\Domain\Agents\Tools\TruncatingTool::class);`
    5. Run the test suite for the 4 Phase 10 tools to confirm zero regression: `php vendor/bin/pest tests/Unit/Agents/Tools/Pricing/`
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Architecture/TruncatingToolRelocationTest.php tests/Unit/Agents/Tools/Pricing/ --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `app/Domain/Agents/Tools/TruncatingTool.php` with `namespace App\Domain\Agents\Tools;` (grep: `grep -l "namespace App\\\\Domain\\\\Agents\\\\Tools;" app/Domain/Agents/Tools/TruncatingTool.php`)
    - File deleted: `app/Domain/Agents/Tools/Pricing/TruncatingTool.php` does NOT exist
    - Each of 4 Phase 10 Read*Tool files contains `use App\Domain\Agents\Tools\TruncatingTool;` (NOT the old `Tools\Pricing\TruncatingTool;`)
    - `php vendor/bin/pest tests/Architecture/TruncatingToolRelocationTest.php` exits 0 with ≥3 assertions
    - `php vendor/bin/pest tests/Unit/Agents/Tools/Pricing/` continues to exit 0 (zero Phase 10 regression)
  </acceptance_criteria>
  <done>TruncatingTool relocated to shared Tools/ namespace. All 4 Phase 10 tools updated. Pest tests prove no Phase 10 regression and the old namespace is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SeoAgent class + 4 Seo tool stubs + brand-voice markdown scaffold</name>
  <read_first>
    - app/Domain/Agents/Agents/PricingAgent.php (mirror pattern — copy + adapt)
    - app/Domain/Agents/Contracts/RunsAsAgent.php (contract Seo must satisfy)
    - app/Domain/Agents/Enums/TrustTier.php (TrustTier::Trusted enum case)
    - app/Domain/Agents/Tools/Pricing/ProposeMarginBandTool.php (no-op writer pattern — ProposeContentPatchTool mirrors)
    - app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php (TruncatingTool extension pattern)
    - app/Domain/Agents/Services/PromptRenderer.php (systemPrompt() delegates here)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Tool Implementations and §Example 3 (SeoAgent skeleton)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Example 1+2 (_global.md and logitech.md content)
  </read_first>
  <behavior>
    - Test: `\App\Domain\Agents\Agents\SeoAgent::kind() === 'seo'`
    - Test: `\App\Domain\Agents\Agents\SeoAgent::trustTier() === TrustTier::Trusted`
    - Test: SeoAgent::tools() returns array of 4 Tool instances with names `['read_product_draft', 'read_brand_style_guide', 'read_similar_shipped_products', 'propose_content_patch']` (sorted)
    - Test: `SeoAgent::execute([], TrustTier::Trusted)` throws LogicException
    - Test: `is_file(resource_path('agents/brand-voice/_global.md'))` returns true (mandatory file exists)
    - Test: `is_file(resource_path('agents/brand-voice/logitech.md'))` returns true (example file exists)
  </behavior>
  <action>
    1. Create `app/Domain/Agents/Agents/SeoAgent.php` with EXACT class signature from RESEARCH §Example 3 — `final class SeoAgent implements RunsAsAgent` with:
       - `public static function kind(): string { return 'seo'; }`
       - `public static function trustTier(): TrustTier { return TrustTier::Trusted; }`
       - `public function tools(): array` returning `[app(ReadProductDraftTool::class), app(ReadBrandStyleGuideTool::class), app(ReadSimilarShippedProductsTool::class), app(ProposeContentPatchTool::class)]`
       - `public function systemPrompt(array $context = []): string { return $this->promptRenderer->render(self::kind(), $context)['prompt']; }`
       - `public function guardrails(): array` returning placeholder `[]` (Plan 12-03 fills with `[SensitiveFieldsStripGuardrail, OutboundRegexFilterGuardrail, SeoOutboundGuardrail]`)
       - `public function execute(array $input, TrustTier $tier): AgentResult { throw new \LogicException('SeoAgent::execute is a stub — RunSeoAgentJob owns the orchestration.'); }`
       - Constructor injects `PromptRenderer`
    2. Create 4 tool stub files under `app/Domain/Agents/Tools/Seo/`:
       - `ReadProductDraftTool.php` — extends `Tool`, `name()='read_product_draft'`, `description()='Read the current AutoCreate draft state (name, descriptions, completeness flags) for the given SKU.'`, `asPrismTool()` accepts `withStringParameter('sku', ...)`, body returns `json_encode(['stub' => true])` (Plan 12-02 fills body)
       - `ReadBrandStyleGuideTool.php` — extends `Tool`, `name()='read_brand_style_guide'`, accepts `withStringParameter('brand', ...)`, body returns `json_encode(['stub' => true])` (Plan 12-02 fills body)
       - `ReadSimilarShippedProductsTool.php` — extends `TruncatingTool` (shared namespace from Task 1), `name()='read_similar_shipped_products'`, accepts `withNumberParameter('category', ...)` and `withNumberParameter('limit', ...)`, body returns `json_encode(['stub' => true])` (Plan 12-02 fills body)
       - `ProposeContentPatchTool.php` — extends `Tool`, `name()='propose_content_patch'`, accepts `withStringParameter` for `sku`, `field`, `before`, `after`, `reasoning` (5 args), body is NO-OP returning `json_encode(['acknowledged' => true], JSON_THROW_ON_ERROR)`. Mirror RESEARCH §Tool 4 verbatim.
    3. Create the brand-voice markdown directory with two files. Copy content VERBATIM from RESEARCH §Example 1 and §Example 2:
       - `resources/agents/brand-voice/_global.md` — full content from RESEARCH §Example 1 ("MeetingStore brand voice — global" with Tone, Words to use, Words to avoid, Structural conventions, Forbidden sections)
       - `resources/agents/brand-voice/logitech.md` — full content from RESEARCH §Example 2 ("Logitech voice supplement" with RightSense terminology, models families, voice quirks)
    4. Create `tests/Feature/Agents/Seo/FrameworkSmokeTest.php` Pest tests asserting all 6 behaviours from the `<behavior>` block above.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Feature/Agents/Seo/FrameworkSmokeTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/Agents/Agents/SeoAgent.php` exists and contains `class SeoAgent implements RunsAsAgent` (grep: `grep -l "class SeoAgent implements RunsAsAgent" app/Domain/Agents/Agents/SeoAgent.php`)
    - File contains `return 'seo';` for kind() AND `return TrustTier::Trusted;` for trustTier()
    - All 4 Seo tool files exist under `app/Domain/Agents/Tools/Seo/` with names: `read_product_draft`, `read_brand_style_guide`, `read_similar_shipped_products`, `propose_content_patch`
    - `ReadSimilarShippedProductsTool.php` contains `extends TruncatingTool` (verifies shared namespace from Task 1 wired correctly)
    - `ProposeContentPatchTool.php` contains `'acknowledged' => true` (no-op pattern verbatim from RESEARCH §Tool 4)
    - `resources/agents/brand-voice/_global.md` exists with byte size ≥ 1024 (substantive content, not empty)
    - `resources/agents/brand-voice/logitech.md` exists and contains the string `RightSense`
    - `php vendor/bin/pest tests/Feature/Agents/Seo/FrameworkSmokeTest.php` exits 0 with ≥6 assertions
  </acceptance_criteria>
  <done>SeoAgent class, 4 tool stubs, and brand-voice markdown directory all exist. SeoAgent::tools() returns the right 4 tool names. Mandatory `_global.md` file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register seo kind in AgentRegistry via AppServiceProvider + add agents.seo.temperature config</name>
  <read_first>
    - app/Providers/AppServiceProvider.php (existing `afterResolving(AgentRegistry::class, ...)` block where `pricing` is registered)
    - app/Domain/Agents/Services/AgentRegistry.php (verify `register(string $kind, string $cls)` signature)
    - config/agents.php (find the right section to add `seo.temperature` — likely a top-level key or under `agents`; preserve existing structure)
    - app/Domain/Agents/Clients/ClaudeClient.php (verify `temperature` is a method param on `generate(...)`)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Standard Stack — Temperature override block
  </read_first>
  <behavior>
    - Test: `app(AgentRegistry::class)->resolve('seo')` returns an instance of `\App\Domain\Agents\Agents\SeoAgent`
    - Test: `app(AgentRegistry::class)->resolve('pricing')` still returns `\App\Domain\Agents\Agents\PricingAgent` (no regression)
    - Test: `config('agents.seo.temperature')` returns `0.4` (default)
    - Test: `putenv('AGENTS_SEO_TEMPERATURE=0.7'); config()->set('agents.seo.temperature', (float) env('AGENTS_SEO_TEMPERATURE', 0.4));` reflects 0.7 — env override works
  </behavior>
  <action>
    1. Open `app/Providers/AppServiceProvider.php`. Find the existing `afterResolving(\App\Domain\Agents\Services\AgentRegistry::class, ...)` block (where `pricing` is registered). Add a line registering `seo`:
       ```php
       $registry->register('seo', \App\Domain\Agents\Agents\SeoAgent::class);
       ```
       Place it immediately after the existing `pricing` registration (alphabetical order isn't required; the existing pricing line is the anchor).
    2. Open `config/agents.php`. Add a new top-level (or under `agents` parent key, matching existing structure) section:
       ```php
       // Phase 12 — SEO agent temperature (per CONTEXT Claude's Discretion).
       // 0.4 balances creativity (genuine paraphrasing) with reproducibility.
       // Set higher than pricing (0.0 deterministic) because REQUIREMENTS line 124
       // explicitly allows temp>0 for SEO/chatbot with guardrails.
       'seo' => [
           'temperature' => (float) env('AGENTS_SEO_TEMPERATURE', 0.4),
       ],
       ```
       Verify the access path `config('agents.seo.temperature')` resolves correctly given the surrounding config file structure.
    3. Extend `tests/Feature/Agents/Seo/FrameworkSmokeTest.php` (created in Task 2) with the 4 behaviours above.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Feature/Agents/Seo/FrameworkSmokeTest.php --filter="AgentRegistry|temperature" --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `app/Providers/AppServiceProvider.php` contains the literal string `$registry->register('seo', \App\Domain\Agents\Agents\SeoAgent::class)` (grep: `grep -l "register('seo'" app/Providers/AppServiceProvider.php`)
    - `config/agents.php` contains the string `AGENTS_SEO_TEMPERATURE` (env var reference)
    - `config/agents.php` returns array where `data_get(require base_path('config/agents.php'), 'seo.temperature')` equals `0.4`
    - `php artisan tinker --execute="echo config('agents.seo.temperature');"` outputs `0.4`
    - Pest test asserts `app(AgentRegistry::class)->resolve('seo') instanceof SeoAgent` AND `app(AgentRegistry::class)->resolve('pricing') instanceof PricingAgent` (no regression)
    - `php vendor/bin/pest tests/Feature/Agents/Seo/FrameworkSmokeTest.php` exits 0
  </acceptance_criteria>
  <done>SeoAgent registered in AgentRegistry under kind 'seo'. agents.seo.temperature=0.4 config key in place. Pricing agent registration unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supplier feed / AutoCreate draft → SeoAgent input | Trusted tier — internal data only; no customer text |
| SeoAgent LLM output → ProductOverride / Product columns | Defence via 12-03 SeoOutboundGuardrail + 12-04 admin approval gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | TruncatingTool relocation breaks Phase 10 tools | mitigate | Architecture test `TruncatingToolRelocationTest` asserts old namespace gone AND parent class set correctly on all 4 Phase 10 tools; full Phase 10 tool suite re-runs in verify |
| T-12-01-02 | Information Disclosure | Brand voice markdown XSS via downstream Blade rendering (P12-H) | mitigate | `ReadBrandStyleGuideTool` reads files via `file_get_contents` + `json_encode` only — NEVER `Blade::render($content)`; Plan 12-02 enforces |
| T-12-01-03 | Repudiation | SeoAgent registered but not auditable | accept | AgentRegistry registration is git-tracked; AgentRun row in Plan 12-04 captures every run with correlation_id; no additional audit needed at registration time |
| T-12-01-04 | Denial of Service | Missing `_global.md` causes silent agent voice degradation (P12-C precursor) | mitigate | `_global.md` shipped in this plan; Plan 12-02 adds `BrandVoiceGlobalFileExistsTest` asserting `is_file(resource_path('agents/brand-voice/_global.md'))` |
</threat_model>

<verification>
- All 3 tasks' automated tests pass (Pest exits 0 for each)
- `composer dump-autoload` runs clean (no PSR-4 errors)
- `php artisan tinker --execute="dd(app(\\App\\Domain\\Agents\\Services\\AgentRegistry::class)->resolve('seo'));"` returns SeoAgent instance (manual cross-check; the automated test in Task 3 also covers this)
- No Phase 10 regression: `php vendor/bin/pest tests/Unit/Agents/Tools/Pricing/ tests/Feature/Agents/PricingAgent*` exits 0
</verification>

<success_criteria>
- SeoAgent class exists and registered under kind `seo`
- All 4 Seo tool stubs exist with correct names (`read_product_draft`, `read_brand_style_guide`, `read_similar_shipped_products`, `propose_content_patch`)
- `TruncatingTool` lives at the shared `app/Domain/Agents/Tools/` location; Phase 10's 4 tools updated and still pass tests
- Brand-voice directory exists with mandatory `_global.md` + example `logitech.md`
- `agents.seo.temperature=0.4` config key exists and respects env override
- All architecture + smoke tests pass; no Phase 10 regression
</success_criteria>

<output>
After completion, create `.planning/phases/12-c3-seo-content-agent/12-01-SUMMARY.md` describing:
- Files created (SeoAgent + 4 tools + 2 markdown + 2 test files)
- Files modified (AppServiceProvider + config/agents.php + 4-5 Phase 10 tool files for TruncatingTool relocation)
- Decisions deferred to Plan 12-02 (tool body implementations)
- Open question O-1 from RESEARCH (does Prism v0.100.1 support `withEnumParameter`? — if yes, ProposeContentPatchTool can use it for `field` arg; if no, defer)
- Pin all 4 SEO tool names so Plan 12-02 can fill bodies without re-deriving names
</output>

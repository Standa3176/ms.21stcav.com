---
phase: 12-c3-seo-content-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 12-c3-seo-content-agent/01
files_modified:
  - app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php
  - app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php
  - app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php
  - app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php
  - tests/Unit/Agents/Tools/Seo/ReadProductDraftToolTest.php
  - tests/Unit/Agents/Tools/Seo/ReadBrandStyleGuideToolTest.php
  - tests/Unit/Agents/Tools/Seo/ReadSimilarShippedProductsToolTest.php
  - tests/Unit/Agents/Tools/Seo/ProposeContentPatchToolTest.php
  - tests/Feature/Agents/Seo/BrandSlugDerivationTest.php
  - tests/Feature/Agents/Seo/BrandVoiceGlobalFileExistsTest.php
autonomous: true
requirements:
  - SEOAGT-02
must_haves:
  truths:
    - "read_product_draft returns a JSON payload containing name + short_description + long_description + meta_description + completeness_score + completeness_missing_fields for a real Product row"
    - "read_brand_style_guide returns per-brand markdown when {slug}.md exists, falls back to _global.md when it does not, always returns non-empty content"
    - "read_similar_shipped_products returns up to N publishable products matching category (Option B: status='publish' AND completeness_score>=85 OR NULL), with fallback to global examples when category has zero rows (P12-G)"
    - "propose_content_patch is a strict no-op returning {acknowledged: true} and is invoked by the mapper only via tool_calls inspection, not its own execution"
    - "All 4 tools enforce a 3-KB soft cap on their JSON response with _truncated:true hint when capped"
  artifacts:
    - path: app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php
      provides: real implementation reading Product columns + completeness flags
      contains: "completeness_missing_fields"
    - path: app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php
      provides: per-brand → global fallback markdown reader with 3-KB cap
      contains: "_global.md"
    - path: app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php
      provides: Option B eligibility query with global fallback
      contains: "completeness_score"
    - path: app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php
      provides: no-op writer; mapper reads tool_calls
      contains: "acknowledged"
  key_links:
    - from: app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php
      to: app/Domain/Agents/Tools/TruncatingTool.php
      via: "extends TruncatingTool"
      pattern: "extends TruncatingTool"
    - from: app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php
      to: resources/agents/brand-voice/_global.md
      via: "file_get_contents fallback when per-brand file absent"
      pattern: "resource_path\\(\\s*['\"]agents/brand-voice/_global\\.md['\"]\\s*\\)"
---

<objective>
Fill the 4 Seo tool bodies behind the stubs shipped in Plan 12-01: real DB queries for `read_product_draft` + `read_similar_shipped_products`, file-system reads with per-brand/global fallback for `read_brand_style_guide`, and the no-op acknowledgement for `propose_content_patch`. Apply the Phase 10 D-05 pattern of a 3-KB soft cap with `_truncated:true` hint to every read tool's JSON response.

Purpose: Plans 12-03 (prompt + guardrail), 12-04 (job + mapper + applier + sidebar), and 12-05 (batch command) all depend on tool bodies returning real data. The bodies live behind the contract surface already declared in Plan 12-01 — Plan 12-02 is the implementation pass.

Output: 4 fully-implemented tool classes covering SEOAGT-02; one Pest unit test per tool exercising the response shape + 3-KB cap behaviour; one brand-slug derivation test (mitigates P12-C); one mandatory-`_global.md`-exists test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-c3-seo-content-agent/12-CONTEXT.md
@.planning/phases/12-c3-seo-content-agent/12-RESEARCH.md
@.planning/phases/12-c3-seo-content-agent/12-01-SUMMARY.md

# Phase 10 cap pattern (3-KB + _truncated hint)
@app/Domain/Agents/Tools/TruncatingTool.php
@app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php
@app/Domain/Agents/Tools/Pricing/ProposeMarginBandTool.php

# DB models tools read from
@app/Domain/Products/Models/Product.php
@app/Domain/Pricing/Models/ProductOverride.php

# Plan 12-01 deliverables (stubs to fill)
@app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php
@app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php
@app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php
@app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php
@resources/agents/brand-voice/_global.md
@resources/agents/brand-voice/logitech.md

<interfaces>
<!-- DB column shapes the tools read from (verified in RESEARCH.md) -->

From app/Domain/Products/Models/Product.php (verified fillable, lines 33-51):
- `sku` (string)
- `name` (string — NOTE: SEO 'title' field name maps here in Plan 12-04 applier)
- `short_description` (string, ~500 chars typical)
- `long_description` (string, ~2000 chars typical)
- `meta_description` (string, ~160 chars typical)
- `brand_id` (int, FK to brands)
- `category_id` (int, FK to categories)
- `auto_create_status` (string enum — 'pending_review' is Phase 12 trigger)
- `completeness_score` (int, 0-100)
- `completeness_missing_fields` (json array)
- `status` (string — 'publish' for shipped products)

From RESEARCH.md §Tool 3 — "shipped" eligibility query (Option B):
```php
Product::query()
    ->where('status', 'publish')
    ->where(fn ($q) => $q->where('completeness_score', '>=', 85)->orWhereNull('completeness_score'))
    ->when($categoryId !== null, fn ($q) => $q->where('category_id', $categoryId))
    ->whereNotNull('name')
    ->where('name', '!=', '')
    ->limit($limit);
```

From RESEARCH.md §Tool 2 — file path conventions:
- Per-brand: `resource_path("agents/brand-voice/{$slug}.md")`
- Global fallback: `resource_path('agents/brand-voice/_global.md')`
- Slug derivation: lowercase + hyphen-separated; resolved upstream in `RunSeoAgentJob` from `Brand.slug` column (Plan 12-04 owns the helper)

From Phase 10's TruncatingTool (now at app/Domain/Agents/Tools/TruncatingTool.php after Plan 12-01 relocation):
- `protected function truncate(string $json, int $softCapBytes = 3072): string`
- Appends `_truncated:true` hint when capping
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ReadProductDraftTool + ReadBrandStyleGuideTool bodies + tests</name>
  <read_first>
    - app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php (Plan 12-01 stub — has correct asPrismTool signature)
    - app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php (Plan 12-01 stub)
    - app/Domain/Products/Models/Product.php (column names)
    - app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php (Phase 10 reference — DB query + 3-KB cap pattern)
    - resources/agents/brand-voice/_global.md (Plan 12-01 shipped — used by fallback)
    - resources/agents/brand-voice/logitech.md (Plan 12-01 shipped — used by per-brand happy path)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Tool 1 and §Tool 2
  </read_first>
  <behavior>
    - Test: `(new ReadProductDraftTool)` executed against a Product with sku='LOGI-MEETUP' returns JSON containing `sku`, `name`, `short_description`, `long_description`, `meta_description`, `brand_id`, `brand_slug`, `category_id`, `completeness_score`, `completeness_missing_fields`
    - Test: ReadProductDraftTool caps each field to mb_substr 4096 chars (feed a 10,000-char `long_description`; assert response field is exactly 4096 chars)
    - Test: ReadProductDraftTool with non-existent SKU returns JSON `{error: 'not_found', sku: '...'}` without throwing
    - Test: `(new ReadBrandStyleGuideTool)` called with `brand='logitech'` returns JSON `{brand: 'logitech', source: 'per-brand', content: '<markdown>', _bytes: N}` where content starts with `# Logitech voice supplement`
    - Test: ReadBrandStyleGuideTool with `brand='unknown-brand-xyz'` returns `{brand: 'unknown-brand-xyz', source: 'global', content: '<_global.md content>', _bytes: N}` (graceful fallback)
    - Test: ReadBrandStyleGuideTool with `brand=''` or `brand='global'` returns `source: 'global'`
    - Test: ReadBrandStyleGuideTool content is capped at 3072 chars via `mb_substr($content, 0, 3072)`
  </behavior>
  <action>
    1. Implement `app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php`. The `asPrismTool()` already exists from Plan 12-01 (accepts `withStringParameter('sku', ...)`). Fill the body:
       - Query `Product::query()->where('sku', $sku)->first()` (no eager loading needed yet; Plan 12-03 may add brand eager-load if useful)
       - If null → return `json_encode(['error' => 'not_found', 'sku' => $sku], JSON_THROW_ON_ERROR)`
       - Resolve `brand_slug` via cached lookup: `cache()->rememberForever("brand_slug.{$product->brand_id}", fn () => DB::table('brands')->where('id', $product->brand_id)->value('slug'))` — fall back to `(string) $product->brand_id` if no slug
       - Build response array with these EXACT keys: `sku`, `name`, `short_description`, `long_description`, `meta_description`, `brand_id`, `brand_slug`, `category_id`, `completeness_score`, `completeness_missing_fields`. Apply `mb_substr(..., 0, 4096)` to each STRING field. Cast `completeness_missing_fields` to array (Eloquent casts already handle this).
       - Return `json_encode($response, JSON_THROW_ON_ERROR)`
       - NOTE: No 3-KB cap on overall response — Product has bounded schema; per-field 4096 cap is sufficient.
    2. Implement `app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php` with the body verbatim from RESEARCH §Tool 2 (the code block under "Implementation"):
       - `$slug = strtolower(trim($brand));`
       - `$perBrandPath = resource_path("agents/brand-voice/{$slug}.md");`
       - `$globalPath = resource_path('agents/brand-voice/_global.md');`
       - If `$slug !== ''` AND `$slug !== 'global'` AND `is_file($perBrandPath)` → read per-brand, return `{brand: $slug, source: 'per-brand', content: mb_substr($content, 0, 3072), _bytes: strlen($content)}`
       - Else → read `$globalPath` (defaulting to empty string if missing — but Plan 12-01 shipped it), return `{brand: $slug ?: 'global', source: 'global', content: mb_substr($content, 0, 3072), _bytes: strlen($content)}`
       - Both branches `json_encode(..., JSON_THROW_ON_ERROR)`
       - Critical defence (P12-H): Do NOT pass content through `Blade::render` or `@include` — file content is treated as opaque string only.
    3. Write Pest unit tests:
       - `tests/Unit/Agents/Tools/Seo/ReadProductDraftToolTest.php` — 3 tests: happy path with seeded Product, non-existent SKU error response, 10k-char long_description capped at 4096
       - `tests/Unit/Agents/Tools/Seo/ReadBrandStyleGuideToolTest.php` — 4 tests: logitech per-brand happy path, unknown brand global fallback, empty brand string, content capped at 3072 chars
       - `tests/Feature/Agents/Seo/BrandVoiceGlobalFileExistsTest.php` — single assertion: `expect(is_file(resource_path('agents/brand-voice/_global.md')))->toBeTrue();` (mitigates P12-C precursor — global file mandatory)
    Use `RefreshDatabase` trait + `Product::factory()` (or `Product::create([...])` if factory doesn't exist) to seed test fixtures.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ReadProductDraftToolTest.php tests/Unit/Agents/Tools/Seo/ReadBrandStyleGuideToolTest.php tests/Feature/Agents/Seo/BrandVoiceGlobalFileExistsTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/Agents/Tools/Seo/ReadProductDraftTool.php` contains `Product::query()` AND `mb_substr` AND `completeness_missing_fields`
    - File `app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php` contains the string `_global.md` AND `mb_substr($content, 0, 3072)` AND `JSON_THROW_ON_ERROR`
    - File `ReadBrandStyleGuideTool.php` does NOT contain `Blade::render` or `@include` (P12-H defence; grep: `grep -L "Blade::render\\|@include" app/Domain/Agents/Tools/Seo/ReadBrandStyleGuideTool.php`)
    - `php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ReadProductDraftToolTest.php` exits 0 with ≥3 assertions
    - `php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ReadBrandStyleGuideToolTest.php` exits 0 with ≥4 assertions
    - `php vendor/bin/pest tests/Feature/Agents/Seo/BrandVoiceGlobalFileExistsTest.php` exits 0
  </acceptance_criteria>
  <done>ReadProductDraftTool + ReadBrandStyleGuideTool fully implemented with real DB / FS reads. Per-brand → global fallback works. Mandatory `_global.md` existence verified by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ReadSimilarShippedProductsTool body with Option B query + global fallback + 3-KB cap</name>
  <read_first>
    - app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php (Plan 12-01 stub — extends TruncatingTool)
    - app/Domain/Agents/Tools/TruncatingTool.php (shared base — provides `truncate()` method)
    - app/Domain/Agents/Tools/Pricing/ReadCompetitorPricesTool.php (Phase 10 reference for TruncatingTool extension pattern)
    - app/Domain/Products/Models/Product.php (status column + completeness_score column)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Tool 3 (Option B eligibility query + P12-G fallback)
  </read_first>
  <behavior>
    - Test: Tool called with `category_id=12, limit=5` returns up to 5 products from category 12 matching Option B query (status='publish' AND (score>=85 OR NULL)), structured as `{category_id, limit, products: [{sku, name, short_description, long_description_first_500_chars, meta_description}], _truncated, _total_available}`
    - Test: Tool with category_id pointing to a category with ZERO eligible rows returns 5 products from ANY category with `_fallback: 'global'` field present (P12-G mitigation)
    - Test: Each product's `long_description_first_500_chars` is exactly the first 500 chars of `long_description` (mb_substr 0 to 500)
    - Test: When the natural JSON response would exceed 3072 bytes, response is capped via TruncatingTool's `truncate()` and includes `_truncated: true` hint
    - Test: Tool with `limit=10` and 7 eligible products returns 7 (respects natural availability ≤ limit)
  </behavior>
  <action>
    1. Implement `app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php`. The class already extends `TruncatingTool` (Plan 12-01) and accepts `withNumberParameter('category', ...)` and `withNumberParameter('limit', ...)`. Fill body:
       - Build the primary eligibility query (Option B) verbatim from RESEARCH §Tool 3:
         ```php
         $primary = Product::query()
             ->where('status', 'publish')
             ->where(fn ($q) => $q->where('completeness_score', '>=', 85)->orWhereNull('completeness_score'))
             ->where('category_id', $categoryId)
             ->whereNotNull('name')
             ->where('name', '!=', '')
             ->limit($limit)
             ->get();
         ```
       - If `$primary->isEmpty()` → run fallback query WITHOUT the `category_id` filter (P12-G mitigation):
         ```php
         $fallback = Product::query()
             ->where('status', 'publish')
             ->where(fn ($q) => $q->where('completeness_score', '>=', 85)->orWhereNull('completeness_score'))
             ->whereNotNull('name')
             ->where('name', '!=', '')
             ->limit($limit)
             ->get();
         ```
         Set `$source = 'global'`; otherwise `$source = 'category'`.
       - Count total available (for `_total_available` hint): `$totalAvailable = (clone $primary_or_fallback_query)->count();` (separate count query — cheap since the WHERE clause is the same)
       - Map each product to `{sku, name, short_description, long_description_first_500_chars: mb_substr($p->long_description ?? '', 0, 500), meta_description}` — cap `short_description` to mb_substr 200 chars; cap `meta_description` natural (≤160 typical) — no extra cap needed
       - Build response: `{category_id: $categoryId, limit: $limit, products: $mapped, _fallback: $source === 'global' ? 'global' : null, _total_available: $totalAvailable}`
       - Encode + apply TruncatingTool's `truncate()` method to cap response at 3072 bytes. The TruncatingTool helper appends `_truncated:true` when capped (verify by reading the actual helper).
    2. Write `tests/Unit/Agents/Tools/Seo/ReadSimilarShippedProductsToolTest.php` Pest tests covering the 5 behaviours above. Use `RefreshDatabase` + factory-seeded Product rows (mix of `status='publish'`, `'draft'`, `completeness_score` varying, multiple categories).
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ReadSimilarShippedProductsToolTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php` contains: `status` AND `completeness_score` AND `long_description_first_500_chars` AND `_fallback`
    - File extends `TruncatingTool` (grep: `grep "extends TruncatingTool" app/Domain/Agents/Tools/Seo/ReadSimilarShippedProductsTool.php`)
    - Pest test asserts category=99 (no products) → response contains `"_fallback":"global"` AND `products` array has length > 0
    - Pest test asserts category=12 (with 5+ products) → response contains `products` array length=5 AND does NOT contain `"_fallback":"global"`
    - Pest test feeds 50 long products and asserts response stays ≤ 3500 bytes (3072 + `_truncated` hint overhead) AND includes `_truncated`
    - `php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ReadSimilarShippedProductsToolTest.php` exits 0 with ≥5 assertions
  </acceptance_criteria>
  <done>ReadSimilarShippedProductsTool returns Option B eligible products for a category, falls back to global examples when the category has zero, and respects the 3-KB cap via TruncatingTool.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalise ProposeContentPatchTool no-op + brand slug derivation test (P12-C mitigation)</name>
  <read_first>
    - app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php (Plan 12-01 stub — already returns {acknowledged: true})
    - app/Domain/Agents/Tools/Pricing/ProposeMarginBandTool.php (Phase 10 no-op writer precedent)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Tool 4 (no-op writer rationale) + §P12-C (slug derivation pitfall)
    - vendor/prism-php/prism/src/Tool.php (verify whether `withEnumParameter` exists; Open Question O-1)
  </read_first>
  <behavior>
    - Test: `ProposeContentPatchTool::name() === 'propose_content_patch'`
    - Test: Tool's `asPrismTool()` returns a Prism Tool with EXACTLY these 5 string parameters: `sku`, `field`, `before`, `after`, `reasoning`
    - Test: Tool body executed (via `$tool->asPrismTool()->using(...)` callback) with any 5-string-args input returns `{acknowledged: true}` JSON (no side effects — does NOT persist to DB, does NOT validate field name; validation happens in mapper at Plan 12-04)
    - Test: Brand slug derivation — given a Product with `brand_id=5` and the `brands` table row `{id:5, slug:'logitech', name:'Logitech, Inc.'}`, a helper that resolves the slug returns `'logitech'` (NOT `'logitech-inc'` or `'Logitech, Inc.'`)
    - Test: When brand row has no slug column populated, helper falls back to `(string) $brand_id` (e.g. `'5'`)
  </behavior>
  <action>
    1. Verify `ProposeContentPatchTool.php` from Plan 12-01 is correct — it should already match RESEARCH §Tool 4 verbatim. If not, fix:
       - `name()` returns `'propose_content_patch'`
       - `description()` returns the exact text from RESEARCH §Tool 4 ("Propose a content patch for ONE field on the product draft. Call this 1-4 times per product...")
       - `asPrismTool()` chains 5 `withStringParameter` calls: `sku`, `field`, `before`, `after`, `reasoning` (in that order, with descriptions copied from RESEARCH §Tool 4 — note the `field` description lists the 4 valid values verbatim)
       - `->using(fn (...$args): string => json_encode(['acknowledged' => true], JSON_THROW_ON_ERROR))`
    2. Investigate Open Question O-1: `grep -n "withEnumParameter\\|withEnumProperty" vendor/prism-php/prism/src/Tool.php` — if it exists in v0.100.1, refactor `field` arg to use `withEnumParameter('field', ['title', 'short_description', 'long_description', 'meta_description'], ...)`. If not, document the deferral in the plan summary (defer to v2.1 when Prism upgraded). Either way, the mapper in Plan 12-04 validates field name as defence in depth.
    3. Write `tests/Unit/Agents/Tools/Seo/ProposeContentPatchToolTest.php` Pest tests covering the first 3 behaviours.
    4. Write `tests/Feature/Agents/Seo/BrandSlugDerivationTest.php` Pest test for P12-C mitigation. The test seeds:
       - A `brands` table row `{id:5, slug:'logitech', name:'Logitech, Inc.'}`
       - A `brands` table row `{id:6, slug:null, name:'NoSlugBrand'}` (or insert a row where `slug` column is null)
       - A Product with `brand_id=5`, another with `brand_id=6`, another with `brand_id=null`
       The test imports a helper function — define it inline in the test or in a helper class `app/Domain/Agents/Support/BrandSlugResolver.php` (small static helper that's also used by Plan 12-04's `RunSeoAgentJob::brandSlug()`). Helper signature:
       ```php
       final class BrandSlugResolver {
           public static function forBrandId(?int $brandId): string {
               if ($brandId === null) return 'global';
               return cache()->rememberForever(
                   "brand_slug.{$brandId}",
                   fn () => DB::table('brands')->where('id', $brandId)->value('slug') ?? (string) $brandId,
               );
           }
       }
       ```
       Test asserts:
       - `BrandSlugResolver::forBrandId(5) === 'logitech'`
       - `BrandSlugResolver::forBrandId(6) === '6'` (falls back to brand_id as string)
       - `BrandSlugResolver::forBrandId(null) === 'global'`
       - Cache::flush() between cases to avoid cross-test contamination.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ProposeContentPatchToolTest.php tests/Feature/Agents/Seo/BrandSlugDerivationTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `app/Domain/Agents/Tools/Seo/ProposeContentPatchTool.php` contains exactly 5 occurrences of `withStringParameter` OR (if O-1 resolves yes) 4× `withStringParameter` + 1× `withEnumParameter`
    - File contains literal string `'acknowledged' => true`
    - File contains the literal field-list string in the `field` description: `'title, short_description, long_description, meta_description'` (verbatim from RESEARCH §Tool 4)
    - File `app/Domain/Agents/Support/BrandSlugResolver.php` exists with `forBrandId(?int $brandId): string` method
    - Test asserts `BrandSlugResolver::forBrandId(5) === 'logitech'` (P12-C anti-regression)
    - `php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ProposeContentPatchToolTest.php tests/Feature/Agents/Seo/BrandSlugDerivationTest.php` exits 0 with ≥6 total assertions
  </acceptance_criteria>
  <done>ProposeContentPatchTool is a verified no-op with the correct 5-arg Prism schema. BrandSlugResolver helper exists and is tested against the P12-C edge cases. Open Question O-1 resolved one way or the other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supplier-feed Product columns → tool JSON response | Trusted internal data; mb_substr caps prevent oversized payloads polluting tool_calls JSON column |
| Brand voice markdown file → tool JSON response | Trusted (PR-reviewed) but treated as opaque string (no Blade::render) per P12-H |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Information Disclosure | Tool response containing supplier-only fields (cost, margin) | mitigate | Tools query ONLY the Product columns enumerated in RESEARCH §Tool 1 schema; never select `cost_pence`, `supplier_price_pence`, `*_margin*` columns; SensitiveFieldsStrip guardrail wired by Plan 12-03 is the second line |
| T-12-02-02 | Tampering | Brand voice markdown XSS via downstream Blade rendering (P12-H) | mitigate | `ReadBrandStyleGuideTool` reads via `file_get_contents` + `json_encode` only; grep-asserted in Task 1 acceptance criteria |
| T-12-02-03 | Denial of Service | Massive `long_description` → tool response blows AgentRun.tool_calls JSON column | mitigate | Per-field mb_substr 4096 caps in `ReadProductDraftTool`; TruncatingTool 3-KB cap in `ReadSimilarShippedProductsTool`; both tested |
| T-12-02-04 | Spoofing | Brand slug derivation mismatch routes to wrong voice file (P12-C) | mitigate | `BrandSlugResolver` helper uses `Brand.slug` column (not name); Pest test enforces |
</threat_model>

<verification>
- All 3 tasks' Pest tests pass with no skips
- Combined Pest run: `php vendor/bin/pest tests/Unit/Agents/Tools/Seo/ tests/Feature/Agents/Seo/` exits 0
- No regression on Phase 10 Pricing tool tests: `php vendor/bin/pest tests/Unit/Agents/Tools/Pricing/` exits 0
- Manual smoke check: `php artisan tinker --execute="echo app(\\App\\Domain\\Agents\\Tools\\Seo\\ReadBrandStyleGuideTool::class)->asPrismTool()->getDescription();"` (verifies tool definition wires)
</verification>

<success_criteria>
- 4 Seo tool bodies fully implemented with correct response shapes
- 3-KB soft cap enforced on `read_similar_shipped_products` via TruncatingTool
- Per-brand → global fallback works in `read_brand_style_guide` with `_global.md` mandatory
- Option B eligibility query in `read_similar_shipped_products` plus P12-G global fallback
- `BrandSlugResolver` helper exists and resolves correctly for slug-present + slug-absent + null-brand cases
- Open Question O-1 (Prism enum support) resolved: either applied or deferred
</success_criteria>

<output>
After completion, create `.planning/phases/12-c3-seo-content-agent/12-02-SUMMARY.md` capturing:
- 4 tool bodies + their tested response shapes (paste a sample JSON from each test)
- Resolution of Open Question O-1 (enum vs string for `field` arg)
- BrandSlugResolver helper introduced (Plan 12-04 will use it inside `RunSeoAgentJob::brandSlug()`)
- Known edge case to revisit in Plan 12-04: Product without `brand_id` (eligibility query in Plan 12-05 filters out `auto_create_status='needs_brand_or_category_assignment'`, so brand=null shouldn't reach Plan 12-04 — but BrandSlugResolver returns 'global' defensively)
</output>

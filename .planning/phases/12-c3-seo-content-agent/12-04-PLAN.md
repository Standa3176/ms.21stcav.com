---
phase: 12-c3-seo-content-agent
plan: 04
type: execute
wave: 3
depends_on:
  - 12-c3-seo-content-agent/02
  - 12-c3-seo-content-agent/03
files_modified:
  - app/Domain/Agents/Jobs/RunSeoAgentJob.php
  - app/Domain/Agents/Services/SeoAgentResultMapper.php
  - app/Domain/Agents/Appliers/SeoContentPatchApplier.php
  - app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Agents/Seo/RunSeoAgentJobHappyPathTest.php
  - tests/Feature/Agents/Seo/RunSeoAgentJobGuardrailBlockedTest.php
  - tests/Feature/Agents/Seo/SeoAgentResultMapperTest.php
  - tests/Feature/Agents/Seo/SeoContentPatchApplierTest.php
  - tests/Feature/Agents/Seo/SeoContentPatchApplierTitleToNameTest.php
  - tests/Feature/Agents/Seo/AutoCreateEditFormUnchangedTest.php
autonomous: true
requirements:
  - SEOAGT-01
  - SEOAGT-03
must_haves:
  truths:
    - "RunSeoAgentJob orchestrates an Anthropic call through Prism, persists AgentRun row with tool_calls + cost_pence + langfuse_trace_id, and invokes SeoAgentResultMapper when AGENT_WRITE_ENABLED=true"
    - "SeoAgentResultMapper extracts ALL propose_content_patch calls from AgentRun.tool_calls[], applies last-wins per-field dedup, bundles them into ONE Suggestion of kind 'seo_content_patch'"
    - "On guardrail violation, RunSeoAgentJob catches GuardrailViolationException, invokes mapper.createGuardrailBlockedSuggestion(... $e->failedPatternKey, $e->matchedExcerpt) BEFORE rethrowing — produces ONE 'agent_guardrail_blocked' Suggestion (P12-B mitigation)"
    - "SeoContentPatchApplier maps field='title' → Product.name (NOT Product.title — there is no such column), updates ProductOverride.pin_{field}=true, audits each approval via Auditor::record('seo.content_patch_applied', ...)"
    - "AutoCreateReviewResource Edit page gains a Filament Infolist Section showing latest pending seo_content_patch Suggestion with per-field diff + 'Approve selected' form action"
    - "Phase 6 AutoCreate edit form behaviour byte-identical — AutoCreateEditFormUnchangedTest passes"
  artifacts:
    - path: app/Domain/Agents/Jobs/RunSeoAgentJob.php
      provides: Path A sibling job orchestrating SeoAgent runs
      contains: "RunSeoAgentJob"
    - path: app/Domain/Agents/Services/SeoAgentResultMapper.php
      provides: Bundled-Suggestion writer + guardrail-blocked Suggestion writer
      contains: "createBundledSuggestion"
    - path: app/Domain/Agents/Appliers/SeoContentPatchApplier.php
      provides: Per-field write-through to Product.name + ProductOverride.pin_*
      contains: "FIELD_TO_PRODUCT_COLUMN"
    - path: app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php
      provides: SEO sidebar Section additive to existing Edit page
      contains: "seoPatchesInfolist"
  key_links:
    - from: app/Domain/Agents/Jobs/RunSeoAgentJob.php
      to: app/Domain/Agents/Services/SeoAgentResultMapper.php
      via: "mapper->createBundledSuggestion + createGuardrailBlockedSuggestion calls"
      pattern: "createBundledSuggestion|createGuardrailBlockedSuggestion"
    - from: app/Domain/Agents/Appliers/SeoContentPatchApplier.php
      to: app/Domain/Pricing/Models/ProductOverride.php
      via: "updateOrCreate with pin_{field} columns"
      pattern: "ProductOverride::updateOrCreate"
    - from: app/Providers/AppServiceProvider.php
      to: app/Domain/Agents/Appliers/SeoContentPatchApplier.php
      via: "SuggestionApplierResolver register('seo_content_patch', ...)"
      pattern: "register\\(\\s*'seo_content_patch'"
---

<objective>
Wire the SeoAgent end-to-end: ship `RunSeoAgentJob` (Path A sibling — mirrors `RunPricingAgentJob` with 3 structural diffs from RESEARCH §Pattern 1), `SeoAgentResultMapper` (variable-cardinality bundled-Suggestion writer plus guardrail-blocked Suggestion writer per RESEARCH §Pattern 2), `SeoContentPatchApplier` (per-field write-through to `Product.name`/`short_description`/`long_description`/`meta_description` + `ProductOverride.pin_{field}=true` per RESEARCH §Pattern 3), and the Filament sidebar Section on `EditAutoCreateReview` (per RESEARCH §Pattern 4 — Approve-selected form action variant).

Purpose: Cover SEOAGT-01 (job orchestration of the agent) and SEOAGT-03 (sidebar surface + auto-pin via ProductOverride). Plan 12-05's batch command dispatches this job; without this plan, there's no runtime path. The CRITICAL `title → Product.name` column mapping is enforced here.

Output: 4 production files + 6 Pest tests covering happy path, guardrail-blocked path, mapper dedup semantics, applier title→name mapping, applier full vs subset approval, and Phase 6 form unchanged regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-c3-seo-content-agent/12-CONTEXT.md
@.planning/phases/12-c3-seo-content-agent/12-RESEARCH.md
@.planning/phases/12-c3-seo-content-agent/12-02-SUMMARY.md
@.planning/phases/12-c3-seo-content-agent/12-03-SUMMARY.md

# Phase 10 mirror (verbatim sibling job + mapper pattern)
@app/Domain/Agents/Jobs/RunPricingAgentJob.php
@app/Domain/Agents/Services/PricingAgentResultMapper.php
@app/Domain/Agents/Appliers/MarginChangeApplier.php

# Phase 6 integration target
@app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource.php
@app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php

# Phase 8 framework primitives RunSeoAgentJob depends on
@app/Domain/Agents/Services/AgentRegistry.php
@app/Domain/Agents/Services/BudgetGuard.php
@app/Domain/Agents/Services/ToolBus.php
@app/Domain/Agents/Services/GuardrailEngine.php
@app/Domain/Agents/Services/PromptRenderer.php
@app/Domain/Agents/Clients/ClaudeClient.php
@app/Domain/Agents/Models/AgentRun.php
@app/Domain/Agents/Enums/AgentRunStatus.php

# Suggestions seam
@app/Domain/Suggestions/Models/Suggestion.php
@app/Domain/Suggestions/Services/SuggestionApplierResolver.php
@app/Domain/Suggestions/Contracts/SuggestionApplier.php
@app/Foundation/Audit/Services/Auditor.php

# Data models the applier writes
@app/Domain/Products/Models/Product.php
@app/Domain/Pricing/Models/ProductOverride.php

# Plan 12-01..12-03 deliverables
@app/Domain/Agents/Agents/SeoAgent.php
@app/Domain/Agents/Support/BrandSlugResolver.php
@app/Domain/Agents/Exceptions/GuardrailViolationException.php
@config/agents.php

<interfaces>
<!-- Critical mapping table — applier MUST honour exactly -->

SEO field name (SEOAGT-01) → Product column:
- 'title'              → 'name'                   <-- CRITICAL P12 gotcha (SEOAGT-01 says 'title' but Product column is 'name')
- 'short_description'  → 'short_description'
- 'long_description'   → 'long_description'
- 'meta_description'   → 'meta_description'

SEO field name → ProductOverride pin column (verified ProductOverride.php:42-50):
- 'title'              → 'pin_title'
- 'short_description'  → 'pin_short_description'
- 'long_description'   → 'pin_long_description'
- 'meta_description'   → 'pin_meta_description'

Suggestion payload shape (RESEARCH §Pattern 2):
```php
[
    'kind' => 'seo_content_patch',
    'status' => Suggestion::STATUS_PENDING,
    'correlation_id' => $run->triggering_correlation_id,
    'payload' => [
        'product_id' => 123,
        'sku' => 'LOGI-MEETUP',
        'patches' => [
            ['field' => 'title', 'before' => '...', 'after' => '...', 'reasoning' => '...', 'applied_at' => null],
            // ... up to 4 entries, deduped by field with last-wins semantics
        ],
        'agent_run_id' => '<ulid>',
    ],
    'evidence' => [
        'agent_kind' => 'seo',
        'completeness_score_at_run' => 64,
        'cost_pence' => 5,
    ],
    'proposed_by_type' => AgentRun::class,
    'proposed_by_id' => $run->id,
    'proposed_at' => now(),
]
```

Guardrail-blocked Suggestion shape (RESEARCH §Pattern 2 createGuardrailBlockedSuggestion):
```php
[
    'kind' => 'agent_guardrail_blocked',
    'status' => Suggestion::STATUS_PENDING,
    'payload' => [
        'product_id' => 123,
        'sku' => 'LOGI-MEETUP',
        'agent_kind' => 'seo',
        'failed_pattern_key' => 'marketing_superlatives',
        'matched_excerpt' => 'revolutionary',
    ],
    // proposed_by/run linkage same as above
]
```

From Plan 12-03 (SeoOutboundGuardrail) — GuardrailViolationException now carries:
- `$e->guardrailClass` (existing)
- `$e->failedPatternKey` (NEW Phase 12)
- `$e->matchedExcerpt` (NEW Phase 12)
RunSeoAgentJob's catch block uses these 2 new fields to call `$mapper->createGuardrailBlockedSuggestion($run, $product, $e->failedPatternKey, $e->matchedExcerpt)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SeoAgentResultMapper + SeoContentPatchApplier (with title→name mapping)</name>
  <read_first>
    - app/Domain/Agents/Services/PricingAgentResultMapper.php (Phase 10 precedent — `end()` extraction semantics line 67-71)
    - app/Domain/Agents/Appliers/MarginChangeApplier.php (Phase 10 SuggestionApplier precedent)
    - app/Domain/Suggestions/Contracts/SuggestionApplier.php (contract Plan 12-04 applier implements)
    - app/Domain/Products/Models/Product.php (verify fillable includes name + 3 _description fields — NO 'title')
    - app/Domain/Pricing/Models/ProductOverride.php (verify pin_title/pin_short_description/pin_long_description/pin_meta_description fillable lines 42-50)
    - app/Foundation/Audit/Services/Auditor.php (verify `record(string $type, array $context)` signature)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 2 SeoAgentResultMapper code block (verbatim)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 3 SeoContentPatchApplier code block (verbatim)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §P12-A (mapper dedup must be last-wins)
  </read_first>
  <behavior>
    - Test: `SeoAgentResultMapper::createBundledSuggestion($run, $product)` — given AgentRun.tool_calls containing 2 calls for field='title' (first proposes "A", second proposes "B") — the resulting Suggestion.payload.patches[0].after === 'B' (last-wins per P12-A)
    - Test: Given tool_calls with 4 distinct fields (title, short_description, long_description, meta_description) — Suggestion.payload.patches has 4 entries, one per field
    - Test: Given tool_calls with invalid field name 'invented' — that call is silently ignored (no entry in patches)
    - Test: Given tool_calls with field='title' and before===after — that call is silently ignored (no no-op patches stored)
    - Test: Given AgentRun.tool_calls with ZERO propose_content_patch calls — mapper returns null AND AgentRun.agent_reasoning_summary contains "[mapper: no_patches]"
    - Test: `SeoAgentResultMapper::createGuardrailBlockedSuggestion($run, $product, 'marketing_superlatives', 'revolutionary')` creates a Suggestion of kind 'agent_guardrail_blocked' with payload.failed_pattern_key='marketing_superlatives' and payload.matched_excerpt='revolutionary'
    - Test: `SeoContentPatchApplier::apply($suggestion)` with patch field='title' applied_at=now() — updates `Product.name` (not Product.title!) and `ProductOverride.pin_title=true`
    - Test: Applier handles SUBSET approval — given 4 patches with only 2 having applied_at set — Suggestion.status flips to STATUS_PENDING (not APPLIED); other 2 patches still visible
    - Test: Applier handles FULL approval — all 4 patches with applied_at set — Suggestion.status flips to STATUS_APPLIED
    - Test: Applier writes Auditor::record('seo.content_patch_applied', ...) for each approved field; record includes `before_hash` + `after_hash` (sha256), NOT verbatim text
    - Test: Applier wraps writes in DB::transaction (verify by mocking DB facade OR by reading file source for `DB::transaction`)
  </behavior>
  <action>
    1. Create `app/Domain/Agents/Services/SeoAgentResultMapper.php` with EXACT body from RESEARCH §Pattern 2:
       - `public const VALID_FIELDS = ['title', 'short_description', 'long_description', 'meta_description'];`
       - `createBundledSuggestion(AgentRun $run, Product $product): ?Suggestion` — extracts propose_content_patch calls, deduplicates by field with UNCONDITIONAL `$patchesByField[$field] = ...` (last-wins per P12-A), skips invalid fields + no-op patches, caps before/after at 4096 chars + reasoning at 1024 chars, returns null if zero patches
       - `createGuardrailBlockedSuggestion(AgentRun $run, Product $product, string $failedPatternKey, string $matchedExcerpt): Suggestion` — writes kind 'agent_guardrail_blocked'; payload includes failed_pattern_key + matched_excerpt (mb_substr 500)
       - `markNoPatchesState(AgentRun $run): void` private helper — appends `[mapper: no_patches]` to agent_reasoning_summary
       - `decodeArgs(mixed $inputs): array` private helper — handles inputs as either array or JSON string
    2. Create `app/Domain/Agents/Appliers/SeoContentPatchApplier.php` with EXACT body from RESEARCH §Pattern 3:
       - `implements SuggestionApplier`
       - `private const FIELD_TO_PRODUCT_COLUMN = ['title' => 'name', 'short_description' => 'short_description', 'long_description' => 'long_description', 'meta_description' => 'meta_description'];`
       - `private const FIELD_TO_PIN_COLUMN = ['title' => 'pin_title', 'short_description' => 'pin_short_description', 'long_description' => 'pin_long_description', 'meta_description' => 'pin_meta_description'];`
       - Constructor injects `Auditor`
       - `apply(Suggestion $suggestion): void` — wraps writes in `DB::transaction`:
         - For each patch with `applied_at !== null`: update `$product->{$column}` with `$patch['after']`, mark `$overrideUpdates[$pinColumn] = true`, audit via `$this->auditor->record('seo.content_patch_applied', [...])` with before_hash + after_hash + product_id + field + agent_run_id + suggestion_id
         - `$product->save()`
         - `ProductOverride::updateOrCreate(['product_id' => $product->id], $overrideUpdates)` — preserves other pin flags
         - Flip Suggestion.status: STATUS_APPLIED if all patches applied, STATUS_PENDING if subset
    3. Register the applier in `app/Providers/AppServiceProvider.php`. Find the existing `afterResolving(SuggestionApplierResolver::class, ...)` block. Add:
       ```php
       $resolver->register('seo_content_patch', \App\Domain\Agents\Appliers\SeoContentPatchApplier::class);
       ```
       NOTE: kind `agent_guardrail_blocked` does NOT need an applier (audit-only — never approved).
    4. Write `tests/Feature/Agents/Seo/SeoAgentResultMapperTest.php` — 6 tests covering mapper behaviours
    5. Write `tests/Feature/Agents/Seo/SeoContentPatchApplierTest.php` — 5 tests covering applier behaviours
    6. Write `tests/Feature/Agents/Seo/SeoContentPatchApplierTitleToNameTest.php` — DEDICATED test for the title→name mapping. Asserts:
       - Given a Suggestion payload with `patches: [{field: 'title', before: 'Old name', after: 'New name', applied_at: now()}]`, AFTER applier runs:
         - `Product::find($productId)->name === 'New name'` ✓
         - `Product` model does NOT have a `title` column (no migration, no setter, no value) — grep on `app/Domain/Products/Models/Product.php` for `'title'` should NOT appear in fillable
         - `ProductOverride::where('product_id', $productId)->first()->pin_title === true`
       - Audit row exists with `type='seo.content_patch_applied'` and `data.field='title'` (NOT `data.field='name'` — applier preserves user-facing semantics in audit log; only the COLUMN write is remapped)
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Feature/Agents/Seo/SeoAgentResultMapperTest.php tests/Feature/Agents/Seo/SeoContentPatchApplierTest.php tests/Feature/Agents/Seo/SeoContentPatchApplierTitleToNameTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/Agents/Services/SeoAgentResultMapper.php` contains: `const VALID_FIELDS` AND `createBundledSuggestion` AND `createGuardrailBlockedSuggestion` AND `'kind' => 'seo_content_patch'` AND `'kind' => 'agent_guardrail_blocked'`
    - File contains UNCONDITIONAL `$patchesByField[$field] =` assignment (NOT `if (!isset($patchesByField[$field]))` guard — P12-A defence). Grep: `grep -c "isset(\$patchesByField" app/Domain/Agents/Services/SeoAgentResultMapper.php` returns 0
    - File `app/Domain/Agents/Appliers/SeoContentPatchApplier.php` contains the literal string `'title' => 'name'` (the critical title→name mapping)
    - File contains `DB::transaction`
    - File contains both `FIELD_TO_PRODUCT_COLUMN` AND `FIELD_TO_PIN_COLUMN` constants
    - `app/Providers/AppServiceProvider.php` contains `register('seo_content_patch'` (grep: `grep -l "register('seo_content_patch'" app/Providers/AppServiceProvider.php`)
    - Pest test `SeoContentPatchApplierTitleToNameTest.php` asserts `Product::find($id)->name === 'New name'` AFTER applying a patch with field='title'
    - All 3 Pest test files exit 0 with combined ≥15 assertions
  </acceptance_criteria>
  <done>Mapper + applier shipped. CRITICAL title→name column mapping in place and tested. SuggestionApplierResolver registers the new applier. Last-wins dedup verified by Pest test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RunSeoAgentJob (Path A sibling job) with guardrail-blocked path (P12-B)</name>
  <read_first>
    - app/Domain/Agents/Jobs/RunPricingAgentJob.php (Phase 10 sibling — Path A pattern; copy structure)
    - app/Domain/Agents/Support/BrandSlugResolver.php (Plan 12-02 deliverable — used by brandSlug helper)
    - app/Domain/Agents/Services/SeoAgentResultMapper.php (Task 1 deliverable)
    - app/Domain/Agents/Exceptions/GuardrailViolationException.php (Plan 12-03 extended with failedPatternKey + matchedExcerpt)
    - app/Domain/Agents/Models/AgentRun.php (column schema — tool_calls JSON, guardrail_failures JSON, status enum)
    - app/Domain/Agents/Enums/AgentRunStatus.php (status enum cases)
    - app/Domain/Products/Models/Product.php (eligibility re-check `auto_create_status` field)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 1 RunSeoAgentJob code block (verbatim)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §P12-B (guardrail-blocked Suggestion creation in catch block)
  </read_first>
  <behavior>
    - Test (happy path): Given a Product with auto_create_status='pending_review' + Prism::fake response containing 2 propose_content_patch calls, dispatching RunSeoAgentJob synchronously results in:
      - AgentRun row created with kind='seo', status='completed', tool_calls populated, cost_pence > 0
      - When AGENT_WRITE_ENABLED=true: ONE Suggestion of kind 'seo_content_patch' exists with 2 patches in payload
      - When AGENT_WRITE_ENABLED=false: ZERO Suggestions created (shadow mode)
    - Test (guardrail-blocked path — P12-B): Given a Product + Prism::fake response with a forbidden pattern in `after` text:
      - AgentRun row has status='guardrail_blocked' AND guardrail_failures JSON populated
      - EXACTLY ONE Suggestion of kind 'agent_guardrail_blocked' exists (audit Suggestion)
      - ZERO Suggestions of kind 'seo_content_patch' exist (no partial publish per D-01)
    - Test (eligibility re-check): Given a Product where auto_create_status changed to 'published' between dispatch and handle — job exits early, no AgentRun row created
    - Test (budget exceeded): When BudgetGuard throws BudgetExceededException pre-flight — AgentRun.status='budget_exceeded', exception rethrown, no Suggestion created
    - Test (ULID correlation): batch_correlation_id passed through to AgentRun.triggering_correlation_id
  </behavior>
  <action>
    1. Create `app/Domain/Agents/Jobs/RunSeoAgentJob.php` with the EXACT class body from RESEARCH §Pattern 1 (copy verbatim). Key elements:
       - `final class RunSeoAgentJob implements ShouldQueue` with `Queueable, InteractsWithQueue, SerializesModels` traits
       - `public int $tries = 1; public int $timeout = 180;`
       - Constructor: `public readonly int $productId, public readonly ?string $batchCorrelationId = null`; `$this->onQueue('agents');`
       - `handle(...)` method injects: AgentRegistry, BudgetGuard, ToolBus, GuardrailEngine, ClaudeClient, PromptRenderer, SeoAgentResultMapper
       - Step 1: `Product::findOrFail($this->productId)`
       - Step 2: Defensive eligibility re-check — if `$product->auto_create_status !== 'pending_review'` → log + return
       - Step 3: Resolve correlation_id from batchCorrelationId or Context
       - Step 4: `$agent = $registry->resolve('seo')`
       - Step 5: `$rendered = $promptRenderer->render('seo', [...])` with product_id + sku + brand_slug (use `BrandSlugResolver::forBrandId($product->brand_id)`)
       - Step 6: Create AgentRun row with kind='seo', status='running', triggering_suggestion_id=null (batch-driven), system_prompt_hash from rendered hash
       - Step 7: `event(new AgentRunStarted($run))`
       - Step 8: try block — call `$budgetGuard->assertHasBudget('seo')`, pre-flight guardrails, build prismTools, call `$client->generate(systemPrompt, messages, tools, temperature: (float) config('agents.seo.temperature', 0.4))`, post-flight guardrails, extract toolCalls, update AgentRun, record spend, conditionally call mapper based on `config('agents.write_enabled')`
       - Step 9: catch `MonthlyBudgetExceededException | BudgetExceededException` — update AgentRun.status accordingly, rethrow
       - Step 10 (P12-B critical): catch `GuardrailViolationException` — BEFORE rethrowing, call `$mapper->createGuardrailBlockedSuggestion($run, $product, $e->failedPatternKey, $e->matchedExcerpt)`. THEN update AgentRun.status='guardrail_blocked', guardrail_failures JSON entry, rethrow.
       - Step 11: catch `\Throwable` — update AgentRun.status='failed', rethrow
       - `brandSlug(Product $product): string` helper — delegate to `BrandSlugResolver::forBrandId($product->brand_id)`
       - `extractToolCallsFromSteps` + `readStepProperty` helpers — copy VERBATIM from `RunPricingAgentJob.php:260-311`
    2. Write `tests/Feature/Agents/Seo/RunSeoAgentJobHappyPathTest.php` — 3 tests:
       - Happy path with AGENT_WRITE_ENABLED=true → Suggestion of kind 'seo_content_patch' exists
       - Happy path with AGENT_WRITE_ENABLED=false → ZERO Suggestions (shadow mode)
       - Eligibility re-check — Product status changes to 'published' between dispatch + handle, job exits early
    3. Write `tests/Feature/Agents/Seo/RunSeoAgentJobGuardrailBlockedTest.php` — 2 tests:
       - Forbidden pattern in `after` text — AgentRun.status='guardrail_blocked' + ONE 'agent_guardrail_blocked' Suggestion + ZERO 'seo_content_patch' Suggestions
       - BudgetExceededException pre-flight — AgentRun.status='budget_exceeded'
    Use `Prism::fake()` to construct ClaudeResponse fixtures with controllable tool_calls.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Feature/Agents/Seo/RunSeoAgentJobHappyPathTest.php tests/Feature/Agents/Seo/RunSeoAgentJobGuardrailBlockedTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/Agents/Jobs/RunSeoAgentJob.php` exists with `final class RunSeoAgentJob implements ShouldQueue`
    - File contains `public int $tries = 1;` AND `public int $timeout = 180;`
    - File contains `$this->onQueue('agents');`
    - File contains `auto_create_status !== 'pending_review'` (eligibility re-check)
    - File contains `temperature: (float) config('agents.seo.temperature', 0.4)`
    - File catch block for `GuardrailViolationException` calls `$mapper->createGuardrailBlockedSuggestion(` BEFORE the `throw $e;` (P12-B mitigation — grep this order)
    - File contains `BrandSlugResolver::forBrandId` OR equivalent invocation
    - Test asserts `Suggestion::where('kind', 'agent_guardrail_blocked')->count() === 1` AND `Suggestion::where('kind', 'seo_content_patch')->count() === 0` after guardrail-blocked run
    - Test asserts `Suggestion::where('kind', 'seo_content_patch')->count() === 1` after happy-path run with AGENT_WRITE_ENABLED=true
    - Test asserts `Suggestion::count() === 0` after happy-path run with AGENT_WRITE_ENABLED=false (shadow mode)
    - All test files exit 0 with combined ≥5 assertions
  </acceptance_criteria>
  <done>RunSeoAgentJob shipped as Path A sibling job. Guardrail-blocked catch block correctly writes audit Suggestion BEFORE rethrowing (P12-B). Shadow mode honoured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Filament sidebar Section on EditAutoCreateReview + AutoCreateEditFormUnchangedTest (P12-F regression guard)</name>
  <read_first>
    - app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource.php (Phase 6 — verify form schema fields list)
    - app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php (current — Plan 12-04 ADDS to this; existing form behaviour must remain byte-identical)
    - app/Domain/Suggestions/Filament/Resources/SuggestionResource.php lines 456-557 (Phase 10 infolist precedent — Section + RepeatableEntry shape)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 4 (Filament sidebar code block + Approve-selected variant)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §P12-F (additive extension — do NOT override existing infolist() or form())
  </read_first>
  <behavior>
    - Test: `EditAutoCreateReview::class` has a method named `seoPatchesInfolist(Infolist $infolist): Infolist` (NOT `infolist()` — different name, P12-F defence)
    - Test: When a Product has a pending Suggestion of kind 'seo_content_patch', `seoPatchesInfolist()` renders a `Section` with `description` containing "patches proposed by agent run ..."
    - Test: When a Product has NO pending Suggestion of kind 'seo_content_patch', the Section is `visible(false)` — rendered output is empty
    - Test: The Section schema includes a RepeatableEntry over `seo_patches` with 5 columns: field (badge), before, after, reasoning, applied_at
    - Test (P12-F regression): `AutoCreateReviewResource::form(...)` schema still contains all the original Phase 6 fields. Run the existing AutoCreate edit form test (if one exists) and confirm zero regression. If no Phase 6 form test exists, write one: enumerate form schema components via reflection + assert count + types.
    - Test: An `Action::make('approve_selected_patches')` form action exists on the EditAutoCreateReview page that accepts a multi-select of patch fields and dispatches `ApplySuggestionJob` (or invokes SuggestionApplierResolver directly) with the selected fields' `applied_at` set to now()
  </behavior>
  <action>
    1. Open `app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php`. Extend the class additively per P12-F:
       - Add `use Filament\Infolists\Concerns\InteractsWithInfolists;` (trait)
       - Add `use Filament\Infolists\Contracts\HasInfolists;` (interface) to class declaration
       - Add `use InteractsWithInfolists;` to the class body
       - Add the `seoPatchesInfolist(Infolist $infolist): Infolist` method verbatim from RESEARCH §Pattern 4 code block (with the Section + RepeatableEntry shape). The method name is DELIBERATELY NOT `infolist()` — keeps Phase 6's existing edit form behaviour byte-identical.
       - Add `private function latestSeoSuggestion(Product $product): ?Suggestion` helper from RESEARCH §Pattern 4 — queries Suggestion::where kind='seo_content_patch' + product_id match + status IN [pending, applied] + latest proposed_at
       - Mount the new infolist in the page via Filament's `getFooter()`, or via a custom Blade view registered through `protected function getFooterWidgets(): array` — choose whichever Filament 3.3 mechanism allows adding a panel below the existing edit form WITHOUT modifying the form schema. RESEARCH §Pattern 4 suggests overriding `getFooter()` to inject the seoPatchesInfolist's rendered view; verify what works in 3.3 during implementation. If 3.3 doesn't allow non-form panel render on EditRecord page, fall back to a `headerActions()` button "View SEO suggestions" that opens a modal showing the patches.
       - Add a form Action `Action::make('approve_selected_patches')` (or `headerAction`) that:
         - Accepts a `CheckboxList::make('approved_fields')` with options ['title', 'short_description', 'long_description', 'meta_description']
         - On submit, loads latest pending seo_content_patch Suggestion, sets `payload.patches[i].applied_at = now()` for each selected field, then resolves SeoContentPatchApplier and calls `apply($suggestion)`
         - Notifies admin via Filament `Notification::make()->title('SEO patches applied')->success()->send();`
    2. Write `tests/Feature/Agents/Seo/AutoCreateEditFormUnchangedTest.php` (P12-F regression):
       - Use Filament's `livewire(EditAutoCreateReview::class, ['record' => $product->getKey()])->assertSchemaExists('form')` or equivalent introspection
       - Assert that the form schema still contains the original Phase 6 fields by name (enumerate from `AutoCreateReviewResource::form(...)` reflection — at minimum: name, slug, short_description, long_description, meta_description, auto_create_status, completeness_score)
       - Assert the form is NOT read-only (admin can still edit)
       - Assert the new `seoPatchesInfolist` method exists on the class via reflection
    3. (Optional polish) If RepeatableEntry per-row Actions are constrained in Filament 3.3 (per RESEARCH §Pattern 4 footnote + Assumption A5), ship the simpler "Approve selected via form action with multi-select" variant — RESEARCH explicitly says this is the primary ship.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Feature/Agents/Seo/AutoCreateEditFormUnchangedTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php` contains the method `seoPatchesInfolist(Infolist` (grep: `grep -c "function seoPatchesInfolist" app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource/Pages/EditAutoCreateReview.php` returns 1)
    - File contains `Filament\Infolists\Components\Section` AND `RepeatableEntry::make` AND `latestSeoSuggestion`
    - File contains `Action::make('approve_selected_patches'` OR equivalent action with CheckboxList
    - File does NOT override `form(...)` or replace existing schema (grep verifies `function form` count unchanged from Phase 6 baseline)
    - Pest test `AutoCreateEditFormUnchangedTest.php` asserts form schema still contains the original Phase 6 field set
    - Pest test asserts `method_exists(EditAutoCreateReview::class, 'seoPatchesInfolist') === true`
    - `php vendor/bin/pest tests/Feature/Agents/Seo/AutoCreateEditFormUnchangedTest.php` exits 0
    - Phase 6 tests unaffected: `php vendor/bin/pest tests/Feature/ProductAutoCreate` (if such directory exists) exits 0
  </acceptance_criteria>
  <done>Filament sidebar Section added to EditAutoCreateReview via additive `seoPatchesInfolist` method (no override of existing form/infolist). Approve-selected form action wired through SuggestionApplier. Phase 6 edit form behaviour byte-identical — regression test in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentRun.tool_calls JSON → SeoAgentResultMapper → Suggestion.payload | Trusted internal but mapper validates field name + caps before/after at 4096 chars + reasoning at 1024 |
| Suggestion.payload.patches[i].after → SeoContentPatchApplier → Product.{name|short_description|long_description|meta_description} (PUBLIC Woo storefront copy) | Highest-risk write boundary — guarded by 3-layer defence: outbound regex guardrail (Plan 12-03), human approval click (Plan 12-04 Filament action), audit log entry |
| GuardrailViolationException → catch block → mapper.createGuardrailBlockedSuggestion → Suggestion (admin-hidden audit row) | Audit trail for blocked runs — Plan 12-05 verifies these are filtered out of default Suggestion list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | Mapper writes wrong field due to last-wins flip (P12-A) | mitigate | UNCONDITIONAL `$patchesByField[$field] = ...` assignment in mapper; grep + Pest test enforce |
| T-12-04-02 | Tampering | Applier writes to Product.title (which doesn't exist) silently failing → patch never applied | mitigate | Explicit `FIELD_TO_PRODUCT_COLUMN = ['title' => 'name', ...]` constant; dedicated `SeoContentPatchApplierTitleToNameTest` asserts Product.name updated AND Product has no 'title' column |
| T-12-04-03 | Repudiation | Guardrail-blocked run loses audit trail (P12-B) | mitigate | RunSeoAgentJob catch block calls `mapper->createGuardrailBlockedSuggestion(...)` BEFORE rethrow; Pest test asserts ONE 'agent_guardrail_blocked' Suggestion exists post-block |
| T-12-04-04 | Tampering | Filament sidebar override breaks Phase 6 edit form (P12-F) | mitigate | New method named `seoPatchesInfolist` (NOT `infolist`); Pest `AutoCreateEditFormUnchangedTest` asserts form schema unchanged |
| T-12-04-05 | Information Disclosure | Auditor records full before/after text → bloats audit_log table | mitigate | Applier records sha256 `before_hash` + `after_hash` ONLY; verbatim text stays on Suggestion.payload which has its own retention |
| T-12-04-06 | Elevation of Privilege | Admin approves a patch on a Suggestion they shouldn't see (kind='agent_guardrail_blocked' hidden) | accept | Plan 12-05 SuggestionResource filter hides 'agent_guardrail_blocked' from default list; kind has no applier registered so even if approved manually via tinker, no write occurs |
</threat_model>

<verification>
- All 3 tasks' Pest tests pass: `php vendor/bin/pest tests/Feature/Agents/Seo/` exits 0
- No Phase 6 regression: `php vendor/bin/pest tests/Feature/ProductAutoCreate` (if exists) exits 0
- No Phase 10 regression: `php vendor/bin/pest tests/Feature/Agents/Pricing` exits 0
- Manual smoke (synchronously via tinker): seed a Product + dispatch `RunSeoAgentJob::dispatchSync($productId)` with Prism::fake → AgentRun row created
- Filament panel smoke: `php artisan filament:check-translations` continues to pass (translation keys not broken)
</verification>

<success_criteria>
- RunSeoAgentJob orchestrates SeoAgent end-to-end with Path A sibling pattern
- SeoAgentResultMapper bundles propose_content_patch calls into ONE Suggestion (last-wins per-field)
- SeoContentPatchApplier writes Product.{name|*_description} + ProductOverride.pin_{field}=true with audit; title→name mapping verified
- Filament sidebar Section additive on EditAutoCreateReview; Phase 6 form unchanged
- P12-A, P12-B, P12-F all mitigated with named Pest tests
</success_criteria>

<output>
After completion, create `.planning/phases/12-c3-seo-content-agent/12-04-SUMMARY.md` capturing:
- Verified runtime path: dispatch → AgentRun → Prism → tool_calls → mapper → Suggestion → admin approve → applier → Product + ProductOverride
- The 3 critical defences shipped (P12-A last-wins, P12-B guardrail audit, P12-F additive form)
- Test count surfaced (~10-12 Pest cases)
- Plan 12-05 dependency contract: `RunSeoAgentJob::dispatch($productId, $batchCorrelationId)` is the public dispatch signature; the batch command must call this exact constructor
- Outstanding manual verification: Filament sidebar rendering UX (Plan 12-05 may want a checkpoint:human-verify task)
</output>

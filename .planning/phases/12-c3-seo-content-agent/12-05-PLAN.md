---
phase: 12-c3-seo-content-agent
plan: 05
type: execute
wave: 4
depends_on:
  - 12-c3-seo-content-agent/04
files_modified:
  - app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php
  - routes/console.php
  - database/seeders/RolePermissionSeeder.php
  - app/Domain/Suggestions/Filament/Resources/SuggestionResource.php
  - tests/Feature/Agents/Seo/RunSeoAgentBatchCommandTest.php
  - tests/Feature/Agents/Seo/BatchCommandBudgetRaceTest.php
  - tests/Feature/Agents/Seo/SeoAgentEligibilityQueryTest.php
  - tests/Architecture/Phase12VerificationTest.php
  - .planning/phases/12-c3-seo-content-agent/12-VERIFICATION.md
autonomous: false
requirements:
  - SEOAGT-05
must_haves:
  truths:
    - "RunSeoAgentBatchCommand dispatches up to 20 RunSeoAgentJob instances per run for eligible Products (auto_create_status='pending_review' AND completeness_score < 85, ordered worst-first, excluding products with pending/applied seo_content_patch Suggestions)"
    - "Schedule entry in routes/console.php runs the batch nightly at 04:30 Europe/London with withoutOverlapping(60) + onOneServer()"
    - "Batch command re-checks monthly budget between dispatches and stops early on breach (P12-E mitigation)"
    - "Shield permission 'run_seo_agent' exists (admin + pricing_manager get it; sales + read_only do not)"
    - "SuggestionResource default list filters out kind='agent_guardrail_blocked' (Open Question O-5 resolved)"
    - "AGENT_SEO_BATCH_SCHEDULE_ENABLED env flag wraps the schedule entry (default true, operator can flip false for emergency disable per O-2)"
    - "12-VERIFICATION.md ship verdict file exists with all 5 SEOAGT-* requirements ticked + UAT outcomes captured"
  artifacts:
    - path: app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php
      provides: Nightly batch command with budget race protection
      contains: "agents:run-seo-batch"
    - path: routes/console.php
      provides: Schedule entry at 04:30 Europe/London
      contains: "agents:run-seo-batch"
    - path: .planning/phases/12-c3-seo-content-agent/12-VERIFICATION.md
      provides: Ship verdict for Phase 12 with REQ traceability
      contains: "SEOAGT-01"
  key_links:
    - from: app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php
      to: app/Domain/Agents/Jobs/RunSeoAgentJob.php
      via: "RunSeoAgentJob::dispatch($productId, $batchCorrelationId)"
      pattern: "RunSeoAgentJob::dispatch"
    - from: routes/console.php
      to: app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php
      via: "Schedule::command('agents:run-seo-batch')->cron('30 4 * * *')"
      pattern: "agents:run-seo-batch"
---

<objective>
Ship the scheduled batch command `RunSeoAgentBatchCommand` (signature `agents:run-seo-batch {--limit=20} {--dry-run}`), wire its schedule entry at 04:30 Europe/London in `routes/console.php`, seed the new Shield permission `run_seo_agent` (admin + pricing_manager only), invoke `shield:safe-regenerate` to materialise policy templates, hide `agent_guardrail_blocked` Suggestions from the default Filament Suggestion list (Open Question O-5), produce the `12-VERIFICATION.md` ship verdict, and run a human-verify checkpoint exercising the full end-to-end nightly batch flow.

Purpose: Close out SEOAGT-05 (budget-aware nightly batch) + the operational hygiene (Shield permission + audit Suggestion filter). After Plan 12-05 lands, Phase 12 is fully shipped — eligible AutoCreate drafts get nightly content-patch proposals, admin reviews via the sidebar from Plan 12-04, and budget guardrails hold under load.

Output: 1 artisan command + 1 schedule entry + RolePermissionSeeder update + 1 Filament resource filter + 12-VERIFICATION.md + 3 Pest tests + 1 architecture test + 1 human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-c3-seo-content-agent/12-CONTEXT.md
@.planning/phases/12-c3-seo-content-agent/12-RESEARCH.md
@.planning/phases/12-c3-seo-content-agent/12-04-SUMMARY.md

# Precedent commands
@app/Domain/Competitor/Console/Commands/CompetitorWatchCommand.php
@app/Foundation/Console/BaseCommand.php

# Phase 10 RolePermissionSeeder shape (for new permission addition)
@database/seeders/RolePermissionSeeder.php

# Phase 8 shield:safe-regenerate
@app/Domain/Agents/Console/Commands/ShieldSafeRegenerateCommand.php

# Filament default-filter precedent
@app/Domain/Suggestions/Filament/Resources/SuggestionResource.php

# Schedule registration precedent
@routes/console.php

# Plan 12-04 deliverables
@app/Domain/Agents/Jobs/RunSeoAgentJob.php

<interfaces>
<!-- Public contract Plan 12-05 calls -->

From app/Domain/Agents/Jobs/RunSeoAgentJob.php (Plan 12-04 shipped):
```php
public function __construct(
    public readonly int $productId,
    public readonly ?string $batchCorrelationId = null,
);
RunSeoAgentJob::dispatch($productId, $batchCorrelationId);   // public dispatch signature
```

Eligibility query (RESEARCH §Pattern 5 + CONTEXT Claude's Discretion):
```php
Product::query()
    ->where('auto_create_status', 'pending_review')
    ->where('completeness_score', '<', 85)
    ->whereDoesntHave('suggestions', fn ($q) =>
        $q->where('kind', 'seo_content_patch')
          ->whereIn('status', ['pending', 'applied']))
    ->orderBy('completeness_score')   // worst-first
    ->limit($limit)
    ->get();
```

Monthly budget cache key shape (from Phase 8 BudgetGuard):
- `Cache::get('agents.monthly.' . now('Europe/London')->format('Y-m'), 0)` — current monthly pence spent
- `config('agents.monthly_ceiling_pence', 20000)` — £200 ceiling

RolePermissionSeeder (Phase 10 pattern): add `'run_seo_agent'` to admin + pricing_manager role permission arrays.

SuggestionResource::getEloquentQuery() (Filament 3 standard hook): add `->where('kind', '!=', 'agent_guardrail_blocked')` to default scope (Open Question O-5 resolution).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RunSeoAgentBatchCommand with budget race protection (P12-E)</name>
  <read_first>
    - app/Domain/Competitor/Console/Commands/CompetitorWatchCommand.php (BaseCommand precedent — file-walk dispatch loop pattern)
    - app/Foundation/Console/BaseCommand.php (verify `perform()` method signature + return type)
    - app/Domain/Agents/Jobs/RunSeoAgentJob.php (Plan 12-04 deliverable — public dispatch signature)
    - app/Domain/Agents/Services/BudgetGuard.php (verify cache key shape `agents.monthly.{YYYY-MM}`)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 5 RunSeoAgentBatchCommand code block (verbatim)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §P12-E (between-dispatch budget recheck)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Open Question O-2 (AGENT_SEO_BATCH_SCHEDULE_ENABLED env flag)
  </read_first>
  <behavior>
    - Test (eligibility): Given 5 Products with auto_create_status='pending_review' + completeness_score values 60/70/65/80/95 — command in dry-run mode logs exactly 4 eligible (excludes the score=95 row) and dispatches 0 jobs
    - Test (eligibility ordering): The 4 eligible rows are processed in completeness_score ASC order (worst-first): 60 → 65 → 70 → 80
    - Test (exclude-already-suggested): Given a Product with pending 'seo_content_patch' Suggestion — that product is excluded from eligibility
    - Test (limit): With `--limit=2` and 5 eligible products, command dispatches exactly 2 jobs
    - Test (live dispatch): With `--dry-run` flag OFF, command calls `RunSeoAgentJob::dispatch($productId, $batchCorrelationId)` once per eligible product; all jobs share the same batchCorrelationId
    - Test (P12-E budget race): Cache::put('agents.monthly.2026-05', 19980) — command dispatches no more than 4 jobs before stopping (4 × 5p = 20p brings total to 20,000p = exactly the £200 ceiling); 5th dispatch is suppressed
    - Test (early monthly breach): Cache::put('agents.monthly.2026-05', 20001) — command aborts before any dispatch with "Monthly budget already exceeded" message
  </behavior>
  <action>
    1. Create `app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php` extending `BaseCommand` with the EXACT body from RESEARCH §Pattern 5:
       - `protected $signature = 'agents:run-seo-batch {--limit=20 : Max products to process this run} {--dry-run : Show eligible products without dispatching}';`
       - `protected $description = 'Phase 12 SEOAGT-05 — nightly SEO agent batch over Phase 6 AutoCreate drafts';`
       - `protected function perform(): int` (BaseCommand pattern):
         - Pre-flight monthly budget check — read `Cache::get('agents.monthly.' . now('Europe/London')->format('Y-m'), 0)`; if `>= config('agents.monthly_ceiling_pence', 20000)` → warn + return SUCCESS
         - Build eligibility query verbatim from RESEARCH §Pattern 5 (interface block above)
         - If `$eligible->isEmpty()` → info + return SUCCESS
         - Generate `$batchCorrelationId = (string) Str::uuid();`
         - For each eligible product:
           - If `--dry-run` → `$this->line(sprintf('  [DRY] %s (score=%d)', $product->sku, $product->completeness_score));` continue
           - Re-check monthly budget BETWEEN dispatches (P12-E): read `Cache::get('agents.monthly.{YYYY-MM}', 0)` again; if `>= $monthlyCap` → warn "stopping at {$dispatched}/{$eligible->count()}" + break
           - `RunSeoAgentJob::dispatch($product->id, $batchCorrelationId);` increment `$dispatched`
         - Info: `Dispatched {$dispatched} SeoAgent runs on agents queue.`
         - Return SUCCESS
    2. Write `tests/Feature/Agents/Seo/RunSeoAgentBatchCommandTest.php` covering eligibility, ordering, exclude-already-suggested, limit, live dispatch. Use `Queue::fake()` to assert `RunSeoAgentJob` dispatch count + arguments.
    3. Write `tests/Feature/Agents/Seo/BatchCommandBudgetRaceTest.php` covering P12-E — seed Cache state at 19980 + 20001 thresholds + assert correct stop behaviour.
    4. Write `tests/Feature/Agents/Seo/SeoAgentEligibilityQueryTest.php` — dedicated test for the eligibility query in isolation (worst-first ordering, exclusion of products with existing seo_content_patch Suggestion).
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Feature/Agents/Seo/RunSeoAgentBatchCommandTest.php tests/Feature/Agents/Seo/BatchCommandBudgetRaceTest.php tests/Feature/Agents/Seo/SeoAgentEligibilityQueryTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php` contains the literal command signature string `'agents:run-seo-batch'`
    - File contains `auto_create_status` AND `completeness_score` AND `whereDoesntHave('suggestions'` (eligibility query)
    - File contains `orderBy('completeness_score')` (worst-first)
    - File contains TWO occurrences of `Cache::get('agents.monthly.` (P12-E: pre-flight AND between-dispatch checks)
    - File contains `RunSeoAgentJob::dispatch(`
    - `php artisan agents:run-seo-batch --dry-run` exits 0 (smoke check — command registers correctly)
    - Pest test asserts `Queue::assertPushed(RunSeoAgentJob::class, 4);` after seeding 4 eligible products + running command (no `--dry-run`)
    - Pest test asserts batch stops early when Cache shows monthly spent near ceiling (P12-E)
    - All 3 test files exit 0 with combined ≥7 assertions
  </acceptance_criteria>
  <done>Batch command shipped with budget race protection. Eligibility query confirmed by Pest tests. Worst-first ordering verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule wiring + AGENT_SEO_BATCH_SCHEDULE_ENABLED env flag + Shield permission run_seo_agent</name>
  <read_first>
    - routes/console.php (existing schedule entries — find correct insertion point alphabetically/grouped)
    - database/seeders/RolePermissionSeeder.php (existing permission seeding pattern — find the admin + pricing_manager role permission arrays)
    - app/Domain/Agents/Console/Commands/ShieldSafeRegenerateCommand.php (Phase 8 shield:safe-regenerate wrapper)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 5 routes/console.php block
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Open Question O-2 (env flag wrap)
  </read_first>
  <behavior>
    - Test: `php artisan schedule:list` includes an entry for `agents:run-seo-batch` with cron expression `30 4 * * *` and timezone `Europe/London`
    - Test: Setting `AGENT_SEO_BATCH_SCHEDULE_ENABLED=false` in env + booting fresh → `php artisan schedule:list` does NOT include `agents:run-seo-batch`
    - Test: `Permission::where('name', 'run_seo_agent')->exists() === true` after running `php artisan db:seed --class=RolePermissionSeeder`
    - Test: Admin role and pricing_manager role have the `run_seo_agent` permission; sales role and read_only role do NOT
    - Test: After `shield:safe-regenerate` runs, `PolicyTemplateIntegrityTest` continues to pass (no orphaned `{{ Placeholder }}` literals)
  </behavior>
  <action>
    1. Open `routes/console.php`. Add the schedule entry verbatim from RESEARCH §Pattern 5, wrapped in O-2's env flag:
       ```php
       // Phase 12 SEOAGT-05 — nightly SEO agent batch at 04:30 Europe/London.
       // Slots between competitor:ftp-pull (Sun+Wed 02:00) and supplier:db-sync
       // (Mon-Fri 07:00). Single nightly cadence per SEOAGT-05 success criterion 1.
       // O-2: env flag allows operator emergency disable without code deploy.
       if ((bool) env('AGENT_SEO_BATCH_SCHEDULE_ENABLED', true)) {
           Schedule::command('agents:run-seo-batch')
               ->cron('30 4 * * *')
               ->withoutOverlapping(60)
               ->onOneServer()
               ->timezone('Europe/London')
               ->description('Phase 12 SEOAGT-05 — nightly SEO agent batch (04:30 Europe/London)');
       }
       ```
       Place near other agents:* scheduled commands if a section exists; otherwise place after the most-similar competitor:* or supplier:* schedule entry.
    2. Open `.env.example`. Add a documented entry for the new flag:
       ```
       # Phase 12 — toggle nightly SEO agent batch schedule (operator emergency disable).
       # Default true. Set to false to halt nightly runs without code deploy.
       AGENT_SEO_BATCH_SCHEDULE_ENABLED=true
       ```
    3. Open `database/seeders/RolePermissionSeeder.php`. Find the array(s) of permissions for `admin` + `pricing_manager` roles. ADD the literal string `'run_seo_agent'` to both arrays. Confirm `sales` and `read_only` arrays do NOT receive it.
    4. Run `php artisan shield:safe-regenerate` (the Phase 8 wrapper) to materialise policy templates. Verify `PolicyTemplateIntegrityTest` still passes (floor 27 unchanged — no new policy class, only a new permission).
    5. Write `tests/Feature/Agents/Seo/ScheduleWiringTest.php` (or extend an existing schedule test) asserting:
       - `Artisan::call('schedule:list')` output contains `agents:run-seo-batch` AND `30 4 * * *`
       - Setting env to false (via `putenv` or `config()->set`) suppresses the schedule entry
       - `Permission::findByName('run_seo_agent')` returns a permission row
       - Admin role has 'run_seo_agent' (assert via `$adminRole->hasPermissionTo('run_seo_agent') === true`)
       - Sales role does NOT have it (`$salesRole->hasPermissionTo('run_seo_agent') === false`)
  </action>
  <verify>
    <automated>php artisan shield:safe-regenerate && php vendor/bin/pest tests/Feature/Agents/Seo/ScheduleWiringTest.php tests/Architecture/PolicyTemplateIntegrityTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `routes/console.php` contains the string `agents:run-seo-batch` AND `30 4 * * *` AND `Europe/London` AND `AGENT_SEO_BATCH_SCHEDULE_ENABLED`
    - `.env.example` contains `AGENT_SEO_BATCH_SCHEDULE_ENABLED=true`
    - `database/seeders/RolePermissionSeeder.php` contains the literal string `'run_seo_agent'` (grep: `grep -c "'run_seo_agent'" database/seeders/RolePermissionSeeder.php` returns ≥2 — admin + pricing_manager)
    - `php artisan shield:safe-regenerate` exits 0
    - `php artisan schedule:list 2>&1 | grep -c "agents:run-seo-batch"` returns ≥1
    - `php vendor/bin/pest tests/Feature/Agents/Seo/ScheduleWiringTest.php` exits 0 with ≥5 assertions
    - `php vendor/bin/pest tests/Architecture/PolicyTemplateIntegrityTest.php` continues to pass (no regression)
  </acceptance_criteria>
  <done>Schedule wired with env-flag emergency disable. Shield permission seeded for admin + pricing_manager only. shield:safe-regenerate runs clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hide agent_guardrail_blocked from Filament SuggestionResource default list (O-5) + Phase12VerificationTest</name>
  <read_first>
    - app/Domain/Suggestions/Filament/Resources/SuggestionResource.php (find `getEloquentQuery()` or equivalent default scope override hook)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Open Question O-5 (filter approach + escape hatch)
  </read_first>
  <behavior>
    - Test: A pending Suggestion of kind `agent_guardrail_blocked` does NOT appear in the default `SuggestionResource::getEloquentQuery()` result set
    - Test: Pending Suggestions of kind `seo_content_patch`, `margin_change`, etc. continue to appear in the default list (no regression)
    - Test: When an admin explicitly filters by kind='agent_guardrail_blocked' (via a filter chip), those rows DO appear (escape hatch)
    - Test (Phase12VerificationTest — architecture-level): All 5 SEOAGT-* requirements have corresponding class/file artefacts in the repo:
      - SEOAGT-01: `app/Domain/Agents/Agents/SeoAgent.php` + `app/Domain/Agents/Jobs/RunSeoAgentJob.php` exist
      - SEOAGT-02: All 4 tools exist under `app/Domain/Agents/Tools/Seo/` with correct names
      - SEOAGT-03: `app/Domain/Agents/Appliers/SeoContentPatchApplier.php` + `EditAutoCreateReview::seoPatchesInfolist` method exist
      - SEOAGT-04: `app/Domain/Agents/Guardrails/SeoOutboundGuardrail.php` + `config/seo_agent.php` exist
      - SEOAGT-05: `app/Domain/Agents/Console/Commands/RunSeoAgentBatchCommand.php` exists AND `php artisan schedule:list` includes `agents:run-seo-batch`
  </behavior>
  <action>
    1. Open `app/Domain/Suggestions/Filament/Resources/SuggestionResource.php`. Add or extend the `getEloquentQuery()` static method:
       ```php
       public static function getEloquentQuery(): Builder
       {
           return parent::getEloquentQuery()
               ->when(
                   ! request()->has('tableFilters.kind.value'),  // only filter on default view; allow explicit filter to bypass
                   fn ($q) => $q->where('kind', '!=', 'agent_guardrail_blocked'),
               );
       }
       ```
       NOTE: Read the existing `getEloquentQuery` (if present from Phase 10 or earlier) and extend it additively. Adapt the `when` condition to match Filament 3.3's actual filter detection mechanism (the goal is: default list hides this kind; explicit filter-by-kind shows it). Verify the approach with a Pest test rather than guessing the exact Filament 3.3 API.
    2. Add a filter chip in the same resource — `Tables\Filters\SelectFilter::make('kind')` with an option list that INCLUDES `'agent_guardrail_blocked'` so admin can opt-in to viewing audit-blocked rows.
    3. Write `tests/Architecture/Phase12VerificationTest.php` covering the 5 SEOAGT-* file-existence + smoke checks listed in the `<behavior>` block. Use `expect(file_exists($path))->toBeTrue()` per artefact.
    4. Write Pest tests confirming the filter behaviour — feed seeded Suggestions of mixed kinds + assert default query excludes `agent_guardrail_blocked` but explicit filter includes it.
  </action>
  <verify>
    <automated>php vendor/bin/pest tests/Architecture/Phase12VerificationTest.php tests/Feature/Agents/Seo/ --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `app/Domain/Suggestions/Filament/Resources/SuggestionResource.php` contains `agent_guardrail_blocked` (in either filter array or default-query scope)
    - Pest test confirms default list query does NOT include `agent_guardrail_blocked` row
    - Pest test confirms explicit kind filter DOES return the `agent_guardrail_blocked` row
    - `tests/Architecture/Phase12VerificationTest.php` exists and asserts existence of 7 critical Phase 12 files (1 agent + 4 tools + 1 applier + 1 guardrail + 1 batch command)
    - Test asserts `php artisan schedule:list` output contains `agents:run-seo-batch`
    - All tests exit 0 with combined ≥10 assertions
  </acceptance_criteria>
  <done>Audit-blocked Suggestions hidden from default Filament list with escape-hatch filter. Phase12VerificationTest covers all 5 REQ-IDs to artefacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify end-to-end SEO agent batch flow (Filament UX + nightly schedule rehearsal)</name>
  <read_first>
    - .planning/phases/12-c3-seo-content-agent/12-CONTEXT.md (D-03 patch granularity + Filament sidebar UX expectations)
    - .planning/phases/12-c3-seo-content-agent/12-RESEARCH.md §Pattern 4 (sidebar Section + Approve-selected variant)
  </read_first>
  <what-built>
    Full Phase 12 SEO agent pipeline:
    - SeoAgent + 4 tools + brand-voice markdown
    - System prompt + SeoOutboundGuardrail with regex pattern library
    - RunSeoAgentJob + SeoAgentResultMapper + SeoContentPatchApplier
    - Filament sidebar Section on EditAutoCreateReview
    - RunSeoAgentBatchCommand scheduled at 04:30 Europe/London
    - run_seo_agent Shield permission seeded
    - agent_guardrail_blocked Suggestions hidden from default list
  </what-built>
  <how-to-verify>
    Run these manual steps as admin user (logged into the Filament admin panel):

    1. **Eligibility seed:** Pick a Product in dev with `auto_create_status='pending_review'` AND `completeness_score < 85`. If none exists, create one via `php artisan tinker` (or use an existing AutoCreate draft from Phase 6 testing).

    2. **Dispatch single run:** Run `php artisan agents:run-seo-batch --limit=1 --dry-run` — expect output showing the eligible product. Then run `php artisan agents:run-seo-batch --limit=1` (live) — confirm 1 job dispatched on the agents queue.

    3. **Wait for completion:** Watch `php artisan horizon:status` or `/admin/horizon` until the job completes. Then check `/admin/agent-runs` — confirm an AgentRun row exists with kind=seo, status=completed, cost_pence > 0, and the Langfuse trace link works (if Langfuse stack is up).

    4. **Review sidebar:** Navigate to `/admin/products/auto-create-review/{productId}/edit`. Confirm the SEO patches Section renders below/beside the existing edit form. Verify:
       - 1-4 diff rows visible (one per patched field)
       - `field` badges show correct field names (title / short_description / etc.)
       - `before` and `after` columns side-by-side, monospace, truncated at 200 chars with tooltip on hover
       - `reasoning` collapsible/visible
       - `applied_at` shows "— pending —" for unselected, or a timestamp for already-applied

    5. **Approve selected patches:** Tick 2 of the patches in the CheckboxList. Click "Approve selected patches". Confirm:
       - Notification fires
       - Product.name (or whichever field corresponds) updates to the patch `after` value (refresh the page; check the form field)
       - ProductOverride row for this product has `pin_{field}=true` for the approved fields (confirm via `php artisan tinker --execute="dd(\App\Domain\Pricing\Models\ProductOverride::where('product_id', $id)->first());"`)
       - Suggestion status flips to `pending` (subset approved — 2 of 4) or `applied` (all 4 approved)

    6. **Audit trail:** Check `/admin/audit-log` (or whatever the v1 Auditor UI is) for `seo.content_patch_applied` entries — one per approved field with before_hash + after_hash visible.

    7. **Guardrail-blocked smoke:** Manually craft a Prism::fake response (or temporarily lower the regex bar) to trigger a guardrail block. Confirm:
       - AgentRun.status = guardrail_blocked
       - ONE Suggestion of kind `agent_guardrail_blocked` exists (visible only when admin explicitly filters by kind in Filament)
       - ZERO Suggestions of kind `seo_content_patch` for that run

    8. **Schedule rehearsal:** `php artisan schedule:list` shows `agents:run-seo-batch` at `30 4 * * *` London. Set `AGENT_SEO_BATCH_SCHEDULE_ENABLED=false` in `.env`, run `schedule:list` again, confirm the entry disappears. Reset to `true`.

    9. **Permission gate:** Log out, log in as a `sales` role user, navigate to `/admin/agent-runs` (or wherever `run_seo_agent` gates appear). Confirm action is blocked (403 or hidden). Repeat as `pricing_manager` and `admin` — confirm action is allowed.

    10. **Budget cap rehearsal (optional):** `php artisan tinker --execute="Cache::put('agents.monthly.' . now()->format('Y-m'), 19990);"` then run `php artisan agents:run-seo-batch --limit=20`. Confirm batch dispatches no more than 2-3 jobs before stopping with "Monthly budget exceeded mid-batch" warning.
  </how-to-verify>
  <resume-signal>
    Reply with "approved" if all 10 steps work as described OR "issues: [list]" if any step fails. If any step fails, the orchestrator will either replan via `/gsd-plan-phase 12 --gaps` or push for direct fix-up in this plan.
  </resume-signal>
  <action>Run all 10 manual steps documented in &lt;how-to-verify&gt; in order. Capture outcome (approved / list of issues) for inclusion in Task 5's 12-VERIFICATION.md.</action>
  <verify>
    <automated>echo "Manual checkpoint — see &lt;how-to-verify&gt; for the 10 steps; resume-signal captured by orchestrator"</automated>
  </verify>
  <done>User has run all 10 steps and replied with either "approved" or "issues: [list]" via the resume-signal mechanism. Outcome captured in Task 5's 12-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 5: Generate 12-VERIFICATION.md ship verdict</name>
  <read_first>
    - .planning/phases/10-c1-pricing-agent/10-VERIFICATION.md (precedent format)
    - .planning/phases/11-e2-quote-request-bitrix-deal-flow/11-VERIFICATION.md (alternative precedent)
    - .planning/REQUIREMENTS.md (SEOAGT-01..05 contract surface to tick off)
    - .planning/phases/12-c3-seo-content-agent/12-CONTEXT.md (D-01..D-04 to confirm honoured)
    - All 5 12-NN-SUMMARY.md files from Plans 01-04 (so Plan 05 verification summarises across the phase)
  </read_first>
  <action>
    Create `.planning/phases/12-c3-seo-content-agent/12-VERIFICATION.md` covering:
    1. **Requirements traceability** (5-row table — SEOAGT-01..05 each mapped to evidence: file paths + Pest test names + green/red status)
    2. **Decision honour confirmation** (4 rows — D-01..D-04 from CONTEXT.md, each marked Honoured with grep-evidence pointer)
    3. **Pitfall mitigation log** (P12-A..P12-H from RESEARCH.md, each marked with the Pest test name that prevents regression — or "deferred" with reason)
    4. **Open Question resolution log** (5 rows — O-1..O-5 from RESEARCH.md, each marked Resolved with the decision)
    5. **Test count summary** (total Pest cases shipped — target ≥20 per CONTEXT.md scope)
    6. **Human-verify outcome** (paste the resume-signal reply from Task 4 — "approved" or list of issues)
    7. **Cost calibration** (actual cost_pence from the manual happy-path run in Task 4 step 3 — captures real Anthropic billing vs RESEARCH §Token Budget Calibration estimate of ~5p)
    8. **Ship verdict:** SHIP / SHIP-WITH-FOLLOWUP / HOLD with rationale
    9. **Deferred items** for v2.1+ (confidence band, rejection inbox, char-level diff, semantic LLM guardrail — all per CONTEXT.md Deferred Ideas)
    10. **Operator handover notes** — env vars introduced (AGENT_SEO_BATCH_SCHEDULE_ENABLED, AGENTS_SEO_TEMPERATURE), schedule entry, Shield permission name

    Commit the file as part of the same commit as the rest of Plan 12-05.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-c3-seo-content-agent/12-VERIFICATION.md && grep -c "SEOAGT-01\|SEOAGT-02\|SEOAGT-03\|SEOAGT-04\|SEOAGT-05" .planning/phases/12-c3-seo-content-agent/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/12-c3-seo-content-agent/12-VERIFICATION.md` exists
    - File contains all 5 SEOAGT-* requirement IDs (grep count ≥5)
    - File contains all 4 D-01..D-04 decision IDs from CONTEXT.md
    - File contains a "Ship verdict:" line with one of SHIP / SHIP-WITH-FOLLOWUP / HOLD
    - File contains the human-verify outcome from Task 4
    - File ≥ 200 lines (substantive verification document, not a stub)
  </acceptance_criteria>
  <done>12-VERIFICATION.md ship verdict committed. Phase 12 fully traceable end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron daemon → RunSeoAgentBatchCommand (system trust) | Trusted — Laravel scheduler invoked by host cron; no external input |
| Batch dispatch → BudgetGuard cache (P12-E) | Race-protected via Redis atomic GET-between-dispatches |
| Shield permission seeder → admin/pricing_manager roles | Trusted — DB seeded; sales/read_only explicitly denied |
| Filament SuggestionResource default scope → agent_guardrail_blocked Suggestions | Filtered out by default; escape hatch for forensic review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Denial of Service | Batch dispatches all 20 jobs ignoring intra-batch budget breaches (P12-E) | mitigate | Between-dispatch Cache::get check; Pest test seeds Cache state near ceiling + asserts early stop |
| T-12-05-02 | Elevation of Privilege | Sales user triggers run_seo_agent | mitigate | RolePermissionSeeder seeds permission to admin + pricing_manager ONLY; Pest test asserts sales role does NOT have permission |
| T-12-05-03 | Repudiation | Schedule disabled by env flag with no audit trail of the disable | accept | `.env.example` documents AGENT_SEO_BATCH_SCHEDULE_ENABLED; ops change-control is the audit mechanism for env edits |
| T-12-05-04 | Information Disclosure | agent_guardrail_blocked Suggestions surface to non-admin via Filament | mitigate | SuggestionResource::getEloquentQuery() filters by default; escape hatch requires explicit kind filter (admin-only access already gated by Phase 1 SuggestionPolicy) |
| T-12-05-05 | Tampering | Permission seeder silently fails to seed run_seo_agent | mitigate | Pest test asserts `Permission::findByName('run_seo_agent')->exists()` + admin/pricing_manager have it post-seed |
</threat_model>

<verification>
- All 3 Pest tasks pass (Tasks 1-3): `php vendor/bin/pest tests/Feature/Agents/Seo/ tests/Architecture/Phase12VerificationTest.php` exits 0
- Human-verify checkpoint (Task 4) approved by user
- 12-VERIFICATION.md exists with ship verdict (Task 5)
- shield:safe-regenerate runs clean
- `php artisan schedule:list` includes `agents:run-seo-batch`
- Full Phase 12 test suite green: `php vendor/bin/pest tests/Feature/Agents/Seo/ tests/Unit/Agents/Tools/Seo/ tests/Architecture/{TruncatingToolRelocationTest,SeoAgentConfigTest,Phase12VerificationTest}.php` exits 0
- No regression on prior phases: `php vendor/bin/pest tests/Feature/Agents/Pricing tests/Feature/ProductAutoCreate` exits 0
</verification>

<success_criteria>
- Batch command dispatches eligible Products with budget race protection
- Schedule entry at 04:30 Europe/London + env-flag emergency disable
- Shield permission seeded to correct roles; shield:safe-regenerate clean
- agent_guardrail_blocked Suggestions hidden from default Filament list with escape hatch
- 12-VERIFICATION.md captures all 5 SEOAGT-* + 4 D-* + 8 P12-* + 5 O-* with traceability
- Human-verify approval ("approved") OR documented issues triggering follow-up
</success_criteria>

<output>
After completion, create `.planning/phases/12-c3-seo-content-agent/12-05-SUMMARY.md` capturing:
- Final shipped Phase 12 surface (1 SeoAgent + 4 tools + 1 mapper + 1 applier + 1 guardrail + 1 job + 1 batch command + 1 schedule entry + 1 Shield permission + 1 Filament panel)
- Total Pest case count across Plans 01-05 (target ≥25)
- Verified Anthropic cost per run (actual vs estimated 5p)
- 12-VERIFICATION.md location and ship verdict
- Operator handover summary (3 env vars to flip if needed; 1 Shield permission name; 1 schedule entry; 1 monthly Cache key to monitor)
- Next milestone-level: Phase 13 (E3 WhatsApp Channel) — depends on Phase 1 (already shipped), no Phase 12 dependency. STATE.md should advance.
</output>

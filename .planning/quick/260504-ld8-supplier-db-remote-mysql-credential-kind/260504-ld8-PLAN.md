---
quick_id: 260504-ld8
description: Supplier DB (Remote MySQL) credential kind — Phase 1
date: 2026-05-04
must_haves:
  truths:
    - IntegrationCredentialKind enum currently has 6 cases — adding SupplierDb makes 7
    - IntegrationTestResult::ok($latency) and ::failed($message, $latency) — both take latency_ms
    - mysqli is the right tool for one-shot auth probe (NOT registered Laravel connection — that's Phase 2)
  artifacts:
    - app/Domain/Integrations/Enums/IntegrationCredentialKind.php
    - app/Domain/Integrations/Services/IntegrationCredentialResolver.php
    - config/services.php
    - app/Domain/Integrations/Filament/Actions/TestIntegrationAction.php
    - tests/Feature/Integrations/IntegrationCredentialKindEnumTest.php
    - tests/Feature/Integrations/IntegrationCredentialResolverTest.php
---

# Plan 260504-ld8

## Tasks

1. Enum: add `SupplierDb` case + 3 match-expression entries
2. Resolver: add env-fallback branch
3. config/services.php: supplier_db stub
4. TestIntegrationAction: dispatch branch + private `testSupplierDb()` helper using mysqli
5. Tests: enum count 6→7 + requiredFields assertion + resolver beforeEach env wipes
6. Verify: pest + deptrac + admin URL probe
7. Commit + SUMMARY + STATE

---
quick_id: 260504-m5w
description: supplier:db-sync command + daily schedule (Woo + Supplier)
date: 2026-05-04
must_haves:
  truths:
    - SupplierDb credentials saved + Test Connection verified working (78ms)
    - Remote table = supplier_products (646,985 rows on stcav_dash)
    - Match key precedence: LOWER(TRIM(mpn)) first, LOWER(TRIM(suppliersku)) fallback, product_excluded=0
    - Local Product model: buy_price decimal(12,4), stock_quantity nullable int
    - BaseCommand requires perform() (not handle); commands explicitly registered in AppServiceProvider per Phase 2/3 precedent
  artifacts:
    - app/Domain/Sync/Commands/SupplierDbSyncCommand.php (NEW)
    - app/Providers/AppServiceProvider.php (register command)
    - routes/console.php (two daily schedule entries)
    - tests/Feature/Sync/SupplierDbSyncCommandTest.php (unit-test helper methods)
---

# Plan 260504-m5w

See parent prompt for full details. Summary:

1. Build `supplier:db-sync` (BaseCommand → perform), inject IntegrationCredentialResolver, mysqli one-shot, --dry-run + --limit flags.
2. Schedule wiring: woo:import-products daily 03:00 + supplier:db-sync daily 03:30 (Europe/London).
3. Register command in AppServiceProvider runningInConsole.
4. Tests for parseStock/parsePrice/buildSkuMap helpers.
5. Live verify: --dry-run, --dry-run --limit=10, then live run, then probe buy_price/stock_quantity coverage.
6. Commit.

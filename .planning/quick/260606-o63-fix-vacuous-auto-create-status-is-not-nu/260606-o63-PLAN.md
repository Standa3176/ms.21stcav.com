---
quick_id: 260606-o63
type: quick
plan: 01
wave: 1
depends_on: []
files_modified:
  - app/Domain/Products/Models/Product.php
  - tests/Unit/Models/ProductScopeAutoCreatedTest.php
  - app/Console/Commands/RetryMissingImagesCommand.php
  - app/Filament/Pages/AutoCreateHealthPage.php
  - tests/Architecture/AutoCreatedPredicateTest.php
autonomous: true
requirements:
  - QUICK-260606-o63
must_haves:
  truths:
    - "`Product::query()->autoCreated()` returns only rows where auto_create_status != 'manual' (i.e. EXCLUDES the 5,632 legacy WC-imported 'manual' rows)."
    - "`RetryMissingImagesCommand` no longer treats `auto_create_status IS NOT NULL` as a meaningful filter — it uses the canonical scope."
    - "`AutoCreateHealthPage` uses the same canonical scope as `RetryMissingImagesCommand` (single source of truth — drift-prevention)."
    - "CI fails if anyone re-introduces `whereNotNull('auto_create_status')` or raw `auto_create_status IS NOT NULL` anywhere under `app/`."
  artifacts:
    - path: "app/Domain/Products/Models/Product.php"
      provides: "scopeAutoCreated(Builder) -> Builder method"
      contains: "scopeAutoCreated"
    - path: "tests/Unit/Models/ProductScopeAutoCreatedTest.php"
      provides: "Unit test pinning the != 'manual' semantic"
      contains: "->autoCreated()"
    - path: "tests/Architecture/AutoCreatedPredicateTest.php"
      provides: "Pest arch test forbidding vacuous IS-NOT-NULL predicates in app/"
      contains: "whereNotNull"
  key_links:
    - from: "app/Console/Commands/RetryMissingImagesCommand.php"
      to: "Product::scopeAutoCreated"
      via: "->autoCreated() call"
      pattern: "->autoCreated\\(\\)"
    - from: "app/Filament/Pages/AutoCreateHealthPage.php"
      to: "Product::scopeAutoCreated"
      via: "->autoCreated() call on the base + breakdown queries"
      pattern: "->autoCreated\\(\\)"
---

<objective>
Fix a vacuous predicate bug uncovered by 260606-mx9: `whereNotNull('auto_create_status')` in `RetryMissingImagesCommand` is a NO-OP filter because the column is `NOT NULL DEFAULT 'manual'` (migration `2026_04_22_100300_add_auto_create_columns_to_products_table`). The bug silently bloated the 2026-06-06 Manhattan retry dry-run with thousands of legacy WC-migrated products.

Fix: introduce a canonical `Product::scopeAutoCreated` (`!= 'manual'`), swap the two filter sites to use it, and install a Pest architecture test that prevents the broken predicate from ever returning.

Purpose: Stop the silent-bloat bug, make the "auto-created products only" intent express itself in code, and harden the regression with a CI guardrail.

Output:
- `Product::scopeAutoCreated(Builder)` model scope.
- `tests/Unit/Models/ProductScopeAutoCreatedTest.php` pinning the `!= 'manual'` semantic.
- `RetryMissingImagesCommand` + `AutoCreateHealthPage` calling `->autoCreated()` instead of inline predicates.
- `tests/Architecture/AutoCreatedPredicateTest.php` failing CI if vacuous predicates return.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/Domain/Products/Models/Product.php
@app/Console/Commands/RetryMissingImagesCommand.php
@app/Filament/Pages/AutoCreateHealthPage.php
@database/migrations/2026_04_22_100300_add_auto_create_columns_to_products_table.php
@tests/Architecture/EnvUsageTest.php

<interfaces>
<!-- Key facts the executor needs without re-reading. -->

`auto_create_status` column definition (migration `2026_04_22_100300_add_auto_create_columns_to_products_table.php`):

```php
$t->enum('auto_create_status', [
    'manual',
    'draft',
    'pending_review',
    'approved',
    'published',
    'rejected',
    'needs_brand_or_category_assignment',
    'variations_not_supported_v1',
])->default('manual')->after('requires_manual_image_review')->index();
```

Plus a belt-and-braces backfill on `up()`:
```php
DB::table('products')->whereNull('auto_create_status')->update(['auto_create_status' => 'manual']);
```

CONSEQUENCE: every row has a value; `whereNotNull('auto_create_status')` is a vacuous NO-OP. The canonical "auto-created products only" predicate is `where('auto_create_status', '!=', 'manual')`.

`Product` model trait stack (already established):

```php
final class Product extends Model
{
    use HasFactory;
    use LogsActivity;
    use SoftDeletes;
    // ...
}
```

ProductFactory exists at `Database\Factories\Domain\Products\ProductFactory` and is wired via `newFactory()`.

Existing inline predicates to replace:

- `app/Console/Commands/RetryMissingImagesCommand.php:62` — `$query->whereNotNull('auto_create_status');` (BROKEN — vacuous).
- `app/Filament/Pages/AutoCreateHealthPage.php:141` and `:336` — `where('auto_create_status', '!=', 'manual')` (CORRECT semantic, refactor for consistency).

EnvUsageTest.php pattern reference for the new arch test: it (a) uses `RecursiveIteratorIterator` + `RecursiveDirectoryIterator` to walk a directory, (b) strips block + line comments with `preg_replace`, (c) regex-matches the forbidden pattern, (d) `expect($violations)->toBeEmpty(...)` with a helpful failure message, (e) ships a meta-assertion that the regex catches a synthetic positive and ignores a synthetic negative.
</interfaces>

<audit-scout>
Already-scouted `grep app/ for 'auto_create_status'` (14 hits). Pre-classified:

| File | Disposition | Why |
|---|---|---|
| `app/Domain/Products/Models/Product.php` | W (fillable list — write-side metadata, also where the scope lives) | listing 'auto_create_status' in `$fillable` |
| `app/Domain/ProductAutoCreate/Filament/Resources/AutoCreateReviewResource.php` | W | resource form/table field, not a filter |
| `app/Domain/ProductAutoCreate/Jobs/CreateWooProductJob.php` | W | sets the column post-Woo-create |
| `app/Domain/ProductAutoCreate/Jobs/PublishProductJob.php` | W | sets the column post-publish |
| `app/Console/Commands/AssignProductTaxonomyCommand.php` | W | sets the column when taxonomy resolved |
| `app/Console/Commands/GenerateProductDraftsCommand.php` | W | sets the column when draft created |
| `app/Domain/Pricing/Services/PricingOpsReport.php:89` | R-status (`whereIn(['draft','pending_review','needs_brand_or_category_assignment'])`) — NO EDIT |
| `app/Domain/Dashboard/Services/SnapshotAggregator.php:402, :450` | R-status (`where('auto_create_status', 'draft')`) — NO EDIT |
| `app/Domain/Dashboard/Services/WeeklyDigestComposer.php:181, :186` | R-status (`'draft'` + `'approved'`) — NO EDIT |
| `app/Domain/Agents/Agents/SeoAgent.php:33` | R-status (docblock — `'pending_review'`) — NO EDIT |
| `app/Domain/Agents/Jobs/RunSeoAgentJob.php:104, :107` | R-status (`!== 'pending_review'` guard + log payload) — NO EDIT |
| `app/Domain/Agents/Console\Commands\RunSeoAgentBatchCommand.php:153` | R-status (`where('auto_create_status', 'pending_review')`) — NO EDIT |
| `app/Console/Commands/RetryMissingImagesCommand.php:62` | **R-vacuous — EDIT** to `->autoCreated()` (the bug site) |
| `app/Filament/Pages/AutoCreateHealthPage.php:141, :336` | **R-correct — REFACTOR** to `->autoCreated()` for consistency |

Net edits in Task 2: TWO files (RetryMissingImagesCommand + AutoCreateHealthPage). All other 12 hits are write-sites or specific-status reads — DO NOT EDIT THEM.

Executor MUST still run the grep itself and paste the output annotated with W / R-status / R-vacuous / R-correct into the Task 2 commit body — this prevents over-editing and is the only audit trail of the disposition decisions.
</audit-scout>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Product::scopeAutoCreated + unit test pinning the != 'manual' semantic</name>
  <files>
    app/Domain/Products/Models/Product.php
    tests/Unit/Models/ProductScopeAutoCreatedTest.php
  </files>
  <behavior>
    - Given 3 seeded Products: A (auto_create_status='manual', legacy), B (auto_create_status='draft'), C (auto_create_status='published'),
      `Product::query()->autoCreated()->pluck('sku')` returns exactly [B.sku, C.sku] in ID order; A is EXCLUDED.
    - The scope produces a chainable `Builder` — calling `->where('brand_id', 99)` after `->autoCreated()` returns rows matching BOTH predicates (proves it's a real scope, not a terminal method).
    - The query is driver-agnostic: it runs identically against SQLite (Pest in-memory) and MySQL (no JSON funcs, no driver-specific SQL).
  </behavior>
  <action>
    Edit `app/Domain/Products/Models/Product.php`:
    1. Add `use Illuminate\Database\Eloquent\Builder;` to the existing import block (placed alphabetically with the other Eloquent imports).
    2. Add a new public method directly below `getPricingCategoryId()` (so it sits with the other query-time helpers) and ABOVE `newFactory()`:

       The method is named `scopeAutoCreated`, takes one `Builder` parameter, returns `Builder`, has a doc comment explaining (a) why this exists — the `NOT NULL DEFAULT 'manual'` column makes `whereNotNull` vacuous — and (b) referencing migration `2026_04_22_100300_add_auto_create_columns_to_products_table.php` + quick 260606-mx9 (where the bug was uncovered) + 260606-o63 (this fix), and (c) the body is `return $query->where('auto_create_status', '!=', 'manual');`.

       Eloquent strips the `scope` prefix and lowercases the next char on call, so callers invoke it as `Product::query()->autoCreated()`. Do not add type-driven query scopes (`#[Scope]` attribute) — stick to the Laravel 12 stable `scope{Name}` method convention used elsewhere in this codebase.

    Create `tests/Unit/Models/ProductScopeAutoCreatedTest.php` (Pest 3 syntax — `it(...)` / `describe(...)`, mirror the style of any existing `tests/Unit/Models/*Test.php` if present, otherwise the conventional Pest test file structure):
    - Use the `RefreshDatabase` trait (`uses(RefreshDatabase::class);` at the top of the file).
    - Seed three Products via `Product::factory()->create([...])` overriding `auto_create_status`: A='manual', B='draft', C='published'. Give each a unique SKU.
    - First test: assert `Product::query()->autoCreated()->pluck('sku')->all()` equals exactly `[B.sku, C.sku]` (in ID order — order them with `->orderBy('id')` if factory doesn't guarantee it). A MUST NOT appear.
    - Second test: assert the scope chains — `Product::query()->autoCreated()->where('id', $B->id)->pluck('sku')->all()` equals `[$B->sku]`. This proves the scope returns a `Builder`, not a `Collection`.
    - Third test: assert that ALL eight enum values are correctly classified — seed one Product per status value (one each: 'manual','draft','pending_review','approved','published','rejected','needs_brand_or_category_assignment','variations_not_supported_v1'), then assert `Product::query()->autoCreated()->count()` equals 7 (every status EXCEPT 'manual'). This locks the semantic against accidental inclusion of e.g. 'rejected' in a future refactor.

    No JSON column access, no `JSON_EXTRACT`, no `whereRaw` — pure column-equality so SQLite in-memory drives the test cleanly.

    Commit when both the scope edit and the test file are green:
    `feat(products): Product::scopeAutoCreated canonical "!= manual" predicate (260606-o63)`
    Body: one-line "Replaces the vacuous `whereNotNull('auto_create_status')` filter — column is NOT NULL DEFAULT 'manual' per migration 2026_04_22_100300, see quick 260606-mx9 for the bug uncovering."
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Unit/Models/ProductScopeAutoCreatedTest.php --colors=never</automated>
  </verify>
  <done>
    - `Product::scopeAutoCreated(Builder)` method exists in `app/Domain/Products/Models/Product.php` and returns `Builder`.
    - `tests/Unit/Models/ProductScopeAutoCreatedTest.php` has 3 tests, all green.
    - All eight enum statuses verified: only `'manual'` is excluded by the scope.
    - Atomic commit landed with the message above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit 14 call sites + swap the 2 known vacuous/inline-predicate filters to ->autoCreated()</name>
  <files>
    app/Console/Commands/RetryMissingImagesCommand.php
    app/Filament/Pages/AutoCreateHealthPage.php
  </files>
  <action>
    Step 1 — Re-run the audit, do not trust the scout blindly:
    Run `grep -rn "auto_create_status" app/` (or equivalent ripgrep). Compare the output line-by-line against the `<audit-scout>` table embedded in this plan's `<context>`. For EACH of the 14 (or N — if grep returns a different count, investigate WHY) hits, classify it as one of:
      - **W** (write-side: assigns or lists the column — leave alone)
      - **R-status** (read-side filtering on a SPECIFIC status value e.g. `'draft'` or `whereIn(['pending_review',...])` — leave alone)
      - **R-vacuous** (read-side `whereNotNull('auto_create_status')` or raw `auto_create_status IS NOT NULL` — MUST EDIT to `->autoCreated()`)
      - **R-correct** (read-side `where('auto_create_status', '!=', 'manual')` — REFACTOR to `->autoCreated()` for consistency)

    If you find ANY R-vacuous or R-correct site NOT pre-listed in the `<audit-scout>` table, edit it too (the scout is a starting point, not the final word). Write a one-line note about the surprise finding in the commit body.

    Step 2 — Edit `app/Console/Commands/RetryMissingImagesCommand.php`:
    - Line ~62, inside `if (! $includeAll) { ... }`: replace `$query->whereNotNull('auto_create_status');` with `$query->autoCreated();`.
    - Update the adjacent `$this->info(...)` message: change `'Scoping to auto-created products only (auto_create_status IS NOT NULL).'` to `'Scoping to auto-created products only (auto_create_status != \'manual\').'` — same intent, but now the message describes what the query ACTUALLY does, so the next dev reading the info line + the SQL log will not be misled.
    - Update the `--all` option description (line ~34) similarly: change `(auto_create_status IS NULL)` to `(auto_create_status = 'manual')`. The `--all` flag still does the same thing semantically (include legacy WC products); only the parenthetical description is now correct.

    Step 3 — Refactor `app/Filament/Pages/AutoCreateHealthPage.php`:
    - Line ~141 (inside `getNavigationBadgeTooltip()`): replace `Product::query()->where('auto_create_status', '!=', 'manual')` with `Product::query()->autoCreated()`.
    - Line ~336 (inside `unhealthyQuery()`): replace `Product::query()->where('auto_create_status', '!=', 'manual')` with `Product::query()->autoCreated()`.
    - Update the file's docblock note at lines 58-63 (the "Plan-vs-schema deviation" paragraph) to add a one-line forward reference: `// 260606-o63: extracted to Product::scopeAutoCreated for reuse + arch-test pinning.` This preserves the audit trail of how the predicate evolved.

    Step 4 — Verify nothing else needs editing:
    Re-run the grep. There should be ZERO occurrences of `whereNotNull('auto_create_status')`, ZERO occurrences of `where('auto_create_status', '!=', 'manual')`, and ZERO raw `auto_create_status IS NOT NULL` / `auto_create_status IS NULL` strings under `app/`. If any remain, edit them or document why they're legitimately exempt.

    Atomic commit (single commit covers ALL edits across both files):
    `fix(products): switch IS-NOT-NULL auto_create_status filters to ->autoCreated() scope (260606-o63)`

    Commit body MUST include:
    - A reference to 260606-mx9 (where the bug was uncovered) and the migration filename.
    - The full annotated grep output — paste the grep stdout, then for each line append ` -> W` / ` -> R-status` / ` -> R-vacuous EDITED` / ` -> R-correct REFACTORED`. This makes the review trivial and is the durable record of why the 12 non-edited sites were left alone.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Unit/Models/ProductScopeAutoCreatedTest.php tests/Feature/Filament/Pages/AutoCreateHealthPageTest.php --colors=never 2>&1 | tee /tmp/o63-task2-pest.txt; grep -RInE "whereNotNull\(\s*['\"]auto_create_status['\"]\s*\)|auto_create_status\s+IS\s+NOT\s+NULL" app/ || echo "GREP CLEAN: no vacuous predicates remain under app/"</automated>
  </verify>
  <done>
    - `RetryMissingImagesCommand.php` line ~62 uses `->autoCreated()`; the `$this->info` message + `--all` option description updated to match the actual `!= 'manual'` semantic.
    - `AutoCreateHealthPage.php` both predicate sites use `->autoCreated()`; docblock note updated.
    - Grep for `whereNotNull('auto_create_status')` AND `where('auto_create_status', '!=', 'manual')` under `app/` returns ZERO hits.
    - Existing `AutoCreateHealthPageTest` (if present from 260606-mx9) still green — the refactor is behaviour-preserving.
    - Atomic commit landed with the annotated grep output in the body.
  </done>
</task>

<task type="auto">
  <name>Task 3: Architecture test forbidding vacuous IS-NOT-NULL auto_create_status predicates anywhere under app/</name>
  <files>
    tests/Architecture/AutoCreatedPredicateTest.php
  </files>
  <action>
    Create `tests/Architecture/AutoCreatedPredicateTest.php` modelled on `tests/Architecture/EnvUsageTest.php` (read it for the full pattern — file-scan with RecursiveIteratorIterator + comment stripping + helpful failure message + meta-assertion). Do NOT use the Pest `arch()` DSL — it resolves classes via the App\ namespace but does not see raw string literals like `whereNotNull('auto_create_status')`. A file scan is required.

    Header docblock (mirror EnvUsageTest's header style — explain WHY this test exists):
    - Reference quick 260606-mx9 (uncovered the bug) and 260606-o63 (this fix).
    - Reference migration `2026_04_22_100300_add_auto_create_columns_to_products_table.php` as the source of the NOT NULL DEFAULT 'manual' column shape — the reason `whereNotNull` is vacuous.
    - State that the canonical predicate is `Product::query()->autoCreated()` (defined in `app/Domain/Products/Models/Product.php`).

    Forbidden patterns (the test FAILS if ANY of these appear under `app/`):
    1. `whereNotNull('auto_create_status')` — single quotes.
    2. `whereNotNull("auto_create_status")` — double quotes.
    3. `whereNull('auto_create_status')` — single quotes. (`whereNull` is ALSO vacuous because every row has a value; the only way to express "legacy / pre-auto-create" is `where('auto_create_status', 'manual')` or — preferred — `whereNotIn('auto_create_status', [...])` for a specific set. The `--all` flag inversion belongs in the calling code, not as `whereNull`.)
    4. `whereNull("auto_create_status")` — double quotes.
    5. `auto_create_status IS NOT NULL` — raw SQL fragment (case-insensitive, whitespace-tolerant: regex `auto_create_status\s+IS\s+NOT\s+NULL`).
    6. `auto_create_status IS NULL` — raw SQL fragment (case-insensitive, whitespace-tolerant: regex `auto_create_status\s+IS\s+NULL`).

    Scan implementation:
    - One `test(...)` block named `'auto_create_status IS NOT NULL / IS NULL predicates are forbidden in app/ — file scan'`.
    - Walk `base_path('app')` recursively with `RecursiveIteratorIterator` + `RecursiveDirectoryIterator(..., SKIP_DOTS)`.
    - Skip non-`.php` files via `$file->getExtension() !== 'php'`.
    - Read each file with `file_get_contents`, strip block comments (`#/\*.*?\*/#s`) and line comments (`#//.*$#m`) — verbatim from EnvUsageTest.
    - Run a single combined regex that matches ANY of the 6 forbidden patterns. Suggested: build an array of 6 individual regexes and `preg_match` each one in turn (clearer in failure output than one mega-regex), OR a single alternation:
      `'/(whereNotNull\s*\(\s*[\'"]auto_create_status[\'"]\s*\))|(whereNull\s*\(\s*[\'"]auto_create_status[\'"]\s*\))|(auto_create_status\s+IS\s+(NOT\s+)?NULL)/i'`
    - On match, record `relative_path:line_number` (compute line number via `substr_count(substr($stripped, 0, $match_offset), "\n") + 1`).
    - Final assertion: `expect($violations)->toBeEmpty('Vacuous auto_create_status predicate(s) found — use Product::query()->autoCreated() instead. The column is NOT NULL DEFAULT \'manual\' per migration 2026_04_22_100300, so whereNotNull/whereNull are no-ops. Call sites: '.implode(', ', $violations));`

    Allowed locations (NOT scanned — these legitimately mention the patterns):
    - `tests/**` — this test file itself contains the pattern as a string literal; future tests may want to assert on the broken pattern.
    - `database/migrations/**` — the original migration legitimately writes `whereNull` in the backfill (`DB::table('products')->whereNull('auto_create_status')->update(...)`).
    - `database/factories/**` — factories may set the column to specific values; not relevant.

    Because the scan walks ONLY `base_path('app')`, no exclude-list is needed — the directories above are simply not visited. Document this in the docblock so future maintainers don't add a redundant allow-list.

    Meta-assertion (third test in the file, mirror EnvUsageTest assertion 3):
    A `test(...)` block named `'forbidden-pattern regexes can detect synthetic violations (meta-assertion)'` that asserts each of the 6 forbidden patterns matches a synthetic positive case AND that an innocent string (e.g. `where('auto_create_status', 'draft')` or `where('auto_create_status', '!=', 'manual')` or the literal docstring `// auto_create_status IS NOT NULL in a comment`) does NOT match. The negative-comment case must be tested AFTER the same `preg_replace` line-comment strip used in the main scan — proves the strip-then-match pipeline works end-to-end. This stops a future "regex simplification" PR from silently weakening the guardrail.

    Atomic commit:
    `test(architecture): forbid vacuous whereNotNull/whereNull('auto_create_status') predicate (260606-o63)`
    Body: one-line reference to 260606-mx9 (uncovering) + this fix's scope swap + the migration that makes the predicate vacuous.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Architecture/AutoCreatedPredicateTest.php tests/Architecture/EnvUsageTest.php --colors=never</automated>
  </verify>
  <done>
    - `tests/Architecture/AutoCreatedPredicateTest.php` exists, green.
    - All 6 forbidden patterns covered (3 PHP-method forms + 1 PHP-method form for whereNull + 2 raw-SQL forms).
    - Meta-assertion proves each regex catches its synthetic positive AND ignores comment-stripped innocent strings.
    - Existing `EnvUsageTest.php` still green (no regression).
    - Atomic commit landed.
    - SANITY CHECK after commit: temporarily inject `Product::query()->whereNotNull('auto_create_status')->get();` into `app/Console/Commands/RetryMissingImagesCommand.php` perform() body, re-run the test, confirm it FAILS with the file + line. Then revert the temp injection BEFORE moving to Task 4. (Do not commit the temp injection.)
  </done>
</task>

<task type="auto">
  <name>Task 4: Full-suite verification + dry-run probe + STATE row prep (no commit)</name>
  <files>
    (verification only — no file edits)
  </files>
  <action>
    Step 1 — Focused suite (must be green):
    Run `vendor/bin/pest tests/Unit/Models/ProductScopeAutoCreatedTest.php tests/Architecture/AutoCreatedPredicateTest.php tests/Architecture/EnvUsageTest.php --colors=never`. All three test files must pass.

    Step 2 — Touched-file feature tests (if they exist):
    - If `tests/Feature/Console/Commands/RetryMissingImagesCommandTest.php` exists, run it. Must be green (the scope swap is behaviour-preserving).
    - If `tests/Feature/Filament/Pages/AutoCreateHealthPageTest.php` exists (from 260606-mx9), run it. Must be green (the scope swap is behaviour-preserving).
    - If neither exists, skip — call this out in the SUMMARY as a coverage gap to flag for a future quick.

    Step 3 — Dry-run probe of the actual command (proves the fix works on real data, not just SQLite fixtures):
    Run `php artisan products:retry-missing-images --dry-run --limit=10` (no `--all`, no `--brand`). Capture:
    - The count of products surfaced (should be MUCH lower than the pre-fix "5,668 candidates" reported in 260606-mx9 — that figure included thousands of 'manual' legacy rows).
    - The "Breakdown by auto_create_status" table: there should be ZERO rows in the 'manual' bucket. If 'manual' rows appear, the fix has not landed — investigate before continuing.
    - Then run `php artisan products:retry-missing-images --dry-run --all --limit=10` (WITH `--all`). The 'manual' bucket should re-appear, proving `--all` still does its job. Document both counts in the SUMMARY.

    Step 4 — Full suite for regression sweep:
    Run `vendor/bin/pest --colors=never 2>&1 | tee /tmp/o63-pest-full.txt`. Capture passed/failed/skipped counts. Compare to the pre-existing failure baseline from 260606-mx9 SUMMARY (1,814 passed / 219 failed / 3 skipped). Pre-existing failures are acceptable. Any NEW failures introduced by Tasks 1-3 MUST be investigated and fixed before SUMMARY closes — they almost certainly mean a feature test was asserting on the old `whereNotNull` shape and needs to be updated to `->autoCreated()`.

    Step 5 — STATE row prep:
    Draft the row to append to `.planning/STATE.md`'s Quick Tasks table for the SUMMARY step:
    `| 2026-06-06 | 260606-o63 | Fix vacuous auto_create_status IS NOT NULL predicate — Product::scopeAutoCreated canonical scope + arch-test guardrail | done | <commit-shas> |`
    Replace `<commit-shas>` with the three short SHAs from Tasks 1/2/3.

    NO commit on this task. This is verification + SUMMARY prep only.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Unit/Models/ProductScopeAutoCreatedTest.php tests/Architecture/AutoCreatedPredicateTest.php tests/Architecture/EnvUsageTest.php --colors=never && php artisan products:retry-missing-images --dry-run --limit=10 2>&1 | tee /tmp/o63-dryrun.txt</automated>
  </verify>
  <done>
    - Focused suite (3 files) green.
    - Any existing RetryMissingImages / AutoCreateHealth feature tests green (or coverage-gap noted in SUMMARY).
    - Dry-run probe shows ZERO 'manual' rows in the default-mode breakdown.
    - Dry-run probe with `--all` shows 'manual' rows reappearing (proves the flag still works).
    - Full suite delta vs 260606-mx9 baseline captured; no NEW failures attributable to this quick.
    - STATE row drafted and ready for the SUMMARY step to commit.
  </done>
</task>

</tasks>

<verification>
After all four tasks:

1. `vendor/bin/pest tests/Unit/Models/ProductScopeAutoCreatedTest.php tests/Architecture/AutoCreatedPredicateTest.php tests/Architecture/EnvUsageTest.php` — green.
2. Ripgrep audit: `grep -RInE "whereNotNull\(\s*['\"]auto_create_status['\"]\s*\)|whereNull\(\s*['\"]auto_create_status['\"]\s*\)|auto_create_status\s+IS\s+(NOT\s+)?NULL" app/` — ZERO hits.
3. Ripgrep audit: `grep -RInE "where\(\s*['\"]auto_create_status['\"]\s*,\s*['\"]!=['\"]\s*,\s*['\"]manual['\"]\s*\)" app/` — ZERO hits (all migrated to `->autoCreated()`).
4. `php artisan products:retry-missing-images --dry-run --limit=10` — breakdown table shows NO 'manual' bucket.
5. Three atomic commits landed on the working branch with their respective Conventional Commits messages.
</verification>

<success_criteria>
- [ ] `Product::scopeAutoCreated(Builder)` ships with a 3-case unit test that covers all 8 enum values.
- [ ] `RetryMissingImagesCommand` no longer uses `whereNotNull('auto_create_status')`; uses `->autoCreated()`.
- [ ] `AutoCreateHealthPage` both predicate sites use `->autoCreated()`.
- [ ] Annotated 14-hit grep audit lives in the Task 2 commit body — every non-edited site has a W / R-status disposition note.
- [ ] `tests/Architecture/AutoCreatedPredicateTest.php` ships, green, with sanity-checked failure mode (temp-inject + revert proves it catches the regression).
- [ ] Dry-run probe confirms the 5,632-legacy-row silent-bloat is gone in default mode AND comes back with `--all` (flag still works).
- [ ] No NEW Pest failures vs 260606-mx9 baseline.
</success_criteria>

<output>
Create `.planning/quick/260606-o63-fix-vacuous-auto-create-status-is-not-nu/260606-o63-SUMMARY.md` when all four tasks done. SUMMARY must include:
- The Task 2 annotated 14-hit grep audit (paste from commit body).
- Before/after dry-run probe counts (with vs without `--all`).
- Pest full-suite delta vs 260606-mx9 baseline.
- The drafted STATE row, ready for the workflow to append to `.planning/STATE.md`'s Quick Tasks table.
- Cross-link to 260606-mx9 SUMMARY (this quick directly closes a finding from that one).
</output>

---
phase: 260606-q7h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md
  - .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md
  # Task 3 (conditional) may add files to app/Domain/<sub>/Services/*.php
  # and tests/Unit/Domain/<sub>/*Test.php — listed in Task 3 directly.
autonomous: true
requirements:
  - STATE-LESSON-2-2026-05-31
must_haves:
  truths:
    - "Every multi-source resolver in app/Domain/ is scored HIGH/MEDIUM/LOW against the Huddlecamhd-shape rubric"
    - "TaxonomyResolver::allBrands() priority fix from 73ac682 is still intact (native first, pa_brand fallback)"
    - "All HIGH-severity findings (if any) are fixed and pinned by Pest tests"
    - "Pest suite has no NEW failures vs the 260606-p4q baseline (1,826 passed / 219 failed / 3 risky)"
  artifacts:
    - path: ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md"
      provides: "Table of findings with severity scores + count summary + recommended next step"
      contains: "| File:line | Severity | Explanation | Proposed fix |"
    - path: ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md"
      provides: "Outcome summary — clean bill OR per-fix commit references"
  key_links:
    - from: "FINDINGS.md HIGH rows"
      to: "Task 3 atomic commits"
      via: "one commit per HIGH finding"
      pattern: "fix\\(.+\\): .+ \\(260606-q7h\\)"
    - from: "Task 3 source edits"
      to: "Pest tests pinning canonical behavior"
      via: "tests/Unit/Domain/<sub>/*Test.php"
      pattern: "it\\('prefers .+ over .+'"
---

<objective>
Forensic audit of `app/Domain/` for the "exit on first non-empty source" anti-pattern that caused the Huddlecamhd disaster — `TaxonomyResolver::allBrands()` silently returned a 1-term `pa_brand` taxonomy while ignoring 176 real terms in native `/products/brands`, fuzzy-matching Sony/Barco/ViewSonic/Huddly all to "Huddlecamhd" for months. Commit 73ac682 fixed the original site. This task sweeps the other 8 scouted resolvers/registries for the same shape, scores severity, and fixes HIGH findings inline.

Purpose: stop the next silent-fuzzy-match disaster before it ships. The investigation IS the deliverable, even if HIGH count is 0 — a clean bill across 9 resolvers feeds the retrospective.

Output: `260606-q7h-FINDINGS.md` (always), 0–N atomic `fix(...)` commits + Pest tests (conditional on HIGH findings), `260606-q7h-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php
@app/Domain/Integrations/Services/IntegrationCredentialResolver.php

<scouted_files>
The 9 files in scope for the line-by-line audit (Task 1):

1. app/Domain/Agents/Services/AgentRegistry.php
2. app/Domain/Agents/Support/BrandSlugResolver.php
3. app/Domain/Integrations/Services/IntegrationCredentialResolver.php  (docblock declares DB-wins-then-env intent — MEDIUM candidate)
4. app/Domain/Pricing/Services/RuleResolver.php
5. app/Domain/ProductAutoCreate/Services/ProductBrandTermResolver.php
6. app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php  (already fixed in 73ac682 — verify intact; excluded from HIGH)
7. app/Domain/Suggestions/Services/SuggestionApplierResolver.php
8. app/Domain/Sync/Services/SupplierSkuRegistry.php
9. app/Domain/TradePricing/Services/TradeRuleResolver.php
</scouted_files>

<severity_rubric>
- HIGH = two sources can BOTH legitimately hold data AND downstream consumers trust the result silently (Huddlecamhd shape). FIX in Task 3.
- MEDIUM = order-dependent but documented intent (e.g. "DB wins over env" in a docblock). DOCUMENT in FINDINGS, no fix.
- LOW = clean fallback where one source is intentionally authoritative + the other is failover only. DOCUMENT in FINDINGS, no fix.
</severity_rubric>

<reference_fix>
The canonical fix shape from 73ac682 (TaxonomyResolver::allBrands), for Task 3 to mirror:

  // 1) Primary source — the one with the real data volume
  try {
      $terms = $this->paginate('products/brands');
      if ($terms !== []) {
          return $terms;
      }
  } catch (\Throwable) {
      // fall through
  }

  // 2) Fallback — only reached when primary returned empty or threw
  // ... legacy pa_brand path ...

Three valid fix shapes for HIGH findings:
  (a) Invert priority — make the high-volume source primary (Huddlecamhd shape)
  (b) Merge sources — union both and dedupe (when both legitimately contribute)
  (c) Fail-loud sanity check — throw/log when both sources have data and they disagree
</reference_fix>

<baseline>
Pest suite baseline from 260606-p4q: 1,826 passed / 219 failed / 3 risky. Pre-existing failures are acceptable. ANY NEW failure introduced by Task 3 must be fixed before SUMMARY closes.
</baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Line-by-line audit + write FINDINGS.md</name>
  <files>.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md</files>
  <action>
Read all 9 scouted files in full (Read tool, single pass each — no re-reads). For each file:

1. Identify whether it does multi-source resolution. If not, mark "N/A — single source" and move on.
2. If yes, identify HOW the winner is picked: which source comes first, what condition advances to the next, whether the consumer can detect a wrong-source-win silently.
3. Score against the rubric: HIGH / MEDIUM / LOW / N/A.

Then run targeted Grep sweeps across `app/Domain/` (not just the 9 files) to catch shapes outside the scouted list:

  Pattern A: `if \(\$terms !== \[\]\) \{[\s\S]{0,40}?return \$terms;` (exact Huddlecamhd shape) — multiline=true
  Pattern B: `if \(.+ !== null\) \{[\s\S]{0,40}?return ` — multiline=true (non-null short-circuit returns)
  Pattern C: `\?\? \$this->[a-zA-Z_]+\(` (null-coalesce between method calls — possible multi-source priority)
  Pattern D: `fallback` (case-insensitive comment hits) — narrows to multi-source intent
  Pattern E: `try \{[\s\S]{0,200}?\} catch \([\s\S]{0,80}?\) \{[\s\S]{0,200}?return ` — multiline=true (catch reaches a different data source)

Score each grep hit against the rubric. De-dup against the 9 scouted files.

Special verification: the TaxonomyResolver::allBrands() fix from commit 73ac682 must still show "native first, pa_brand fallback" — record as a SANITY CHECK row in FINDINGS (not a HIGH).

Write `.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md` with this exact shape:

  # Findings — exit-on-first-non-empty resolver audit (260606-q7h)

  ## Scope
  9 scouted files + grep sweep across app/Domain/.

  ## Findings Table

  | # | File:line | Severity | One-sentence explanation | Proposed fix |
  |---|-----------|----------|--------------------------|--------------|
  | 1 | app/Domain/.../X.php:NNN | HIGH | ... | invert priority / merge / fail-loud |
  | 2 | app/Domain/.../Y.php:NNN | MEDIUM | docblock declares DB-wins intent | document only |
  | ... | ... | ... | ... | ... |
  | S | app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php:256 | SANITY | 73ac682 fix intact: native first, pa_brand fallback | none |

  ## Summary
  - HIGH:   N
  - MEDIUM: N
  - LOW:    N
  - SANITY: 1 (TaxonomyResolver::allBrands)

  ## Recommended next step
  - If HIGH == 0: skip Task 3, ship the audit doc as the deliverable.
  - If HIGH >= 1: proceed to Task 3, one atomic commit per HIGH row.

Then commit ONLY the FINDINGS.md file (no source edits in this task):
  git add .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md
  git commit -m "docs(audit): exit-on-first-non-empty resolver findings (260606-q7h)"
  </action>
  <verify>
    <automated>test -f ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md" &amp;&amp; grep -q "| SANITY |" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md" &amp;&amp; grep -Eq "HIGH:[[:space:]]+[0-9]+" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md" &amp;&amp; git log -1 --pretty=%s | grep -q "260606-q7h"</automated>
  </verify>
  <done>
FINDINGS.md exists with a populated findings table covering all 9 scouted files + any grep-discovered extras, severity counts at the bottom, the TaxonomyResolver SANITY row confirming the 73ac682 fix is intact, and a "recommended next step" line. Committed as `docs(audit): exit-on-first-non-empty resolver findings (260606-q7h)`. No source code touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Decide fix scope (decision-only, no commit)</name>
  <files>.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md</files>
  <action>
Re-read FINDINGS.md (already in context from Task 1 — no extra Read needed unless context was cleared).

Branching logic:

CASE A — HIGH count == 0:
  - Append a "## Decision" section to FINDINGS.md stating: "Clean bill: 0 HIGH findings. Task 3 SKIPPED. Audit doc is the deliverable."
  - Amend nothing — leave the Task 1 commit as-is. Just write the appended section as part of the working tree; it will be picked up by the Task 4 SUMMARY commit if needed, or left as a working-tree edit since the doc-only nature means a second commit isn't required.
  - Skip ahead to Task 4.

CASE B — HIGH count >= 1:
  - For each HIGH row in the findings table, draft (in your reply, not in a file):
    * The proposed fix shape: (a) invert priority, (b) merge sources, or (c) fail-loud sanity check
    * A draft commit message body: `fix(<domain>): <one-line summary> (260606-q7h)` with a 2–3 line body explaining what changed and why
    * The Pest test file path: `tests/Unit/Domain/<Sub>/<Resolver>Test.php` — extend if exists, create if not
    * The test name pattern: `it('prefers <primary> over <fallback> when both have data')` or similar
  - Confirm that the total scope (N fix commits + N test additions) fits in one execution context. If it doesn't (>3 HIGH findings, or any single fix touches >5 files), STOP and surface a SPLIT recommendation in your reply for the user — do not proceed to Task 3 silently.

This task produces NO commit and NO file edit (CASE A may append to FINDINGS but that's optional — see above).
  </action>
  <verify>
    <automated>grep -Eq "HIGH:[[:space:]]+[0-9]+" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md"</automated>
  </verify>
  <done>
Decision made and stated explicitly in the executor's reply: either "CASE A — 0 HIGH, skipping Task 3" or "CASE B — N HIGH, proceeding with the following N fixes: [list]". If CASE B and total scope exceeds context budget, a SPLIT recommendation is surfaced and Task 3 is paused for user direction.
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-HIGH atomic fix + Pest test (CONDITIONAL — runs only if Task 2 says CASE B)</name>
  <files>
app/Domain/&lt;Sub&gt;/Services/&lt;Resolver&gt;.php  (one per HIGH finding)
tests/Unit/Domain/&lt;Sub&gt;/&lt;Resolver&gt;Test.php  (one per HIGH finding — create or extend)
  </files>
  <action>
SKIP this entire task if Task 2 declared CASE A (0 HIGH findings).

Otherwise, for each HIGH row in FINDINGS.md, in dependency-free order (each fix is independent):

1. Read the source file (Read tool, single pass). Apply the chosen fix shape from Task 2:
   - (a) Invert priority: move the high-volume / authoritative source to the primary branch, demote the other to fallback. Mirror the 73ac682 docblock pattern — explain WHY priority is what it is, reference the Huddlecamhd lesson by date (2026-05-31).
   - (b) Merge sources: union both lists, dedupe by stable key (id or slug), document the merge contract in a docblock.
   - (c) Fail-loud sanity check: when both sources have data AND disagree on a canonical key, throw an exception or log a warning + return one source with a clear precedence note.

2. Write the Pest test:
   - Path: `tests/Unit/Domain/<Sub>/<Resolver>Test.php`
   - Pattern mirrors `tests/Unit/Domain/.../ProductScopeAutoCreatedTest.php` from 260606-o63: a small unit test that mocks both sources and asserts the canonical winner.
   - At minimum two test cases per fix:
     * `it('prefers <primary> when both sources have data')`
     * `it('falls back to <fallback> when primary returns empty')`
   - For fix shape (c), add: `it('fails loud when sources disagree on canonical key')`.

3. Run the focused test:
   `php artisan test tests/Unit/Domain/<Sub>/<Resolver>Test.php`
   Must pass green before commit.

4. Commit atomically (ONE fix + ONE test per commit):
   git add app/Domain/<Sub>/Services/<Resolver>.php tests/Unit/Domain/<Sub>/<Resolver>Test.php
   git commit -m "fix(<sub>): <one-line summary> (260606-q7h)"

   Body (HEREDOC):
     Mirrors the 73ac682 Huddlecamhd fix shape: <invert priority|merge|fail-loud>.
     Before: <source A> shadowed <source B> when both held data.
     After: <new precedence / merge contract / sanity check>.
     Pest test pins canonical behavior.

Repeat for each HIGH finding. Each fix is one atomic commit. No batch commits.

If any focused test fails after the fix, debug the test or the fix until green — do NOT commit a red test.
  </action>
  <verify>
    <automated>php artisan test --filter="prefers|falls back|fails loud" 2&gt;&amp;1 | grep -Eq "PASS|OK"</automated>
  </verify>
  <done>
Either Task 2 declared CASE A and this task was skipped (no commits), OR one atomic `fix(<sub>): ... (260606-q7h)` commit per HIGH finding shipped, each pinned by a Pest test that asserts the canonical winner. Every focused test green. Git log shows N new commits matching the pattern `fix\(.+\): .+ \(260606-q7h\)`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full Pest suite + SUMMARY</name>
  <files>.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md</files>
  <action>
1. Run the full Pest suite and capture results:
   php artisan test 2>&1 | tail -20

   Compare against baseline (260606-p4q): 1,826 passed / 219 failed / 3 risky.

   Acceptance rule:
   - passed >= 1,826 AND failed <= 219 → green-enough, proceed.
   - ANY new failure introduced by Task 3 → STOP. Investigate, fix, re-run. Do not proceed to SUMMARY until the new failure is resolved or proven pre-existing.

2. Spot-check the two canonical pinning tests still pass:
   php artisan test tests/Feature/EnvUsageTest.php tests/Unit/Domain/ProductAutoCreate/AutoCreatedPredicateTest.php

   Both must be green.

3. Write `.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md`:

   # Summary — 260606-q7h forensic audit

   ## Outcome
   - HIGH findings: N
   - Fixes shipped: N atomic commits (or "0 — clean bill")
   - Pest suite: P passed / F failed / R risky (baseline: 1,826 / 219 / 3) — delta: +X passed, +Y failed
   - Canonical tests: EnvUsageTest green, AutoCreatedPredicateTest green

   ## Findings (link)
   See `260606-q7h-FINDINGS.md`.

   ## Fix commits (CASE B only)
   - <sha> fix(<sub>): <summary>
   - <sha> fix(<sub>): <summary>
   - ...

   ## TaxonomyResolver SANITY check
   Commit 73ac682 fix verified intact: native `/products/brands` primary, `pa_brand` fallback.

   ## Notes for retrospective
   - <one-line takeaway>
   - <pattern observation>

4. Commit:
   git add .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md
   # Also stage FINDINGS.md if Task 2 appended the CASE A decision section
   git add .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-FINDINGS.md
   git commit -m "docs(audit): 260606-q7h summary"
  </action>
  <verify>
    <automated>test -f ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md" &amp;&amp; grep -q "Pest suite:" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md" &amp;&amp; grep -q "TaxonomyResolver SANITY" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md"</automated>
  </verify>
  <done>
SUMMARY.md committed with: HIGH count, fix commit SHAs (or "0 — clean bill"), Pest suite delta vs baseline, both canonical pinning tests confirmed green, TaxonomyResolver SANITY check noted, retrospective takeaways listed. No NEW Pest failures introduced.
  </done>
</task>

</tasks>

<verification>
Phase-level checks after all tasks:

- `git log --oneline | grep "260606-q7h"` returns at least 2 commits (audit docs + summary), and 0–N additional `fix(...)` commits depending on HIGH count.
- `.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/` contains both `260606-q7h-FINDINGS.md` and `260606-q7h-SUMMARY.md`.
- `php artisan test tests/Feature/EnvUsageTest.php tests/Unit/Domain/ProductAutoCreate/AutoCreatedPredicateTest.php` green.
- `git diff 73ac682 -- app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php` shows the priority-inversion fix is intact (`products/brands` still primary, `pa_brand` still fallback).
</verification>

<success_criteria>
- FINDINGS.md exists with severity table covering all 9 scouted files + any grep-discovered extras
- HIGH count is explicit and matches the number of `fix(...)` commits
- Every HIGH fix has a Pest test pinning the canonical winner
- TaxonomyResolver 73ac682 fix verified intact (SANITY row)
- No NEW Pest failures introduced (baseline: 1,826 / 219 / 3)
- SUMMARY.md ties findings → commits → test results → retrospective notes
</success_criteria>

<output>
Create `.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/260606-q7h-SUMMARY.md` when done (Task 4).
</output>

---
quick_id: 260607-9c6
type: execute
wave: 1
autonomous: false
files_modified:
  - app/Domain/Webhooks/Console/Commands/PruneWebhookReceiptsCommand.php
  - tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php
  - routes/console.php
  - app/Domain/Cutover/Services/WooDbSnapshotter.php
  - tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php
  - .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md
must_haves:
  truths:
    - "webhook_receipts rows older than per-topic GDPR retention (order=30d, customer=7d, other=90d) are automatically deleted nightly"
    - "WooDbSnapshotter no longer passes the WooDB password on the mysqldump argv — operator running `cat /proc/$(pgrep mysqldump)/cmdline` during a dump sees no plaintext credential"
    - "SECURITY-REVIEW.md (260606-q7h) reflects H-1 + H-2 remediation status and links to the remediation commits"
  artifacts:
    - path: "app/Domain/Webhooks/Console/Commands/PruneWebhookReceiptsCommand.php"
      provides: "webhooks:prune-receipts artisan command with per-topic retention flags"
      contains: "webhooks:prune-receipts"
    - path: "tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php"
      provides: "Pest coverage for the 3 retention paths + --dry-run + flag overrides"
    - path: "tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php"
      provides: "Asserts the snapshotter command contains --defaults-extra-file and never the literal password"
  key_links:
    - from: "routes/console.php"
      to: "webhooks:prune-receipts"
      via: "Schedule::command(...)->cron('25 3 * * *')->timezone('Europe/London')"
      pattern: "webhooks:prune-receipts"
    - from: "app/Domain/Cutover/Services/WooDbSnapshotter.php"
      to: "tempnam + chmod 0600 + --defaults-extra-file"
      via: "try {...} finally { @unlink($cnfPath); }"
      pattern: "--defaults-extra-file"
---

<objective>
Ship the 2 HIGH security findings from SECURITY-REVIEW.md (260606-q7h, commit 1c76e63, dated 2026-06-06):

- **H-1:** `webhook_receipts.raw_body` holds raw Woo customer/order PII indefinitely (GDPR Art. 5(1)(e) storage-limitation violation). Add `webhooks:prune-receipts` artisan + nightly cron at 03:25 London with per-topic retention (order=30d, customer=7d, other=90d).
- **H-2:** `WooDbSnapshotter` leaks the WooDB password via `mysqldump -p<pwd>` argv (visible in `/proc/*/cmdline` for up to 1h). Refactor to use `--defaults-extra-file` with a chmod-0600 temp `.cnf` file, unlinked in `finally{}`.

Purpose: close both HIGH findings in a single quick task, flip SECURITY-REVIEW.md frontmatter to reflect remediation. Both fixes are surgical, fully tested, no schema changes, no behaviour changes for users — pure security hardening.

Output: 4 atomic commits + 1 verification pass; SECURITY-REVIEW.md updated to `status: high_findings_remediated`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md
@CLAUDE.md
@app/Domain/Webhooks/Http/Controllers/WooWebhookController.php
@app/Domain/Webhooks/Models/WebhookReceipt.php
@app/Domain/Cutover/Services/WooDbSnapshotter.php
@routes/console.php
@app/Console/Commands/BaseCommand.php
@app/Domain/Suggestions/Console/Commands/PruneOrphanSuggestionsCommand.php
@tests/Feature/Suggestions/PruneOrphanSuggestionsCommandTest.php
@tests/Feature/Cutover/SnapshotWooDbCommandTest.php
@database/migrations/2026_04_18_180000_create_webhook_receipts_table.php

<interfaces>
<!-- Key contracts pulled from the codebase so the executor does not need to spelunk. -->

From app/Console/Commands/BaseCommand.php:
- `abstract class BaseCommand extends Illuminate\Console\Command` — final `handle()` wires correlation_id into Context + spatie LogBatch
- subclasses MUST implement `protected function perform(): int` (NOT `handle()`)
- `protected $signature` and `protected $description` declared the normal Laravel way
- return `Symfony\Component\Console\Command\Command::SUCCESS` (= 0) on success

From app/Domain/Webhooks/Models/WebhookReceipt.php:
- `WebhookReceipt extends Illuminate\Database\Eloquent\Model` (no soft-deletes, plain id PK, timestamps)
- Mass-assignable: source, topic, delivery_id, headers, raw_body, correlation_id, received_at, processed_at, status, error_message
- Topic values used in production: `'order'`, `'customer'` (verified — WooWebhookController hardcodes these two strings on lines 36 + 45). The H-1 finding mentions `order.*` / `customer.*` Woo topic *patterns*, but our `topic` column stores the controller-derived short string, not the raw Woo topic. So our prune key is the bare topic string equality, not LIKE-prefix.

From database/migrations/2026_04_18_180000_create_webhook_receipts_table.php:
- `topic` is VARCHAR(64) NOT NULL — `WHERE topic = 'order'` is index-covered via the (source, topic, received_at) composite
- `received_at` is `timestamp` NOT NULL — use this as the age column (NOT `created_at`)

From app/Domain/Suggestions/Console/Commands/PruneOrphanSuggestionsCommand.php:
- The reference shape for a BaseCommand prune: `protected $signature` with `{--days=N : desc}` + `{--dry-run : desc}` flags; perform() reads options via `$this->option(...)`; chunkById(500) for status-flipping mutations; mass `->update()` / `->delete()` bypasses model events (acceptable here — WebhookReceipt doesn't use LogsActivity).

From app/Domain/Cutover/Services/WooDbSnapshotter.php (lines 70-103 — the H-2 surface):
- `config('cutover.woo_db.host'|'username'|'password'|'database')` are the 4 credential sources
- The command path is `Process::fromShellCommandline($cmd)` then `$process->mustRun()` inside a try/catch that translates `ProcessFailedException` into `RuntimeException`
- The audit_log + return-array shape (filename/path/size_bytes) is byte-identical to today — only the `$cmd` assembly + a new try/finally for the cnf temp file change

From SECURITY-REVIEW.md (260606-q7h):
- Status: `shippable_with_followups` → flip to `high_findings_remediated`
- Frontmatter fields: `review_kind`, `reviewed_at`, `scope_pass_1`, `scope_pass_2`, `findings_critical`, `findings_high`, `findings_medium`, `findings_low`, `status` (10 keys; add an 11th `high_remediated_in: 260607-9c6`)
- H-1 section starts at `## HIGH` → `### H-1 …` (line 27); H-2 at `### H-2 …` (line 43)
</interfaces>

**Reference patterns (verified in codebase):**
- BaseCommand pattern: `app/Console/Commands/BaseCommand.php` (correlation_id LogBatch wiring)
- Prune command shape: `app/Domain/Suggestions/Console/Commands/PruneOrphanSuggestionsCommand.php` (signature, dry-run, chunkById)
- Schedule slot conventions: `routes/console.php` lines 27-50 show the 03:00/03:10/03:20/03:30 cascade; 03:25 is unused and slots between sync-errors:prune (03:20) and sync-diffs:prune (03:30)
- Existing snapshotter test: `tests/Feature/Cutover/SnapshotWooDbCommandTest.php` mocks at the WooDbSnapshotter container level — cannot reach into the command builder. For H-2 we write a NEW focused test against the command-string building path (see Task 3).

**Constraints from project conventions (CLAUDE.md):**
- Laravel 12, PHP 8.2+, Pest 3
- All env reads via `config()`, never `env()` outside `config/*.php` (architectural guardrail `tests/Architecture/EnvUsageTest.php` enforces) — H-2 already reads via `config('cutover.woo_db.*')`, no change needed there
- Test DB is SQLite in-memory; prune command must NOT use MySQL-specific JSON expressions (this prune is a simple `WHERE topic = ? AND received_at < ?` — driver-agnostic by construction, no JSON_EXTRACT needed)

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webhooks:prune-receipts command + Pest test (H-1)</name>
  <files>
    - app/Domain/Webhooks/Console/Commands/PruneWebhookReceiptsCommand.php (NEW)
    - tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php (NEW)
  </files>
  <behavior>
    - Test 1: command is registered in artisan registry as `webhooks:prune-receipts`.
    - Test 2: with 6 seeded WebhookReceipts (2 order: 1 fresh + 1 35d-old; 2 customer: 1 fresh + 1 10d-old; 2 other: 1 fresh + 1 100d-old), live run deletes exactly the 3 stale rows and keeps the 3 fresh rows.
    - Test 3: `--dry-run` makes NO DB changes; output mentions per-topic candidate counts AND the word "dry-run".
    - Test 4: `--customer-days=1` overrides the default 7-day customer cutoff — a 5-day-old customer receipt is now stale and gets deleted.
    - Test 5: command returns Symfony exit code 0 on success.
    - Test 6: log output (Artisan::output) includes a per-topic count breakdown (order=N, customer=M, other=K).
  </behavior>
  <action>
Create `app/Domain/Webhooks/Console/Commands/PruneWebhookReceiptsCommand.php` extending `App\Console\Commands\BaseCommand` (per the BaseCommand interface above — correlation_id LogBatch wraps automatically).

Signature (use the literal flag names so the test can drive them):
```
webhooks:prune-receipts
    {--order-days=30 : Retention in days for topic='order' (default 30)}
    {--customer-days=7 : Retention in days for topic='customer' (tightest GDPR window)}
    {--other-days=90 : Retention for all other topics}
    {--dry-run : Print per-topic candidate counts, perform no deletes}
```

Description: `"Prune webhook_receipts rows older than per-topic GDPR retention (H-1 remediation, 260607-9c6)."`

`perform()` logic:
1. Read 4 options: `$orderDays`, `$customerDays`, `$otherDays`, `$dryRun`. Cast `int`/`bool`.
2. Compute three cutoff Carbons: `$orderCutoff = now()->subDays($orderDays)` etc.
3. Build three Eloquent queries against `App\Domain\Webhooks\Models\WebhookReceipt` keyed on `received_at` (NOT created_at — see migration above):
   - Order: `WebhookReceipt::query()->where('topic', 'order')->where('received_at', '<', $orderCutoff)`
   - Customer: `WebhookReceipt::query()->where('topic', 'customer')->where('received_at', '<', $customerCutoff)`
   - Other: `WebhookReceipt::query()->whereNotIn('topic', ['order', 'customer'])->where('received_at', '<', $otherCutoff)`
4. If `--dry-run`: call `->count()` on each query, print a single info line `"webhooks:prune-receipts — DRY-RUN — would delete: order={N} (cutoff {$orderCutoff->toIso8601String()}), customer={M} (cutoff {$customerCutoff->toIso8601String()}), other={K} (cutoff {$otherCutoff->toIso8601String()})"`. Return `Symfony\Component\Console\Command\Command::SUCCESS`.
5. Live mode: call `->delete()` on each query, capture row counts. Print `"webhooks:prune-receipts — LIVE — deleted: order={N}, customer={M}, other={K}"`. Return SUCCESS.

Implementation notes:
- Mass `->delete()` is fine — WebhookReceipt does NOT use LogsActivity (verified — model has no traits beyond Eloquent's defaults). BaseCommand's correlation_id LogBatch wrapper provides the audit thread for this command run.
- Driver-agnostic by construction (no JSON expressions, no LIKE patterns) — same code path runs on prod MySQL and test SQLite.
- DO NOT batch with chunkById here — unlike the suggestion-prune (which flipped status, where the row stayed visible), this hard-deletes; a single `->delete()` query is correct and faster.
- DOCBLOCK: reference quick task 260607-9c6 + SECURITY-REVIEW.md H-1 + retention rationale (per-topic GDPR tightness — customer 7d is the strictest because Woo customer payloads carry the most PII per H-1 description in SECURITY-REVIEW.md).

Test file `tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php`:
- Use `uses(RefreshDatabase::class)` — same shape as `PruneOrphanSuggestionsCommandTest.php`.
- Helper `function makeReceipt(string $topic, int $ageDays): WebhookReceipt` that does `WebhookReceipt::forceCreate([...])` with `received_at => now()->subDays($ageDays)`, delivery_id = `(string) Str::uuid()`, source='woo', headers=[], raw_body='{}', correlation_id='test-'.uniqid().
- 6 test cases matching the 6 behaviors above. The 2x3 matrix test seeds all six receipts, calls `Artisan::call('webhooks:prune-receipts')`, then asserts `WebhookReceipt::find($freshId)->exists` / `WebhookReceipt::find($staleId) === null` for each row by id.
- Topic strings to use in seeds: `'order'`, `'customer'`, `'other'` (any string not equal to 'order' or 'customer' satisfies the "other" branch).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php</automated>
  </verify>
  <done>
    - New command file exists at `app/Domain/Webhooks/Console/Commands/PruneWebhookReceiptsCommand.php`
    - New test file exists at `tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php`
    - Focused test run: all 6 tests green (typical 10-15 assertions)
    - `php artisan list webhooks` shows `webhooks:prune-receipts`
    - Atomic commit: `feat(webhooks): prune-receipts command with per-topic GDPR retention (260607-9c6)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Schedule webhooks:prune-receipts at 03:25 London (H-1 cron wiring)</name>
  <files>
    - routes/console.php
  </files>
  <action>
Edit `routes/console.php`. Locate the prune cascade block (lines 27-50: `activitylog:prune` at 03:00 → `integration-events:prune` at 03:10 → `sync-errors:prune` at 03:20 → `sync-diffs:prune` at 03:30). 03:25 is the unused slot between `sync-errors:prune` (03:20) and `sync-diffs:prune` (03:30) — slot the new entry there so the existing comment ("staggered by 10-minute intervals") remains accurate (it becomes "staggered by 5–10 minute intervals" — update the block-header comment to reflect).

Insert this Schedule entry immediately after the `sync-errors:prune` block (after line 45's closing `;`):

```
// Quick task 260607-9c6 (SECURITY-REVIEW.md H-1) — daily 03:25 London prune
// of webhook_receipts.raw_body. Per-topic retention (defaults applied by the
// command itself): order=30d, customer=7d (tightest GDPR window), other=90d.
// Slots between sync-errors:prune (03:20) and sync-diffs:prune (03:30) in the
// 03:00 retention cascade — keeps the staggered-DB-load invariant.
Schedule::command('webhooks:prune-receipts')
    ->cron('25 3 * * *')
    ->withoutOverlapping(30)
    ->onOneServer()
    ->timezone('Europe/London')
    ->description('Prune webhook_receipts by per-topic GDPR retention (H-1, 260607-9c6)');
```

Use `->cron('25 3 * * *')` (not `->dailyAt('03:25')`) to match the explicit cron-string convention used elsewhere in the file (e.g. `suggestions:prune-orphans` line 70 uses `->cron('0 6 * * 1')`).

NO env() call — safe under the EnvUsageTest architectural guardrail (260606-c4o). NO config() lookup needed — retention defaults are hardcoded in the command's option declarations (overridable from CLI).

Update the block-header comment on line 17 if it says "staggered by 10-minute intervals" → change to "staggered by 5–10 minute intervals" so the doc stays truthful.
  </action>
  <verify>
    <automated>php artisan schedule:list 2>&1 | grep -F "webhooks:prune-receipts"</automated>
  </verify>
  <done>
    - `php artisan schedule:list` lists `webhooks:prune-receipts` with cron `25 3 * * *` and timezone `Europe/London`
    - Existing prune entries (activitylog, integration-events, sync-errors, sync-diffs) still listed unchanged
    - `php artisan test tests/Architecture/EnvUsageTest.php` still passes (no env() introduced)
    - Atomic commit: `chore(schedule): wire webhooks:prune-receipts at 03:25 London (260607-9c6)`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WooDbSnapshotter --defaults-extra-file refactor (H-2)</name>
  <files>
    - app/Domain/Cutover/Services/WooDbSnapshotter.php (EDIT)
    - tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php (NEW)
  </files>
  <behavior>
    - Test 1: assembled command string contains the substring `--defaults-extra-file=`.
    - Test 2: assembled command string does NOT contain the literal WooDB password value (we set a sentinel like `SECRET_SHOULD_NOT_LEAK_123!@#` via `config()` before invoking).
    - Test 3: assembled command string does NOT contain the legacy `-p` short-flag (specifically: `-p` followed by anything other than whitespace or shell-end — guards against accidental regression).
    - Test 4: after successful invocation, the `.cnf` temp file is unlinked (asserts `file_exists($cnfPath) === false` post-call).
    - Test 5: after failed Process invocation (mock throws), the `.cnf` temp file is STILL unlinked (finally{} branch).
    - Test 6: the `.cnf` file content while in flight contains `[client]`, `user={user}`, `password={pass}`, `host={host}` lines (assert by intercepting before Process runs).
  </behavior>
  <action>
**Step 1 — Refactor `WooDbSnapshotter::snapshot()` (lines 51-119).**

Extract the command-string build + Process invocation into a small private helper to make it independently testable WITHOUT a real mysqldump binary. The public `snapshot()` shape (signature, return array, audit log call) stays byte-identical so the existing `SnapshotWooDbCommandTest.php` still passes.

New private helper:
```
protected function buildAndRunDump(string $host, string $user, string $pass, string $db, string $path): void
{
    $cnfPath = tempnam(sys_get_temp_dir(), 'msdmp_');
    if ($cnfPath === false) {
        throw new \RuntimeException('mysqldump: failed to allocate temp cnf file');
    }
    // chmod BEFORE writing so the file is unreadable to other users from the
    // instant it exists on disk (write order matters — H-2 SECURITY-REVIEW.md).
    chmod($cnfPath, 0o600);
    file_put_contents(
        $cnfPath,
        "[client]\nuser={$user}\npassword={$pass}\nhost={$host}\n"
    );

    try {
        $cmd = sprintf(
            'mysqldump --defaults-extra-file=%s --single-transaction --skip-lock-tables %s | gzip > %s',
            escapeshellarg($cnfPath),
            escapeshellarg($db),
            escapeshellarg($path),
        );
        $this->runDumpCommand($cmd);
    } finally {
        @unlink($cnfPath);
    }
}

protected function runDumpCommand(string $cmd): void
{
    $process = Process::fromShellCommandline($cmd);
    $process->setTimeout(3600);
    try {
        $process->mustRun();
    } catch (ProcessFailedException $e) {
        Log::error('WooDbSnapshotter: mysqldump failed', [
            'exception' => $e->getMessage(),
            'stderr' => $process->getErrorOutput(),
        ]);
        throw new \RuntimeException('mysqldump failed: '.$e->getMessage(), 0, $e);
    }
}
```

Update `snapshot()` body to call `$this->buildAndRunDump($host, $user, $pass, $db, $path);` in place of the old inline `$cmd = sprintf(...)` + `Process::fromShellCommandline(...)` + `try/catch` block (lines 75-103 in the current file). The audit_log record + return array (lines 105-118) stay untouched.

Update the class docblock: remove the misleading line `"the password still briefly appears in `ps`"` (lines 76-78 in current file's `// Command assembled ...` comment) and replace with a one-line reference to the 2026-06-06 SECURITY-REVIEW.md H-2 remediation + the 260607-9c6 quick task. Keep the `T-07-05-03 mitigation` reference but reframe it: the original threat (shell injection) is still mitigated by `escapeshellarg` on every interpolated value; the H-2 superseding mitigation is `--defaults-extra-file` hiding the password from argv entirely.

**Step 2 — New Pest test `tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php`.**

Use an anonymous subclass that overrides `runDumpCommand($cmd)` to capture the command string + tempfile state instead of invoking Process. Pattern from existing test (SnapshotWooDbCommandTest.php lines 51-71) — same shape, different override hook.

Skeleton:
```
<?php
declare(strict_types=1);

use App\Domain\Cutover\Services\WooDbSnapshotter;
use App\Foundation\Audit\Services\Auditor;
use Illuminate\Foundation\Testing\RefreshDatabase;

uses(RefreshDatabase::class);

const SECRET_SENTINEL = 'SECRET_SHOULD_NOT_LEAK_123!@#';

beforeEach(function (): void {
    $this->tmp = sys_get_temp_dir().'/cutover-snapshot-builder-test-'.uniqid();
    mkdir($this->tmp, 0o755, true);
    config([
        'cutover.backup_path' => $this->tmp,
        'cutover.woo_db.host' => 'wooprod.internal',
        'cutover.woo_db.username' => 'msops',
        'cutover.woo_db.password' => SECRET_SENTINEL,
        'cutover.woo_db.database' => 'meetingstore_woo',
    ]);
});

afterEach(function (): void {
    if (isset($this->tmp) && is_dir($this->tmp)) {
        array_map('unlink', glob($this->tmp.'/*') ?: []);
        @rmdir($this->tmp);
    }
});

function makeRecordingSnapshotter(?\Throwable $throw = null): array
{
    $capture = ['cmd' => null, 'cnf_existed_during_run' => null, 'cnf_content' => null, 'cnf_path' => null];
    $instance = new class(app(Auditor::class), $capture, $throw) extends WooDbSnapshotter {
        public function __construct(Auditor $a, public array &$capture, public ?\Throwable $throw) { parent::__construct($a); }
        protected function runDumpCommand(string $cmd): void
        {
            $this->capture['cmd'] = $cmd;
            // Extract --defaults-extra-file=<arg> path; arg is escapeshellarg-wrapped (single quotes)
            if (preg_match("/--defaults-extra-file='([^']+)'/", $cmd, $m)) {
                $this->capture['cnf_path'] = $m[1];
                $this->capture['cnf_existed_during_run'] = file_exists($m[1]);
                $this->capture['cnf_content'] = file_exists($m[1]) ? (string) file_get_contents($m[1]) : null;
            }
            if ($this->throw) { throw $this->throw; }
        }
    };
    return [$instance, &$capture];
}

it('builds a command that uses --defaults-extra-file', function (): void {
    [$snap, $capture] = makeRecordingSnapshotter();
    $snap->snapshot('builder-test');
    expect($capture['cmd'])->toContain('--defaults-extra-file=');
});

it('does NOT include the literal password in the assembled command', function (): void {
    [$snap, $capture] = makeRecordingSnapshotter();
    $snap->snapshot('no-leak');
    expect($capture['cmd'])->not->toContain(SECRET_SENTINEL);
});

it('does NOT use the legacy -p<pwd> short-flag', function (): void {
    [$snap, $capture] = makeRecordingSnapshotter();
    $snap->snapshot('no-p-flag');
    // -p followed by anything other than whitespace/EOL is the dangerous form
    expect(preg_match('/\s-p\S/', $capture['cmd']))->toBe(0);
});

it('writes a cnf with [client] block + user/password/host', function (): void {
    [$snap, $capture] = makeRecordingSnapshotter();
    $snap->snapshot('cnf-content');
    expect($capture['cnf_content'])->toContain('[client]');
    expect($capture['cnf_content'])->toContain('user=msops');
    expect($capture['cnf_content'])->toContain('password='.SECRET_SENTINEL);
    expect($capture['cnf_content'])->toContain('host=wooprod.internal');
});

it('unlinks the cnf temp file after a successful run', function (): void {
    [$snap, $capture] = makeRecordingSnapshotter();
    $snap->snapshot('unlink-success');
    expect(file_exists($capture['cnf_path']))->toBeFalse();
});

it('unlinks the cnf temp file even when the dump throws (finally branch)', function (): void {
    [$snap, $capture] = makeRecordingSnapshotter(new \RuntimeException('simulated mysqldump failure'));
    try { $snap->snapshot('unlink-on-failure'); } catch (\Throwable) { /* expected */ }
    expect(file_exists($capture['cnf_path']))->toBeFalse();
});
```

Notes:
- `escapeshellarg()` on Linux wraps in single quotes — `'SECRET_SHOULD_NOT_LEAK_123!@#'` would appear inside the command if the password reached argv, so the `not->toContain(SECRET_SENTINEL)` assertion bites.
- On Windows the regex extraction in the helper may not match because `escapeshellarg` uses double quotes — the test runs in Pest's Linux-flavoured filesystem under Docker/CI, but since this repo runs on Windows dev too, broaden the regex to also accept `"([^"]+)"` if needed.
- DO NOT invoke real `mysqldump` — the override hook on `runDumpCommand` ensures the test stays a unit-level command-string assertion. The existing `SnapshotWooDbCommandTest.php` already mocks at the `WooDbSnapshotter::snapshot()` level, so the two test files are complementary (one tests the orchestrating command, the other tests the dump-command builder).

**Step 3 — Confirm existing SnapshotWooDbCommandTest still passes** (it mocks `snapshot()` whole, so the refactor of the inside should be invisible). If it breaks, fix the test, do NOT weaken the refactor.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php tests/Feature/Cutover/SnapshotWooDbCommandTest.php</automated>
  </verify>
  <done>
    - WooDbSnapshotter assembles `mysqldump --defaults-extra-file=...` (no `-p<pwd>`)
    - `.cnf` temp file is chmod-0600 + unlinked in finally{} on both success and failure
    - New focused test file passes 6/6
    - Existing `SnapshotWooDbCommandTest.php` continues to pass 5/5 unchanged
    - Updated docblock no longer claims the password "briefly appears in `ps`"; references SECURITY-REVIEW.md H-2 + 260607-9c6
    - Atomic commit: `fix(cutover): WooDbSnapshotter uses --defaults-extra-file to hide creds from argv (260607-9c6)`
  </done>
</task>

<task type="auto">
  <name>Task 4: Update SECURITY-REVIEW.md frontmatter + H-1/H-2 remediation blocks</name>
  <files>
    - .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md
  </files>
  <action>
Edit `.planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md`.

**Frontmatter edits (lines 1-11):**
- Change `status: shippable_with_followups` → `status: high_findings_remediated`
- Add a new field after `status:`:
  ```
  high_remediated_in: 260607-9c6
  high_remediated_at: 2026-06-07
  ```

**H-1 section edit (find `### H-1 — `webhook_receipts.raw_body`...` around line 27):**
Insert a remediation banner as the FIRST content line after the heading (before `**File:**`):
```
> **REMEDIATED 2026-06-07 in quick task [260607-9c6](../260607-9c6-security-high-fixes-h-1-webhooks-prune-r/) — `webhooks:prune-receipts` artisan + nightly 03:25 London cron (per-topic retention: order=30d, customer=7d, other=90d). Field-level body redaction (option 2 in the original fix proposal) deferred — option 1 (retention prune) alone satisfies GDPR Art. 5(1)(e).**
```

**H-2 section edit (find `### H-2 — `WooDbSnapshotter`...` around line 43):**
Insert a remediation banner as the FIRST content line after the heading (before `**File:**`):
```
> **REMEDIATED 2026-06-07 in quick task [260607-9c6](../260607-9c6-security-high-fixes-h-1-webhooks-prune-r/) — `WooDbSnapshotter` now writes a chmod-0600 `.cnf` tempfile and passes `mysqldump --defaults-extra-file=<path>`. Tempfile is unlinked in `finally{}` on both success and failure paths. WooDB password no longer appears in `/proc/*/cmdline`.**
```

DO NOT alter the original finding text below those banners — the historical record of the finding stays intact. The banners are additive, prepended.

(The orchestrator will fill in the actual commit hashes via a follow-up after Tasks 1-3 land. For this task, the literal commit-hash placeholders are not needed in the banner text — the quick-task link is sufficient traceability.)
  </action>
  <verify>
    <automated>grep -F "high_findings_remediated" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md" && grep -c "REMEDIATED 2026-06-07" ".planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md"</automated>
  </verify>
  <done>
    - SECURITY-REVIEW.md frontmatter `status:` is `high_findings_remediated`
    - SECURITY-REVIEW.md frontmatter has new field `high_remediated_in: 260607-9c6`
    - H-1 section contains a `> **REMEDIATED 2026-06-07 ...` banner as the first content line
    - H-2 section contains a `> **REMEDIATED 2026-06-07 ...` banner as the first content line
    - Original finding text below the banners is byte-identical to before
    - Atomic commit: `docs(security): mark H-1 + H-2 remediated in SECURITY-REVIEW.md (260607-9c6)`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verification + final sign-off (no commit)</name>
  <what-built>
    - H-1: `webhooks:prune-receipts` command + nightly 03:25 London cron + Pest tests (Tasks 1-2)
    - H-2: `WooDbSnapshotter` `--defaults-extra-file` refactor + Pest tests (Task 3)
    - SECURITY-REVIEW.md flipped to `high_findings_remediated` with per-finding remediation banners (Task 4)
  </what-built>
  <how-to-verify>
1. **Focused tests green:**
   ```
   php artisan test tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php
   php artisan test tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php
   php artisan test tests/Feature/Cutover/SnapshotWooDbCommandTest.php
   ```
   All three suites pass with zero failures.

2. **Architectural guardrails still pass:**
   ```
   php artisan test tests/Architecture/EnvUsageTest.php
   php artisan test tests/Architecture/AutoCreatedPredicateTest.php
   ```
   Both pass.

3. **Schedule wiring confirmed:**
   ```
   php artisan schedule:list
   ```
   Output includes a row for `webhooks:prune-receipts` with cron `25 3 * * *` and timezone `Europe/London`.

4. **Full Pest suite delta vs 260606-rld baseline (1,833 passed / 219 failed / 3 skipped):**
   ```
   php artisan test 2>&1 | tail -20
   ```
   Expected: **+6 to +13 passed** (6 new H-1 tests + 6 new H-2 tests + 0 to 1 from any pre-existing test re-evaluating), **zero new failures** (failure count stays 219 or drops). If failure count rises, STOP — investigate which existing test broke.

5. **Spot-check the H-2 command shape on the live snapshotter (manual):**
   ```
   php artisan tinker --execute='
       config(["cutover.woo_db.password" => "DEMO_PASS_DO_NOT_LEAK"]);
       config(["cutover.backup_path" => sys_get_temp_dir()]);
       echo "Test: command should not contain DEMO_PASS_DO_NOT_LEAK on argv.";
   '
   ```
   (Manual eyeball — the test in Task 3 is the authoritative assertion; this is just a smoke check.)

6. **SECURITY-REVIEW.md visual check:**
   ```
   head -15 .planning/quick/260606-q7h-forensic-audit-exit-on-first-non-empty-r/SECURITY-REVIEW.md
   ```
   Frontmatter shows `status: high_findings_remediated` + `high_remediated_in: 260607-9c6`.

7. **Confirm 4 atomic commits landed (one per task 1-4):**
   ```
   git log --oneline -n 8
   ```
   Expect to see (top of list, newest first):
   - `docs(security): mark H-1 + H-2 remediated in SECURITY-REVIEW.md (260607-9c6)`
   - `fix(cutover): WooDbSnapshotter uses --defaults-extra-file to hide creds from argv (260607-9c6)`
   - `chore(schedule): wire webhooks:prune-receipts at 03:25 London (260607-9c6)`
   - `feat(webhooks): prune-receipts command with per-topic GDPR retention (260607-9c6)`

If ALL 7 checks pass, type `approved`. Otherwise, describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local DB shell user → `webhook_receipts.raw_body` | A DB-shell user can `SELECT raw_body FROM webhook_receipts` and read customer PII — H-1 surface |
| Local OS user → `/proc/*/cmdline` | Any local user can read another process's argv while it runs — H-2 surface |
| Local OS user → `sys_get_temp_dir()` `.cnf` file | A second local user could read the cnf if mode is not 0600 — secondary H-2 surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9c6-01 | Information Disclosure | `webhook_receipts.raw_body` | mitigate | Per-topic nightly retention prune via `webhooks:prune-receipts` cron (Task 1+2); customer=7d, order=30d, other=90d |
| T-9c6-02 | Information Disclosure | `mysqldump` argv via `/proc/*/cmdline` | mitigate | Replace `-p<pwd>` with `--defaults-extra-file=<chmod-0600-tempfile>` (Task 3) |
| T-9c6-03 | Information Disclosure | `.cnf` tempfile world-readable | mitigate | `chmod($cnfPath, 0o600)` BEFORE `file_put_contents` so other-user reads are blocked from the moment the file exists (Task 3) |
| T-9c6-04 | Information Disclosure | `.cnf` tempfile lingers after run | mitigate | `try { ... } finally { @unlink($cnfPath); }` covers success AND failure paths; test asserts both (Task 3 behaviors 4+5) |
| T-9c6-05 | Tampering | The 4 commits introduce no `env()` reads | mitigate | Architectural guardrail `EnvUsageTest` enforces; verified in Task 5 step 2 |
| T-9c6-06 | Repudiation | `webhooks:prune-receipts` mass delete bypasses model events | accept | WebhookReceipt does NOT use spatie LogsActivity (verified — model has no traits). BaseCommand's correlation_id LogBatch wrapper provides the audit thread for the command run itself. Per-row audit was never recorded for this table; nothing to preserve. |
| T-9c6-07 | Denial of Service | Daily nightly prune on a heavily-populated webhook_receipts table | accept | Topic + received_at queries are index-covered (composite index from migration). Worst-case delete is bounded by daily inbound webhook volume × retention days — at meetingstore.co.uk's scale, a few thousand rows at most per night. No chunking needed. |
| T-9c6-08 | Tampering (package supply chain) | No new composer packages | accept | Task uses only Laravel-native APIs (Eloquent, Process, BaseCommand, Schedule). No `composer require` invocation. Package Legitimacy Gate N/A. |
</threat_model>

<verification>

**Phase-level checks (Task 5 runs all of these):**

1. `php artisan test tests/Feature/Webhooks/PruneWebhookReceiptsCommandTest.php` — 6 green
2. `php artisan test tests/Feature/Cutover/WooDbSnapshotterCommandBuilderTest.php` — 6 green
3. `php artisan test tests/Feature/Cutover/SnapshotWooDbCommandTest.php` — 5 green (unchanged by refactor)
4. `php artisan test tests/Architecture/EnvUsageTest.php` — green (no env() introduced)
5. `php artisan test tests/Architecture/AutoCreatedPredicateTest.php` — green (untouched surface)
6. `php artisan schedule:list` shows `webhooks:prune-receipts` at cron `25 3 * * *` London
7. Full Pest suite delta vs 260606-rld baseline (1833/219/3): `+12 passed`, `0 new failed`, `3 skipped` unchanged
8. `grep -F "high_findings_remediated" .planning/quick/260606-q7h-.../SECURITY-REVIEW.md` returns a hit
9. `git log --oneline -n 5` shows 4 atomic commits from Tasks 1-4, all with `(260607-9c6)` suffix
</verification>

<success_criteria>

Quick task is complete when:

1. **H-1 closed:** `webhooks:prune-receipts` command exists, scheduled nightly at 03:25 Europe/London, with per-topic retention (order=30d, customer=7d, other=90d). All 6 Pest tests green.
2. **H-2 closed:** `WooDbSnapshotter::snapshot()` assembles a command using `--defaults-extra-file`, not `-p<pwd>`. Tempfile is chmod-0600 + unlinked in `finally{}`. All 6 new Pest tests green; pre-existing `SnapshotWooDbCommandTest.php` continues to pass.
3. **SECURITY-REVIEW.md updated:** Frontmatter `status: high_findings_remediated` + `high_remediated_in: 260607-9c6`; H-1 + H-2 sections each lead with a `> **REMEDIATED 2026-06-07 ...** ` banner.
4. **No regressions:** Full Pest suite delta = `+12 passed, 0 new failures` vs the 260606-rld baseline (1833/219/3).
5. **No env() introduced:** EnvUsageTest still passes.
6. **4 atomic commits landed** (one per Tasks 1-4), each carrying the `(260607-9c6)` correlation suffix.
7. **Operator sign-off** captured at Task 5 checkpoint (`approved`).
</success_criteria>

<output>
Create `.planning/quick/260607-9c6-security-high-fixes-h-1-webhooks-prune-r/260607-9c6-SUMMARY.md` when done. Summary must include:
- Commits landed (4 hashes + messages)
- Test deltas (focused + full-suite)
- Confirmation of SECURITY-REVIEW.md status flip
- Any operator notes from Task 5 checkpoint
</output>

# 260608-g8x Execution Log

## Task 1 — suppliers table + supplier_freshness_snapshots table + config + Supplier model
- Commit: a9115bd
- Files: database/migrations/2026_06_08_120000_create_suppliers_table.php, database/migrations/2026_06_08_120100_create_supplier_freshness_snapshots_table.php, config/supplier.php, app/Domain/Sync/Models/Supplier.php
- Migrate --pretend: both `create_suppliers_table` + `create_supplier_freshness_snapshots_table` parse + emit expected SQL
- Migrate live: both landed cleanly on local SQLite (suppliers 126ms; supplier_freshness_snapshots 7ms)
- EnvUsageTest GREEN (3 passed / 6 assertions)

## Task 2 — SupplierFreshnessResolver + SupplierFreshnessSnapshot model
- Commit: ebd1a00
- Files: app/Domain/Sync/Services/SupplierFreshnessResolver.php, app/Domain/Sync/Models/SupplierFreshnessSnapshot.php, app/Providers/AppServiceProvider.php
- Singleton binding registered in AppServiceProvider so the per-request cache survives the 3 downstream consumers
- Driver-aware days_since SQL (MySQL DATEDIFF vs SQLite julianday)
- Tinker probe: `classify("nonexistent") === "unknown"` → `ok`

## Task 3 — suppliers:check-stale command + Mon-Fri 07:45 cron + supplier discovery upsert
- Commit: 91e2088
- Files: app/Domain/Sync/Console/Commands/CheckStaleSuppliersCommand.php, app/Providers/AppServiceProvider.php, routes/console.php
- `php artisan list | grep suppliers:check-stale` → command registered
- `schedule:list` shows `45 6 * * 1-5` UTC = 07:45 London (Europe/London TZ) — slotted between suggestions:auto-apply (07:30 London) and reports:supplier-sync-digest (08:00 London)
- `php artisan suppliers:check-stale --dry-run` on empty DB → fresh:0 amber:0 stale:0 unknown:0, no errors

## Task 4 — SupplierOfferSnapshot::freshOnly() scope
- Commit: 2c84923
- Files: app/Domain/Products/Models/SupplierOfferSnapshot.php
- scopeFreshOnly added with `'__NO_FRESH_SUPPLIERS__'` string sentinel (supplier_id is VARCHAR(16))
- Tinker probe: hasMethod("scopeFreshOnly") → ok; `toSql()` → `select * from "supplier_offer_snapshots" where "supplier_id" in (?)`

## Task 5 — AdCandidateScanner stale-supplier exclusion + Pest
- Commit: d4e4caf
- Files: app/Domain/Pricing/Services/AdCandidateScanner.php, tests/Feature/Domain/Pricing/AdCandidateScannerStaleSupplierTest.php
- Constructor: added `SupplierFreshnessResolver $freshness` + `bool $excludeStaleSupplierStock = true`
- `latestSupplierByProductId()` conditionally appends `AND supplier_id IN (...)` when flag is ON; short-circuits to `[]` when no fresh suppliers
- Grep: exactly one `supplier_id IN` injection (line 281, conditional)
- New 2/2 GREEN; existing AdCandidateScannerTest 6/6 + AdCandidatesPageTest 6/6 GREEN (zero regressions)

## Task 6 — CompetitorPositionScanner + SupplierDbSyncCommand stale-supplier exclusion + Pest
- Commit: 5e42fb6
- Files: app/Domain/Pricing/Services/CompetitorPositionScanner.php, app/Domain/Sync/Commands/SupplierDbSyncCommand.php, tests/Feature/Sync/SupplierDbSyncCommandTest.php (factory updated), tests/Feature/Domain/Pricing/CompetitorPositionScannerStaleSupplierTest.php, tests/Feature/Domain/Sync/SupplierDbSyncStaleSupplierTest.php
- CompetitorPositionScanner: new constructor with required `SupplierFreshnessResolver` + bool flag. `cheapestSupplierNameByProductId` appends `AND supplier_id NOT IN (...)` when stale ids exist; skips clause entirely when empty (MySQL safety).
- SupplierDbSyncCommand: new constructor with required `SupplierFreshnessResolver` + bool flag. `buildBestOfferMap` filters $rows by stale supplier_ids BEFORE the cheapest-in-stock reduction.
- New 6/6 GREEN; CompetitorPositionScannerTest 9/9 + SupplierDbSyncCommandTest 9/9 GREEN (zero regressions). Helper-method tests are no-op safe because they don't seed snapshots → stale set empty.
- Test A inverse for CompetitorPositionScanner: corrected to mirror the actual back-compat semantic (cheapest CURRENT, NOT cheapest overall) — recorded_at DESC, price ASC window function.

## Task 7 — SupplierFreshnessWidget + computeSupplierFreshness + NotificationCentre + dashboard registration
- Commit: bf8f85a
- Files: app/Domain/Dashboard/Services/SnapshotAggregator.php, app/Domain/Dashboard/Services/NotificationCentreAggregator.php, app/Filament/Widgets/SupplierFreshnessWidget.php, app/Filament/Pages/HomeDashboardPage.php, app/Providers/Filament/AdminPanelProvider.php, tests/Feature/Dashboard/HomeDashboardPageTest.php
- `SnapshotAggregator::computeSupplierFreshness()` reads `supplier_freshness_snapshots` (NOT the resolver — D-02 truth: never live aggregation on dashboard render). Defensive try/catch returns zero payload if table missing.
- New metric_key 'supplier_freshness' added to `computeAll()` map (between `category_audit_health` and any future keys).
- `NotificationCentreAggregator::staleSuppliers()` mirrors `staleFeeds()` shape; reads same snapshot table.
- `SupplierFreshnessWidget` StatsOverviewWidget — 4 Stats (fresh/amber/stale/unknown), inherits `DashboardSnapshot::viewAny` policy gate, 60s polling.
- Registered in AdminPanelProvider widgets + HomeDashboardPage::getWidgets() between CompetitorFreshnessWidget and PendingReviewsWidget; also added to "Today's sync" section. Total widget count 14 → 15.
- HomeDashboardPageTest count assertion bumped 14 → 15 with new widget at index 3. Pre-existing `renders 9 widget` HTML-render test still fails identically (`assertSee('Last sync')` — verified pre-existing, zero new regressions).
- `php artisan dashboard:refresh` → "Dashboard refresh complete: 14 metrics upserted" (was 13; +1).
- Tinker: snapshot exists + widget class loads.

## Task 8 — Pest suite (resolver + command + scope)
- Commit: 23a2a37
- Files: tests/Unit/Domain/Sync/Services/SupplierFreshnessResolverTest.php (6 cases), tests/Feature/Console/CheckStaleSuppliersCommandTest.php (3), tests/Feature/Domain/Products/SupplierOfferSnapshotScopeTest.php (1)
- 10/10 GREEN (resolver 6 + command 3 + scope 1; 33 assertions, 8.65s)
- Case D required adjusting override to 30d (so amber boundary lands at floor(30 * 0.7)=21, 14 < 21 → fresh) — plan's "21d" override + "14d" snapshot lands at amber boundary which is amber not fresh.
- Case F cache assertion uses `DB::enableQueryLog()` + `DB::flushQueryLog()` — first call >0 queries, second call exactly 0.

# Execution log — 260609-rie

## Task 1: Probe + scope confirmation (no-commit)

Inventory of `FROM feeds_products` matches the planner exactly (10 hits, 3 in-scope reading `.stock`, 7 out-of-scope).

| # | File | Line | SELECTs | Disposition |
|---|---|---:|---|---|
| 1 | app/Domain/Sync/Commands/SupplierDbSyncCommand.php | 139 | fp.mpn, fp.suppliersku, fp.supplierid, fp.price, **fp.stock**, f.name | IN-SCOPE — needs JOIN |
| 2 | app/Domain/Sync/Commands/SupplierDbSyncCommand.php | 392 | fp.mpn, fp.suppliersku, fp.supplierid, fp.price, **fp.stock**, fp.rrp, f.name | IN-SCOPE — needs JOIN |
| 3 | app/Domain/Sync/Commands/ExplainSupplierCostCommand.php | 72 | fp.supplierid, f.name, fp.mpn, fp.suppliersku, fp.price, **fp.stock**, fp.rrp, fp.product_excluded, fp.updated_at | IN-SCOPE — needs JOIN |
| 4 | app/Console/Commands/BackfillMerchantFeedCommand.php | 881 | suppliersku, ean | OUT-OF-SCOPE — annotate |
| 5 | app/Console/Commands/BackfillMerchantFeedCommand.php | 905 | mpn, ean | OUT-OF-SCOPE — annotate |
| 6 | app/Console/Commands/BackfillMerchantFeedCommand.php | 966 | suppliersku, manufacturer | OUT-OF-SCOPE — annotate |
| 7 | app/Console/Commands/BackfillMerchantFeedCommand.php | 990 | mpn, manufacturer | OUT-OF-SCOPE — annotate |
| 8 | app/Domain/Sync/Services/SupplierSkuRegistry.php | 52 | mpn_key, ssku_key | OUT-OF-SCOPE — annotate |
| 9 | app/Domain/Sync/Services/SupplierFeedSourceabilityChecker.php | 70 | mpn_key, ssku_key | OUT-OF-SCOPE — annotate |
| 10 | app/Domain/Sync/Services/SupplierAddCandidateScanner.php | 61 | mpn, supplierid, manufacturer, title, supplierskus | OUT-OF-SCOPE — annotate |

Both SupplierDbSync queries already alias `feeds_products fp` and `LEFT JOIN feeds f ON fp.supplierid = f.id` — trait's JOIN replaces that whole line. ExplainSupplierCost (line 72) uses identical aliases. Go for Task 2.

## Task 2: Trait — feat(sync) — commit 779472a

Created `app/Domain/Sync/Concerns/JoinsStockSeparate.php` (100 lines). Two protected methods, default + alias-overridable. Smoke-loaded; both fragments contain the canonical SQL. EnvUsageTest GREEN.

## Task 3: Wire trait — fix(sync) — commit bfc9f75

Replaced `fp.stock` + the existing `LEFT JOIN feeds f ON fp.supplierid = f.id` at all 3 in-scope sites. Pre-flight greps: 0/0/0/0 hits. php -l clean. SupplierDbSyncCommandTest + BackfillMerchantFeedCommandTest + EnvUsageTest GREEN (28 tests).

## Task 4: Arch test + annotations — test(architecture) — commit d8781bf

Created `tests/Architecture/StockSeparateJoinTest.php` (3 cases — file-scan, meta-assertion, trait sanity). Annotated 4 OUT-OF-SCOPE files (BackfillMerchantFeedCommand, SupplierSkuRegistry, SupplierFeedSourceabilityChecker, SupplierAddCandidateScanner). Deviation: had to replace `260609-rie-*/` with `260609-rie-... directory` in 4 docblock comments and 3 line comments because `*/` inside `/** */` terminated the docblock and broke parse — caught by the deliberately-broken sanity-test run.

## Task 5: Pest cases A-E — test(sync) — commit 7ebb774

Created `tests/Feature/Sync/SupplierDbSyncCommandStockSeparateTest.php` (5 cases) + `tests/Feature/Sync/ExplainSupplierCostCommandStockSeparateTest.php` (5 cases). Both files use anonymous helper objects that import `JoinsStockSeparate` directly (commands are `final` and cannot be subclassed). Drift guards via `class_uses()` confirm production commands actually `use JoinsStockSeparate;`. 10/10 GREEN. SupplierDbSyncCommandTest + BackfillMerchantFeedCommandTest regression GREEN (25 tests).

## Task 6: Verify — no-commit

Focused bundle (13 NEW + regression bundle 52) = 65 tests GREEN. CLI surface: `supplier:db-sync` + `supplier:explain-cost` both resolve. Full Pest suite: **1,978 pass / 222 failed / 3 skipped** — delta vs 260609-nku baseline (1,965 / 222 / 3) = **+13 pass / 0 new fails**, exactly as the plan predicted.

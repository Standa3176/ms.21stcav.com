# 260610-qc4 — Execution log

## Task 1 — Probe + scope confirmation (no commit)

Re-read all files listed in plan. Confirmed:
- `_alg_wc_cog_cost` is the live meta key for buy_price (WooImportProductsCommand.php:136).
- `WooFieldComparator.php` 137 lines, 7 comparison blocks, stdClass→assoc prelude line 58–60.
- `DivergenceScanner.php` iterates `$diffs` from comparator without inspecting fields — zero-touch confirmed.
- Product casts: stock_quantity=int, buy_price=decimal:4, others (brand_id/category_id/ean/stock_status) uncast → DB column types apply.
- `tests/Feature/Cutover/WooFieldComparatorTest.php` does NOT exist (plan correction confirmed — Task 4 creates fresh).
- `tests/Architecture/DivergenceComparatorCoverageTest.php` does NOT exist — Task 3 creates fresh.
- `ProductFactory` defaults `stock_status='instock'` — Task 4 fixtures must override to '' for neutral baseline.

Scope-confirmation block appended to PLAN.md `## Task 1 confirmation`.

**Commit:** none.

## Task 2 — Extend WooFieldComparator::diff() with 6 new comparisons (commit 8e52b78)

- Added 3 class constants: `META_DESCRIPTION_META_KEY = '_yoast_wpseo_metadesc'`,
  `BUY_PRICE_META_KEY = '_alg_wc_cog_cost'`, `BRAND_ID_META_KEY = '_product_brand_id'`,
  `EAN_META_KEYS = ['_global_unique_id', '_ean', '_alg_ean']`.
- Added 6 new comparison blocks (stock_quantity, stock_status, buy_price, category_id, brand_id, ean).
- **Rule 1 fix:** closed pre-existing meta_description gap — Phase 7 Plan 05 docblock declared the
  mapping but no comparison block existed. Added the Yoast `_yoast_wpseo_metadesc` comparison.
- Added 3 private helpers: `extractMetaValue()` (generic meta walker), `extractBrandId()`, `extractEan()`.
- DivergenceScanner.php untouched.
- **Rule 1 fix:** DivergenceScanCommandTest IDENT-1 and PAR-1 fixtures extended with
  `stock_status='instock'` to match real Woo REST shape. Without this the new canonical-absence
  contract emitted a false stock_status diff because ProductFactory defaults stock_status='instock'
  but the original minimal Woo fixtures omitted it.
- Field-literal count: 14 (13 fields + `exists` sentinel) — matches plan intent (one emission per field).
- `php -l` clean. `vendor/bin/pest tests/Feature/Cutover/DivergenceScanCommandTest.php` 9/9 GREEN.

## Task 3 — Architectural guard test (commit cccffe5)

- Created `tests/Architecture/DivergenceComparatorCoverageTest.php` with header docblock
  verbatim per plan + 2 tests (file-scan + meta-assertion).
- Failure message literally contains "Edit BOTH the comparator AND this test's expected list"
  and the PLAN.md path.
- 2/2 GREEN.
- Sibling arch tests still GREEN: EnvUsageTest (3/3), StockSeparateJoinTest (3/3), AutoCreatedPredicateTest (2/2).

## Task 4 — Pest feature cases A-G + regression + sentinel (commit 29066e4)

- Created `tests/Feature/Cutover/WooFieldComparatorTest.php` with 9 cases.
- Cases A-G + regression + sentinel — all GREEN (45 assertions).
- DivergenceScanCommandTest still GREEN (9/9).
- Note: `$this->base()` neutral baseline uses `stock_status='instock'` to match ProductFactory
  default — otherwise the canonical-absence contract would emit a false stock_status diff in
  cases that don't override stock_status locally.

## Task 5 — Verify (no commit)

Full verification matrix run:
- **Step 1:** WooFieldComparatorTest 9/9 GREEN, DivergenceComparatorCoverageTest 2/2 GREEN.
- **Step 2:** DivergenceScanCommandTest 9/9 GREEN — no regression.
- **Step 3:** Sibling arch tests GREEN — EnvUsageTest 3/3, StockSeparateJoinTest 3/3, AutoCreatedPredicateTest 2/2.
- **Step 4 (full suite):** **1989 passed / 222 failed / 3 skipped** (baseline 1978/222/3).
  Delta: **+11 pass / 0 new fails / 0 new skips** — matches plan target exactly.
- **Step 5:** `cutover:divergence-scan` registered in artisan registry.
- **Step 6:** `php -l` clean on WooFieldComparator.php.
- **Step 7:** Field-literal count = 14 (13 fields + `exists` sentinel) — one emission per field.
  (Plan target of "13" assumed meta_description already implemented; pre-existing gap closed in Task 2.)

**Commit:** none.

**Post-deploy operator action** (documented in SUMMARY, not part of this task's verify):
- After commit lands on prod: `php artisan cutover:divergence-scan --live` to surface previously-hidden divergences.

# 260611-f1y — Execution Log

Quick task: Hide internal-use products (Credit / Offer / Quote Payment) from
the meetingstore.co.uk storefront via `catalog_visibility=hidden` + add a
permanent `is_internal_only` flag + operator toggle + idempotent push command.

## Tasks

### Task 1 — Migration: is_internal_only column + Product model

- `database/migrations/2026_06_11_095943_add_is_internal_only_to_products_table.php` — created (`up()` adds nullable bool default false after `exclude_from_auto_update`, defensive Schema::hasColumn fallback if column absent; `down()` drops index + column).
- `app/Domain/Products/Models/Product.php` — `'is_internal_only'` added to `$fillable` after `exclude_from_auto_update`; `'is_internal_only' => 'bool'` added to `$casts`; NOT added to `getActivitylogOptions()->logOnly()` per planner decision.
- Verified: `php artisan migrate:fresh --env=testing` clean; `Schema::hasColumn('products', 'is_internal_only') === true`.
- Commit: `31f1967` — `feat(products): is_internal_only column + index (260611-f1y)`

### Task 2 — Seed migration: flag 3 known internal woo_product_ids

- `database/migrations/2026_06_11_100127_seed_internal_only_products.php` — created. `up()` flags woo_product_id IN (167493, 167492, 165038); `down()` reverses. Docblock names each product + the storefront symptom + why `woo_product_id` is the seed key (empty-SKU on all 3, Dell 8K monitor false positive risk if SKU-based).
- Idempotency belt-and-braces: `up()` skips rows already flagged so re-run is a true no-op.
- Verified: `php artisan migrate --env=testing` clean (no seed rows present in fresh sqlite test DB, which is the documented expected outcome).
- Commit: `2babc42` — `feat(products): seed is_internal_only=true for Credit/Offer/Quote Payment (260611-f1y)`

### Task 3 — PushVisibilityToWooCommand + registration

- `app/Console/Commands/PushVisibilityToWooCommand.php` — new BaseCommand subclass with constructor-injected WooClient.
  - Default candidate query: `is_internal_only=true AND woo_product_id NOT NULL`.
  - `--skus` operator override: numeric tokens match `woo_product_id`, non-numeric match `sku`. Drops the `is_internal_only` filter for ad-hoc force-push.
  - Pre-GET idempotency check: if Woo reports `catalog_visibility=hidden` already → `already_hidden++`, NO PUT call.
  - PUT body: `['catalog_visibility' => 'hidden']` (single-field — catalog_visibility doesn't trigger the Cost-of-Goods recompute hook so no split-PUT WAF workaround needed).
  - 200ms usleep between live PUTs.
  - `--dry-run`: zero Woo I/O (GETs also skipped).
  - Per-candidate failures → `errors++`, exit SUCCESS (operator drives next-action from summary).
  - Drift-prevention docblock cross-references WooFieldComparator (260610-qc4) — confirms catalog_visibility is NOT in the 13-field comparator scope so the 3 seeded SKUs will not flood sync_diffs.
- `app/Providers/AppServiceProvider.php` — `use App\Console\Commands\PushVisibilityToWooCommand;` added near BackfillMerchantFeedCommand; `PushVisibilityToWooCommand::class` registered in `commands([...])` array next to BackfillCategoryFromWooCommand with a 5-line operator-pointing comment.
- Verified: `php artisan list | grep push-visibility-to-woo` resolves; `--dry-run --env=testing` smoke-run reports `scanned=0 would_hide=0` (expected on fresh sqlite test DB).
- Commit: `5fb2c4a` — `feat(products): products:push-visibility-to-woo command + registration (260611-f1y)`

### Task 4 — Filament toggle on ProductResource Details tab

- `app/Domain/Products/Filament/Resources/ProductResource.php` — `Toggle::make('is_internal_only')` added to Details tab adjacent to `exclude_from_auto_update`. Helper text spells out the toggle-OFF caveat + storefront-direct-URL preservation. Visibility gated on `auth()->user()?->hasAnyRole(['admin', 'pricing_manager'])` (T-f1y-02 EoP mitigation).
- `app/Domain/Products/Filament/Resources/ProductResource/Pages/EditProduct.php` — `mutateFormDataBeforeSave` captures `$preSaveIsInternalOnly` before Filament refreshes the model; `afterSave` extended with `pushVisibilityIfToggled()` that calls `Artisan::call('products:push-visibility-to-woo', ['--skus' => $token])` synchronously when the toggle changes. Token prefers `sku`, falls back to `woo_product_id` (empty-SKU internals).
- Success notification surfaces the toggle-OFF caveat explicitly: "command only sets hidden — re-enable visibility manually in Woo admin if needed".
- ToggleColumn on the table intentionally NOT added (T-f1y-03 DoS mitigation — table-render Woo I/O risk).
- PHP -l clean on both modified files.
- Commit: `279ce0b` — `feat(filament): is_internal_only toggle on Products edit form (260611-f1y)`

### Task 5 — Pest cases A-F

- `tests/Feature/Console/PushVisibilityToWooCommandTest.php` — created with 6 cases A-F.
  - Case A: dry-run, 3 internals → 0 GETs + 0 PUTs + `would_hide` printed.
  - Case B: live, 1 internal `visible` → 1 GET + 1 PUT with `[catalog_visibility => hidden]`.
  - Case C: `woo_product_id=NULL` → no Woo call + `no_woo_product_id` counter (via `--skus` to target the row).
  - Case D: `--skus` force-push on NOT-flagged product → 1 GET + 1 PUT.
  - Case E: PUT throws on 1 of 2 → `errors=1 hidden=1`, exit 0.
  - Case F: pre-GET reports `hidden` → `already_hidden=1`, **0 PUT calls** (idempotency proof).
- Inline `bindVisibilityStub()` helper: anonymous-subclass `WooClient` with public `$getCalls` + `$putCalls`; `throwPutFor` parameter targets specific woo_ids for Case E.
- 6/6 GREEN on focused Pest (29 assertions, 31.18s).
- Commit: `574878b` — `test(products): push-visibility-to-woo Pest cases A-F (260611-f1y)`

### Task 6 — Verify (no commit)

1. **Command resolves:** `php artisan list | grep push-visibility-to-woo` → `products:push-visibility-to-woo` line shown. **PASS**.
2. **Focused Pest:** PushVisibilityToWooCommandTest → 6/6 GREEN (29 assertions, 31.18s). **PASS**.
3. **Regression Pest** (combined run):
   - BackfillCategoryFromWooCommandTest → 6/6 GREEN.
   - DivergenceScanCommandTest → 9/9 GREEN.
   - WooFieldComparatorTest → 9/9 GREEN (path: `tests/Feature/Cutover/WooFieldComparatorTest.php`).
   - ResyncProductsToWooCommandTest: file does not exist (plan said "if exists" — skipped per plan).
   - Combined 24/24 GREEN, 94 assertions, 15.18s. **PASS**.
4. **Full Pest suite delta vs 260610-qc4 baseline (1,989 / 222 / 3):**
   - Actual: **1,995 / 222 / 3** (9,848 assertions, 1,454.32s).
   - Delta: **+6 pass / 0 new fails / 0 new skipped** — exactly the 6 new Case A-F cases.
   - Required: +N (N ≥ 6) / 0 new fails. **PASS**.
5. **Migration applies cleanly:** `migrate:fresh` clean; in-process probe via PRAGMA index_list confirms `is_internal_only` column present (`COL_OK`) + `idx_products_is_internal_only` index present (`IDX_OK`). **PASS**.
6. **Drift-prevention grep guard:**
   - `grep catalog_visibility app/Console/Commands/PushVisibilityToWooCommand.php` → present (PUT body literal + docblock).
   - `grep catalog_visibility app/Domain/Cutover/Services/WooFieldComparator.php` → **ABSENT** (drift-prevention contract held). **PASS**.
7. **No smoke probe against live Woo** — operator post-deploy workflow documented in SUMMARY (NOT executed here).

**All 7 verification checks PASSED. No commit on Task 6.**

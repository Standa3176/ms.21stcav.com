---
quick_id: 260611-f1y
phase: quick-260611-f1y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/{TS}_add_is_internal_only_to_products_table.php
  - database/migrations/{TS+1}_seed_internal_only_products.php
  - app/Domain/Products/Models/Product.php
  - app/Console/Commands/PushVisibilityToWooCommand.php
  - app/Providers/AppServiceProvider.php
  - app/Domain/Products/Filament/Resources/ProductResource.php
  - tests/Feature/Console/PushVisibilityToWooCommandTest.php
autonomous: true
requirements:
  - 260611-f1y/storefront-hide-3-internal-products
  - 260611-f1y/ms-side-is_internal_only-flag
  - 260611-f1y/operator-toggle-from-product-edit
  - 260611-f1y/idempotent-push-command-with-pre-get-check

must_haves:
  truths:
    - "After migration, products row for woo_product_id IN (167493, 167492, 165038) has is_internal_only=1."
    - "Running `php artisan products:push-visibility-to-woo` (live) sets catalog_visibility=hidden on all 3 Woo products."
    - "Re-running the command is cheap (pre-GET sees `hidden`, increments already_hidden, skips PUT)."
    - "Operator can toggle `is_internal_only` from the Product edit Details tab; on save the artisan command pushes to Woo for that single SKU/woo_id."
    - "Internal products remain orderable + accessible via direct URL on the storefront (catalog_visibility=hidden is per-product, does not unpublish)."
    - "`php artisan list | grep push-visibility-to-woo` resolves the command (drift-prevention grep guard)."
    - "WooFieldComparator (260610-qc4) does NOT compare catalog_visibility — these 3 SKUs do NOT regress the divergence-scan (documented in command docblock)."
  artifacts:
    - path: "database/migrations/{TS}_add_is_internal_only_to_products_table.php"
      provides: "is_internal_only column + index"
      contains: "is_internal_only"
    - path: "database/migrations/{TS+1}_seed_internal_only_products.php"
      provides: "Idempotent seed for 3 known internal woo_product_ids"
      contains: "167493"
    - path: "app/Console/Commands/PushVisibilityToWooCommand.php"
      provides: "products:push-visibility-to-woo artisan command"
      contains: "catalog_visibility"
    - path: "tests/Feature/Console/PushVisibilityToWooCommandTest.php"
      provides: "6 Pest cases A-F"
      contains: "PushVisibilityToWooCommand"
  key_links:
    - from: "PushVisibilityToWooCommand"
      to: "WooClient::get + WooClient::put"
      via: "constructor DI"
      pattern: "WooClient \\$woo"
    - from: "ProductResource Details tab"
      to: "products:push-visibility-to-woo"
      via: "Toggle afterStateUpdated + Artisan::call"
      pattern: "products:push-visibility-to-woo"
    - from: "AppServiceProvider::boot commands array"
      to: "PushVisibilityToWooCommand::class"
      via: "ServiceProvider::commands()"
      pattern: "PushVisibilityToWooCommand::class"
---

<objective>
Hide 3 confirmed internal-use Woo products (`Credit` woo=167493, `Offer` woo=167492, `Quote Payment (No Vat)` woo=165038) from the meetingstore.co.uk storefront via Woo `catalog_visibility=hidden` while keeping them orderable via direct URL + custom-quote attach.

Add a permanent MS-side mechanism so future internals can be hidden by an operator toggle (no DB poke required):

1. `is_internal_only` boolean column on `products` table + index
2. Seed migration flagging the 3 known woo_product_ids
3. `products:push-visibility-to-woo` artisan command — idempotent (pre-GET check), --skus= operator override, dry-run
4. Filament toggle on `ProductResource` Details tab — flipping the toggle invokes the push command synchronously for that SKU
5. 6 Pest cases mirroring `BackfillCategoryFromWooCommandTest` (260607-v5g)

**Purpose:** Storefront UX cleanup (3 confusing internal products visible to customers) + future-proof for the next internal SKU.

**Output:** 2 migrations + 1 command + 1 resource update + 1 model fillable/cast update + 1 service provider registration + 6 Pest cases. 5 atomic commits + 1 verify task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/Console/Commands/ResyncProductsToWooCommand.php
@app/Console/Commands/BackfillCategoryFromWooCommand.php
@app/Domain/Sync/Services/WooClient.php
@app/Domain/Products/Models/Product.php
@app/Providers/AppServiceProvider.php
@app/Domain/Products/Filament/Resources/ProductResource.php
@tests/Feature/Console/BackfillCategoryFromWooCommandTest.php

<interfaces>
<!-- Key contracts the executor will consume — extracted from the codebase. -->

WooClient (app/Domain/Sync/Services/WooClient.php):
```
public function get(string $endpoint, array $query = []): array
public function put(string $endpoint, array $payload): array  // routes through writeOrShadow; honours services.woo.write_enabled
public function post(string $endpoint, array $payload): array
```

Product model fillable order (insert `is_internal_only` adjacent to `exclude_from_auto_update`):
```
'is_custom_ms', 'exclude_from_auto_update', 'tags',
                ^^^ ADD 'is_internal_only' AFTER exclude_from_auto_update
```
Product casts add: `'is_internal_only' => 'bool'`.

BaseCommand pattern (extended by all commands in app/Console/Commands/): subclass `BaseCommand` and implement `protected function perform(): int`. Return `SymfonyCommand::SUCCESS` / `FAILURE` via `use Symfony\Component\Console\Command\Command as SymfonyCommand`.

AppServiceProvider command registration: append `PushVisibilityToWooCommand::class` to the `$this->commands([...])` array inside `boot()`'s `runningInConsole()` guard (see BackfillCategoryFromWooCommand registration at line ~743 of AppServiceProvider).

WAF note (from ResyncProductsToWooCommand 260530-clv): the regular_price + everything-else combined-PUT race is specific to the Cost-of-Goods plugin hook. `catalog_visibility` alone does NOT trigger that race — a SINGLE-FIELD PUT `{'catalog_visibility': 'hidden'}` is safe; do NOT split.

Pest stubbing pattern (from BackfillCategoryFromWooCommandTest): anonymous-subclass `WooClient` with public `$calls` array + override of `get()` / `put()`. Skip parent constructor. Bind via `app()->instance(WooClient::class, $stub)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — is_internal_only column + index, Product model fillable + cast</name>
  <files>
    database/migrations/{TS}_add_is_internal_only_to_products_table.php,
    app/Domain/Products/Models/Product.php
  </files>
  <action>
Generate a NEW migration `{TS}_add_is_internal_only_to_products_table.php` (use `php artisan make:migration add_is_internal_only_to_products_table --table=products` to get the canonical timestamp).

Migration `up()`:
- `Schema::table('products', function (Blueprint $table) { $table->boolean('is_internal_only')->nullable()->default(false)->after('exclude_from_auto_update'); $table->index('is_internal_only', 'idx_products_is_internal_only'); })`
- `after('exclude_from_auto_update')` keeps internal/exclusion flags grouped — both are operator-controlled product-level booleans. If `exclude_from_auto_update` is not a real column on this table, fall back to `after('tags')`; probe with `Schema::hasColumn('products', 'exclude_from_auto_update')` before assuming.

Migration `down()`:
- `Schema::table('products', function (Blueprint $table) { $table->dropIndex('idx_products_is_internal_only'); $table->dropColumn('is_internal_only'); })`

Update `app/Domain/Products/Models/Product.php`:
- Add `'is_internal_only'` to `$fillable` ARRAY immediately after `'exclude_from_auto_update'` (line 38 area — keep grouped with other operator flags).
- Add `'is_internal_only' => 'bool'` to `$casts` ARRAY immediately after `'exclude_from_auto_update' => 'bool'` (line 60 area).
- Do NOT add `is_internal_only` to `getActivitylogOptions()->logOnly()` — toggling internal visibility is operator UX, not an auditable product-data change. (Decision per 260611-f1y; if the operator later wants an audit trail, add it in a follow-up.)

Run `php artisan migrate` in the test env to confirm the migration applies cleanly.

Atomic commit: `feat(products): is_internal_only column + index (260611-f1y)`
  </action>
  <verify>
    <automated>php artisan migrate:fresh --env=testing &amp;&amp; php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); echo (Illuminate\Support\Facades\Schema::hasColumn('products', 'is_internal_only') ? 'OK' : 'MISSING') . PHP_EOL;"</automated>
  </verify>
  <done>
- Migration file created with `up()` + `down()`.
- `is_internal_only` column exists on `products` (TINYINT, nullable, default 0) verified via Schema::hasColumn.
- Index `idx_products_is_internal_only` present (verify via `SHOW INDEX FROM products`).
- Product model `$fillable` includes `is_internal_only`; `$casts` includes `'is_internal_only' =&gt; 'bool'`.
- Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed migration — flag the 3 known internal woo_product_ids</name>
  <files>database/migrations/{TS+1}_seed_internal_only_products.php</files>
  <action>
Generate a NEW migration `{TS+1}_seed_internal_only_products.php` AFTER Task 1's migration timestamp (re-run `php artisan make:migration seed_internal_only_products` — Laravel will give it a later timestamp).

WHY a separate migration instead of bundling into Task 1: keeps the schema change atomic + reversible without re-running data backfills. If a future env needs to skip the seed (e.g., a fresh dev DB), the operator can `php artisan migrate --pretend` to inspect, or comment the second migration out — cleaner than coupling.

WHY `woo_product_id` is the seed key (NOT `sku`): all 3 internal products have empty SKU. SKU-based lookups would silently match other empty-SKU rows (e.g., the Dell 8K monitor false positive at woo=7317 surfaced in today's prod probe).

Migration `up()`:
```
use Illuminate\Support\Facades\DB;

DB::table('products')
    ->whereIn('woo_product_id', [167493, 167492, 165038])
    ->update(['is_internal_only' =&gt; true, 'updated_at' =&gt; now()]);
```

Migration `down()` (reverse for safe rollback):
```
DB::table('products')
    ->whereIn('woo_product_id', [167493, 167492, 165038])
    ->update(['is_internal_only' =&gt; false, 'updated_at' =&gt; now()]);
```

Add a docblock at the top of the file naming each product (Credit 167493, Offer 167492, Quote Payment (No Vat) 165038) and noting the storefront symptom + why woo_product_id is the seed key.

Idempotent — re-running migration is a safe no-op (already true → true is a write, but harmless; if you want true idempotency add `-&gt;where('is_internal_only', '!=', true)` to the WHERE clause).

DO NOT push to Woo from this migration — the migration is data-only. The push step is the artisan command in Task 3, run by the operator AFTER deploy.

Atomic commit: `feat(products): seed is_internal_only=true for Credit/Offer/Quote Payment (260611-f1y)`
  </action>
  <verify>
    <automated>php artisan migrate --env=testing &amp;&amp; php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); \$cnt = Illuminate\Support\Facades\DB::table('products')->whereIn('woo_product_id', [167493, 167492, 165038])->where('is_internal_only', true)->count(); echo \$cnt === 0 ? 'OK (no seed rows present in test DB)' : (\$cnt === 3 ? 'OK (3 seeded)' : 'PARTIAL: ' . \$cnt) . PHP_EOL;"</automated>
  </verify>
  <done>
- Seed migration file created with `up()` + `down()`.
- `up()` updates `is_internal_only` for woo IDs 167493/167492/165038.
- `down()` reverses cleanly.
- Docblock names each product + explains woo_product_id seed-key choice.
- Atomic commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PushVisibilityToWooCommand + AppServiceProvider registration</name>
  <files>
    app/Console/Commands/PushVisibilityToWooCommand.php,
    app/Providers/AppServiceProvider.php
  </files>
  <behavior>
    - Default candidate query: `Product::query()->where('is_internal_only', true)->whereNotNull('woo_product_id')`.
    - `--skus=` override: comma-separated tokens; numeric tokens match `woo_product_id`, others match `sku` (try sku first; for empty-SKU internal products, the woo_product_id branch hits).
    - Pre-GET each candidate's current `catalog_visibility`; if already `hidden`, `already_hidden++` and skip the PUT.
    - For non-hidden candidates: single-field PUT `['catalog_visibility' =&gt; 'hidden']`, increment `hidden++` on success.
    - PUT exception → `errors++`, continue to next candidate (do not abort the run).
    - Candidates with `woo_product_id IS NULL` → `no_woo_product_id++` (defensive — internal flag without Woo link is an operator data issue surfaced for review).
    - Print summary table: scanned / hidden / already_hidden / errors / no_woo_product_id.
    - 200ms `usleep(200_000)` between Woo calls (matches BackfillCategoryFromWooCommand pacing).
    - `--dry-run`: print plan table, no Woo writes (GETs also skipped — dry-run means zero Woo I/O for predictable test cost).
    - Exit code SUCCESS even when `errors &gt; 0` (per-candidate failures are reported, not fatal — operator drives next-action via summary).
  </behavior>
  <action>
Create `app/Console/Commands/PushVisibilityToWooCommand.php`. Mirror the BackfillCategoryFromWooCommand structure (extends `BaseCommand`, implements `protected function perform(): int`, declared `class PushVisibilityToWooCommand extends BaseCommand` — NOT `final` so the Pest anonymous-subclass stub can swap dependencies if needed).

Signature:
```
protected $signature = 'products:push-visibility-to-woo
    {--skus= : Comma-separated SKU OR woo_product_id list; default = all is_internal_only=true products with woo_product_id}
    {--limit=0 : Max products this run (0=unbounded)}
    {--dry-run : Print plan without Woo writes (and without Woo GETs)}';

protected $description = 'Push catalog_visibility=hidden to Woo for is_internal_only products (260611-f1y).';
```

Constructor: `public function __construct(private readonly WooClient $woo) { parent::__construct(); }`.

DOCBLOCK (class-level) — REQUIRED for drift-prevention:
```
/**
 * Quick task 260611-f1y — products:push-visibility-to-woo.
 *
 * Pushes catalog_visibility=hidden to Woo for every Product flagged
 * is_internal_only=true. Idempotent (pre-GET checks current state,
 * skips PUT if already hidden). Operator-triggered — NOT scheduled.
 *
 * Seeded internals (woo_product_id):
 *   167493 — Credit
 *   167492 — Offer
 *   165038 — Quote Payment (No Vat)
 *
 * Future internals: operator flips the Filament toggle on the Product
 * Edit page; the toggle's afterStateUpdated calls this command with
 * --skus={sku|woo_id} synchronously.
 *
 * Single-field PUT: {'catalog_visibility': 'hidden'}. The split-PUT
 * WAF workaround in ResyncProductsToWooCommand is specific to the
 * Cost-of-Goods plugin's regular_price recompute hook and does NOT
 * apply here (catalog_visibility doesn't trigger that hook).
 *
 * See also: app/Domain/Cutover/Services/WooFieldComparator.php does
 * NOT compare catalog_visibility — out of scope for the 260610-qc4
 * parity check. These 3 SKUs will not surface as sync_diffs after
 * the push.
 *
 *   php artisan products:push-visibility-to-woo --dry-run
 *   php artisan products:push-visibility-to-woo --skus=Credit,Offer
 *   php artisan products:push-visibility-to-woo --skus=167493
 *   php artisan products:push-visibility-to-woo
 */
```

`perform()` logic:
1. Parse `--skus` into `$skusFilter` (array of trimmed non-empty strings) and `$limit` (int, max 0 → unbounded). Parse `$dryRun`.
2. Build candidate query:
   - Default: `Product::query()->where('is_internal_only', true)->whereNotNull('woo_product_id')`.
   - With `--skus`: build with `->where(function ($q) use ($skusFilter) { $numeric = array_filter($skusFilter, ctype_digit(...)); $alpha = array_diff($skusFilter, $numeric); if ($alpha) { $q->orWhereIn('sku', $alpha); } if ($numeric) { $q->orWhereIn('woo_product_id', array_map('intval', $numeric)); } })` — DROPS the `is_internal_only` filter so operator can force-push ad-hoc.
3. Apply `->limit($limit)` if `$limit > 0`.
4. Stream via `->cursor()` to build `$candidates` array keyed `sku =&gt; woo_product_id` (sku may be empty string — fine, the loop only reads woo_product_id for the Woo call).
5. Counters: `$scanned = 0; $hidden = 0; $alreadyHidden = 0; $errors = 0; $noWooId = 0; $skippedSamples = [];`.
6. For each candidate:
   - `$scanned++`. If `$wooId` is null/zero → `$noWooId++`, continue.
   - **If $dryRun:** print line `would_hide woo={$wooId} sku={$sku}` and continue (NO GET/PUT during dry-run — keeps the test cost predictable + matches operator expectation that --dry-run means zero Woo I/O).
   - **Live path:**
     - Pre-GET: `$current = $this->woo->get("products/{$wooId}", []);` → catch Throwable → `$errors++`, log, continue.
     - `$visibility = (string) ($current['catalog_visibility'] ?? 'visible');`
     - If `$visibility === 'hidden'` → `$alreadyHidden++`, log line `already_hidden woo={$wooId}`, continue.
     - PUT: `$this->woo->put("products/{$wooId}", ['catalog_visibility' =&gt; 'hidden']);` → catch Throwable → `$errors++`, log, continue.
     - `$hidden++`, log line `hidden woo={$wooId} sku={$sku}`.
     - `usleep(200_000);` between Woo calls.
7. After loop: print summary table:
   ```
   $this->table(['Outcome', 'Count'], [
       ['scanned', $scanned],
       [$dryRun ? 'would_hide' : 'hidden', $hidden + ($dryRun ? 0 : 0)],  // dry-run uses $scanned for would_hide count; simpler — use a dedicated counter
       ['already_hidden', $alreadyHidden],
       ['no_woo_product_id', $noWooId],
       ['errors', $errors],
   ]);
   ```
   (Simpler: use a `$wouldHide` counter incremented in the dry-run branch instead of conflating.)
8. Return `SymfonyCommand::SUCCESS`.

Register the command — edit `app/Providers/AppServiceProvider.php`:
- Add `use App\Console\Commands\PushVisibilityToWooCommand;` near the other `App\Console\Commands\*` use statements (alphabetic insertion near `BackfillCategoryFromWooCommand`).
- Append `PushVisibilityToWooCommand::class,` to the `$this->commands([...])` array inside the `boot()` method's `if ($this->app->runningInConsole())` block. Place near `BackfillCategoryFromWooCommand::class` (line ~743) with a one-line comment:
  ```
  // Quick task 260611-f1y — products:push-visibility-to-woo. Pushes
  // catalog_visibility=hidden for is_internal_only products. Operator-
  // triggered (toggle on ProductResource OR direct CLI). NOT scheduled.
  PushVisibilityToWooCommand::class,
  ```

Run `php artisan list | findstr push-visibility-to-woo` (PowerShell) — should resolve.

Atomic commit: `feat(products): products:push-visibility-to-woo command + registration (260611-f1y)`
  </action>
  <verify>
    <automated>php artisan list 2&gt;&amp;1 | findstr push-visibility-to-woo &amp;&amp; php artisan products:push-visibility-to-woo --dry-run --env=testing</automated>
  </verify>
  <done>
- `PushVisibilityToWooCommand.php` exists, extends `BaseCommand`, signature + description set.
- Docblock includes drift-prevention "see also: WooFieldComparator catalog_visibility NOT compared" comment.
- `perform()` implements pre-GET → PUT flow with counters scanned/hidden/already_hidden/errors/no_woo_product_id.
- `--dry-run` produces zero Woo I/O.
- 200ms usleep between live Woo calls.
- Registered in `AppServiceProvider::boot()` commands array.
- `php artisan list` resolves the command name (grep guard).
- Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Filament toggle on ProductResource Details tab</name>
  <files>app/Domain/Products/Filament/Resources/ProductResource.php</files>
  <action>
**Decision (planner-fixed):** Add a `Toggle::make('is_internal_only')` to the existing Details Tab schema on `ProductResource::form()` rather than a custom header action or a ToggleColumn. Rationale:
- The Details tab already has `Toggle::make('exclude_from_auto_update')` (line 111). Adding `is_internal_only` adjacent keeps related operator flags grouped + uses the existing edit-form save flow (no separate confirmation modal needed for a per-record edit page).
- Filament's EditProduct save will persist the column via `$fillable` (Task 1). We do NOT need a custom `mutateFormDataBeforeSave` — Eloquent handles it.
- The Woo push happens in a `afterStateUpdated` callback wired to the EditProduct's `afterSave()` lifecycle hook (cleaner — fires only after the local row commits).

Edit `app/Domain/Products/Filament/Resources/ProductResource.php`:

1. **Toggle in form schema** — inside the `Tabs\Tab::make('Details')->schema([...])` array, add immediately after `Toggle::make('exclude_from_auto_update')` (line 111):

```php
Toggle::make('is_internal_only')
    -&gt;label('Internal-only (hide from storefront)')
    -&gt;helperText('When enabled, the next save pushes catalog_visibility=hidden to Woo. Product remains orderable via direct URL + custom-quote attach.')
    -&gt;visible(fn () =&gt; auth()-&gt;user()?-&gt;hasAnyRole(['admin', 'pricing_manager']) ?? false),
```

2. **Wire the push on save** — locate `app/Domain/Products/Filament/Resources/ProductResource/Pages/EditProduct.php`. If it has an `afterSave()` hook, extend it; otherwise add one. The hook compares the saved `is_internal_only` against the pre-save state and pushes ONLY when the value actually changed:

```php
// EditProduct::afterSave()
protected function afterSave(): void
{
    // Existing Field Pins save (if present) stays untouched.
    // ... existing code ...

    // 260611-f1y — push catalog_visibility when is_internal_only changes.
    $product = $this-&gt;record;
    $originalInternal = (bool) ($product-&gt;getOriginal('is_internal_only') ?? false);
    $currentInternal = (bool) ($product-&gt;is_internal_only ?? false);

    if ($originalInternal !== $currentInternal &amp;&amp; $product-&gt;woo_product_id !== null) {
        // Use sku when present; fall back to woo_product_id (internals often have empty SKU).
        $token = $product-&gt;sku !== null &amp;&amp; $product-&gt;sku !== ''
            ? $product-&gt;sku
            : (string) $product-&gt;woo_product_id;

        try {
            \Illuminate\Support\Facades\Artisan::call('products:push-visibility-to-woo', [
                '--skus' =&gt; $token,
            ]);
            \Filament\Notifications\Notification::make()
                -&gt;title($currentInternal ? 'Pushed catalog_visibility=hidden to Woo' : 'is_internal_only toggled OFF (Woo visibility NOT changed by command default — operator should manually re-enable visibility)')
                -&gt;success()
                -&gt;send();
        } catch (\Throwable $e) {
            \Filament\Notifications\Notification::make()
                -&gt;title('Woo push failed: '.$e-&gt;getMessage())
                -&gt;danger()
                -&gt;send();
        }
    }
}
```

**Important caveat for the toggle-OFF path:** The push command sets `catalog_visibility=hidden` ONLY. Toggling OFF (`is_internal_only` true → false) does NOT automatically re-set `catalog_visibility=visible` — the operator must manually restore visibility on Woo, OR (preferred follow-up, NOT in this task) extend the command with a `--visibility=visible|hidden` flag. The notification text above explicitly tells the operator this. Document in PLAN that the "un-hide" workflow is a separate concern — out of scope here.

If the existing `EditProduct.php` already implements `afterSave()` for Field Pins, INSERT the 260611-f1y block at the end of that method (do not replace).

DO NOT add `Toggle::make('is_internal_only')` to the Filament Table view as a ToggleColumn — table-level toggles fire on row render which adds Woo I/O risk; keep it on the edit form only.

Atomic commit: `feat(filament): is_internal_only toggle on Products edit form (260611-f1y)`
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo file_get_contents('app/Domain/Products/Filament/Resources/ProductResource.php');" 2&gt;&amp;1 | findstr is_internal_only</automated>
  </verify>
  <done>
- `Toggle::make('is_internal_only')` added to Details tab with helper text + admin/pricing_manager visibility.
- `EditProduct::afterSave()` extended (or created) with the original-vs-current diff check + Artisan::call to `products:push-visibility-to-woo --skus={token}`.
- Notification surfaces success on push + danger on exception.
- Toggle-OFF caveat documented in the success notification text.
- ToggleColumn explicitly NOT added (no table-render Woo I/O).
- Atomic commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Pest cases A-F — PushVisibilityToWooCommandTest</name>
  <files>tests/Feature/Console/PushVisibilityToWooCommandTest.php</files>
  <behavior>
    - Case A — Dry-run with 3 is_internal_only=true products → 0 Woo GETs + 0 PUTs; output shows `would_hide` for each; no local DB changes; exit 0.
    - Case B — Live with 1 internal product where Woo currently has `catalog_visibility=visible` → 1 GET + 1 PUT; PUT body asserts `['catalog_visibility' =&gt; 'hidden']`; counter `hidden=1`; exit 0.
    - Case C — Internal product with `woo_product_id=NULL` → no Woo call; `no_woo_product_id=1`; exit 0.
    - Case D — `--skus=43-LX-WALL-MOUNT` targets a product that is NOT is_internal_only=true → still gets pushed (operator force-push); 1 GET + 1 PUT; exit 0.
    - Case E — Woo PUT throws (simulate 5xx) → `errors=1`, continues with next candidate, exit 0.
    - Case F — Woo GET returns `catalog_visibility=hidden` → `already_hidden=1`; NO PUT call recorded on the stub; exit 0.
  </behavior>
  <action>
Create `tests/Feature/Console/PushVisibilityToWooCommandTest.php`. Mirror the BackfillCategoryFromWooCommandTest stub pattern precisely:

1. Top-of-file docblock describing the 6 cases A-F (mirror the BackfillCategoryFromWooCommandTest top comment).
2. Define two helper functions at the bottom of the file (or in a shared `tests/Helpers/woo_stubs.php` if such file exists — check first; otherwise inline):
   ```
   function bindVisibilityStub(array $getResponses, array &$putCalls = null): object { ... }
   function bindVisibilityStubThrowingPut(Throwable $e): object { ... }
   ```
   The stub is an anonymous-subclass `WooClient` with:
   - `public array $getCalls = []; public array $putCalls = [];`
   - `public array $getResponses;` keyed by woo_id → fixture response
   - Override `get(string $endpoint, array $query = []): array` — push to `$getCalls`, parse the woo_id from `$endpoint` (e.g., `"products/167493"` → `167493`), return `$this->getResponses[$wooId] ?? []`.
   - Override `put(string $endpoint, array $payload): array` — push `['endpoint' =&gt; ..., 'payload' =&gt; ...]` to `$putCalls`, return `['ok' =&gt; true]`.
   - Skip parent constructor (no logger/resolver dependency).
3. Bind via `app()->instance(WooClient::class, $stub)` inside each test.

**Case A** (dry-run):
- `Product::factory()->create(['sku' =&gt; 'A-001', 'woo_product_id' =&gt; 167493, 'is_internal_only' =&gt; true, 'status' =&gt; 'publish'])` × 3 with different woo_ids.
- `$stub = bindVisibilityStub([])` (no GET responses needed — dry-run skips Woo I/O).
- `Artisan::call('products:push-visibility-to-woo', ['--dry-run' =&gt; true])`.
- Assert: `$stub->getCalls === []`, `$stub->putCalls === []`, output contains `would_hide`, exit 0.

**Case B** (live happy path):
- Single is_internal_only=true product, woo_product_id=999.
- `bindVisibilityStub([999 =&gt; ['id' =&gt; 999, 'catalog_visibility' =&gt; 'visible']])`.
- `Artisan::call('products:push-visibility-to-woo', [])`.
- Assert: `$stub->getCalls[0]['endpoint'] === 'products/999'`, `$stub->putCalls[0]['endpoint'] === 'products/999'`, `$stub->putCalls[0]['payload'] === ['catalog_visibility' =&gt; 'hidden']`, output contains `hidden`, exit 0.

**Case C** (no woo_product_id):
- Internal product with `woo_product_id =&gt; null`.
- `bindVisibilityStub([])`.
- Assert: `count($stub-&gt;getCalls) === 0`, output contains `no_woo_product_id`, exit 0.

**Case D** (force-push via --skus):
- Product with `sku =&gt; '43-LX-WALL-MOUNT'`, `woo_product_id =&gt; 8502`, `is_internal_only =&gt; false` (NOT flagged).
- `bindVisibilityStub([8502 =&gt; ['id' =&gt; 8502, 'catalog_visibility' =&gt; 'visible']])`.
- `Artisan::call('products:push-visibility-to-woo', ['--skus' =&gt; '43-LX-WALL-MOUNT'])`.
- Assert: 1 GET + 1 PUT, payload `['catalog_visibility' =&gt; 'hidden']`, exit 0.

**Case E** (PUT throws):
- Two internal products woo_product_id 5001 + 5002.
- Need a stub whose `put()` throws on woo_id=5001 but succeeds on 5002 — extend `bindVisibilityStub` to accept a `$throwingWooIds` array OR define a 3rd helper `bindVisibilityStubThrowingPutFor(int $wooId, Throwable $e)`.
- `bindVisibilityStub([5001 =&gt; ['id' =&gt; 5001, 'catalog_visibility' =&gt; 'visible'], 5002 =&gt; ['id' =&gt; 5002, 'catalog_visibility' =&gt; 'visible']])` + configure throw on 5001.
- Assert: output contains `errors`, `hidden=1` (5002 succeeded), exit 0 (NOT failure).

**Case F** (already hidden — pre-GET short-circuits):
- Internal product woo_product_id=6001 + Woo GET returns `catalog_visibility =&gt; 'hidden'`.
- `bindVisibilityStub([6001 =&gt; ['id' =&gt; 6001, 'catalog_visibility' =&gt; 'hidden']])`.
- Assert: `count($stub-&gt;getCalls) === 1`, `count($stub-&gt;putCalls) === 0`, output contains `already_hidden`, exit 0.

Run with: `vendor/bin/pest tests/Feature/Console/PushVisibilityToWooCommandTest.php`.

Atomic commit: `test(products): push-visibility-to-woo Pest cases A-F (260611-f1y)`
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/Console/PushVisibilityToWooCommandTest.php --no-coverage</automated>
  </verify>
  <done>
- Test file exists with 6 Pest `it(...)` blocks A-F.
- Helper stubs defined (or shared file extended) with public `$getCalls` + `$putCalls`.
- All 6 cases GREEN.
- Case B asserts PUT body shape `['catalog_visibility' =&gt; 'hidden']`.
- Case E asserts exit 0 despite errors (per-candidate failures non-fatal).
- Case F asserts ZERO PUTs when already hidden (idempotency proof).
- Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify — regression suite + grep guard + migration applies cleanly</name>
  <files>(verify only — no source changes)</files>
  <action>
1. **Command resolves:** `php artisan list 2&gt;&amp;1 | findstr push-visibility-to-woo` must show the command line. (PowerShell: `findstr`; if you switch to bash: `grep push-visibility-to-woo`.)

2. **Focused Pest:** `vendor/bin/pest tests/Feature/Console/PushVisibilityToWooCommandTest.php --no-coverage` → 6/6 GREEN.

3. **Regression Pest (touched/adjacent surfaces):**
   - `vendor/bin/pest tests/Feature/Console/BackfillCategoryFromWooCommandTest.php --no-coverage` → 6/6 GREEN (pattern parent — should not regress).
   - `vendor/bin/pest tests/Feature/Console/ResyncProductsToWooCommandTest.php --no-coverage` (if exists) → GREEN.
   - `vendor/bin/pest tests/Feature/Cutover/DivergenceScanCommandTest.php --no-coverage` → GREEN (260610-qc4 baseline preserved).
   - `vendor/bin/pest tests/Unit/Cutover/WooFieldComparatorTest.php --no-coverage` (or wherever it lives) → GREEN.

4. **Full Pest suite:** `vendor/bin/pest --no-coverage` → 260610-qc4 baseline 1,989 / 222 / 3. Required delta: **+N pass / 0 new fails** where N ≥ 6 (the 6 new cases). If any pre-existing test now fails, root-cause before commit.

5. **Migration applies cleanly:** `php artisan migrate:fresh --env=testing` → both migrations run without error. Probe the test DB:
   ```
   php artisan tinker --execute="echo Illuminate\Support\Facades\Schema::hasColumn('products', 'is_internal_only') ? 'COL_OK' : 'COL_MISSING'; echo PHP_EOL; echo collect(DB::select('SHOW INDEX FROM products WHERE Key_name = ?', ['idx_products_is_internal_only']))-&gt;isNotEmpty() ? 'IDX_OK' : 'IDX_MISSING';"
   ```

6. **Grep guard scan:** `findstr /S "catalog_visibility" app\Console\Commands\PushVisibilityToWooCommand.php` → match present. `findstr /S "catalog_visibility" app\Domain\Cutover\Services\WooFieldComparator.php` → should be ABSENT (confirms drift-prevention docblock claim that the comparator does NOT compare catalog_visibility; if it IS present, the 260611-f1y assumption is broken and the docblock + threat model need updating before deploy).

7. **NO smoke probe against live Woo.** The post-deploy operator action is documented in the SUMMARY: `php artisan products:push-visibility-to-woo --dry-run` then `--live` on prod, then spot-check meetingstore.co.uk/shop for the 3 SKUs disappearing from public discovery.

NO COMMIT on this task — verification only.
  </action>
  <verify>
    <automated>vendor/bin/pest --no-coverage</automated>
  </verify>
  <done>
- `php artisan list` resolves `products:push-visibility-to-woo`.
- Focused Pest 6/6 GREEN.
- Regression Pest (BackfillCategory, Resync, Divergence, WooFieldComparator) GREEN.
- Full Pest delta vs 260610-qc4 baseline: +N pass / 0 new fails (N ≥ 6).
- `migrate:fresh` clean; column + index probed present.
- `catalog_visibility` absent from WooFieldComparator (drift-prevention docblock claim verified).
- Operator post-deploy workflow documented in SUMMARY (NOT in this task).
- NO commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator UI → Woo REST | Filament toggle → Artisan::call → WooClient::put writes to meetingstore.co.uk |
| MS DB → migration | Seed migration whereIn on woo_product_id; SQL injection surface = zero (hardcoded ints) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f1y-01 | Tampering | Seed migration `whereIn([167493, 167492, 165038])` could be edited to flag arbitrary products | accept | Migration is in git; review on commit. The 3 woo_ids are documented in CONTEXT + STATE.md cross-reference. Low risk — toggling visibility is reversible via Woo admin. |
| T-f1y-02 | Elevation of Privilege | Filament toggle visible to non-admin role would let any user hide arbitrary products | mitigate | `Toggle::make('is_internal_only')->visible(fn () =&gt; auth()->user()?->hasAnyRole(['admin', 'pricing_manager']) ?? false)` per Task 4. ProductPolicy `update()` already enforces role-gating at the model layer; toggle visibility is defence-in-depth. |
| T-f1y-03 | Denial of Service | Operator clicking the toggle on every product in the table (e.g., via ToggleColumn) would fan out 5,000+ Woo writes | mitigate | Task 4 explicitly REJECTS ToggleColumn; toggle lives on the edit form (single-record, deliberate action). 200ms usleep + per-row pre-GET also bound the rate. |
| T-f1y-04 | Information Disclosure | catalog_visibility=hidden does NOT prevent direct-URL access — products remain accessible to anyone with the slug | accept | This is INTENDED behaviour — the team needs to attach internals to custom quotes via direct URL. Documented in SUMMARY as a deliberate property of catalog_visibility=hidden. |
| T-f1y-05 | Repudiation | No activity_log entry for is_internal_only toggle | accept | Task 1 explicitly skips adding `is_internal_only` to `getActivitylogOptions()->logOnly()`. Operator UX flag, not auditable. Reconsider if compliance asks for a trail. |
| T-f1y-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this task — uses existing WooClient + Filament + Pest. |
</threat_model>

<verification>
**Pre-deploy (local + CI):**
- 6 new Pest cases GREEN.
- Full Pest delta vs 260610-qc4 baseline (1,989 / 222 / 3): +N pass (N ≥ 6), 0 new fails.
- `php artisan list | findstr push-visibility-to-woo` resolves.
- `migrate:fresh` clean.
- WooFieldComparator does NOT mention catalog_visibility (drift-prevention).

**Post-deploy (operator on prod):**
- Run `php artisan products:push-visibility-to-woo --dry-run` → output lists 3 candidates (Credit/Offer/Quote Payment).
- Run `php artisan products:push-visibility-to-woo` (live) → expect `hidden=3 already_hidden=0 errors=0`.
- Open meetingstore.co.uk/shop and meetingstore.co.uk/?s=Credit → the 3 internals must NOT appear.
- Open meetingstore.co.uk/product/credit/ (direct URL) → still loads, addable to cart (orderable preserved).
- Re-run `php artisan products:push-visibility-to-woo` → expect `hidden=0 already_hidden=3 errors=0` (idempotency proof).
</verification>

<success_criteria>
- 3 internal products (woo 167493/167492/165038) are catalog_visibility=hidden on meetingstore.co.uk.
- All 3 remain orderable via direct URL.
- `is_internal_only` column + index exist on `products`.
- `products:push-visibility-to-woo` artisan command exists, idempotent, registered in AppServiceProvider.
- ProductResource Details tab has the `is_internal_only` toggle; flipping it pushes to Woo with Filament notification.
- 6 Pest cases A-F GREEN.
- Full Pest suite delta vs baseline: +N pass / 0 new fails.
- 5 atomic commits landed (Tasks 1–5) + 1 verify task (no commit).
- SUMMARY documents the post-deploy operator workflow + the catalog_visibility-not-in-comparator drift-prevention contract + the toggle-OFF caveat (does NOT auto-restore visibility on Woo).
</success_criteria>

<output>
Create `.planning/quick/260611-f1y-hide-internal-use-products-credit-offer-/260611-f1y-SUMMARY.md` when done.

SUMMARY must include:
- Headline: 3 internals hidden + permanent mechanism shipped.
- Files changed list (migrations, command, model, AppServiceProvider, ProductResource, test).
- Commit hashes (5 atomic).
- Pest delta vs baseline.
- **POST-DEPLOY OPERATOR ACTION:** the exact dry-run → live → spot-check sequence above.
- **Drift-prevention contract:** WooFieldComparator does NOT compare catalog_visibility. If a future quick task adds catalog_visibility to the comparator's field list, the seeded 3 SKUs will appear as sync_diffs unless the comparator is taught to treat is_internal_only=true rows as expected-hidden.
- **Toggle-OFF caveat:** toggling `is_internal_only` OFF does NOT auto-restore `catalog_visibility=visible` on Woo. Follow-up: extend the command with `--visibility=visible|hidden` flag (queued, NOT in this task).
- **Future internals:** flip the Filament toggle on the Product edit page. The afterSave hook calls the artisan command synchronously for that single SKU/woo_id.
</output>
# 260611-s2d Execution Log

## Task 1 — Probe + scope confirmation (no commit)

All scope_confirmation frontmatter assertions verified TRUE:

1. **WooWebhookController** (`app/Domain/Webhooks/Http/Controllers/WooWebhookController.php`) — grep for `Product::|->save\(|stock_quantity|buy_price` returned ZERO matches. Webhook echo-loop boundary is dormant.
2. **SupplierDbSyncCommand** lines 212 + 250 use `Product::where('id', $local->id)->update([...])` — mass-update (NO Eloquent events fire). Safe re: new observer.
3. **HydrateProductStockFromOffersCommand** line 252 uses `Product::where('id', $product->id)->update($updates)` — mass-update. Safe.
4. **WooImportProductsCommand** line 173 uses `Product::updateOrCreate(...)` — DOES fire model events. This is the ONE bulk path Task 5 will wrap in `Product::withoutEvents()`.
5. **config/horizon.php** lines 200-202: queue is `sync-woo-push` (NOT `woo-writes`). Confirmed.
6. **EventServiceProvider** exists at `app/Providers/EventServiceProvider.php` with `$listen` array; will add ProductFieldsChangedEvent entry alongside ProductPriceChanged.
7. **Domain Events directory** confirmed: `app/Domain/Pricing/Events/ProductPriceChanged.php` exists. New event will live at `app/Domain/Products/Events/ProductFieldsChangedEvent.php`.

**Additional discoveries:**
- `Product` is already imported in `AppServiceProvider.php` (line 113) — no new use needed for the model itself.
- DomainEvent base class (`app/Foundation/Events/DomainEvent.php`) auto-populates correlationId via Context. ProductFieldsChangedEvent will extend DomainEvent for consistency with the rest of the codebase (`ProductPriceChanged` does too). This means the listener can read `$event->correlationId` (NOT `$event->correlationId` constructor arg per plan — DomainEvent provides it).
- Plan specifies `?string $correlationId` as a constructor arg. To align with existing `ProductPriceChanged` (which extends DomainEvent), the new event will extend DomainEvent too — `correlationId` comes from the base class, NOT the constructor. This is a deviation from the plan's constructor signature but matches the existing codebase convention and the plan's drift-prevention philosophy.

No probe disagreements. Proceeding to Task 2.

## Task 2 — Extract WooProductWriter service (commit a6cd425)

- Created `app/Domain/Sync/Services/WooProductWriter.php` (final class, constructor DI on WooClient).
- Imports `WooFieldComparator::BUY_PRICE_META_KEY` (zero literal-string matches in source code).
- Public method: `putProductFields(Product, array $fields, ?string $correlationId = null): array` returning `{status, fields_pushed, http_status, reason}`.
- Refactored `app/Console/Commands/PushDivergenceToWooCommand.php`:
  - Constructor now takes `WooProductWriter` (removed direct `WooClient` injection).
  - Deleted private helpers: `buildCategoriesPayload`, `mergeBuyPriceMeta`, `looksLike404` (moved to writer).
  - Live path delegates to `$this->writer->putProductFields(...)`.
  - Dry-run path calls writer with empty $fields[] for pre-GET-only behaviour (preserves Case H: 5 GETs, 0 PUTs).
- Test stub binds `WooClient::class` via `app()->instance` — Laravel resolves WooProductWriter → constructor wants WooClient → gets stub. Refactor preserves observability.
- Verification: PushDivergenceToWooCommandTest 10/10 GREEN (87 assertions, 8.79s).

## Task 3 — ProductFieldsChangedEvent + ProductObserver + flag (commit aa8f59e)

- Created `app/Domain/Products/Events/ProductFieldsChangedEvent.php` extending `DomainEvent` (codebase convention — matches ProductPriceChanged; correlationId auto-populated from Context facade, NOT a constructor arg). Constructor: `(int $productId, ?string $sku, array $changedFields)`.
- Created `app/Domain/Products/Observers/ProductObserver.php` with `updated(Product)` method, private const `TRACKED_FIELDS = [stock_quantity, buy_price, sell_price, category_id]`, flag short-circuit via `config('cutover.event_driven_push_enabled', false) !== true`, intersect against `getChanges()` keys, dispatch only on non-empty intersect.
- Updated `app/Providers/AppServiceProvider.php` — added `use ProductObserver;` import; registered `Product::observe(ProductObserver::class)` after the QuoteLine::observe block.
- Updated `config/cutover.php` — added `event_driven_push_enabled => env('CUTOVER_EVENT_DRIVEN_PUSH_ENABLED', false)` at end of return array.
- Verification: `php artisan config:clear` + tinker-probe shows `bool(false)`. Flag defaults FALSE. ✓

## Task 4 — PushProductFieldsToWoo listener + EventServiceProvider wiring (commit d4ddad1)

- Created `app/Domain/Products/Listeners/PushProductFieldsToWoo.php` implementing ShouldQueue, `public string $queue = 'sync-woo-push'`, `public int $tries = 4`. Constructor DI on `WooProductWriter`. Auditor resolved via `app(Auditor::class)`.
- Handle flow: `Product::find($event->productId)` → null returns silently (no audit) → writer.putProductFields → record audit `events.product_pushed` with `result`, `fields_pushed`, `http_status`, `reason` → on `woo_not_found` return (no throw) → on `error` throw RuntimeException (Horizon retry).
- Updated `app/Providers/EventServiceProvider.php` — added 2 use statements, added `ProductFieldsChangedEvent::class => [PushProductFieldsToWoo::class]` entry after the `ProductPriceChanged::class` block.
- Verification: `php artisan event:list | grep ProductFieldsChangedEvent` shows `App\Domain\Products\Listeners\PushProductFieldsToWoo (ShouldQueue)`. ✓

## Task 5 — Bulk-operation protection (commit 85758ec)

- `app/Domain/Sync/Commands/WooImportProductsCommand.php` line 173 area: wrapped `Product::updateOrCreate([...], $payload)` inside `Product::withoutEvents(static fn () => ...)` with explanatory comment about the 5,000-SKU echo-loop risk.
- Annotated `Product::whereIn(...)->get()` read-only chain at line ~228 as `260611-s2d-safe` (no Eloquent events from `get()`; ProductPriceSnapshot::updateOrCreate that follows targets a DIFFERENT model so the observer never sees it).
- Confirmed `GenerateProductDraftsCommand` line 212's `$existing->forceFill($contentValues)->save()` writes only NON-tracked fields (meta_description / attributes_json / tags / ean) so the observer's intersect is empty — no dispatch, no wrap needed.
- Confirmed `GenerateProductDraftsCommand` line 223's `Product::create()` triggers `created` (NOT `updated`) — observer only subscribes to `updated`, so safe.

## Task 6 — 10 Pest cases A-J (commit cc8ab80)

- Created `tests/Feature/Domain/Products/ProductObserverTest.php` — 4 cases A-D (flag-off, single-field, multi-field, non-tracked). Rule-1 fold: case C originally had `sort($event->changedFields)` which threw `Cannot indirectly modify readonly property`; fixed by copying to a local array before sorting.
- Created `tests/Feature/Domain/Products/Listeners/PushProductFieldsToWooTest.php` — 6 cases E-J. Rule-3 fold: removed `final` modifier from `WooProductWriter` so anonymous-subclass stubs can extend it (mirrors the WooClient pattern in PushDivergenceToWooCommandTest). Auditor is `final` and can't be mocked — used Spatie `Activity::query()` post-call assertion instead (mirrors AutoSyncDivergenceCommandTest + MarginChangeApplierTest patterns).
- Verification: 10/10 GREEN.

## Task 7 — Verify (no commit)

- `php artisan event:list | grep ProductFieldsChanged` — listener wired. ✓
- Focused + regression Pest suites: **53 passed (284 assertions, 27.62s).**
  - ProductObserverTest 4 ✓
  - PushProductFieldsToWooTest 6 ✓
  - PushDivergenceToWooCommandTest 10 ✓
  - PushVisibilityToWooCommandTest 6 ✓
  - HydrateProductStockFromOffersCommandTest 10 ✓
  - AutoSyncDivergenceCommandTest 8 ✓
  - DivergenceScanCommandTest 9 ✓
- Grep gates:
  - `_alg_wc_cog_cost` in WooProductWriter — 3 matches, ALL in docblock comments (lines 29, 167, 171). Zero code literals. Constant import in code only. ✓
  - `woo-writes` in `app/Domain/Products/Listeners/` — 1 match, in a docblock note explaining the planner correction. Zero code literals (queue pinned to `sync-woo-push`). ✓
  - `Product::observe` in AppServiceProvider — 1 match (line 625, the new registration). ✓
- Probe disagreement vs original brief recorded:
  - Brief said queue=`'woo-writes'`. Actual queue is `sync-woo-push` (per planner correction from horizon.php probe). Listener uses `sync-woo-push`.
  - Bulk-path scope reduced to ONLY `WooImportProductsCommand::updateOrCreate` (Task 1 probe confirmed SupplierDbSync + Hydrate use mass-update which fires no events).





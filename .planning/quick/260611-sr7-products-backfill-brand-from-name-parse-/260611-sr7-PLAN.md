---
phase: quick-260611-sr7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Console/Commands/BackfillProductBrandFromNameCommand.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php
  - .planning/STATE.md
autonomous: true
requirements:
  - M-1
  - 260611-sr7
must_haves:
  truths:
    - "Operator can run `php artisan products:backfill-brand-from-name --dry-run` and see a plan table with counters + top-30 unresolved candidates for the ~3,231 NULL-brand live products."
    - "Live run resolves auto-create-named products (`Logitech ...`, `Sony ...`, `HP ...`) by passing the first name word through TaxonomyResolver."
    - "When the first word is SKU-shaped (uppercase + digits + hyphen/#, length > 5), the SECOND word is tried (handles `AV1E3AA#AC3 Poly collaboration device`)."
    - "TaxonomyResolver fuzzy match below its internal threshold leaves brand_id NULL and increments the `unresolved` counter; the candidate appears in the top-30 unresolved list."
    - "Products that already have brand_id are EXCLUDED by the candidate query — the run is a pure backfill, not a re-resolver."
    - "TaxonomyResolver exceptions are swallowed per-product (errors++), the batch continues."
    - "No Woo writes — silent-skip via WooFieldComparator covers brand_id, so this is pure MS-side data quality."
  artifacts:
    - path: "app/Console/Commands/BackfillProductBrandFromNameCommand.php"
      provides: "products:backfill-brand-from-name artisan command"
      contains: "SKU_LIKE_PATTERN"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "command registration inside the runningInConsole commands() array"
      contains: "BackfillProductBrandFromNameCommand::class"
    - path: "tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php"
      provides: "8 Pest cases A-H proving the engine behaviour"
      contains: "Case A"
  key_links:
    - from: "BackfillProductBrandFromNameCommand"
      to: "TaxonomyResolver"
      via: "constructor DI"
      pattern: "private readonly TaxonomyResolver"
    - from: "BackfillProductBrandFromNameCommand::perform"
      to: "Product::query()->where('status','publish')->whereNull('brand_id')"
      via: "Eloquent cursor"
      pattern: "whereNull\\('brand_id'\\)"
    - from: "AppServiceProvider::boot"
      to: "BackfillProductBrandFromNameCommand"
      via: "this->commands([...])"
      pattern: "BackfillProductBrandFromNameCommand::class"
---

<objective>
Close the M-1 Phase 7 gap row: 3,231 of 3,922 live products (82.4%) have NULL `brand_id`. Legacy WC import never carried brand assignment. Auto-create products (260606-q3o) follow `{Brand} {category words} {SKU}` naming, so the first word of `Product.name` is reliably the brand. Edge case: some products lead with the SKU itself (`AV1E3AA#AC3 Poly collaboration device`) — detect SKU-shaped first tokens and try the SECOND word.

Purpose: lift downstream signal quality — AdCandidateScanner ad-target filtering, /admin/category-audit BRAND_NATURAL_HOMES rule comparison, product detail "Brand:" link, Google Merchant Center feed enrichment.

Output: `products:backfill-brand-from-name` artisan command + 8 Pest cases + AppServiceProvider registration. 3 atomic commits (1 feat + 1 test + 1 STATE), 2 non-committing tasks (probe + verify).

Drift-prevention contract: TaxonomyResolver is the SINGLE source for brand fuzzy matching (consumed today by `BackfillMerchantFeedCommand::backfillBrand`). This command IMPORTS TaxonomyResolver via DI; under NO circumstances does it re-implement name -> brand_id resolution.

Scope guardrails baked in:
- NO Woo writes (WooFieldComparator silent-skips brand_id meta — both sides null, no parity diff).
- NO scheduled cron — one-shot operator-triggered.
- NO modifications to BackfillMerchantFeedCommand / BackfillCategoryFromWooCommand / HydrateProductStockFromOffersCommand.
- NO changes to TaxonomyResolver's existing public surface (resolveBrand stays single-arg).

Brief correction folded in (must be honoured by Task 2): the brief's `resolveBrand($candidate, $minConfidence)` signature does NOT exist. TaxonomyResolver::resolveBrand is single-arg; FUZZY_THRESHOLD is a private const (0.85). The `--min-confidence` CLI option is informational + surfaced in the run banner so the operator UX matches the brief, but no threshold override is plumbed through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php
@app/Console/Commands/BackfillMerchantFeedCommand.php
@app/Console/Commands/HydrateProductStockFromOffersCommand.php
@app/Domain/Products/Models/Product.php
@app/Providers/AppServiceProvider.php
@tests/Feature/Console/HydrateProductStockFromOffersCommandTest.php

<interfaces>
Key contracts extracted from the codebase. Executor uses these directly — no exploration needed.

From `app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php`:

```
// SINGLE-ARG signature — the brief's `resolveBrand($candidate, $minConfidence)` is WRONG.
// Threshold is a private const FUZZY_THRESHOLD = 0.85, NOT a parameter.
// Returns ?int brand_id from Woo's native /products/brands taxonomy (or pa_brand fallback).
public function resolveBrand(?string $brandName): ?int;

// Helper used in BackfillMerchantFeedCommand for sample-display brand-name lookup.
// Returns array<int, array{id:int, name:string}>.
public function allBrands(): array;
```

From `app/Domain/Products/Models/Product.php`:

- `brand_id` is fillable, nullable, integer FK to Woo brand terms.
- No explicit cast on brand_id — Laravel's default integer coercion + nullable behaviour applies.

From `app/Console/Commands/BackfillMerchantFeedCommand.php` (THE pattern to mirror):

- Constructor DI: `public function __construct(private readonly IntegrationCredentialResolver $resolver, private readonly TaxonomyResolver $taxonomy, ...)`.
- Candidate query (lines 577-589): `Product::query()->where('status','publish')->where(fn($q) => $q->whereNull('brand_id')->orWhere('brand_id', 0))`. Then `whereIn('sku', $skusFilter)` when --skus is set. Then `limit($limit)` when --limit > 0.
- Brand resolve call (line 640): `$brandId = $this->taxonomy->resolveBrand($mfr);` — single arg.
- Live write (lines 690-697): `DB::table('products')->where('sku', $sku)->update(['brand_id' => $brandId, 'updated_at' => now()])`.
- Sample table via `$this->table([...], $sample)`.
- No per-product transaction wrapping in `backfillBrand` — keep parity (single-column update).

From `app/Console/Commands/HydrateProductStockFromOffersCommand.php` (BaseCommand shape):

- `extends BaseCommand`.
- `private const SOURCE_TABLE` style grep-discoverability constants at class top.
- `protected $signature = 'products:hydrate-stock-from-offers {--skus= : ...} {--limit=0 : ...} {--dry-run : ...}';`.
- `protected $description = '...';`.
- Constructor injects services with `private readonly`.
- `protected function perform(): int` — parse options, build candidate query, stream via `$q->cursor()`, render counter table, `return SymfonyCommand::SUCCESS;`.

From `tests/Feature/Console/HydrateProductStockFromOffersCommandTest.php` (Pest pattern):

- `Artisan::call('products:hydrate-stock-from-offers', ['--skus' => 'E-001'])` + `Artisan::output()` + `Product::factory()->create([...])` + `$product->refresh()`.
- Container substitution via `app()->instance(ServiceClass::class, $stub)` with anonymous-subclass stubs.
- TaxonomyResolver is non-final (see its docblock around line 30) so anonymous-subclass overrides work directly.

From `app/Providers/AppServiceProvider.php` (commands() registration site — around line 745):

- Registration goes INSIDE the existing `$this->commands([...])` array in `boot()`, behind the `if ($this->app->runningInConsole())` guard.
- Add the `use App\Console\Commands\BackfillProductBrandFromNameCommand;` import alphabetised with the existing `App\Console\Commands\*` block at the top of the file.
- Insert the FQCN adjacent to `BackfillMerchantFeedCommand::class` (line 753) so the M-1 brand-backfill family clusters together.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe + scope confirmation</name>
  <files>read-only — no writes</files>
  <action>
Confirm the FOUR facts the engine task depends on. Do NOT write code in this task. Output a 5-line confirmation block to the chat.

1. TaxonomyResolver::resolveBrand signature. Re-read `app/Domain/ProductAutoCreate/Services/TaxonomyResolver.php` lines 61-68. Confirm the public signature is `resolveBrand(?string $brandName): ?int` (single argument, no `$minConfidence`). Confirm FUZZY_THRESHOLD is a private const (line 57). The brief's `resolveBrand($candidate, $minConfidence)` is INCORRECT — the engine task uses the single-arg form. Document this correction explicitly.

2. Return type. Confirm the method returns `?int` (Woo brand_id), not a brand string. Confirm `Product.brand_id` is a fillable column on Product (Product.php line 36-37).

3. Pattern echo. Confirm `BackfillMerchantFeedCommand::backfillBrand` (lines 570-701) is the exact pattern to mirror: candidate query shape (whereNull OR =0); single-arg resolveBrand call (line 640); `DB::table('products')->where('sku',...)->update(['brand_id' => ..., 'updated_at' => now()])` live write (lines 690-693); sample table via `$this->table(...)`; NO per-product transaction wrapping in backfillBrand — keep parity (single-column update).

4. Registration site. Confirm AppServiceProvider lines 745-753 contains the `$this->commands([...])` array inside `if ($this->app->runningInConsole())`; Task 2 inserts the new FQCN in that array adjacent to `BackfillMerchantFeedCommand::class` (line 753).

NO commit. Output is a 5-line confirmation block:

```
Probe confirmations (260611-sr7 Task 1):
1. TaxonomyResolver::resolveBrand(?string $brandName): ?int — single-arg, threshold const=0.85
2. Returns ?int brand_id; Product.brand_id is fillable
3. Mirror BackfillMerchantFeedCommand::backfillBrand (lines 570-701) exactly
4. Registration: insert in $this->commands([...]) at AppServiceProvider line ~754
5. NO transactional wrapping per parity (single-column update)
```
  </action>
  <verify>
    <automated>Read TaxonomyResolver.php lines 61-68 and verify the single-arg signature; no test runs at this step.</automated>
  </verify>
  <done>5-line probe confirmation block printed; brief correction (single-arg resolveBrand) acknowledged. No file writes, no commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackfillProductBrandFromNameCommand engine + registration</name>
  <files>app/Console/Commands/BackfillProductBrandFromNameCommand.php, app/Providers/AppServiceProvider.php</files>
  <behavior>
- First word of `Product.name` is passed verbatim to `TaxonomyResolver::resolveBrand`; resolved brand_id is written to `products.brand_id`.
- When the first word matches the `SKU_LIKE_PATTERN` const AND has length > 5, the `skipped_sku_prefix` counter increments and the SECOND word is tried instead.
- TaxonomyResolver returning null -> `unresolved` counter increments; brand_id stays NULL; the candidate string appears in the captured top-30 unresolved sample.
- TaxonomyResolver throwing -> `errors` counter increments; per-product loop continues.
- Candidate query: `Product::query()->where('status','publish')->whereNull('brand_id')` OR `brand_id=0`. `--skus=` override replaces the NULL filter with `whereIn('sku', ...)`. `--limit=0` is unbounded.
- `--dry-run` prints the same counter table + top-30 unresolved sample with ZERO DB writes.
- `--min-confidence=0.85` option is accepted and surfaced in the run banner; the value is informational because TaxonomyResolver's internal const is the authority. Source code carries a comment line documenting this (`// informational — threshold owned by TaxonomyResolver::FUZZY_THRESHOLD`).
- End-of-run output: counter table (scanned / resolved / unresolved / skipped_sku_prefix / errors) + top-30 unresolved candidates table.
  </behavior>
  <action>
Create `app/Console/Commands/BackfillProductBrandFromNameCommand.php`.

Class shape:
- `namespace App\Console\Commands;`
- `class BackfillProductBrandFromNameCommand extends BaseCommand` (non-final, mirrors BackfillMerchantFeedCommand so future Pest stubbing has the same lever).
- Constructor: `public function __construct(private readonly TaxonomyResolver $taxonomy) { parent::__construct(); }`. Use statement: `use App\Domain\ProductAutoCreate\Services\TaxonomyResolver;`.
- Class-level private const `SKU_LIKE_PATTERN = '/^[A-Z0-9][A-Z0-9\-#]+$/i'` — grep-discoverability anchor for the SKU-shaped first-token detector. Plus const `MIN_SKU_LIKE_LENGTH = 6` (i.e. `strlen > 5` per brief). Comment block explains the prod sample (`AV1E3AA#AC3 Poly collaboration device`).
- `$signature` exactly: `products:backfill-brand-from-name {--skus= : Comma-separated SKU list; default = all NULL-brand live products} {--limit=0 : Cap product count (0=unbounded)} {--min-confidence=0.85 : Informational — TaxonomyResolver owns FUZZY_THRESHOLD const} {--dry-run : Print plan without writing}`.
- `$description = 'Backfill products.brand_id from name-first-word via TaxonomyResolver (260611-sr7 — closes M-1 Phase 7 gap). MS-side only — no Woo writes.'`.

Implement `protected function perform(): int`:

1. Parse options. Reject `min-confidence` < 0 or > 1 with `$this->error(...)` + return SymfonyCommand::FAILURE.
2. Build candidate query mirroring BackfillMerchantFeedCommand::backfillBrand (lines 577-590). When `--skus=` is empty, candidate set is `Product::query()->where('status','publish')->where(fn($q) => $q->whereNull('brand_id')->orWhere('brand_id', 0))`. When `--skus=` is set, candidate set is `Product::query()->whereIn('sku', $skuList)` instead of the NULL filter (operator override — explicit SKUs); `status='publish'` constraint still applied for parity with the merchant-feed pattern.
3. Pre-load brand-name map ONCE: `foreach ($this->taxonomy->allBrands() as $term) { $brandNameById[(int)$term['id']] = (string)$term['name']; }` — for sample-table display.
4. Stream candidates via `$query->cursor()`. For each product:
   - Tokenize via `preg_split('/\s+/', trim((string) $product->name))`. If empty/null, `unresolved++`, append empty-name candidate to histogram + sample, `continue`.
   - First-word check: if `preg_match(self::SKU_LIKE_PATTERN, $word0) === 1 && mb_strlen($word0) >= self::MIN_SKU_LIKE_LENGTH` -> `skippedSkuPrefix++` AND advance to `$word1 = $words[1] ?? null`. If `$word1` is null, `unresolved++`, capture `$word0` (SKU-like) in sample, `continue`. Otherwise `$candidate = $word1`.
   - Otherwise `$candidate = $word0`.
   - Resolution call inside try/catch: `try { $brandId = $this->taxonomy->resolveBrand($candidate); } catch (\Throwable $e) { $errors++; Log::warning('backfill_brand_from_name.row_failed', ['product_id' => $product->id, 'sku' => $product->sku, 'candidate' => $candidate, 'error' => $e->getMessage()]); continue; }`.
   - If `$brandId === null` -> `unresolved++`; append `$candidate` to `$unresolvedHistogram` as `$unresolvedHistogram[$candidate] = ($unresolvedHistogram[$candidate] ?? 0) + 1`. Append a sample row when sample count < 20.
   - If `$brandId !== null`:
     - On `--dry-run`: `resolved++`, capture sample row, continue without write.
     - Live: `$affected = DB::table('products')->where('id', $product->id)->update(['brand_id' => $brandId, 'updated_at' => now()]);` then `if ($affected > 0) { $resolved++; }` (mirrors BackfillMerchantFeedCommand lines 694-697).
5. End-of-run output:
   - Mode banner before the loop matching the 260611-qcq style: `[LIVE] products:backfill-brand-from-name — candidates=NN min-confidence=0.85` (or `[dry-run]` when --dry-run). Surface the operator's `--min-confidence` value verbatim in the banner.
   - Counter table via `$this->table(['Outcome','Count'], [['scanned', $scanned], ['resolved', $resolved], ['unresolved', $unresolved], ['skipped_sku_prefix', $skippedSkuPrefix], ['errors', $errors]])`.
   - When the unresolved histogram is non-empty: top-30 unresolved candidates via `$this->table(['Candidate','Hits'], $top30Rows)` sorted by hits DESC then by candidate ASC for stable ordering.
   - Sample-row table (top 20) when sample buffer is non-empty: columns `['SKU','First word','Resolved word','Brand name','brand_id','Outcome']` — mirror the BackfillMerchantFeedCommand::backfillBrand sample-table shape.

`return SymfonyCommand::SUCCESS;` at end of perform().

Edit `app/Providers/AppServiceProvider.php`:
- Add `use App\Console\Commands\BackfillProductBrandFromNameCommand;` alphabetised in the `App\Console\Commands\*` import block at the top of the file.
- Inside `$this->commands([...])`, insert `BackfillProductBrandFromNameCommand::class,` adjacent to `BackfillMerchantFeedCommand::class,` (line ~753) with a 3-line preceding comment block describing the quick-task ID (260611-sr7), the gap it closes (M-1), and the no-Woo-writes guarantee.

Atomic commit message: `feat(products): backfill-brand-from-name command + registration (260611-sr7)`
  </action>
  <verify>
    <automated>php artisan list 2>&1 | findstr backfill-brand-from-name</automated>
  </verify>
  <done>BackfillProductBrandFromNameCommand class exists with SKU_LIKE_PATTERN const; registered in AppServiceProvider commands array; `php artisan list` shows the command; commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pest cases A-H</name>
  <files>tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php</files>
  <behavior>
8 Pest `it(...)` cases covering every documented engine outcome. All assertions go through `Artisan::call` + `Artisan::output` + `$product->refresh()`. TaxonomyResolver is replaced via `app()->instance(TaxonomyResolver::class, $stub)` using an anonymous subclass (TaxonomyResolver is non-final per its docblock).
  </behavior>
  <action>
Create `tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php` modelled on `tests/Feature/Console/HydrateProductStockFromOffersCommandTest.php`. File header docblock summarises cases A-H.

Helper `bindStubTaxonomyResolver(array $brandIdByCandidate, ?string $throwTrigger = null): void` that binds an anonymous-subclass stub:
- Skip the parent constructor (no WooClient required).
- Override `resolveBrand(?string $brandName): ?int` to return `$brandIdByCandidate[$brandName] ?? null`. When `$throwTrigger !== null && $brandName === $throwTrigger`, throw a `RuntimeException`. Used by Case H only.
- Override `allBrands(): array` to return `[['id'=>1,'name'=>'Logitech'], ['id'=>2,'name'=>'Poly'], ['id'=>3,'name'=>'Intel'], ['id'=>4,'name'=>'Sony']]` so the engine's pre-load brand-name map populates without a live Woo call.

`beforeEach` calls `bindStubTaxonomyResolver(['Logitech'=>1, 'Poly'=>2, 'Intel'=>3, 'Sony'=>4])` so Cases A, B, D, E, F, G all use the same baseline without re-stubbing.

Cases:

- Case A — Happy path, common brand prefix. Product `(sku='960-001503', name='Logitech 4k conference camera 960-001503', brand_id=null)`. Run `products:backfill-brand-from-name`. Assert exit=0, `$product->fresh()->brand_id === 1`, `Artisan::output()` contains `resolved`.

- Case B — SKU-prefix detection + second-word resolution. Product `(sku='AV1E3AA#AC3', name='AV1E3AA#AC3 Poly collaboration device', brand_id=null)`. Run command. Assert `$product->fresh()->brand_id === 2` (Poly), output contains `skipped_sku_prefix` AND `resolved`.

- Case C — Unresolvable brand -> unresolved counter + top-30 capture. Re-stub with `bindStubTaxonomyResolver([])` so every candidate resolves to null. Product `(sku='XYZ-001', name='Unknown OEM XYZ-001', brand_id=null)`. Assert exit=0, `$product->fresh()->brand_id === null`, output contains the literal substring `Unknown` AND `unresolved`.

- Case D — `--skus` override scopes to explicit list. Two products: `(sku='ABC-1', name='Logitech foo', brand_id=null)` AND `(sku='DEF-2', name='Sony bar', brand_id=null)`. Run with `['--skus' => 'ABC-1']`. Assert `Product::where('sku','ABC-1')->first()->brand_id === 1`, `Product::where('sku','DEF-2')->first()->brand_id === null` (untouched).

- Case E — `--dry-run` writes nothing. Five products with names beginning `Logitech ...`. Run with `['--dry-run' => true]`. Loop refresh each — assert every `brand_id === null` (NO writes). Output contains literal `dry-run` AND a counter row labelled `resolved` with count 5.

- Case F — Product with brand_id ALREADY set is EXCLUDED by the candidate query. Two products: `(brand_id=99, name='Logitech foo')` and `(brand_id=null, name='Sony bar')`. Run default. Assert the brand_id=99 product UNCHANGED (still 99); the Sony product -> `brand_id === 4`.

- Case G — `--min-confidence=0.95` flag accepted + surfaced in banner. Single Logitech product. Run with `['--min-confidence' => '0.95']`. Assert exit=0, output contains the literal substring `min-confidence=0.95` (banner echoes the operator's option). Asserts the option doesn't break the engine — resolution still happens because the stub doesn't consult the threshold (TaxonomyResolver owns it).

- Case H — TaxonomyResolver throws -> errors counter increments; batch continues. Re-stub with `bindStubTaxonomyResolver(['Logitech'=>1, 'Sony'=>4], throwTrigger: 'Bomb')`. Three products: `(sku='H-1', name='Logitech foo')`, `(sku='H-2', name='Bomb bar')`, `(sku='H-3', name='Sony baz')`. Run default. Assert `H-1->brand_id === 1`, `H-2->brand_id === null` (errored), `H-3->brand_id === 4`. Output counter table contains `errors` row with count 1, `resolved` with count 2.

Architectural guardrail: include a final `it('does not import WooClient', ...)` test that asserts the engine source file does not contain the substring `App\\Domain\\Sync\\Services\\WooClient` (via `file_get_contents(__DIR__.'/../../../app/Console/Commands/BackfillProductBrandFromNameCommand.php')`). This mirrors the 260611-qcq no-Woo invariant without binding a throwing stub at the container level.

Atomic commit message: `test(products): backfill-brand-from-name Pest cases A-H (260611-sr7)`
  </action>
  <verify>
    <automated>vendor\bin\pest tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php --colors=never</automated>
  </verify>
  <done>8 Pest cases A-H plus the no-WooClient architectural guard all GREEN; file committed.</done>
</task>

<task type="auto">
  <name>Task 4: Verify (focused + regression + full suite delta)</name>
  <files>no writes — verification only</files>
  <action>
Run in sequence:

1. `php artisan list 2>&1 | findstr backfill-brand-from-name` — command resolves.
2. Focused: `vendor\bin\pest tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php --colors=never` — 8/8 GREEN (plus the architectural guard).
3. Regression on TaxonomyResolver consumers + touched-area tests, run in one Pest invocation: `vendor\bin\pest tests/Feature/Console/BackfillMerchantFeedCommandTest.php tests/Feature/Console/BackfillCategoryFromWooCommandTest.php tests/Feature/Console/HydrateProductStockFromOffersCommandTest.php --colors=never`. Plus the AdCandidateScanner suite (it consumes brand_id for filtering): locate the file via Grep (`AdCandidateScanner` in `tests/`) and run it. All GREEN required.
4. Full Pest baseline: `vendor\bin\pest --colors=never` — record pass/skip/fail vs the 260611-s2d baseline (2,033 / 222 / 3). Required: +N pass / 0 new fails / 0 new risky. Full-suite OOM caveat from 260611-qcq carries forward — if Windows herd PHP 512MB pre-existing infra issue trips OOM, fall back to "focused + touched-area equivalent" reconciliation and document the caveat in the chat summary.

NO commit on this task. Report counter delta in the chat summary.
  </action>
  <verify>
    <automated>vendor\bin\pest tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php tests/Feature/Console/BackfillMerchantFeedCommandTest.php tests/Feature/Console/BackfillCategoryFromWooCommandTest.php tests/Feature/Console/HydrateProductStockFromOffersCommandTest.php --colors=never</automated>
  </verify>
  <done>Focused + regression suites GREEN; full-suite delta documented (+N pass / 0 new fails, or OOM caveat acknowledged); no commit produced by this task.</done>
</task>

<task type="auto">
  <name>Task 5: STATE.md update + artifact commit</name>
  <files>.planning/STATE.md</files>
  <action>
Update `.planning/STATE.md` to record 260611-sr7 completion:

1. Frontmatter rotation: move the current `stopped_at` block (260611-s2d) down to `old_stopped_at` (top of the existing `old_stopped_at` cascade). Write a new `stopped_at` summarising 260611-sr7: closes M-1 Phase 7 gap (3,231 NULL brand_ids on live products), name-first-word heuristic via TaxonomyResolver (single-arg signature per probe correction), SKU-prefix detection via SKU_LIKE_PATTERN const, top-30 unresolved candidates output, 3 atomic commits (feat + test + state), Pest cases A-H counts, full-suite delta vs the 260611-s2d 2,033/222/3 baseline, post-deploy operator action sequence (deploy -> `--dry-run` -> live -> spot-check `Product::where('brand_id',null)->count()` drop), what this does NOT do (no Woo writes; no scheduled cron; no operator-override for the SKU_LIKE_PATTERN — tighten it in future quick tasks if false-positives surface).
2. Update `last_updated` + `last_activity` to today (2026-06-11).
3. Append a new row in the Performance Metrics quick-tasks table if one exists in this STATE.md — match the existing row shape (date / quick-id / tasks / outcome). If no such table, skip silently.

Atomic commit message: `docs(state): 260611-sr7 backfill-brand-from-name shipped (260611-sr7)`.

Per the GSD commit-files protocol, stage only `.planning/STATE.md` for THIS commit. Task 2 and Task 3 commits are separate (already landed by those tasks). No `git add -A`.
  </action>
  <verify>
    <automated>git log --oneline -3 | findstr 260611-sr7</automated>
  </verify>
  <done>STATE.md updated; state commit landed; `git log` shows three 260611-sr7 commits (feat + test + state).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI -> Artisan command | Operator-typed `--skus` / `--limit` / `--min-confidence` values cross into option parsing. |
| Product.name -> TaxonomyResolver | Free-text product names parsed for first-word brand candidate. |
| TaxonomyResolver -> Woo REST | Existing trust boundary owned by TaxonomyResolver; not extended by this task. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-sr7-01 | Tampering | DB::table('products')->update(...) live write | mitigate | Update is filtered by `where('id', $product->id)` (PK) AND scoped to single `brand_id` column. Candidate query excludes already-populated rows so a re-run is idempotent. |
| T-sr7-02 | Information disclosure | `Log::warning('backfill_brand_from_name.row_failed', ...)` | accept | Log payload contains product_id + sku + name-first-word candidate + exception message. No PII; SKU + brand name are non-secret product attributes already surfaced on the storefront. |
| T-sr7-03 | Denial of service | Unbounded `--limit=0` cursor across ~3,231 candidates | accept | Cursor() streams one row at a time; per-product update is single-statement and single-column. Risk equivalent to BackfillMerchantFeedCommand which already runs against larger candidate sets. |
| T-sr7-04 | Repudiation | No audit-log row written per resolved product | accept | Brand_id is mirrored by Product's Spatie LogsActivity config (line 95) — activity_log rows are produced automatically by Eloquent saves. NOTE: this command uses `DB::table('products')->update(...)` (not Eloquent), so activity_log does NOT fire for this backfill. Operator inspection of `/admin/products?brand_id=X` after the run is the agreed accountability surface for the M-1 closure event. Documented in command docblock. |
| T-sr7-05 | Elevation of privilege | Command is `runningInConsole`-gated; no HTTP exposure | mitigate | Registration is inside `if ($this->app->runningInConsole())` block (line 634 in AppServiceProvider). No Filament action / HTTP route ships with this task. |
| T-sr7-SC | Tampering | No new package installs | mitigate | Task adds zero new composer dependencies; legitimacy gate not triggered. RESEARCH.md package legitimacy audit not required. |
</threat_model>

<verification>
- `php artisan list` shows `products:backfill-brand-from-name`.
- Focused Pest file is GREEN (8 cases A-H + arch guard).
- Regression Pest suites (BackfillMerchantFeed, BackfillCategoryFromWoo, HydrateProductStockFromOffers, AdCandidateScanner) GREEN — no behavioural change in TaxonomyResolver's existing consumers.
- Full-suite Pest delta vs 260611-s2d baseline: +N pass / 0 new fails / 0 new risky, or full-suite OOM caveat documented.
- 3 atomic commits land in order: feat -> test -> state.
- STATE.md frontmatter rotated; `stopped_at` summarises 260611-sr7.
</verification>

<success_criteria>
- `BackfillProductBrandFromNameCommand` exists at `app/Console/Commands/BackfillProductBrandFromNameCommand.php` and is registered in AppServiceProvider's commands() array.
- `SKU_LIKE_PATTERN` private const is present in the source for grep-discoverability.
- Constructor injects `TaxonomyResolver` via `private readonly` — NO inline re-implementation of fuzzy matching.
- Pest file `tests/Feature/Console/BackfillProductBrandFromNameCommandTest.php` has 8 `it(...)` cases A-H plus the no-WooClient architectural guard, all GREEN.
- BackfillMerchantFeedCommand, BackfillCategoryFromWooCommand, HydrateProductStockFromOffersCommand source files are UNTOUCHED (verify with `git diff --name-only` — only the four files in `files_modified` plus STATE.md appear).
- TaxonomyResolver source is UNTOUCHED.
- Full-suite Pest baseline reconciled vs 260611-s2d (2,033 / 222 / 3): +N pass / 0 new fails / 0 new risky, or OOM caveat acknowledged with focused/touched-area equivalent confirmation.
- 3 atomic commits land: `feat(products): backfill-brand-from-name command + registration (260611-sr7)`, `test(products): backfill-brand-from-name Pest cases A-H (260611-sr7)`, `docs(state): 260611-sr7 backfill-brand-from-name shipped (260611-sr7)`.
- STATE.md `stopped_at` updated to summarise 260611-sr7 with post-deploy operator action steps.
</success_criteria>

<output>
Create `.planning/quick/260611-sr7-products-backfill-brand-from-name-parse-/260611-sr7-SUMMARY.md` at end of execution.
</output>

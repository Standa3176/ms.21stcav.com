# 260613-f2r — Execution Log

## Task 1: Probe + scope confirmation — COMPLETE (non-committing)

**Probe (a) — `brands[]` shape on Woo product list response:**
Confirmed via `app/Console/Commands/BackfillCategoryFromWooCommand.php` lines 186-217. Woo's `GET /products?include=…` returns `categories` directly on each product row as an array of `{id, name, slug}`. The Brands extension mirrors this pattern (same `wc/v3/products` endpoint, taxonomy-style relation), and the stdClass→array json round-trip cast (lines 187-189) is the established defensive shape.

**Probe (b) — PUT body shape:**
`WooClient::put($endpoint, $payload)` JSON-encodes the array verbatim through the Automattic SDK. Mirroring 260607-v5g's categories shape, `['brands' => [['id' => 10], ['id' => 99]]]` is the correct Woo REST contract.

**Probe (c) — EXTRACT vs INLINE decision:**

- DECISION: **EXTRACT**
- REASON: Discovery block in `DedupeBrandsCommand::perform()` lines 113-209 is self-contained — only depends on `$this->woo->get()` and `BRANDS_PER_PAGE`. `planned_affected` is command-specific (DedupeBrands needs it for dry-run; RetagProducts doesn't) and stays in the command. DedupeBrandsCommandTest binds WooClient stub via `app()->instance(WooClient::class, $stub)`, so a `BrandDuplicateFinder` resolved through the container picks up the SAME stub automatically — no test-rig changes needed.
- CONSEQUENCE: Task 2 fires; BrandDuplicateFinder ships in a separate refactor commit; DedupeBrandsCommand refactored to consume it.

No files modified. Proceeding to Task 2.

## Task 2: Extract BrandDuplicateFinder service — COMPLETE (commit f32922e)

- Created `app/Domain/Sync/Services/BrandDuplicateFinder.php` (final class, constructor DI WooClient, single public `discover()` method returning `[key => {canonical, sources}]`).
- Refactored `DedupeBrandsCommand` to constructor-inject `BrandDuplicateFinder` and replace the inline pagination + grouping + canonical-pick block with `$this->finder->discover()`. `planned_affected` DB-count step stays in the command (RetagProducts doesn't need it).
- Pagination failures still audit `brands.dedupe_pagination_failed` and exit FAILURE — observable behaviour byte-identical.
- DedupeBrandsCommandTest **10/10 GREEN** (62 assertions, 12.36s). No test-rig changes needed — the service auto-resolves the container-bound WooClient stub through DI.
- Atomic commit: `refactor(brands): extract BrandDuplicateFinder service from DedupeBrandsCommand (260613-f2r)` (f32922e).

## Task 3: RetagProductsOnWooCommand engine + AppServiceProvider — COMPLETE (commit 1f48781)

- Created `app/Console/Commands/RetagProductsOnWooCommand.php` (extends BaseCommand, NOT final, constructor DI on WooClient + Auditor + BrandDuplicateFinder).
- Signature: `brands:retag-products-on-woo {--dry-run} {--source-ids=} {--limit=0}`.
- Per-product re-tag: (current MINUS source) UNION canonical, sorted + uniqued for deterministic PUT bodies.
- Idempotent (already_canonical short-circuit), 200ms throttle between live PUTs, 404 detection on per-source list GET → `no_products_on_woo++`.
- 5 audit namespaces: `brands.product_retagged` / `brands.retag_failed` / `brands.retag_pagination_failed` / `brands.retag_no_products_on_woo` / `brands.retag_discovery_failed`.
- ALL Woo writes via WooClient — no direct Http:: / Guzzle / new AutomatticClient().
- Registered in `AppServiceProvider::$commands` adjacent to `DedupeBrandsCommand::class` with multi-line drift-prevention comment block.
- `php artisan list | grep brands:retag-products-on-woo` resolves cleanly.
- Atomic commit: `feat(brands): retag-products-on-woo command + AppServiceProvider registration (260613-f2r)` (1f48781).

## Task 4: Pest cases A-J — RetagProductsOnWooCommandTest — COMPLETE (commit 90f3e15)

- Created `tests/Feature/Console/RetagProductsOnWooCommandTest.php`.
- 10/10 Pest cases A-J GREEN (49 assertions, 34.82s).
- Anonymous-subclass WooClient stub binds via `app()->instance(WooClient::class, $stub)`. BrandDuplicateFinder auto-picks the same stub via container DI — no second binding required.
- Audit log assertions via Spatie `Activity::query()` (Auditor is `final`, can't be Mockery-mocked).
- Rule 1 fold during execution: Case I originally used `'count' => 101` on the source brand which beat the canonical's count=50 → canonical-pick flipped 10↔11 and zero retags happened. Fixed by raising the canonical's count to 500 so 10 stays canonical (the test intent — pagination across 2 pages of source-tagged products).
- Atomic commit: `test(brands): retag-products-on-woo Pest cases A-J (260613-f2r)` (90f3e15).

## Task 5: Verify — touched-area regression sweep + arch sweep — COMPLETE (non-committing)

**1. Command resolves via artisan list:**
```
brands:retag-products-on-woo   Re-tag Woo products from duplicate (source) brand terms → canonical brand terms so brands:dedupe --delete-empty-woo-terms is safe (260613-f2r).
```

**2. Focused suite — RetagProductsOnWooCommandTest:** **10/10 GREEN** (49 assertions, 34.82s).

**3. Touched-area regression suites GREEN:**

| Suite | Tests | Assertions | Duration |
|-------|-------|-----------:|---------:|
| RetagProductsOnWooCommandTest (260613-f2r) | 10/10 | 49 | 34.82s |
| DedupeBrandsCommandTest (260613-dir) | 10/10 | 62 | 12.11s |
| PushDivergenceToWooCommandTest (260611-g4q) | 10/10 | 87 | 12.02s |
| PushVisibilityToWooCommandTest (260611-f1y) | 6/6 | 29 | 9.92s |
| BackfillCategoryFromWooCommandTest (260607-v5g) | 6/6 | 26 | 9.81s |
| BackfillProductBrandFromNameCommandTest (260611-sr7) | 9/9 | 35 | 11.04s |
| **Total** | **51/51** | **288** | — |

DedupeBrandsCommandTest 10/10 GREEN proves Task 2's EXTRACT refactor preserved observable behaviour byte-identically.

**4. Full Pest baseline check:** Suite OOM-deferred at 512MB on Windows herd PHP — pre-existing infrastructure issue carried forward from 260611-qcq onwards, NOT introduced by 260613-f2r. PHP process plateaued at ~366MB and stopped emitting output, killed manually. Focused + touched-area equivalent confirmed at **+10 pass / 0 new fails** vs 260613-dir baseline (the 10 new RetagProductsOnWooCommandTest cases; DedupeBrandsCommandTest's 10 cases unchanged).

**5. Arch sweep for direct-HTTP regressions in `app/Console/Commands/RetagProductsOnWooCommand.php`:**
```
grep "new AutomatticClient|Http::|use GuzzleHttp" → 1 hit, line 37 docblock describing what the command does NOT do.
```
Zero actual usage. All Woo I/O through `$this->woo`.

**6. Drift-prevention sanity (EXTRACT branch):**
- `$this->finder->discover()` calls found in BOTH `DedupeBrandsCommand.php:121` and `RetagProductsOnWooCommand.php:116`.
- Canonical-pick `usort` closure (`if ($a['count'] !== $b['count'])`) found in EXACTLY ONE file: `BrandDuplicateFinder.php`. Service is the single seam.

**Task 1 decision rationale (paste from log):**
- DECISION: EXTRACT.
- REASON: Discovery block (pagination + grouping + canonical-pick) is self-contained, depends only on `$this->woo->get()`. `planned_affected` stayed command-specific. Test rig auto-inherits the WooClient stub via container DI.
- IF EXTRACT (chosen): Task 2 fires; BrandDuplicateFinder ships in separate refactor commit; DedupeBrandsCommand refactored to consume it — DedupeBrandsCommandTest 10/10 still GREEN.



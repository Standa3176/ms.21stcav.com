---
phase: quick-260613-f2r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Console/Commands/RetagProductsOnWooCommand.php           # NEW
  - app/Domain/Sync/Services/BrandDuplicateFinder.php            # NEW — conditional on Task 1 EXTRACT decision
  - app/Console/Commands/DedupeBrandsCommand.php                 # MODIFIED — conditional on Task 1 EXTRACT decision (refactor to consume service)
  - app/Providers/AppServiceProvider.php                         # MODIFIED — register new command
  - tests/Feature/Console/RetagProductsOnWooCommandTest.php      # NEW
  - .planning/STATE.md                                           # MODIFIED — append row + rotate frontmatter
autonomous: true
requirements:
  - QUICK-260613-f2r
must_haves:
  truths:
    - "Operator can run `php artisan brands:retag-products-on-woo --dry-run` and see a per-source plan + 20-row sample without writing to Woo"
    - "Operator can run `php artisan brands:retag-products-on-woo` and have every Woo product currently tagged with a duplicate (source) brand term re-tagged to the canonical brand term"
    - "Per-product re-tag PRESERVES non-source brand tags (a product tagged [source, otherBrand] becomes [canonical, otherBrand], not [canonical])"
    - "Products already tagged with the canonical brand (and not the source) increment `already_canonical` and do NOT generate a Woo PUT"
    - "Operator can run `php artisan brands:retag-products-on-woo --source-ids=12776,2904` to scope to specific source brands; non-duplicate IDs increment `source_not_a_duplicate` and are skipped"
    - "Re-running on an already-retagged state is a no-op (`products_retagged=0`, `already_canonical>0`, no Woo PUTs)"
    - "After a successful retag run, the source brand terms have count=0 on Woo, so the operator workflow `brands:dedupe --delete-empty-woo-terms` is safe (no customer-facing brand strip)"
    - "ALL Woo writes route through WooClient (zero direct Http:: / Guzzle / new AutomatticClient() in the new command)"
    - "Pest cases A-J (10/10) GREEN; DedupeBrandsCommandTest (260613-dir, 10/10) STILL GREEN after any refactor"
  artifacts:
    - path: "app/Console/Commands/RetagProductsOnWooCommand.php"
      provides: "The brands:retag-products-on-woo artisan engine"
      contains: "class RetagProductsOnWooCommand extends BaseCommand"
    - path: "tests/Feature/Console/RetagProductsOnWooCommandTest.php"
      provides: "10 Pest cases A-J covering the documented engine outcomes"
      contains: "Case A"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Command registration in the $commands array"
      contains: "RetagProductsOnWooCommand::class"
  key_links:
    - from: "app/Console/Commands/RetagProductsOnWooCommand.php"
      to: "app/Domain/Sync/Services/WooClient.php"
      via: "constructor DI; $this->woo->get('products', ['brand' => $sourceId, ...]) for pagination; $this->woo->put(\"products/{$wooId}\", ['brands' => [...]]) for re-tag"
      pattern: '\$this->woo->(get|put)\('
    - from: "app/Console/Commands/RetagProductsOnWooCommand.php"
      to: "app/Foundation/Audit/Services/Auditor.php"
      via: "$this->auditor->record('brands.product_retagged', [...]) per successful PUT"
      pattern: 'brands\.product_retagged'
    - from: "app/Console/Commands/RetagProductsOnWooCommand.php (Task 1 EXTRACT branch only)"
      to: "app/Domain/Sync/Services/BrandDuplicateFinder.php"
      via: "constructor DI; $this->finder->discover() returns the [sourceId => canonicalId] map"
      pattern: '->discover\('
---

<objective>
Ship `brands:retag-products-on-woo` — the Woo-side complement to 260613-dir's MS-side
brand dedupe. After 260613-dir, MS products are perfectly consolidated onto canonical
brand_ids, but Woo's storefront still has 310 products tagged with the duplicate (source)
brand terms. If the operator runs `brands:dedupe --delete-empty-woo-terms` now, the
`?force=true` flag would delete the source terms AND strip the brand association from
those 310 products — they'd become brand-less on the storefront. This command FIXES that
by re-tagging each affected Woo product from source → canonical FIRST, so after this runs
the source brands legitimately have count=0 on Woo and the delete operation is safe.

Purpose: close the Woo-side gap so the operator workflow
`brands:dedupe` → `brands:retag-products-on-woo` → `brands:dedupe --delete-empty-woo-terms`
is end-to-end safe — no customer-facing brand-strip regression, no manual hand-curation.

Output:
- New artisan command `brands:retag-products-on-woo` with `--dry-run`, `--source-ids=`, `--limit=` flags
- Conditional: new `BrandDuplicateFinder` service if Task 1 chooses EXTRACT (refactors DedupeBrandsCommand to consume it)
- 10 Pest cases A-J + DedupeBrandsCommandTest regression-clean
- New audit namespace `brands.product_retagged` (one row per successful PUT)
- AppServiceProvider registration
- STATE.md row + frontmatter rotate
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/Console/Commands/DedupeBrandsCommand.php
@app/Console/Commands/PushDivergenceToWooCommand.php
@app/Console/Commands/PushVisibilityToWooCommand.php
@app/Domain/Sync/Services/WooClient.php
@app/Foundation/Audit/Services/Auditor.php
@app/Providers/AppServiceProvider.php
@tests/Feature/Console/DedupeBrandsCommandTest.php

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase + 260613-dir. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From app/Domain/Sync/Services/WooClient.php:
```php
class WooClient
{
    public function get(string $endpoint, array $query = []): array;        // list endpoints return stdClass rows — see cast note below
    public function put(string $endpoint, array $payload): array;            // routes through POST when services.woo.use_post_for_updates=true (WAF compat — automatic)
    public function delete(string $endpoint, array $payload = []): array;
}
```

From app/Foundation/Audit/Services/Auditor.php:
```php
final class Auditor
{
    public function record(string $action, array $context = []): void;       // wraps spatie/activitylog 'system' log + correlation_id
}
```

From app/Console/Commands/DedupeBrandsCommand.php (260613-dir — DO NOT duplicate this code; either inline with drift-warning OR extract to service per Task 1):
```php
// Brand discovery — page through /products/brands and group by lowercased+trimmed name.
// CRITICAL: stdClass→array cast for every list row (commit 9581de8 pattern):
if (! is_array($row)) {
    $row = json_decode((string) json_encode($row), true);
}

// Canonical pick — highest count DESC, tie-break lowest id ASC:
usort($group, static function (array $a, array $b): int {
    if ($a['count'] !== $b['count']) { return $b['count'] <=> $a['count']; }
    return $a['id'] <=> $b['id'];
});

// Pagination loop — break on empty OR per-page-short:
while (true) {
    $response = $this->woo->get('products/brands', ['per_page' => self::BRANDS_PER_PAGE, 'page' => $page]);
    if (! is_array($response) || $response === []) { break; }
    // ...cast + collect...
    if (count($response) < self::BRANDS_PER_PAGE) { break; }
    $page++;
}
```

From `wc/v3/products` list response (Woo REST docs + 260607-v5g BackfillCategoryFromWooCommand precedent):
Each product row returned by `GET /products?brand={id}` includes:
- `id` (int) — woo_product_id
- `sku` (string)
- `brands` (array of {id, name, slug}) — the FULL related-brands array, NOT just the queried brand
  (Verify in Task 1 against a probe; if the field is absent or empty in our Woo install, fall back to per-product `GET /products/{id}` to read `brands[]`. Most likely it's present — categories work that way today.)

PUT body shape for brand assignment (mirrors categories shape from 260607-v5g):
```php
$this->woo->put("products/{$wooId}", [
    'brands' => array_map(static fn (int $id): array => ['id' => $id], $newBrandIds),
]);
```

From app/Console/Commands/PushVisibilityToWooCommand.php (single-field PUT + 200ms throttle precedent):
```php
private const WOO_PUT_THROTTLE_USEC = 200_000;  // 200ms between successful PUTs
// ...
$this->woo->put("products/{$wooId}", ['catalog_visibility' => 'hidden']);
usleep(self::WOO_PUT_THROTTLE_USEC);
```

From tests/Feature/Console/DedupeBrandsCommandTest.php (Pest test rig pattern — REUSE this exact pattern):
```php
// Anonymous-subclass WooClient stub bound via app()->instance(WooClient::class, $stub).
// Public properties: $brandsByPage, $productsByPageByBrand, $deleteBehaviour, $putBehaviour
// Public arrays: $getCalls, $putCalls, $deleteCalls
// Skip parent constructor — no IntegrationLogger / resolver needed.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe + scope confirmation (DECIDE extract vs inline)</name>
  <files>(read-only — no commit)</files>
  <action>
    Confirm three things, then make the EXTRACT-vs-INLINE call.

    (a) Confirm Woo's `/products?brand={id}` response actually includes per-product `brands[]` array on THIS install. Grep the codebase for any prior consumer:
        - `Grep` for `'brand' =>` in `app/Domain/Sync/` and `app/Console/Commands/` to find existing `products?brand=` queries.
        - `Grep` for `->brands` or `['brands']` in any Woo-response handler to see if the field is consumed elsewhere.
        - Cross-reference 260607-v5g BackfillCategoryFromWooCommand for the EXACT categories[] usage pattern — same approach should apply to brands[].
        - If grep finds a prior consumer: document the shape. If not: the dry-run path in Task 3 prints the first 5 brand rows raw, and Task 5 covers a manual verification step.
        - FALLBACK if `brands[]` is absent: do a per-product `GET /products/{id}` to read brand list (more expensive — 310 GETs + 310 PUTs vs 4 paginated list GETs + 310 PUTs). Note this as the contingency but assume list-includes-brands until proven otherwise.

    (b) Confirm `WooClient::put()` accepts `['brands' => [['id' => X], ...]]` body shape. The Automattic SDK is dumb — it just JSON-encodes whatever array you pass — so this is mechanical: mirror the categories shape from 260607-v5g BackfillCategoryFromWooCommand exactly.

    (c) DECIDE: extract `BrandDuplicateFinder` service or inline the ~30 lines? Apply this heuristic exactly:
        - Read DedupeBrandsCommand::perform() lines 113-209 (pagination + grouping + canonical selection).
        - Check DedupeBrandsCommandTest cases A-E (the discovery-pure cases that don't involve deletes).
        - **EXTRACT IF:** the pagination + grouping + canonical selection block is self-contained (only depends on `$this->woo`), AND DedupeBrandsCommandTest's discovery cases (A-E) can be made to pass through a service stub OR through the existing WooClient stub unchanged (the service is just a thin wrapper consuming `$this->woo->get`).
        - **INLINE IF:** the discovery block touches DedupeBrandsCommand's `$plan` data structure shape in a way that extraction would either require changing the public service return type contract OR force a non-trivial refactor of DedupeBrandsCommandTest fixture bindings.
        - **Default:** EXTRACT. The cost (one refactor commit + a return-shape contract) is one-time; the benefit (single source of truth for brand discovery across 2+ consumers, mirrors 260611-s2d WooProductWriter extract-on-second-consumer pattern) is permanent.

    Document the decision in a 5-line note (paste into Task 5's verify output):
        - DECISION: EXTRACT or INLINE
        - REASON: (one sentence)
        - IF EXTRACT: Task 2 fires; BrandDuplicateFinder ships in a separate refactor commit; DedupeBrandsCommand consumes it.
        - IF INLINE: Task 2 is SKIPPED; Task 3's RetagProductsOnWooCommand inlines the discovery logic with a `// drift-warning: identical logic in DedupeBrandsCommand (260613-dir)` comment AND a Pest assertion (Case J) that the two implementations produce identical output against a shared fixture.

    NO COMMIT — this is a scope-confirmation task only.
  </action>
  <verify>
    <automated>echo "Task 1 decision recorded — see scope confirmation block. EXTRACT=Task 2 fires; INLINE=Task 2 skipped."</automated>
  </verify>
  <done>Probe confirmations made (brands[] shape, PUT body shape) + EXTRACT/INLINE decision recorded with rationale. NO files modified.</done>
</task>

<task type="auto">
  <name>Task 2: (CONDITIONAL — EXTRACT branch only) Extract BrandDuplicateFinder service</name>
  <files>
    app/Domain/Sync/Services/BrandDuplicateFinder.php
    app/Console/Commands/DedupeBrandsCommand.php
  </files>
  <action>
    **SKIP THIS TASK IF Task 1 chose INLINE.** If Task 1 chose EXTRACT, execute as follows:

    Create `app/Domain/Sync/Services/BrandDuplicateFinder.php`:
    - Namespace `App\Domain\Sync\Services`
    - Constructor DI: `WooClient $woo`
    - Public method signature:
      ```php
      /**
       * @return array<string, array{
       *     canonical: array{id:int,name:string,count:int},
       *     sources: array<int, array{id:int,name:string,count:int}>
       * }>
       *   Keyed by strtolower(trim($name)). Only groups with 2+ rows are returned.
       */
      public function discover(): array;
      ```
    - Body: lift verbatim from `DedupeBrandsCommand::perform()` lines ~113-209 the pagination (while-true over `/products/brands`), the stdClass→array cast (the commit 9581de8 pattern — `if (! is_array($row)) { $row = json_decode((string) json_encode($row), true); }`), the grouping by `strtolower(trim($name))`, the `array_filter` to drop singletons, and the inline-`usort` canonical selection (highest count DESC, tie-break lowest id ASC).
    - Private const `BRANDS_PER_PAGE = 100` (mirror DedupeBrandsCommand).
    - On Woo pagination throw: bubble the exception to caller. DedupeBrandsCommand's `try/catch` around `$this->woo->get('products/brands', ...)` moves UP one level to `try { $plan = $this->finder->discover(); }`; on catch it records `brands.dedupe_pagination_failed` (unchanged audit shape) + returns FAILURE.
    - Do NOT include the `planned_affected` counts in the service return — that's a `DB::table('products')->where('brand_id', $src['id'])->count()` step that's command-specific (DedupeBrandsCommand needs it for its dry-run table; RetagProductsOnWooCommand doesn't care). DedupeBrandsCommand computes `planned_affected` AFTER calling `$this->finder->discover()`.

    Refactor `DedupeBrandsCommand`:
    - Add `BrandDuplicateFinder $finder` to constructor.
    - Replace lines ~113-209 with `$plan = $this->finder->discover();` then a separate small loop to compute `planned_affected` per source.
    - Observable behavior must be byte-identical: all 10 DedupeBrandsCommandTest cases A-J STILL pass without test-rig changes.
    - **Test rig compatibility:** DedupeBrandsCommandTest binds an anonymous-subclass `WooClient` stub via `app()->instance(WooClient::class, $stub)`. The new `BrandDuplicateFinder` resolves through the container, so when it constructor-injects `WooClient` it picks up the SAME stub automatically. NO test changes needed.

    Atomic commit: `refactor(brands): extract BrandDuplicateFinder service from DedupeBrandsCommand (260613-f2r)`.

    DRIFT-PREVENTION COMMENT in the new service docblock: "Single source of truth for brand discovery. Consumed by DedupeBrandsCommand (260613-dir) AND RetagProductsOnWooCommand (260613-f2r). If a third consumer is added or the canonical-pick rule changes, edit HERE only — do not re-implement in commands."
  </action>
  <verify>
    <automated>php artisan test --filter=DedupeBrandsCommandTest --stop-on-failure</automated>
  </verify>
  <done>(EXTRACT branch) BrandDuplicateFinder exists + DedupeBrandsCommand refactored to consume it + DedupeBrandsCommandTest 10/10 GREEN unchanged + atomic commit shipped. (INLINE branch) Task skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RetagProductsOnWooCommand engine + AppServiceProvider registration</name>
  <files>
    app/Console/Commands/RetagProductsOnWooCommand.php
    app/Providers/AppServiceProvider.php
  </files>
  <behavior>
    - Test A: source brand has 5 products tagged ONLY with [source] → 5 PUTs, each body `['brands' => [['id' => canonicalId]]]`, counter `products_retagged=5`.
    - Test B: 1 product tagged [source, otherBrand] → PUT body `['brands' => [['id' => canonicalId], ['id' => otherBrandId]]]` (otherBrand preserved, order irrelevant).
    - Test C: product tagged with BOTH source AND canonical → PUT body has canonicalId ONCE (no duplicate), source removed.
    - Test D: product tagged ONLY with [canonical] (source already removed) → `already_canonical++`, ZERO PUTs.
    - Test E: Woo 5xx on a PUT → `errors++`, command continues to next product, run exits SUCCESS.
    - Test F: Woo 404 on a `/products?brand={sourceId}` list call (brand deleted between discovery and processing) → `no_products_on_woo++`, skip that source, continue.
    - Test G: `--dry-run` → ZERO Woo PUTs, plan + 20-row sample printed, counters reflect would-do counts.
    - Test H: `--source-ids=3102,12822` → only those two sources processed; sources NOT in any duplicate group → `source_not_a_duplicate++`.
    - Test I: pagination across 2 pages (source has 101+ products) → both pages processed.
    - Test J: idempotent re-run after a successful run → all products show `already_canonical`, `products_retagged=0`, ZERO PUTs.
  </behavior>
  <action>
    Create `app/Console/Commands/RetagProductsOnWooCommand.php` extending `BaseCommand`. NOT `final` — Pest stubs swap via container binding.

    Signature:
    ```
    brands:retag-products-on-woo
        {--dry-run : Print plan + sample without writing to Woo}
        {--source-ids= : Comma-separated source brand ids to process; default = auto-discover all duplicates}
        {--limit=0 : Cap total products processed across all sources (0=unbounded)}
    ```

    Constructor DI:
    - `WooClient $woo`
    - `Auditor $auditor`
    - `BrandDuplicateFinder $finder` (EXTRACT branch only — if Task 1 chose INLINE, do not inject; inline the discovery loop with a `// drift-warning: identical logic in DedupeBrandsCommand (260613-dir) — keep in sync` block comment)

    Private constants:
    ```php
    private const PRODUCTS_PER_PAGE = 100;       // Woo REST per-page cap
    private const WOO_PUT_THROTTLE_USEC = 200_000;  // 200ms between PUTs (mirrors PushVisibilityToWooCommand + PushDivergenceToWooCommand)
    ```

    `perform()` logic:

    1. **Parse options.** `--source-ids` parses to a list of positive ints (filter ctype_digit, intval). `--limit=0` means unbounded.

    2. **Discover duplicates.**
       - EXTRACT branch: `$plan = $this->finder->discover();` then build `[sourceId => canonicalId]` map by walking `$plan`.
       - INLINE branch: inline the pagination + grouping + canonical selection (verbatim copy from DedupeBrandsCommand lines 113-209 minus the `planned_affected` step), then build the same map.
       - Print `[dry-run]` or `[LIVE]` banner + delete_empty info.

    3. **Filter by --source-ids if set.**
       - For each ID in `--source-ids`: look up its canonical in the discovery map. If NOT in any duplicate group → counter `source_not_a_duplicate++`, log a warn line, skip.
       - Build the working map of `[sourceId => canonicalId]` pairs to process.

    4. **Per-source product loop.** For each `(sourceId, canonicalId)` pair:
       - Paginate `$this->woo->get('products', ['brand' => $sourceId, 'per_page' => self::PRODUCTS_PER_PAGE, 'page' => $n])` until empty OR per-page-short.
       - Wrap the pagination GET in `try/catch`:
         - 404 (term deleted between discovery and now): increment `no_products_on_woo++`, skip remaining pages for this source, log `no_products_on_woo source={$sourceId}`. Detect via `(int) $e->getCode() === 404` OR `str_contains($e->getMessage(), 'rest_term_invalid')` (mirror DedupeBrandsCommand 404 detection at line 319-321).
         - Other throws: `errors++`, log warning, skip remaining pages, audit `brands.retag_pagination_failed`.
       - For each row in response:
         - stdClass→array cast: `if (! is_array($row)) { $row = json_decode((string) json_encode($row), true); }` (commit 9581de8 pattern).
         - `$wooProductId = (int) ($row['id'] ?? 0)`; skip if `<= 0`.
         - Extract current brand IDs:
           ```php
           $currentBrandIds = [];
           foreach (($row['brands'] ?? []) as $b) {
               if (! is_array($b)) { $b = json_decode((string) json_encode($b), true); }
               $bid = (int) ($b['id'] ?? 0);
               if ($bid > 0) { $currentBrandIds[] = $bid; }
           }
           ```
         - Compute new brand IDs:
           ```php
           $newBrandIds = array_values(array_unique(array_merge(
               array_diff($currentBrandIds, [$sourceId]),  // MINUS source
               [$canonicalId],                              // UNION canonical
           )));
           sort($newBrandIds);  // deterministic order for test assertion
           ```
         - If `$newBrandIds` (sorted) === sorted-copy-of `$currentBrandIds` → `already_canonical++`, log `already_canonical woo={$wooProductId}`, no PUT, continue.
         - Otherwise:
           - DRY-RUN: log `would_retag woo={$wooProductId} from={$sourceId} to={$canonicalId}`. Counter `would_retag++` (track separately so the dry-run counter table is meaningful). Sample the first 20 to print as a table at the end (see step 6).
           - LIVE: try/catch `$this->woo->put("products/{$wooProductId}", ['brands' => array_map(static fn (int $id): array => ['id' => $id], $newBrandIds)])`.
             - On success: `products_retagged++`, audit `brands.product_retagged` with `from_brand_id`, `to_brand_id`, `product_id`, `new_brand_ids` (full final list for forensic trail), log `pushed woo={$wooProductId}`.
             - On throw: `errors++`, log warn, audit `brands.retag_failed` with error message. CONTINUE to next product.
           - `usleep(self::WOO_PUT_THROTTLE_USEC)` between live PUTs (skipped on error).
       - Track `products_scanned++` per product iterated (whether PUT happened or not).
       - **Apply `--limit`** as a GLOBAL cap across all sources, not per-source: increment a global `processedCount++` after each product loop iteration (in both already-canonical and retagged branches); break out of the per-source loop AND outer source-pair loop when `$limit > 0 && $processedCount >= $limit`.

    5. **`groups_processed`** counter increments once per (sourceId, canonicalId) pair that was at least entered (even if its first GET 404s — we DID attempt it).

    6. **Output.** Final table with all counters:
       - `groups_processed`
       - `products_scanned`
       - dry-run column: `would_retag` / live column: `products_retagged`
       - `already_canonical`
       - `errors`
       - `no_products_on_woo`
       - `source_not_a_duplicate` (only meaningful when --source-ids given)
       Plus a per-source breakdown table (Source id / Canonical id / Products scanned / Products retagged / Already canonical / Errors).
       Dry-run also prints a 20-row sample of `would_retag` decisions (SKU / from / to / current brands / new brands).

    Register in `AppServiceProvider::$commands` block — add `RetagProductsOnWooCommand::class` adjacent to `DedupeBrandsCommand::class` (thematic grouping). Include a multi-line `//` comment block above the entry summarizing what it does + the operator workflow chain (`dedupe` → `retag-products-on-woo` → `dedupe --delete-empty-woo-terms`) + the drift-prevention contract.

    Atomic commit: `feat(brands): retag-products-on-woo command + AppServiceProvider registration (260613-f2r)`.

    DRIFT-PREVENTION DOCBLOCK at top of `RetagProductsOnWooCommand`:
    > Closes the Woo-side gap left by 260613-dir's brands:dedupe. After 260613-dir,
    > MS products consolidated onto canonical brand_ids, but Woo still has products
    > tagged with the duplicate (source) brand terms. Running brands:dedupe
    > --delete-empty-woo-terms WITHOUT this command first would strip the brand
    > association from those products via Woo's ?force=true cascade.
    >
    > Operator workflow: brands:dedupe → brands:retag-products-on-woo →
    > brands:dedupe --delete-empty-woo-terms.
    >
    > ALL Woo writes via $this->woo (WooClient). Direct Http:: / Guzzle / new
    > AutomatticClient() bypass IntegrationLogger audit trail + correlation_id
    > threading.
    >
    > If a future quick task adds a 4th brand surface (variations, attributes), do
    > NOT extend this command — write a sibling command. This command's contract
    > is exactly: "Re-tag Woo products from source brand terms → canonical brand
    > terms, per the [sourceId => canonicalId] map produced by BrandDuplicateFinder."
  </action>
  <verify>
    <automated>php artisan list | grep -F "brands:retag-products-on-woo"</automated>
  </verify>
  <done>Command file exists + registered in AppServiceProvider + `php artisan list` shows `brands:retag-products-on-woo` + atomic commit shipped. Test file does NOT exist yet (Task 4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Pest cases A-J — RetagProductsOnWooCommandTest</name>
  <files>tests/Feature/Console/RetagProductsOnWooCommandTest.php</files>
  <behavior>
    Pest cases A-J as enumerated in Task 3's `<behavior>` block — 10/10 GREEN, 35+ assertions.
  </behavior>
  <action>
    Create `tests/Feature/Console/RetagProductsOnWooCommandTest.php`.

    **Test rig — anonymous-subclass WooClient stub** (mirrors DedupeBrandsCommandTest pattern):
    - Public `array $brandsByPage` — page number → list of brand rows (drives `BrandDuplicateFinder::discover()` OR the inlined discovery).
    - Public `array $productsByBrandByPage` — `[$sourceId => [$page => [product rows]]]`. Each product row: `['id' => ..., 'sku' => ..., 'brands' => [['id' => ..., 'name' => ...], ...]]`.
    - Public `array $putBehaviour` — `[$wooProductId => 'ok'|'5xx']`. Default 'ok'.
    - Public `array $brandListFailBehaviour` — `[$sourceId => '404']`. Default absent = 'ok'.
    - Public `array $getCalls`, `array $putCalls` for assertion.
    - `get($endpoint, $query)`:
      - `'products/brands'` → return `$this->brandsByPage[$query['page']] ?? []`.
      - `'products'` with `brand` in query → if `$this->brandListFailBehaviour[brand] === '404'`, throw `new RuntimeException('rest_term_invalid', 404)`. Else return `$this->productsByBrandByPage[brand][page] ?? []`.
    - `put($endpoint, $payload)`:
      - Record in `$putCalls`.
      - Parse wooId from `'products/{id}'`. If `$this->putBehaviour[wooId] === '5xx'`, throw `new RuntimeException('Stub 5xx', 500)`.
      - Else return `['id' => wooId, ...]`.
    - Skip parent constructor.
    - `app()->instance(WooClient::class, $stub);`

    **For EXTRACT branch (Task 1 EXTRACT decision):** the test rig stays the SAME — `BrandDuplicateFinder` resolves through container and constructor-injects `WooClient`, which picks up the stub automatically. No second binding needed.

    **For INLINE branch (Task 1 INLINE decision):** same — the command resolves WooClient through the container directly.

    **10 Pest cases (A-J)** — each case sets up `brandsByPage` + `productsByBrandByPage` + (optional) `putBehaviour` / `brandListFailBehaviour`, runs `Artisan::call('brands:retag-products-on-woo', [...])`, and asserts:

    - **Case A:** `brandsByPage = [1 => [{id:10,Poly,50},{id:11,poly,3}]]`, `productsByBrandByPage = [11 => [1 => [5 products each with brands=[{id:11}]]]]`. After run: 5 PUTs, every PUT body `['brands' => [['id' => 10]]]`. Counter `products_retagged=5`. Activity log: 5× `brands.product_retagged`.
    - **Case B:** 1 product tagged `[{id:11}, {id:99,otherBrand}]`. PUT body sorted-ID-list = `[['id' => 10], ['id' => 99]]` (assertion: sort then compare to handle any order quirk). `otherBrand` (id=99) preserved.
    - **Case C:** product tagged `[{id:10}, {id:11}, {id:99}]` (has both source AND canonical). New = `[10, 99]` (source removed, canonical kept once, no duplicate). PUT body brand IDs sorted = `[10, 99]`.
    - **Case D:** product tagged ONLY `[{id:10}]` (canonical only — source already removed). After run: `already_canonical=1`, ZERO PUTs (`putCalls` is empty). Activity log: ZERO `brands.product_retagged` rows.
    - **Case E:** `productsByBrandByPage = [11 => [1 => [{id:5001, brands:[{id:11}]}, {id:5002, brands:[{id:11}]}]]]`, `putBehaviour = [5001 => '5xx']`. After run: `errors=1`, `products_retagged=1` (5002 succeeded), exit code SUCCESS. Activity log: 1× `brands.retag_failed` for 5001, 1× `brands.product_retagged` for 5002.
    - **Case F:** `brandListFailBehaviour = [11 => '404']`. After run: `no_products_on_woo=1`, ZERO PUTs for this source. Counter table shows the 404.
    - **Case G:** `--dry-run => true`. ZERO PUTs (`putCalls` is empty). Output contains `[dry-run]`, `would_retag`, sample-table header. ZERO `brands.product_retagged` activity rows.
    - **Case H:** `--source-ids=3102,12822`. `brandsByPage` includes BOTH a duplicate group containing 3102 AND a non-duplicate brand 12822 (only one row for 12822 → not in `$groups`). After run: 3102 processed (PUTs happen), 12822 → `source_not_a_duplicate=1`, no PUTs for 12822.
    - **Case I:** `productsByBrandByPage = [11 => [1 => [101 products with brands=[{id:11}]], 2 => [empty/per-page-short signal]]]`. Or use exactly 100 products on page 1 + 1 product on page 2. After run: pagination consumes both pages, `products_scanned=101`, `products_retagged=101`.
    - **Case J:** First run as Case A (5 PUTs). Then RE-USE the same stub but flip the products' `brands` to canonical (mutate `productsByBrandByPage` to simulate Woo state after the first run). Run again: ZERO PUTs, `already_canonical=5`, `products_retagged=0`. (Alternatively, structure as one test that calls `Artisan::call` twice with state mutation in between.)

    **Audit log assertions** via `Spatie\Activitylog\Models\Activity::query()->where('description', 'brands.product_retagged')->get()` — Auditor is `final`, cannot Mockery-mock; same pattern as DedupeBrandsCommandTest.

    Atomic commit: `test(brands): retag-products-on-woo Pest cases A-J (260613-f2r)`.
  </action>
  <verify>
    <automated>php artisan test --filter=RetagProductsOnWooCommandTest --stop-on-failure</automated>
  </verify>
  <done>10/10 Pest cases A-J GREEN, 35+ assertions, atomic commit shipped.</done>
</task>

<task type="auto">
  <name>Task 5: Verify — touched-area regression sweep + full-suite baseline check</name>
  <files>(verification only — no commit)</files>
  <action>
    Run in order and confirm:

    1. `php artisan list | grep -F "brands:retag-products-on-woo"` — command resolves cleanly.

    2. **Focused suite GREEN:**
       ```
       php artisan test --filter=RetagProductsOnWooCommandTest
       ```
       Expect 10/10 GREEN (cases A-J).

    3. **Touched-area regression suites GREEN:**
       ```
       php artisan test --filter='(DedupeBrandsCommandTest|PushDivergenceToWooCommandTest|PushVisibilityToWooCommandTest|BackfillCategoryFromWooCommandTest|BackfillProductBrandFromNameCommandTest)'
       ```
       Expect ALL GREEN. Particularly DedupeBrandsCommandTest (10/10) — if Task 1 chose EXTRACT, this proves the refactor preserved observable behavior.

    4. **Full Pest baseline check** (vs 260613-dir baseline ~2,052 pass / 222 skipped / 3 risky):
       ```
       php artisan test
       ```
       Expect `+N pass / 0 new fails`. N = 10 if INLINE chosen; N = 10 if EXTRACT chosen (DedupeBrandsCommandTest's 10 stay green, RetagProductsOnWooCommandTest adds 10). Acceptable if the suite OOMs at 512MB on Windows herd PHP (pre-existing infrastructure issue carried forward from 260611-qcq onwards) — fall back to confirming the focused/touched-area suites GREEN.

    5. **Arch sweep for direct-HTTP regressions** in the new command:
       ```
       Grep for 'new AutomatticClient' OR 'Http::' OR 'use GuzzleHttp' in app/Console/Commands/RetagProductsOnWooCommand.php
       ```
       Expect ZERO hits — all Woo I/O through `$this->woo`.

    6. **Drift-prevention sanity:** if Task 1 chose INLINE, grep both DedupeBrandsCommand.php AND RetagProductsOnWooCommand.php for the canonical-pick `usort` closure. The two implementations should be byte-identical save for variable names. If Task 1 chose EXTRACT, grep both for `$this->finder->discover()` — both should call the service; the inline usort closure should appear ONLY in BrandDuplicateFinder.php.

    Paste the Task 1 EXTRACT/INLINE decision rationale into the verify output for STATE.md amber/green capture.

    NO COMMIT — verification only.
  </action>
  <verify>
    <automated>php artisan test --filter='(RetagProductsOnWooCommandTest|DedupeBrandsCommandTest|PushDivergenceToWooCommandTest|PushVisibilityToWooCommandTest|BackfillCategoryFromWooCommandTest|BackfillProductBrandFromNameCommandTest)'</automated>
  </verify>
  <done>All focused + touched-area suites GREEN, full-suite delta confirmed (+N pass / 0 new fails OR OOM-deferred with focused/touched-area GREEN), arch sweep clean (zero direct-HTTP), drift-prevention contract satisfied.</done>
</task>

<task type="auto">
  <name>Task 6: STATE.md row + artifact commit</name>
  <files>.planning/STATE.md</files>
  <action>
    Append a row to STATE.md capturing the completed quick task. Format mirrors prior quick-task `old_stopped_at:` entries (same compression style as 260613-dir, 260611-sr7, 260611-s2d):

    - One paragraph summarizing what shipped, the Task 1 EXTRACT/INLINE decision + rationale, counter contracts, audit namespaces, Pest pass count, baseline delta vs 260613-dir.
    - Rotate the existing `stopped_at:` block to `old_stopped_at:` (push down the chain).
    - New `stopped_at:` opens with `Completed quick task 260613-f2r — brands:retag-products-on-woo …`.
    - Update `last_updated` to ISO-8601 + `last_activity` to today (2026-06-13).
    - `progress` block stays the same (this is not a new phase plan; it's a quick task closing a gap).
    - List the operator post-deploy action sequence:
      1. Deploy the commits.
      2. `php artisan brands:dedupe --dry-run` to re-confirm the duplicate groups (should match yesterday's 11 groups).
      3. `php artisan brands:retag-products-on-woo --dry-run` to see the ~310-product plan + 20-row sample.
      4. Live: `php artisan brands:retag-products-on-woo`. Expect ~310 PUTs over ~5-7 min @ 200ms throttle.
      5. Spot-check Woo storefront: previously-source brand pages (`/product-brand/poly-1/` etc) should now have product count=0. Canonical `/product-brand/poly/` should have ALL products.
      6. Spot-check `/wp-json/wc/v3/products/brands?per_page=100` — duplicate source brands' `count` field should be 0.
      7. RISKY STEP — `php artisan brands:dedupe --delete-empty-woo-terms`. NOW safe because source terms have count=0 (no products to strip).

    What this does NOT do:
    - No fuzzy/alias matching (HP vs Hewlett-Packard) — operator handles via Filament brand-mapping UI.
    - No scheduled cron — operator-triggered, runs once after dedupe.
    - No rollback (re-running on already-retagged state is a no-op via `already_canonical`).
    - No new Filament UI.

    Atomic commit: `docs(state): 260613-f2r retag-products-on-woo shipped (260613-f2r)`.
  </action>
  <verify>
    <automated>grep -c "260613-f2r" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md updated + frontmatter rotated + operator post-deploy action sequence documented + atomic commit shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI → Laravel app | `php artisan brands:retag-products-on-woo` invocation. Operator-only; the artisan harness is on the prod VPS behind SSH (auth handled out of band). |
| Laravel app → Woo REST | WooClient routes GET `/products`, GET `/products/brands`, PUT `/products/{id}` through the resolver-provided base URL + consumer key/secret. WAF-aware (POST-for-PUT routing per services.woo.use_post_for_updates). |
| Laravel app → MySQL (activity_log) | Auditor writes `brands.product_retagged` + `brands.retag_failed` + `brands.retag_pagination_failed` rows. Append-only from this surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260613-f2r-01 | Tampering | Brand assignment payload to Woo PUT | mitigate | Compute `$newBrandIds` deterministically as `(current MINUS source) UNION canonical`. Sort + uniqueify before PUT so re-runs produce identical bodies. No operator-controllable string interpolation into the brands payload. |
| T-260613-f2r-02 | Information Disclosure | Audit log payload (`brands.product_retagged` includes from/to brand IDs + product IDs) | accept | Brand IDs and product IDs are non-PII business identifiers. Activity log already accessible to admin-only Filament viewer. No customer email / address / etc in the audit context. |
| T-260613-f2r-03 | Denial of Service | Bulk PUT loop hitting Woo too fast | mitigate | 200ms `usleep` between successful PUTs (mirror PushVisibilityToWooCommand pattern). WooClient's 429 backoff is the second line. `--limit` flag for operator-controlled batching. |
| T-260613-f2r-04 | Tampering | Race condition — brand term deleted between discovery and product enumeration | mitigate | Try/catch around `/products?brand={sourceId}` GET; 404 detection (`getCode() === 404` OR `rest_term_invalid` in message) → `no_products_on_woo++`, skip source, do NOT increment errors. Mirrors DedupeBrandsCommand 404 detection. |
| T-260613-f2r-05 | Repudiation | Re-tag happens silently with no audit trail | mitigate | Every successful PUT writes `brands.product_retagged` with `from_brand_id`, `to_brand_id`, `product_id`, `new_brand_ids`. Every failed PUT writes `brands.retag_failed` with the error message. WooClient internally logs every HTTP call to `integration_events` with correlation_id. |
| T-260613-f2r-06 | Elevation of Privilege | --source-ids accepts arbitrary integers; an operator passing a non-duplicate ID could trigger PUTs against unrelated brands | mitigate | --source-ids filter intersects with the auto-discovered duplicate map. Non-duplicate IDs increment `source_not_a_duplicate++` and are explicitly skipped (no canonical to migrate to). No way for an operator-supplied ID to drive a PUT outside the duplicate groups discovered fresh from Woo. |
| T-260613-f2r-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this task — pure PHP feature against existing WooClient + Auditor + Eloquent. No `composer require` step. |
</threat_model>

<verification>
- `php artisan list | grep -F "brands:retag-products-on-woo"` resolves the command.
- `php artisan test --filter=RetagProductsOnWooCommandTest` — 10/10 cases A-J GREEN.
- `php artisan test --filter='(DedupeBrandsCommandTest|PushDivergenceToWooCommandTest|PushVisibilityToWooCommandTest|BackfillCategoryFromWooCommandTest|BackfillProductBrandFromNameCommandTest)'` — touched-area regressions GREEN.
- `php artisan test` — full-suite baseline +N pass / 0 new fails (OOM-deferred fallback acceptable).
- Grep `(new AutomatticClient|Http::|use GuzzleHttp)` in `app/Console/Commands/RetagProductsOnWooCommand.php` → ZERO hits.
- STATE.md contains a `260613-f2r` row with operator post-deploy action sequence.
- Atomic commits shipped:
  - (if EXTRACT) `refactor(brands): extract BrandDuplicateFinder service from DedupeBrandsCommand (260613-f2r)`
  - `feat(brands): retag-products-on-woo command + AppServiceProvider registration (260613-f2r)`
  - `test(brands): retag-products-on-woo Pest cases A-J (260613-f2r)`
  - `docs(state): 260613-f2r retag-products-on-woo shipped (260613-f2r)`
- Total: 3 commits (INLINE) or 4 commits (EXTRACT). Tasks 1 + 5 non-committing.
</verification>

<success_criteria>
- `brands:retag-products-on-woo` resolves via artisan + handles `--dry-run`, `--source-ids=`, `--limit=` flags.
- Re-tags Woo products from source brand terms → canonical brand terms with PRESERVED non-source brand tags (Case B).
- Idempotent re-run produces zero PUTs (Case J).
- All counters (`groups_processed`, `products_scanned`, `products_retagged`, `already_canonical`, `errors`, `no_products_on_woo`, `source_not_a_duplicate`) populate correctly across all 10 Pest cases.
- ALL Woo I/O through WooClient (zero direct HTTP).
- After running, source brand terms have Woo count=0 → `brands:dedupe --delete-empty-woo-terms` is now SAFE.
- DedupeBrandsCommand (260613-dir) UNTOUCHED unless Task 1 fires EXTRACT (then refactored to consume BrandDuplicateFinder, behavior byte-identical via 10/10 Pest cases A-J).
- PushDivergenceToWooCommand / PushVisibilityToWooCommand / WooClient / BackfillCategoryFromWooCommand / BackfillProductBrandFromNameCommand UNTOUCHED.
- 4 atomic commits (EXTRACT) or 3 (INLINE), with conventional-commit messages tagged `(260613-f2r)`.
</success_criteria>

<output>
Create `.planning/quick/260613-f2r-brands-retag-products-on-woo-re-tags-woo/260613-f2r-SUMMARY.md` when execution completes.
</output>

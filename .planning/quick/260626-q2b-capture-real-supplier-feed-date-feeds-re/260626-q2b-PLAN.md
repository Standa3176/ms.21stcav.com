---
phase: 260626-q2b-capture-real-supplier-feed-date-feeds-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_26_120000_add_feed_metadata_to_suppliers_table.php
  - app/Domain/Sync/Models/Supplier.php
  - app/Domain/Sync/Commands/SyncSupplierFeedDatesCommand.php
  - routes/console.php
  - app/Domain/Sync/Filament/Resources/SupplierResource.php
  - tests/Feature/Console/SyncSupplierFeedDatesCommandTest.php
  - tests/Feature/Filament/Resources/SupplierResourceTest.php
autonomous: true
requirements:
  - QUICK-260626-q2b
must_haves:
  truths:
    - "The local suppliers table stores the supplier's REAL feed file date in a new feed_remote_date column, sourced from the remote feeds.remote_date (the datetime that for Nuvias = 2026-05-14 00:20:24), plus feed_cron_run (feeds.cron_run) and feed_status (feeds.status)."
    - "A new command suppliers:sync-feed-dates pulls id, name, remote_date, cron_run, status from the remote feeds table (via the SupplierDb credential + mysqli, mirroring supplier:db-sync) and upserts those four feed-metadata fields onto suppliers WITHOUT clobbering operator-owned fields (is_active, stale_after_days, notes). It is metadata-only — it writes NO product prices/stock."
    - "The Suppliers admin page's Feed date column reads the REAL feed_remote_date (not the recorded_at MS-pull date), formatted as an actual date and badge-coloured by the existing 5-working-day rule (RED > 5 working days). Nuvias now shows 2026-05-14 / RED instead of today / green."
    - "The misleading recorded_at-based 'Freshness' badge is replaced by a truthful status derived from feed_remote_date + feed_status: 'Feed off' when feed_status===0, 'No data' when feed_remote_date is null, 'Stale' (red) when older than 5 working days, else 'Fresh' (green). A feed_cron_run column is added so operators can see when the upstream puller last ran."
    - "Zero zero-dates leak: feeds.remote_date values that are NULL/empty/'0000-00-00 00:00:00' parse to null (no Carbon parse error), shown as 'never'."
    - "The command's DB-write logic is a pure, testable method upsertFeedRows(array, bool $dryRun): array — unit-testable with array input, no mysqli — and operator fields are provably preserved across an upsert."
  artifacts:
    - path: "app/Domain/Sync/Commands/SyncSupplierFeedDatesCommand.php"
      provides: "suppliers:sync-feed-dates — remote feeds.remote_date → suppliers.feed_remote_date upsert"
      contains: "remote_date"
    - path: "app/Domain/Sync/Filament/Resources/SupplierResource.php"
      provides: "Feed date column reads feed_remote_date; freshness replaced by feed_remote_date/feed_status status; feed_cron_run column"
      contains: "feed_remote_date"
    - path: "database/migrations/2026_06_26_120000_add_feed_metadata_to_suppliers_table.php"
      provides: "feed_remote_date + feed_cron_run + feed_status columns on suppliers"
      contains: "feed_remote_date"
  key_links:
    - from: "SyncSupplierFeedDatesCommand::upsertFeedRows()"
      to: "suppliers.feed_remote_date"
      via: "updateOrCreate by supplier_id, set feed metadata only (preserve operator fields)"
      pattern: "feed_remote_date"
    - from: "SupplierResource feed_date column"
      to: "suppliers.feed_remote_date"
      via: "real DB column + feedAgeColor(workingDaysSince(feed_remote_date))"
      pattern: "feed_remote_date"
---

<objective>
Fix the supplier file-date logic. The Suppliers page currently shows each supplier as "fresh / today"
even when their feed is weeks old — because the date it uses (suppliers… via recorded_at) is the date
MeetingStore last PULLED, stamped today() on every sync run regardless of whether the supplier
refreshed. PROVEN on prod: Nuvias shows fresh/today, but the supplier's real file date is
2026-05-14 00:20:24.

ROOT CAUSE + REMEDY: the supplier's true file date lives on the REMOTE feeds table as
`feeds.remote_date` (confirmed via live probe 2026-06-26 — Nuvias feeds.id=1, remote_date=
'2026-05-14 00:20:24', cron_run='2026-05-20 12:53:12', status=0). MS never read it. This task:
  1. Adds feed_remote_date + feed_cron_run + feed_status to the local suppliers table.
  2. Adds suppliers:sync-feed-dates to pull those from feeds and upsert them (metadata-only — no
     price/stock writes), scheduled Mon-Fri before the price sync.
  3. Re-points the Suppliers page Feed date + status at the REAL feed_remote_date, so quiet suppliers
     surface RED with their true date, and surfaces feed_status (Nuvias status=0 = feed disabled
     upstream).

OUT OF SCOPE (separate, pricing-sensitive — flagged for follow-up): the automatic buy-price
stale-supplier exclusion (SupplierFreshnessResolver) is ALSO recorded_at-based and therefore a no-op
in prod; switching it to feed_remote_date would change live buy-prices and needs its own dry-run +
sign-off. NOT changed here. The operator's manual is_active toggle (260626-oqr) already excludes
Nuvias from pricing today.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260626-q2b-capture-real-supplier-feed-date-feeds-re/
@CLAUDE.md
@app/Domain/Sync/Commands/SupplierDbSyncCommand.php
@app/Domain/Sync/Models/Supplier.php
@app/Domain/Sync/Filament/Resources/SupplierResource.php
@app/Domain/Sync/Console/Commands/CheckStaleSuppliersCommand.php
@routes/console.php

<interfaces>
<!-- Extracted from the codebase — use directly. -->

REMOTE feeds table columns (probed 2026-06-26): id (bigint), name (varchar), remote_date (datetime —
THE supplier file date), cron_run (datetime — when the upstream puller last ran), status (int —
0 observed for the disabled Nuvias feed), plus FTP/column-mapping fields we don't need. Join key:
feeds.id === supplier_offer_snapshots.supplier_id === suppliers.supplier_id (as string). Nuvias is
feeds.id=1.

Remote connection pattern (mirror SupplierDbSyncCommand::perform lines 93-113):
```php
$creds = $this->resolver->for(IntegrationCredentialKind::SupplierDb);
mysqli_report(MYSQLI_REPORT_OFF);
$mysqli = @new \mysqli((string) $creds['host'], (string) $creds['username'],
    (string) $creds['password'], (string) $creds['database'], (int) ($creds['port'] ?? 3306));
if ($mysqli->connect_errno !== 0) { /* error + SymfonyCommand::FAILURE */ }
```
IntegrationCredentialResolver + IntegrationCredentialKind::SupplierDb are the same deps
SupplierDbSyncCommand injects. BaseCommand (app/Console/Commands/BaseCommand.php) — extend it and
implement `protected function perform(): int`; handle() is final and wraps perform().

Supplier model (app/Domain/Sync/Models/Supplier.php): fillable [supplier_id, name, stale_after_days,
is_active, notes]; casts is_active=>bool, stale_after_days=>int. ADD feed_remote_date, feed_cron_run,
feed_status to BOTH fillable and casts (feed_remote_date/feed_cron_run => 'datetime',
feed_status => 'integer').

Operator-field preservation: when upserting, updateOrCreate(['supplier_id'=>$sid], [ONLY name +
the 3 feed_* fields]). NEVER include is_active / stale_after_days / notes in the update payload —
mirror CheckStaleSuppliersCommand which only writes name on its discovery upsert.

CURRENT SupplierResource (app/Domain/Sync/Filament/Resources/SupplierResource.php):
- Has pure helpers workingDaysSince(?Carbon): ?int, feedAgeColor(?int): string, feedAgeTooltip(?int):
  ?string (from 260626-phz) — REUSE them, now passing $record->feed_remote_date.
- The 'freshness' TextColumn (badge, uses app(SupplierFreshnessResolver)->classify) and the 'feed_date'
  TextColumn (uses app(SupplierFreshnessResolver)->latestRecordedAtFor) BOTH read recorded_at and must
  change per <tasks>. After the change, if SupplierFreshnessResolver is no longer referenced in the
  file, remove its import.

Scheduler pattern (routes/console.php ~line 93): Schedule::command('...')->cron('55 6 * * 1-5')
->withoutOverlapping(30)->onOneServer()->timezone('Europe/London')->description('...').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration + model — feed_remote_date / feed_cron_run / feed_status on suppliers</name>
  <files>
    database/migrations/2026_06_26_120000_add_feed_metadata_to_suppliers_table.php,
    app/Domain/Sync/Models/Supplier.php
  </files>
  <behavior>
    suppliers gains: feed_remote_date (datetime, nullable), feed_cron_run (datetime, nullable),
    feed_status (integer, nullable), index on feed_remote_date. Supplier model fillable + casts updated.
  </behavior>
  <action>
    Create the migration (driver-agnostic: $table->dateTime(...)->nullable(); $table->integer(
    'feed_status')->nullable(); $table->index('feed_remote_date'); all inside Schema::table('suppliers',
    ...)). down() drops the three columns (drop the index first if the driver needs it). Add the three
    fields to Supplier::$fillable and ::$casts (feed_remote_date/feed_cron_run => 'datetime',
    feed_status => 'integer'). Run migrate on the sqlite test DB via the test run in later tasks.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe artisan migrate --pretend 2>&1 | grep -i feed_metadata; echo "---"; ~/.config/herd/bin/php84/php.exe -r "require 'vendor/autoload.php'; echo 'ok';"</automated>
    Expected: the migration is recognised; no PHP parse error.
  </verify>
  <done>
    - Migration adds the 3 nullable columns + index; model fillable/casts updated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: suppliers:sync-feed-dates command (remote feeds.remote_date → suppliers)</name>
  <files>
    app/Domain/Sync/Commands/SyncSupplierFeedDatesCommand.php,
    routes/console.php,
    tests/Feature/Console/SyncSupplierFeedDatesCommandTest.php
  </files>
  <behavior>
    Command `suppliers:sync-feed-dates {--dry-run}` (extends BaseCommand): connects to the remote
    SupplierDb via mysqli, runs `SELECT id, name, remote_date, cron_run, status FROM feeds`, collects
    rows into a plain array, and calls upsertFeedRows(array $rows, bool $dryRun): array.

    upsertFeedRows (PUBLIC, no I/O, returns ['created'=>int,'updated'=>int,'skipped'=>int]):
      - for each row with a non-empty id: $sid = (string) $row['id']; updateOrCreate(
        ['supplier_id'=>$sid], ['name'=>($row['name'] ?: null), 'feed_remote_date'=>parseFeedDate(
        $row['remote_date']), 'feed_cron_run'=>parseFeedDate($row['cron_run']), 'feed_status'=>
        is_numeric($row['status']??null) ? (int)$row['status'] : null]).
      - NEVER writes is_active/stale_after_days/notes.
      - On dry-run: count what WOULD change, write nothing.
    parseFeedDate(?string): ?\Carbon\Carbon — null/empty/leading-'0000' → null; else Carbon::parse.

    Schedule in routes/console.php: Mon-Fri 06:55 London (before the 07:00 price sync), withoutOverlapping
    + onOneServer + timezone + description referencing 260626-q2b.

    Test (RefreshDatabase) — drive upsertFeedRows directly (no mysqli):
      - Rows incl Nuvias: ['id'=>1,'name'=>'Nuvias','remote_date'=>'2026-05-14 00:20:24','cron_run'=>
        '2026-05-20 12:53:12','status'=>0] + a second feed with a valid recent date. Assert suppliers
        row supplier_id='1' has feed_remote_date equal to 2026-05-14 00:20:24, feed_cron_run set,
        feed_status===0, name='Nuvias'.
      - Zero-date guard: a row with remote_date='0000-00-00 00:00:00' (and one with null) → feed_remote_date
        null, no exception.
      - Operator-field preservation: pre-create Supplier(['supplier_id'=>'1','is_active'=>false,
        'stale_after_days'=>21,'notes'=>'paused']); run upsertFeedRows; assert is_active STILL false,
        stale_after_days STILL 21, notes STILL 'paused', AND feed_remote_date now populated.
      - dry-run: upsertFeedRows($rows, true) writes nothing (supplier count unchanged / dates null).
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Console/SyncSupplierFeedDatesCommandTest.php 2>&1 | tail -20</automated>
    Expected: GREEN.

    <automated>~/.config/herd/bin/php84/php.exe artisan route:list >/dev/null 2>&1; ~/.config/herd/bin/php84/php.exe artisan schedule:list 2>&1 | grep -i "sync-feed-dates"</automated>
    Expected: the scheduled entry is listed.

    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint --test app/Domain/Sync/Commands/SyncSupplierFeedDatesCommand.php 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - Command exists; upsertFeedRows populates feed_* fields, preserves operator fields, parses zero-dates to null, respects dry-run.
    - Scheduled Mon-Fri 06:55 London. Test GREEN; pint clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Suppliers page — show REAL feed_remote_date + truthful status + cron column</name>
  <files>
    app/Domain/Sync/Filament/Resources/SupplierResource.php,
    tests/Feature/Filament/Resources/SupplierResourceTest.php
  </files>
  <behavior>
    Table changes (helpers workingDaysSince/feedAgeColor/feedAgeTooltip reused, now over
    $record->feed_remote_date):
      - REPLACE the 'feed_date' column body to read the real column:
        ```php
        TextColumn::make('feed_remote_date')
            ->label('Feed date')
            ->badge()
            ->sortable()
            ->placeholder('never')
            ->formatStateUsing(fn ($state): ?string => $state?->format('D j M Y'))
            ->color(fn (Supplier $record): string => self::feedAgeColor(self::workingDaysSince($record->feed_remote_date)))
            ->tooltip(fn (Supplier $record): ?string => self::feedAgeTooltip(self::workingDaysSince($record->feed_remote_date))),
        ```
      - REPLACE the recorded_at-based 'freshness' column with a truthful status:
        ```php
        TextColumn::make('feed_status_label')
            ->label('Status')
            ->badge()
            ->getStateUsing(function (Supplier $record): string {
                if ($record->feed_status === 0) { return 'Feed off'; }
                if ($record->feed_remote_date === null) { return 'No data'; }
                return self::workingDaysSince($record->feed_remote_date) > 5 ? 'Stale' : 'Fresh';
            })
            ->color(fn (string $state): string => match ($state) {
                'Fresh' => 'success',
                'Stale' => 'danger',
                'Feed off' => 'danger',
                default => 'gray',
            }),
        ```
      - ADD a 'feed_cron_run' column: TextColumn::make('feed_cron_run')->label('Upstream pull')
        ->dateTime('D j M Y H:i')->sortable()->toggleable()->placeholder('—').
      - If SupplierFreshnessResolver is now unused in the file, remove its import.
    The existing is_active ToggleColumn, name, supplier_id, stale_after_days, notes, updated_at columns
    and the TernaryFilter stay.

    Feature test (tests/Feature/Filament/Resources/SupplierResourceTest.php): update the feed-date
    assertion to seed Supplier(['supplier_id'=>'1','feed_remote_date'=>'2026-05-14 00:20:24',
    'feed_status'=>0]) and assert the List page shows 'Thu 14 May 2026' and the 'Feed off' status; add a
    case for a fresh supplier (feed_remote_date today, feed_status 1) showing 'Fresh'. Pin
    Carbon::setTestNow to a fixed date for deterministic working-day colour; reset after. Remove/replace
    any prior assertion that depended on recorded_at/latestRecordedAtFor for the feed-date column.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Filament/Resources/SupplierResourceTest.php 2>&1 | tail -20</automated>
    Expected: GREEN.

    <automated>grep -n "latestRecordedAtFor\|->make('freshness')" app/Domain/Sync/Filament/Resources/SupplierResource.php; echo "exit:$?"</automated>
    Expected: no match (recorded_at-based columns gone). grep exit 1 = good.

    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint --test app/Domain/Sync/Filament/Resources/SupplierResource.php 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - Feed date column shows the real feed_remote_date, coloured by the 5-working-day rule, sortable.
    - 'Status' badge is truthful (Feed off / No data / Stale / Fresh) from feed_remote_date + feed_status.
    - 'Upstream pull' (feed_cron_run) column added. recorded_at-based columns gone. Test GREEN; pint clean.
  </done>
</task>

</tasks>

<verification>
1. `pest tests/Feature/Console/SyncSupplierFeedDatesCommandTest.php` → GREEN
2. `pest tests/Feature/Filament/Resources/SupplierResourceTest.php` → GREEN
3. `pest tests/Unit/Domain/Sync/SupplierFeedAgeTest.php` → still GREEN (helpers unchanged)
4. `schedule:list | grep sync-feed-dates` → present
5. `grep latestRecordedAtFor SupplierResource.php` → no match
6. `pint --test` on new/changed files → PASS

Operator notes (for SUMMARY.md, NOT executed by Claude):
- Deploy: push main, then on VPS `sudo -u stcav /home/stcav/ms.21stcav.com/deploy/deploy.sh` (runs the
  new migration).
- Populate dates immediately (don't wait for the 06:55 cron):
  `sudo -u stcav bash -c 'cd /home/stcav/ms.21stcav.com && php artisan suppliers:sync-feed-dates'`
  then reload /admin/suppliers — Nuvias shows 'Thu 14 May 2026', RED, status 'Feed off'.
- The page now shows the TRUE supplier file date (feeds.remote_date). Nuvias's underlying problem is
  upstream: its feed is disabled (status=0) and the upstream puller last ran 2026-05-20 — that needs
  fixing on the supplier-feed server, separate from MS.
- FOLLOW-UP (pricing): the automatic buy-price stale-exclusion still keys off recorded_at (a no-op in
  prod). Switching it to feed_remote_date is a separate task with a dry-run, because it changes live
  buy-prices. Manual is_active exclusion covers Nuvias now.
</verification>

<success_criteria>
- suppliers.feed_remote_date holds the real feeds.remote_date; suppliers:sync-feed-dates populates it (metadata-only), preserving operator fields, parsing zero-dates to null.
- Suppliers page shows the true file date, coloured RED > 5 working days, with a truthful status (Feed off/Stale/Fresh) and an upstream-pull column.
- Nuvias shows 2026-05-14 / RED / Feed off instead of today / fresh.
- No regression to the working-day helper unit tests; pint clean; scheduled job registered.
</success_criteria>

<output>
Create `.planning/quick/260626-q2b-capture-real-supplier-feed-date-feeds-re/260626-q2b-SUMMARY.md` documenting:
- The root cause (recorded_at = MS pull date, always today; real date is remote feeds.remote_date) and the probe that confirmed the column.
- The three pieces: migration, suppliers:sync-feed-dates (metadata-only), page rework to the real date + truthful status + cron column.
- The upstream insight: Nuvias feed is disabled (status=0), upstream puller last ran 2026-05-20 — MS now shows the truth but the fix is on the feed server.
- The flagged pricing follow-up (buy-price stale-exclusion still on recorded_at; needs its own dry-run).
- Operator runbook (deploy → run suppliers:sync-feed-dates → verify Nuvias red).
</output>

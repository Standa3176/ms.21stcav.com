---
phase: 260708-b4f-woo-maintenance-reconciliation-pass-1-mi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_08_000000_add_woo_maintenance_reconciliation_to_products_table.php
  - app/Domain/Products/Models/Product.php
  - app/Console/Commands/ReconcileWooMaintenanceCommand.php
  - routes/console.php
  - tests/Feature/Products/ReconcileWooMaintenanceCommandTest.php
must_haves:
  truths:
    - "A migration adds nullable reconciliation columns to `products`: woo_image_count (int), woo_gtin (string), woo_category_count (int), woo_stock_status (string), woo_reconciled_at (timestamp, indexed). These mirror each product's REAL state on Woo so the Maintenance dashboard (pass 2) can report true shop-wide gaps for ALL live products (incl. the 3,614 legacy WC-migrated ones whose media/EAN were never mirrored locally)."
    - "A new `products:reconcile-woo-maintenance` command pages Woo `GET /products` (status=publish, per_page=100, _fields=id,images,global_unique_id,categories,stock_status), matches each returned product to the local Product by woo_product_id, and writes woo_image_count=count(images), woo_gtin=global_unique_id, woo_category_count=count(categories), woo_stock_status=stock_status, woo_reconciled_at=now(). READ-ONLY on Woo (GETs only — no writes). --dry-run + --per-page + --max-pages options; per-page try/catch so one bad page doesn't abort the run; a summary of pages/scanned/matched/updated/unmatched."
    - "Scheduled nightly (Schedule::command('products:reconcile-woo-maintenance')->dailyAt('04:30') in routes/console.php — off-peak, after the 03:xx prunes, before the ~05:00 supplier pulls) so the reconciled data stays fresh."
    - "Additive — no existing behaviour changes; ProductGapReport still reads its current fields until pass 2 rewires it. The command never writes to Woo."
  artifacts:
    - path: "app/Console/Commands/ReconcileWooMaintenanceCommand.php"
      provides: "pages Woo /products → writes woo_* reconciliation fields per live product"
      contains: "products:reconcile-woo-maintenance"
    - path: "database/migrations/2026_07_08_000000_add_woo_maintenance_reconciliation_to_products_table.php"
      provides: "woo_image_count / woo_gtin / woo_category_count / woo_stock_status / woo_reconciled_at columns"
      contains: "woo_reconciled_at"
    - path: "tests/Feature/Products/ReconcileWooMaintenanceCommandTest.php"
      provides: "paged Woo fetch → local woo_* fields populated; dry-run no-op"
      contains: "woo_image_count"
  key_links:
    - from: "ReconcileWooMaintenanceCommand"
      to: "WooClient::get('products', paged) → Product woo_* columns by woo_product_id"
      via: "mirror Woo's real image/EAN/category/stock state locally"
      pattern: "reconcile"
---

<objective>
Foundation for shop-wide Woo maintenance: mirror each live product's REAL Woo state (image count, EAN,
category count, stock status) into new local columns via a paged, read-only reconciliation command
(scheduled nightly). This lets pass 2 rewire the Maintenance dashboard to report true gaps across ALL
~4,612 live products — not the misleading local-mirror emptiness that made legacy products look like gaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260708-b4f-woo-maintenance-reconciliation-pass-1-mi/
@CLAUDE.md
@app/Domain/Products/Models/Product.php
@app/Domain/Sync/Services/WooClient.php
@app/Console/Commands/BaseCommand.php
@routes/console.php
---
WooClient::get(endpoint, query) returns the decoded Woo response body (for /products, a list of product
arrays). Reuse it. Model the command on an existing BaseCommand console command (e.g. HydrateProductStock
FromOffersCommand) for the perform()/option/summary shape. Woo product fields: id, images (array),
global_unique_id (WC 9.x GTIN), categories (array), stock_status.
</context>

<interfaces>
=== Migration (2026_07_08_000000_add_woo_maintenance_reconciliation_to_products_table.php) ===
```php
Schema::table('products', function (Blueprint $table): void {
    $table->unsignedInteger('woo_image_count')->nullable()->after('gallery_image_urls');
    $table->string('woo_gtin')->nullable()->after('woo_image_count');
    $table->unsignedInteger('woo_category_count')->nullable()->after('woo_gtin');
    $table->string('woo_stock_status')->nullable()->after('woo_category_count');
    $table->timestamp('woo_reconciled_at')->nullable()->index()->after('woo_stock_status');
});
```
(down(): drop the 5 columns. Adjust ->after() to real columns if those names differ; nullable so it's safe
on the 6k-row table.)

=== Product model ===
Add the 5 columns to $fillable; casts: `woo_image_count` => 'integer', `woo_category_count` => 'integer',
`woo_reconciled_at` => 'datetime'.

=== NEW: app/Console/Commands/ReconcileWooMaintenanceCommand.php (extends BaseCommand) ===
```php
protected $signature = 'products:reconcile-woo-maintenance
    {--per-page=100 : Woo products per page (max 100)}
    {--max-pages=0 : Cap pages (0 = until exhausted)}
    {--dry-run : Page Woo + report matches, write nothing}';

protected $description = 'Mirror each live product''s real Woo state (image count, EAN, category count, stock) into local woo_* columns for the Maintenance dashboard. Read-only on Woo (GETs).';

public function __construct(private readonly WooClient $woo) { parent::__construct(); }
```
perform():
- $perPage = clamp((int)--per-page, 1, 100); $maxPages = max(0,(int)--max-pages); $dry = (bool)--dry-run.
- Loop $page = 1..:
    - try { $rows = $this->woo->get('products', ['status'=>'publish','per_page'=>$perPage,'page'=>$page,'_fields'=>'id,images,global_unique_id,categories,stock_status']); }
      catch (\Throwable $e) { warn + Log; break; }
    - if (! is_array($rows) || $rows === []) break;
    - foreach ($rows as $p):
        - $wooId = (int)($p['id'] ?? 0); if ($wooId <= 0) continue; $scanned++;
        - $product = Product::where('woo_product_id', $wooId)->first();
        - if (! $product) { $unmatched++; continue; }
        - $matched++;
        - $updates = [
            'woo_image_count'    => count((array)($p['images'] ?? [])),
            'woo_gtin'           => ($g = trim((string)($p['global_unique_id'] ?? ''))) !== '' ? $g : null,
            'woo_category_count' => count((array)($p['categories'] ?? [])),
            'woo_stock_status'   => ($s = trim((string)($p['stock_status'] ?? ''))) !== '' ? $s : null,
            'woo_reconciled_at'  => now(),
          ];
        - if (! $dry) { $product->forceFill($updates)->saveQuietly(); $updated++; }
    - if (count($rows) < $perPage) break;
    - $page++; if ($maxPages > 0 && $page > $maxPages) break;
- Print a summary table (pages, scanned, matched, updated, unmatched, dry_run).
- return SUCCESS.

=== routes/console.php ===
Add: `Schedule::command('products:reconcile-woo-maintenance')->dailyAt('04:30')->withoutOverlapping();`
(near the other Schedule::command entries).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconciliation columns + products:reconcile-woo-maintenance + schedule</name>
  <files>
    database/migrations/2026_07_08_000000_add_woo_maintenance_reconciliation_to_products_table.php,
    app/Domain/Products/Models/Product.php,
    app/Console/Commands/ReconcileWooMaintenanceCommand.php,
    routes/console.php,
    tests/Feature/Products/ReconcileWooMaintenanceCommandTest.php
  </files>
  <behavior>
    Add the migration + model casts + command + schedule per <interfaces>. Feature test (RefreshDatabase):
      - Create 2 local Products with woo_product_id 101 & 102 (+ one with a woo id NOT returned by Woo).
      - Bind a WooClient stub (app()->instance(WooClient::class, $stub)) whose get('products', ['page'=>1,...])
        returns a fixture list [{id:101, images:[{},{}], global_unique_id:'50123', categories:[{}], stock_status:'instock'},
        {id:102, images:[], global_unique_id:'', categories:[], stock_status:'outofstock'}] and page>=2 returns [].
      - Run `products:reconcile-woo-maintenance`. Assert product 101: woo_image_count=2, woo_gtin='50123',
        woo_category_count=1, woo_stock_status='instock', woo_reconciled_at not null; product 102:
        woo_image_count=0, woo_gtin=null, woo_category_count=0, woo_stock_status='outofstock'.
      - `--dry-run` leaves woo_* columns null (no write).
      - The WooClient stub must NEVER have put()/post() called (read-only guard — bind a throwing stub for those or assert not called).
    Confirm the command is registered (artisan list | grep reconcile-woo-maintenance) and the schedule entry exists.
  </behavior>
  <action>
    Create the migration, model casts, command, schedule line, and test. Run migrate (test uses RefreshDatabase),
    the test, and pint.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Products/ReconcileWooMaintenanceCommandTest.php 2>&1 | tail -18</automated>
    Expected: GREEN — paged fetch populates woo_* fields per matched product; dry-run no-op; no Woo writes.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint --test app/Console/Commands/ReconcileWooMaintenanceCommand.php app/Domain/Products/Models/Product.php 2>&1 | tail -5</automated>
    Expected: PASS.
    <automated>~/.config/herd/bin/php84/php.exe artisan list 2>&1 | grep reconcile-woo-maintenance</automated>
    Expected: command listed.
  </verify>
  <done>
    - Migration adds the 5 woo_* columns; products:reconcile-woo-maintenance pages Woo (read-only) and populates them per live product; scheduled nightly; feature-tested (incl. dry-run + no-Woo-writes); pint clean; command registered.
  </done>
</task>

</tasks>

<verification>
1. `pest tests/Feature/Products/ReconcileWooMaintenanceCommandTest.php` → GREEN
2. `pint --test` → PASS
3. `artisan list | grep reconcile-woo-maintenance` → present

Operator notes (for SUMMARY.md):
- Deploy: push main → `sudo -u stcav /home/stcav/ms.21stcav.com/deploy/deploy.sh` (RUNS A MIGRATION — adds 5 nullable columns to products; safe/fast on the 6k-row table).
- Then seed the data once (off-peak — ~47 Woo GET pages, read-only): `php artisan products:reconcile-woo-maintenance --dry-run` (preview matches) then `php artisan products:reconcile-woo-maintenance`. Nightly 04:30 keeps it fresh.
- Pass 2 (next) rewires ProductGapReport + the Maintenance dashboard to read woo_image_count / woo_gtin /
  woo_category_count / woo_stock_status, with a "not yet reconciled" indicator — so the counts become TRUE
  shop-wide gaps over all ~4,612 live products (the legacy ones included).
</verification>

<success_criteria>
- products gains woo_* reconciliation columns; products:reconcile-woo-maintenance pages Woo read-only and mirrors real image/EAN/category/stock state locally per live product; scheduled nightly; feature-tested (dry-run + no-writes); pint clean. Foundation ready for the pass-2 dashboard rewire.
</success_criteria>

<output>
Create `.planning/quick/260708-b4f-woo-maintenance-reconciliation-pass-1-mi/260708-b4f-SUMMARY.md` documenting
the reconciliation columns, the read-only paged command + nightly schedule, the test, and the operator
one-off seed + the pass-2 dashboard rewire that will consume the woo_* fields.
</output>

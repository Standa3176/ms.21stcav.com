---
phase: 260709-hl0-fix-pre-existing-architecture-test-rot-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Architecture/PriceCalculatorPurityTest.php
  - tests/Architecture/TradePricingNoV1ModificationTest.php
  - tests/Architecture/PinnedFieldsSurviveSyncTest.php
  - tests/Architecture/PinnedQuotePricesSurviveRuleEditTest.php
must_haves:
  truths:
    - "Four pre-existing Architecture test failures are fixed as TEST-ROT (test-only edits; NO production code changes). Verified root causes: (1) PriceCalculatorPurityTest's hard-coded '<=2' caps are stale because a legitimate third pure public method addVat() was added (ex-VAT fix, commit 5804484) — the purity intent (one round() + one config() read per public method, pure int math) is intact. (2) The PriceCalculator sha256 byte-identity pin is stale for the same additive reason — compute()/stripVat() bytes are unchanged and the 80-triple golden fixture is 81/81 green. (3) PinnedFieldsSurviveSyncTest has a decimal-format mismatch + a stale 'local sell_price frozen' assumption. (4) PinnedQuotePricesSurviveRuleEditTest has a RefreshDatabase wiring bug."
    - "PriceCalculatorPurityTest: bump the round-count cap and the config()-count cap from <=2 to <=3 (three pure public methods now: compute + stripVat + addVat) and update the comment. Do NOT touch PriceCalculator.php."
    - "TradePricingNoV1ModificationTest: re-pin the PriceCalculator sha256 to the CURRENT committed file hash. The executor MUST compute it live (same method the test uses / sha256 of app/Domain/Pricing/Services/PriceCalculator.php) after confirming `git diff --stat` shows NO uncommitted changes to that file — pin the freshly-computed value, do not hard-code a pasted hash. (Expected ≈ 43efcb555c7dadc6c7ca583f8f231b82610d63d9caf6775fb0dc93ce9920ed4c.) The RuleResolver pin in the same file is unaffected + must stay green."
    - "PinnedFieldsSurviveSyncTest: (case 2) change the buy_price expectation '2100.00' → '2100.0000' (the decimal(_,4) cast returns 4dp; the value is correct). (case 1) DECISION LOCKED: pin_price is Woo/storefront-only — supplier sync legitimately recomputes the LOCAL sell_price. Replace the stale 'sell_price stays == original' assertion with: assert the local sell_price RECOMPUTED (changed from the original), and keep/assert the existing Woo-side pin-revert (the pin_reverted auditor expectation, which already passes) — documenting that the pin protects the Woo price, not the derived local mirror. Do NOT change PriceRecomputer or any pricing code."
    - "PinnedQuotePricesSurviveRuleEditTest: add `uses(RefreshDatabase::class);` at the file top (after imports, matching sibling Architecture DB-tests) and remove the trailing chained `->uses(RefreshDatabase::class)`. The customer_groups migration already exists + is SQLite-safe; the only issue is the per-test chained form doesn't migrate the in-memory DB before the body. Keep the skipIfMySqlOfflineShipGate() guard."
  artifacts:
    - path: "tests/Architecture/PriceCalculatorPurityTest.php"
      provides: "caps 2→3 for the 3 pure public methods"
      contains: "addVat"
    - path: "tests/Architecture/TradePricingNoV1ModificationTest.php"
      provides: "PriceCalculator sha re-pinned to committed hash"
      contains: "PriceCalculator"
  key_links: []
---

<objective>
Clear four pre-existing tests/Architecture failures that are confirmed TEST-ROT (not real bugs) so `pest` is
trustworthy. All edits are test-only; no production code changes. The Deptrac failures are a SEPARATE task
(260709-hl0-fix-real-deptrac-architecture-violations) — do NOT touch deptrac or app code here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-hl0-fix-pre-existing-architecture-test-rot-p/
@CLAUDE.md
@tests/Architecture/PriceCalculatorPurityTest.php
@tests/Architecture/TradePricingNoV1ModificationTest.php
@tests/Architecture/PinnedFieldsSurviveSyncTest.php
@tests/Architecture/PinnedQuotePricesSurviveRuleEditTest.php
@app/Domain/Pricing/Services/PriceCalculator.php
@tests/Pest.php
---
Investigated root causes (all test-rot, evidence-backed):
- PriceCalculatorPurityTest: 2 failing `it()` — round-count cap `toBeLessThanOrEqual(2)` (~line 144, actual 3) +
  config()-count cap `toBeLessThanOrEqual(2)` (~line 76, actual 3). Cause: addVat() added (commit 5804484). Bump both to 3.
- TradePricingNoV1ModificationTest (~line 44): PriceCalculator sha pinned to 200b4962… but current committed hash
  is 43efcb55…. compute()/stripVat() byte-unchanged; only addVat() appended. Re-pin (compute live).
- PinnedFieldsSurviveSyncTest: case2 (~line 205) '2100.00' vs actual '2100.0000'. case1 (~line 159) asserts
  sell_price == original but SupplierPriceChanged→RecomputePriceListener recomputes it (2430.0000, correct);
  the pin's real job (Woo regular_price revert) still fires (pin_reverted auditor passes). pin is Woo-only (LOCKED).
- PinnedQuotePricesSurviveRuleEditTest (~line 178): uses the chained `})->uses(RefreshDatabase::class)` form →
  no migration before the body → `no such table: customer_groups`. Siblings use file-top `uses(RefreshDatabase::class);`.
</context>

<interfaces>
=== PriceCalculatorPurityTest.php ===
- config()-count assertion (~line 76): `->toBeLessThanOrEqual(2)` → `->toBeLessThanOrEqual(3)`.
- round()-count assertion (~line 144): `->toBeLessThanOrEqual(2)` → `->toBeLessThanOrEqual(3)`.
- Update the nearby comment to name three public methods (compute + stripVat + addVat). (Optional polish: derive
  the cap from the count of public methods to prevent future rot — only if trivial; otherwise the literal 3 is fine.)

=== TradePricingNoV1ModificationTest.php ===
- Confirm `git diff --stat -- app/Domain/Pricing/Services/PriceCalculator.php` is EMPTY (no uncommitted changes).
- Compute the current hash the SAME way the test computes it (read the test to see if it hashes file_get_contents
  or a normalised form), then replace the pinned PriceCalculator literal (~line 44) with that value. Leave the
  RuleResolver pin untouched.

=== PinnedFieldsSurviveSyncTest.php ===
- case 2 (~line 205): `->toBe('2100.00')` → `->toBe('2100.0000')`.
- case 1 (~line 159): remove/replace the `sell_price == $originalSellPrice` assertion. Instead assert the local
  sell_price was recomputed — e.g. `expect((string) $fresh->sell_price)->not->toBe($originalSellPrice)` (proves the
  local mirror is not frozen) — and ensure the existing Woo-side revert expectation (pin_reverted auditor / the
  regular_price PUT) remains asserted (that is the behaviour the pin actually guarantees). Add a short comment:
  "pin_price protects the Woo price; the local sell_price is a derived mirror that recomputes on supplier sync."

=== PinnedQuotePricesSurviveRuleEditTest.php ===
- Add `uses(RefreshDatabase::class);` at the top (after the `use` imports, matching e.g. PinnedFieldsSurviveSyncTest:28).
- Remove the trailing `->uses(RefreshDatabase::class)` chained onto the `it(...)` at ~line 178.
- Keep everything else (skipIfMySqlOfflineShipGate guard, the CustomerGroup::factory() setup).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: fix the four Architecture test-rot files</name>
  <files>
    tests/Architecture/PriceCalculatorPurityTest.php,
    tests/Architecture/TradePricingNoV1ModificationTest.php,
    tests/Architecture/PinnedFieldsSurviveSyncTest.php,
    tests/Architecture/PinnedQuotePricesSurviveRuleEditTest.php
  </files>
  <behavior>
    Apply the <interfaces> edits. tdd=false: these ARE the tests; the app code is the (unchanged, correct) oracle.
    After editing, each of the four files must pass. For the sha re-pin, compute the hash live from the committed
    PriceCalculator.php (do NOT hard-code my quoted value without verifying it matches).
  </behavior>
  <action>
    Edit the four test files. Run them. Confirm no PRODUCTION file changed (git diff must show only tests/Architecture/*).
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Architecture/PriceCalculatorPurityTest.php tests/Architecture/TradePricingNoV1ModificationTest.php tests/Architecture/PinnedFieldsSurviveSyncTest.php tests/Architecture/PinnedQuotePricesSurviveRuleEditTest.php 2>&1 | tail -15</automated>
    Expected: all four GREEN (purity caps pass, sha pin matches, decimal + recompute assertions pass, customer_groups table present).
    <automated>git diff --name-only 2>&1 | grep -vE "^tests/Architecture/" | grep -vE "supplier-probe.json|CompetitorIngestFreshnessColorTest" || echo "ONLY TEST FILES CHANGED"</automated>
    Expected: prints "ONLY TEST FILES CHANGED" (no production code touched; pre-existing unrelated tree changes ignored).
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Architecture 2>&1 | tail -6</automated>
    Expected: the ONLY remaining Architecture failures are the Deptrac ones (handled in the separate 260709 deptrac task) — these four no longer fail.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint --test tests/Architecture 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - The four Architecture test-rot files pass; no production code changed; only Deptrac remains red in tests/Architecture; pint clean.
  </done>
</task>

</tasks>

<verification>
1. The four files → GREEN
2. `git diff` shows only tests/Architecture/* (no prod code)
3. `pest tests/Architecture` → only Deptrac still red
4. `pint --test` → PASS

Operator notes (for SUMMARY.md):
- Deploy: no deploy needed (test-only). These were pre-existing test-rot unrelated to any feature.
- Records the pin_price decision: pin_price is Woo/storefront-only (supplier sync recomputes the local sell_price
  mirror by design) — PinnedFieldsSurviveSyncTest now documents that. No pricing behaviour changed.
- PriceCalculator money-path confirmed unchanged (only an additive pure addVat() method; golden 81/81 green).
</verification>

<success_criteria>
- The four confirmed test-rot Architecture files pass with test-only edits (purity caps 2→3, PriceCalculator sha re-pinned to committed hash, decimal + Woo-only-pin assertions, RefreshDatabase wiring); no production code changed; only Deptrac remains red in tests/Architecture; pint clean.
</success_criteria>

<output>
Create `.planning/quick/260709-hl0-fix-pre-existing-architecture-test-rot-p/260709-hl0-SUMMARY.md` documenting each of
the four fixes (with the confirmed root cause), that no prod code changed, the recorded pin_price=Woo-only decision,
and that Deptrac is handled separately.
</output>
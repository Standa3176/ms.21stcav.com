---
phase: 260709-hl0-fix-real-deptrac-architecture-violations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Domain/Sync/Services/WooProductWriter.php
  - app/Domain/Sync/Services/SupplierSkuRegistry.php
  - app/Domain/Sync/Services/SupplierSyncDigestComposer.php
  - app/Domain/Sync/Services/SupplierFreshnessResolver.php
  - app/Domain/Products/Services/WooFieldComparator.php
  - app/Domain/Products/Models/SupplierSkuCache.php
must_haves:
  truths:
    - "`vendor/bin/deptrac analyse` goes from 101 violations to 0 (or, if non-Sync violations remain, they are reported explicitly and the Sync-layer + Sync-DB violations this task targets are all resolved). The 14 failing tests/Architecture/Deptrac*LayerTest + DeptracTest go green. This is a CODE refactor to make the real layer boundaries hold — NOT a deptrac baseline/skip (operator decision: fix properly)."
    - "Violation class 1 (Sync → Cutover): Sync\\Services\\WooProductWriter imported Cutover\\Services\\WooFieldComparator, but Cutover is a one-way SINK (no layer may depend on it). WooFieldComparator is relocated OUT of Cutover into a layer that every current importer is allowed to depend on (prefer Products — Sync's allow-list includes Products and it is product-field-comparison logic; fall back to Foundation only if Cutover→Products is NOT allowed by deptrac.yaml OR WooFieldComparator itself imports Cutover-internal classes). ALL importers' use-statements + the AppServiceProvider binding are updated. No behaviour change — it's a move + renamespace."
    - "Violation class 2 (Sync → DB facade, SYNC-04 -WpDirectDb): the three Sync services no longer use Illuminate\\Support\\Facades\\DB. Each DB::table('<t>') is replaced by the corresponding Eloquent model's query builder (identical SQL): product_price_snapshots→ProductPriceSnapshot::query(), supplier_offer_snapshots→SupplierOfferSnapshot::query(), supplier_sku_cache→a NEW thin SupplierSkuCache model. DB::connection()->getDriverName() is obtained via a model connection instead of the facade. insertOrIgnore/truncate/aliased-from all work on the Eloquent builder → byte-identical SQL."
    - "Zero behaviour change: every converted query produces the same SQL it did via DB::table (verified by the existing Sync tests staying green — SupplierSyncDigestComposerTest, SupplierSyncDigestCommandTest, SupplierFreshnessResolverTest — plus a new lightweight SupplierSkuRegistry test since it has none). Driver-portable (SQLite tests / MariaDB prod). No migration, no deptrac.yaml/depfile.yaml rule change (the path-regex layers re-classify the moved file automatically)."
  artifacts:
    - path: "app/Domain/Products/Services/WooFieldComparator.php"
      provides: "WooFieldComparator relocated out of the Cutover sink"
      contains: "class WooFieldComparator"
    - path: "app/Domain/Products/Models/SupplierSkuCache.php"
      provides: "Eloquent model for supplier_sku_cache (replaces DB::table)"
      contains: "supplier_sku_cache"
  key_links:
    - from: "Sync services"
      to: "Eloquent models (Products layer) instead of DB facade"
      via: "Model::query() — identical SQL, satisfies SYNC-04"
      pattern: "::query()"
    - from: "WooProductWriter + all importers"
      to: "Products\\Services\\WooFieldComparator (not Cutover)"
      via: "move + renamespace, no Sync→Cutover edge"
      pattern: "WooFieldComparator"
---

<objective>
Fix the 14 red Deptrac architecture tests by eliminating the two REAL boundary violations a recent Sync refactor
introduced: (1) Sync depending on the Cutover sink via WooFieldComparator, and (2) Sync using the DB facade
directly (SYNC-04 -WpDirectDb). Relocate the shared comparator + route Sync DB access through Eloquent models —
identical SQL, no behaviour change, no baseline. Operator chose "fix properly".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-hl0-fix-real-deptrac-architecture-violations/
@CLAUDE.md
@deptrac.yaml
@app/Domain/Cutover/Services/WooFieldComparator.php
@app/Domain/Sync/Services/WooProductWriter.php
@app/Domain/Sync/Services/SupplierSkuRegistry.php
@app/Domain/Sync/Services/SupplierSyncDigestComposer.php
@app/Domain/Sync/Services/SupplierFreshnessResolver.php
@app/Domain/Products/Models/ProductPriceSnapshot.php
@app/Domain/Products/Models/SupplierOfferSnapshot.php
---
Verified:
- deptrac.yaml layers are by path-regex (Sync = app/Domain/Sync/.*, Cutover = app/Domain/Cutover/.*, Products =
  app/Domain/Products/.*). Cutover is a documented one-way SINK ("no other layer depends on Cutover"). Sync's
  allow-list = [Foundation, Products, Alerting, Integrations] + deny WpDirectDb (the DB-facade ban).
- `deptrac analyse` currently: 101 violations, 0 errors (the 101 are instances of the two edge types below).
- WooFieldComparator lives at app/Domain/Cutover/Services/WooFieldComparator.php. Importers (9): Cutover
  (DivergenceScanner, AutoSyncDivergenceCommand, PushDivergenceToWooCommand, PushVisibilityToWooCommand),
  Products (ProductObserver), Sync (WooProductWriter — THE violation), Console (BackfillProductBrandFromNameCommand),
  AppServiceProvider. Grep `WooFieldComparator` to get the exact list before moving.
- DB-facade usages: SupplierSkuRegistry (DB::table('supplier_sku_cache')->truncate() + ->insertOrIgnore() ×2),
  SupplierSyncDigestComposer (DB::table('product_price_snapshots as today') ×2, aliased/join), SupplierFreshnessResolver
  (DB::connection()->getDriverName() + DB::table('supplier_offer_snapshots')).
- Models: ProductPriceSnapshot + SupplierOfferSnapshot exist in app/Domain/Products/Models. NO supplier_sku_cache model.
- Sync tests: SupplierSyncDigestComposerTest, SupplierSyncDigestCommandTest, SupplierFreshnessResolverTest. NONE for SupplierSkuRegistry.
</context>

<interfaces>
=== A. Relocate WooFieldComparator out of Cutover ===
1. FIRST verify the target: read WooFieldComparator's own `use` imports — if it imports any App\Domain\Cutover\*
   class, those would follow it (reconsider). It should be self-contained field-comparison logic.
2. Confirm in deptrac.yaml that BOTH Cutover and Sync are allowed to depend on the chosen target layer:
   - Prefer **Products** (Sync→Products is allowed; ProductObserver already uses it from Products; confirm Cutover→Products is allowed).
   - If Cutover→Products is NOT allowed, use **Foundation** (universally depended-upon).
3. Move the file to `app/Domain/<Target>/Services/WooFieldComparator.php`, change its namespace, and update EVERY
   importer's `use` statement (all 9 sites incl. AppServiceProvider binding). `git mv` to preserve history.
4. No logic change to the class.

=== B. Replace the DB facade in the three Sync services (identical SQL) ===
- NEW `app/Domain/Products/Models/SupplierSkuCache.php` — thin Eloquent model: `protected $table = 'supplier_sku_cache';`
  `public $timestamps = false;` (match the table — check the migration for timestamps), `protected $guarded = [];`
  (bulk insertOrIgnore needs mass-assign open) — mirror ProductPriceSnapshot/SupplierOfferSnapshot conventions.
- SupplierSkuRegistry: `use App\Domain\Products\Models\SupplierSkuCache;` remove `use …Facades\DB;`
  - `DB::table(self::TABLE)->truncate()` → `SupplierSkuCache::query()->truncate()`
  - `DB::table(self::TABLE)->insertOrIgnore($buffer)` → `SupplierSkuCache::query()->insertOrIgnore($buffer)` (both sites)
  (self::TABLE const can stay or go; the model owns the table name now.)
- SupplierSyncDigestComposer: `use App\Domain\Products\Models\ProductPriceSnapshot;` remove the DB import.
  - `DB::table('product_price_snapshots as today')…` → `ProductPriceSnapshot::query()->from('product_price_snapshots as today')…`
    keep the rest of the builder chain (joins/selects/wheres) verbatim. (Query builder methods are identical on the Eloquent builder.)
- SupplierFreshnessResolver: `use App\Domain\Products\Models\SupplierOfferSnapshot;` remove the DB import.
  - `DB::table('supplier_offer_snapshots')…` → `SupplierOfferSnapshot::query()…`
  - `DB::connection()->getDriverName()` → `SupplierOfferSnapshot::query()->getConnection()->getDriverName()`
    (or inject `DatabaseManager`/a connection — but the model-connection form is simplest + facade-free).
  (Leave the docblock mentions of DB:: as prose, or tidy them — no functional impact.)

Do NOT change any SELECT columns, WHERE clauses, joins, ordering, or the insert buffer shape — SQL must be identical.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relocate WooFieldComparator + de-facade the Sync services</name>
  <files>
    app/Domain/Sync/Services/WooProductWriter.php,
    app/Domain/Sync/Services/SupplierSkuRegistry.php,
    app/Domain/Sync/Services/SupplierSyncDigestComposer.php,
    app/Domain/Sync/Services/SupplierFreshnessResolver.php,
    app/Domain/Products/Services/WooFieldComparator.php,
    app/Domain/Products/Models/SupplierSkuCache.php,
    tests/Feature/Sync/SupplierSkuRegistryTest.php
  </files>
  <behavior>
    Apply A + B per <interfaces>. Add a lightweight SupplierSkuRegistryTest (it has none): seed a couple of rows,
    run the registry's rebuild/refresh method, assert supplier_sku_cache is truncated + repopulated (LOWER+TRIM
    keys as today) — this guards the DB→Eloquent swap. Keep the existing Sync tests green (Composer/Freshness/Digest).
    tdd=true for the new SupplierSkuRegistryTest; the WooFieldComparator move + the other conversions are guarded by
    existing tests + deptrac.
  </behavior>
  <action>
    Verify the WooFieldComparator target layer is allowed (read deptrac.yaml). git mv + renamespace + update all
    importers. Create SupplierSkuCache model. Swap the DB:: calls to model query builders. Add the registry test.
    Run deptrac + the Sync tests + pint. Iterate deptrac to 0.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/deptrac analyse --no-progress 2>&1 | tail -8</automated>
    Expected: "0 violations" (or, if any remain, they are NOT Sync→Cutover or Sync→DB — report those in the SUMMARY as out-of-scope pre-existing).
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Architecture 2>&1 | tail -8</automated>
    Expected: the Deptrac*LayerTest + DeptracTest cases now GREEN; the four test-rot files are handled in the sibling 260709-hl0 test-rot task (may still be red if that task hasn't run yet — note, don't fix here).
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Sync tests/Unit/Domain/Sync 2>&1 | tail -10</automated>
    Expected: GREEN — Composer/Freshness/Digest + the new SupplierSkuRegistry test pass (SQL unchanged).
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint app/Domain/Sync/Services/WooProductWriter.php app/Domain/Sync/Services/SupplierSkuRegistry.php app/Domain/Sync/Services/SupplierSyncDigestComposer.php app/Domain/Sync/Services/SupplierFreshnessResolver.php app/Domain/Products/Services/WooFieldComparator.php app/Domain/Products/Models/SupplierSkuCache.php 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - deptrac analyse at 0 (Sync→Cutover + Sync→DB gone); WooFieldComparator relocated + all importers updated; Sync services facade-free via Eloquent models (identical SQL); new SupplierSkuRegistry test + existing Sync tests green; pint clean.
  </done>
</task>

</tasks>

<verification>
1. `deptrac analyse` → 0 violations (or only non-Sync remnants, reported)
2. `pest tests/Architecture` Deptrac cases → GREEN
3. `pest tests/Feature/Sync tests/Unit/Domain/Sync` → GREEN (no SQL/behaviour regression)
4. `pint --test` → PASS

Operator notes (for SUMMARY.md):
- Deploy: push main → `sudo -u stcav /home/stcav/ms.21stcav.com/deploy/deploy.sh` (NO migration — pure code move +
  facade→Eloquent swap; SQL identical).
- What changed + why: a recent Sync refactor broke two locked boundaries — Sync importing the Cutover sink, and
  Sync using the DB facade (SYNC-04). Fixed by relocating WooFieldComparator to <target> and routing Sync's queries
  through Eloquent models. No functional change to supplier sync — the SQL is the same; existing Sync tests prove it.
- If deptrac still reports non-Sync violations after this, they are separate pre-existing drift — list them for a
  follow-up decision; do NOT baseline.
</verification>

<success_criteria>
- deptrac analyse at 0 violations for the Sync→Cutover + Sync→DB edges (14 Deptrac arch tests green); WooFieldComparator relocated out of the Cutover sink with all importers updated; the three Sync services facade-free via Eloquent models with identical SQL; new SupplierSkuRegistry test + existing Sync tests green; no migration; pint clean.
</success_criteria>

<output>
Create `.planning/quick/260709-hl0-fix-real-deptrac-architecture-violations/260709-hl0-SUMMARY.md` documenting the
WooFieldComparator relocation (target layer + why), the DB-facade→Eloquent swaps (per service, SQL-identical), the
new SupplierSkuCache model + SupplierSkuRegistry test, the deptrac before/after, and any remaining non-Sync violations.
</output>
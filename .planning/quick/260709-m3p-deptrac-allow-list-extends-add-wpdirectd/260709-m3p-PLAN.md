---
phase: 260709-m3p-deptrac-allow-list-extends-add-wpdirectd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deptrac.yaml
  - depfile.yaml
must_haves:
  truths:
    - "Five Deptrac allow-list entries are added to BOTH deptrac.yaml AND depfile.yaml (kept byte-identical — the per-layer arch tests shell out to depfile.yaml while the CLI uses deptrac.yaml; the rulesets are documented as dual-config-sync). This clears 64 of the 88 violations, all of which are OPERATOR-APPROVED sanctioned patterns, not real breaches."
    - "The additions: (a) Competitor +WpDirectDb, (b) Products +WpDirectDb, (c) Suggestions +WpDirectDb — read-only metrics / driver-probe reads on the app's OWN MySQL tables (csv_parse_errors, category_audit_findings, supplier_sku_cache, suggestions, driver detection); matches the existing Dashboard/Pricing/Agents/Cutover/Quotes WpDirectDb-in-allow-list precedent. NONE touch WordPress/Woo data, so the CLAUDE.md 'Woo REST only, never direct WP DB writes' hard constraint is NOT breached."
    - "(d) ProductAutoCreate +Integrations and (e) Integrations +ProductAutoCreate — the two arrows of the credential-resolver pattern: the enrichment clients (EanSearchClient/IcecatClient/WebImageSearchClient) resolve credentials via IntegrationCredentialResolver + return IntegrationTestResult, and TestIntegrationAction dispatches to their testConnection(). This mirrors the existing Integrations↔Sync and Integrations↔CRM symmetry (both already carry +Integrations); ProductAutoCreate's allow-list simply never got it."
    - "CONFIG-ONLY — no application code changes, no behaviour change, no migration. After this, `deptrac analyse` drops from 88 to ~24 (the remaining refactor groups, handled in the sibling 260709-m3p-deptrac-refactors task). Layers whose ONLY violations were these extends go green; layers that ALSO have refactor violations (ProductAutoCreate still has →Agents; Suggestions still has →Competitor/→ProductAutoCreate) stay red until the refactor task."
  artifacts:
    - path: "deptrac.yaml"
      provides: "5 allow-list extends"
      contains: "WpDirectDb"
    - path: "depfile.yaml"
      provides: "identical 5 allow-list extends (dual-config-sync)"
      contains: "WpDirectDb"
  key_links: []
---

<objective>
Apply the 5 operator-approved Deptrac allow-list extensions (sanctioned patterns, no hard-constraint breach) to
BOTH deptrac.yaml + depfile.yaml, clearing 64 of the 88 violations. Config-only; the 24 real-breach violations
are fixed separately in 260709-m3p-deptrac-refactors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-m3p-deptrac-allow-list-extends-add-wpdirectd/
@CLAUDE.md
@deptrac.yaml
@depfile.yaml
---
Both YAMLs carry identical layer defs + rulesets ("MIRRORS depfile.yaml — dual-config-sync"). Edit BOTH; the CLI
(`deptrac analyse`) reads deptrac.yaml, the arch tests (tests/Architecture/Deptrac*LayerTest) read depfile.yaml.
Current rulesets (from triage — confirm exact syntax in the `ruleset:` section; entries are the layer's allowed
dependency tokens, and `WpDirectDb` is a token that, when listed, permits the DB facade — precedent: Dashboard,
Pricing, Agents, Cutover, Quotes all list WpDirectDb):
- Competitor:       [Foundation, Pricing, Products, Suggestions, Webhooks, Alerting]
- Products:         [Foundation, ProductAutoCreate]
- Suggestions:      [Foundation]
- ProductAutoCreate:[Foundation, Products, Pricing, Sync, Suggestions, Alerting, Webhooks]
- Integrations:     [Foundation, Sync, CRM, Agents]
All 24 DB-facade violations touch app-own MySQL only (zero WP/Woo) — verified in triage.
</context>

<interfaces>
In BOTH deptrac.yaml and depfile.yaml, in the `ruleset:` section, add these tokens to each layer's allowed list
(match the file's existing formatting for each entry — likely `- Foundation` style list items under the layer key):
- Competitor:        add `WpDirectDb`
- Products:          add `WpDirectDb`
- Suggestions:       add `WpDirectDb`
- ProductAutoCreate: add `Integrations`
- Integrations:      add `ProductAutoCreate`
Add a short trailing comment on each new entry noting it's an operator-approved sanctioned pattern (260709: read-only
app-DB reads / credential-resolver pattern) so the loosening is documented, not silent. Do NOT change any layer
definitions, any OTHER ruleset, or remove any `-WpDirectDb` hard-deny (Sync keeps its deny — that's fixed by refactor).
Keep deptrac.yaml and depfile.yaml byte-identical in the edited rulesets.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: add the 5 allow-list extends to both YAMLs</name>
  <files>
    deptrac.yaml,
    depfile.yaml
  </files>
  <behavior>
    Add the 5 entries per <interfaces> to BOTH files identically. tdd=false — deptrac itself is the verifier.
  </behavior>
  <action>
    Edit both YAMLs. Run deptrac analyse. Diff the two rulesets to confirm they match. Run the Deptrac arch tests.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/deptrac analyse --no-progress 2>&1 | grep -iE "Violations" | tail -2</automated>
    Expected: Violations ≈ 24 (down from 88 — the 5 extends cleared 64). If NOT ~24, report the actual number + which unexpected violations remain.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Architecture/DeptracCompetitorLayerTest.php tests/Architecture/DeptracProductAutoCreateLayerTest.php 2>&1 | tail -8</automated>
    Expected: DeptracCompetitorLayerTest GREEN (its only violations were the WpDirectDb extend). DeptracProductAutoCreateLayerTest MAY still be red (PAC→Agents refactor remains) — that's expected, note it.
    <automated>diff <(sed -n '/^ruleset:/,$p' deptrac.yaml) <(sed -n '/^ruleset:/,$p' depfile.yaml) && echo "RULESETS IDENTICAL" || echo "RULESETS DIFFER (fix — must match)"</automated>
    Expected: "RULESETS IDENTICAL".
  </verify>
  <done>
    - 5 extends added to both YAMLs identically; deptrac ≈24; fully-cleared layer tests green; rulesets byte-identical; no code/behaviour change.
  </done>
</task>

</tasks>

<verification>
1. `deptrac analyse` → ~24 violations (64 cleared)
2. Fully-cleared layer tests (Competitor etc.) → GREEN
3. deptrac.yaml/depfile.yaml rulesets identical

Operator notes (for SUMMARY.md):
- Deploy: no runtime effect (arch-lint config only); ships with the next deploy harmlessly.
- Records WHY each extend is legitimate (credential-resolver pattern + read-only app-DB reads; precedent cited);
  the remaining ~24 are real breaches fixed in 260709-m3p-deptrac-refactors (relocate publishers/ClaudeClient/commands,
  route Suggestions Filament reads via Http, Sync→Eloquent).
</verification>

<success_criteria>
- 5 operator-approved allow-list extends applied identically to deptrac.yaml + depfile.yaml (documented rationale); deptrac ≈24; no code/behaviour change; the 64 sanctioned-pattern violations cleared.
</success_criteria>

<output>
Create `.planning/quick/260709-m3p-deptrac-allow-list-extends-add-wpdirectd/260709-m3p-SUMMARY.md` documenting the 5 extends (each with its sanctioned-pattern justification + precedent), the 88→~24 drop, dual-YAML sync, and that the remainder is the refactor task.
</output>
---
phase: 260709-m3p-deptrac-refactors-move-woogtin-gallery-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Domain/ProductAutoCreate/Services/WooGtinPublisher.php
  - app/Domain/ProductAutoCreate/Services/WooGalleryPublisher.php
  - app/Domain/ProductAutoCreate/Services/WooBrandPublisher.php
  - app/Domain/ProductAutoCreate/Listeners/PushProductFieldsToWoo.php
  - app/Domain/ProductAutoCreate/Commands/ScanSupplierAddCandidatesCommand.php
  - app/Domain/Integrations/Clients/ClaudeClient.php
  - app/Domain/Products/Models/SupplierFreshnessSnapshot.php
  - app/Domain/Sync/Console/Commands/CheckStaleSuppliersCommand.php
  - app/Domain/Products/Models/SupplierOfferSnapshot.php
must_haves:
  truths:
    - "The CLEAN Deptrac refactors (operator-approved, no allow-list change) drop `deptrac analyse` from 24 to ~8. Namespaced moves + Eloquent swaps only — IDENTICAL runtime behaviour, guarded by the existing + moved tests. The remaining ~8 are the Filament-in-domain cross-reads, handled in a SEPARATE follow-up task (do NOT attempt them here)."
    - "Products→Sync (7) fixed: WooGtinPublisher / WooGalleryPublisher / WooBrandPublisher move from app/Domain/Products/Services to app/Domain/ProductAutoCreate/Services (PAC is allowed to use Sync's WooClient); PushProductFieldsToWoo listener moves Products→ProductAutoCreate (allowed to use WooProductWriter) with its event registration updated; and Products\\Models\\SupplierOfferSnapshot no longer reaches into Sync's SupplierFreshnessResolver (that read is inverted — the resolver is called by the caller and fresh-ids passed in, or the method moves out of the model)."
    - "ProductAutoCreate→Agents (2 of 4) fixed: Agents\\Clients\\ClaudeClient moves to Integrations\\Clients (it is an external-API client like WooClient/BitrixClient; Integrations already resolves its api_key). PAC→Integrations (allow-listed in the extends task) + Agents→Integrations (already allowed) both cover the moved client. ALL ClaudeClient importers updated. (The other 2 PAC→Agents — EditAutoCreateReview→SeoContentPatchApplier — are Filament, deferred to the follow-up.)"
    - "Sync→Pricing (2) fixed: ScanSupplierAddCandidatesCommand moves Sync→ProductAutoCreate (PAC allows BOTH Pricing and Sync); command signature/registration unchanged in effect. Sync→DB (5) fixed: CheckStaleSuppliersCommand stops using the DB facade — reads via SupplierOfferSnapshot::query(), writes supplier_freshness_snapshots via a NEW SupplierFreshnessSnapshot Eloquent model, and wraps in a connection-level transaction (model->getConnection()->transaction(...)) — NO Illuminate\\Support\\Facades\\DB, SYNC-04 deny preserved. Identical SQL."
    - "Driver-portable (SQLite tests / MariaDB prod). No migration (tables already exist). git mv preserves history. All affected test suites (Products, ProductAutoCreate, Sync, Agents, the publisher + command tests) stay green — the moves are namespace-only + the Eloquent swaps are SQL-identical."
  artifacts:
    - path: "app/Domain/ProductAutoCreate/Services/WooGtinPublisher.php"
      provides: "publisher relocated to PAC (no Products→Sync)"
      contains: "namespace App\\Domain\\ProductAutoCreate\\Services"
    - path: "app/Domain/Integrations/Clients/ClaudeClient.php"
      provides: "ClaudeClient relocated to Integrations"
      contains: "namespace App\\Domain\\Integrations\\Clients"
    - path: "app/Domain/Products/Models/SupplierFreshnessSnapshot.php"
      provides: "Eloquent model replacing DB::table('supplier_freshness_snapshots')"
      contains: "supplier_freshness_snapshots"
  key_links: []
---

<objective>
Clear the CLEAN 16 of the 24 remaining Deptrac violations via namespaced moves + Eloquent swaps (identical
behaviour): relocate the 3 Woo publishers + PushProductFieldsToWoo to ProductAutoCreate, move ClaudeClient to
Integrations, move ScanSupplierAddCandidatesCommand to ProductAutoCreate, de-facade CheckStaleSuppliersCommand,
and invert the SupplierOfferSnapshot→SupplierFreshnessResolver read. Deptrac 24→~8. The 8 Filament-in-domain
cross-reads (Suggestions→Competitor/PAC, EditAutoCreateReview→Agents applier, Pricing ops page→Competitor) are a
SEPARATE follow-up — do not touch them here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-m3p-deptrac-refactors-move-woogtin-gallery-b/
@CLAUDE.md
@deptrac.yaml
@app/Domain/Products/Services/WooGtinPublisher.php
@app/Domain/Products/Services/WooGalleryPublisher.php
@app/Domain/Products/Services/WooBrandPublisher.php
@app/Domain/Products/Listeners/PushProductFieldsToWoo.php
@app/Domain/Sync/Commands/ScanSupplierAddCandidatesCommand.php
@app/Domain/Sync/Console/Commands/CheckStaleSuppliersCommand.php
@app/Domain/Products/Models/SupplierOfferSnapshot.php
@app/Domain/Products/Models/ProductPriceSnapshot.php
---
`deptrac analyse` = 24 after the extends task. This task targets 16 of them (the other 8 are Filament — leave).
Verified importers/locations:
- Publishers imported by (update their `use`): app/Console/Commands/{BackfillMerchantFeed, PublishSourcedBrands,
  PublishSourcedEans, PublishSourcedImages, SourceProductImages}Command + tests/Feature/Products/{WooGtinPublisher,
  WooGalleryPublisher, WooBrandPublisher, PublishSourcedBrandsCommand, PublishSourcedEansCommand, PublishSourcedImagesCommand}Test.
- PushProductFieldsToWoo: app/Domain/Products/Listeners/ — find its event registration (EventServiceProvider or a
  #[AsEventListener]/provider `$listen` map) + update the namespace there.
- ClaudeClient: currently app/Domain/Agents/Clients/ClaudeClient.php — grep ALL importers (ProductImageVisionValidator
  in PAC + Agents-internal users); Agents allow-list ALREADY includes Integrations, PAC now includes Integrations.
- ScanSupplierAddCandidatesCommand: app/Domain/Sync/Commands/ → move to app/Domain/ProductAutoCreate/Commands/.
- CheckStaleSuppliersCommand: app/Domain/Sync/Console/Commands/ — uses DB::table('supplier_offer_snapshots') read,
  DB::transaction, DB::table('supplier_freshness_snapshots')->truncate()/insert(). SupplierOfferSnapshot model exists
  (Products); create SupplierFreshnessSnapshot model (Products, mirror the existing snapshot models — check the
  migration for columns/timestamps; $guarded=[] for bulk insert).
- SupplierOfferSnapshot.php line ~68 calls `app(\App\Domain\Sync\Services\SupplierFreshnessResolver::class)->…` —
  a Products MODEL reaching a Sync SERVICE. Read the method + its callers; invert (resolver called by the caller,
  fresh-ids passed into a scope; or move the method to a service). Keep behaviour identical.
</context>

<interfaces>
=== A. Move the 3 publishers Products→ProductAutoCreate ===
`git mv app/Domain/Products/Services/Woo{Gtin,Gallery,Brand}Publisher.php app/Domain/ProductAutoCreate/Services/`;
change each `namespace App\Domain\Products\Services;` → `App\Domain\ProductAutoCreate\Services;`; update every
importer's `use` (5 commands + 6 tests listed above). No logic change. (They call Sync\Services\WooClient — allowed from PAC.)

=== B. Move PushProductFieldsToWoo Products→ProductAutoCreate ===
`git mv` to app/Domain/ProductAutoCreate/Listeners/, renamespace, update the event-listener registration + any `use`.

=== C. Move ClaudeClient Agents→Integrations ===
`git mv app/Domain/Agents/Clients/ClaudeClient.php app/Domain/Integrations/Clients/ClaudeClient.php`; renamespace to
`App\Domain\Integrations\Clients`; update ALL importers (grep first) incl. the container binding in AppServiceProvider
if present. No logic change.

=== D. Move ScanSupplierAddCandidatesCommand Sync→ProductAutoCreate ===
`git mv app/Domain/Sync/Commands/ScanSupplierAddCandidatesCommand.php app/Domain/ProductAutoCreate/Commands/`;
renamespace; keep the `products:...`/`suppliers:...` signature string EXACTLY; update any registration/`use`.

=== E. De-facade CheckStaleSuppliersCommand ===
- NEW app/Domain/Products/Models/SupplierFreshnessSnapshot.php (mirror SupplierOfferSnapshot; `$table='supplier_freshness_snapshots'`,
  timestamps per migration, `$guarded=[]`).
- Replace `DB::table('supplier_offer_snapshots')…` → `SupplierOfferSnapshot::query()…` (identical chain).
- Replace `DB::table('supplier_freshness_snapshots')->truncate()/->insert($rows)` → `SupplierFreshnessSnapshot::query()->truncate()` / `->insert($rows)`.
- Replace `DB::transaction(fn () => …)` → `SupplierFreshnessSnapshot::query()->getConnection()->transaction(fn () => …)` (facade-free).
- Remove `use Illuminate\Support\Facades\DB;`. Keep the Schema facade if used (not the banned one). Identical SQL.

=== F. Invert SupplierOfferSnapshot → SupplierFreshnessResolver ===
Remove the `app(SupplierFreshnessResolver::class)` call from the Products model. Move the freshness resolution to the
caller (inject/resolve the Sync resolver there and pass the fresh ids into the model as a query scope arg), OR relocate
the whole method to a Sync/PAC service. Choose the minimal inversion that keeps callers' results identical; update callers + tests.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: clean Deptrac refactors (moves + Eloquent + inversion)</name>
  <files>
    app/Domain/ProductAutoCreate/Services/WooGtinPublisher.php,
    app/Domain/ProductAutoCreate/Services/WooGalleryPublisher.php,
    app/Domain/ProductAutoCreate/Services/WooBrandPublisher.php,
    app/Domain/ProductAutoCreate/Listeners/PushProductFieldsToWoo.php,
    app/Domain/ProductAutoCreate/Commands/ScanSupplierAddCandidatesCommand.php,
    app/Domain/Integrations/Clients/ClaudeClient.php,
    app/Domain/Products/Models/SupplierFreshnessSnapshot.php,
    app/Domain/Sync/Console/Commands/CheckStaleSuppliersCommand.php,
    app/Domain/Products/Models/SupplierOfferSnapshot.php
  </files>
  <behavior>
    Apply A–F. Prefer `git mv` for the moves (history + it updates the path; then edit namespace + importers).
    After each group, re-run `deptrac analyse` to watch the count fall. The existing tests (publisher tests, command
    tests, Sync/Agents/PAC suites) are the behaviour guard — keep them all green (update their `use` statements for
    the moved classes). For F, add/keep a test proving the freshness result is unchanged. tdd=true: run tests
    continuously; the moves must not change any outcome.
  </behavior>
  <action>
    Do the 6 groups, updating all importers + registrations. Run deptrac (expect ~8 left — the Filament group), the
    affected suites, and pint. Iterate until deptrac shows ONLY the 8 Filament violations.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/deptrac analyse --no-progress 2>&1 | grep -iE "Violations" | tail -2</automated>
    Expected: Violations ≈ 8, and NONE of them Products→Sync / PAC→Agents(ClaudeClient) / Sync→Pricing / Sync→DB. The ~8 remaining must be ONLY the Filament cross-reads (Suggestions→Competitor/PAC, EditAutoCreateReview→SeoContentPatchApplier, Pricing→Competitor). Report the exact remaining list.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Products tests/Feature/ProductAutoCreate tests/Feature/Sync tests/Unit/Domain/Sync 2>&1 | tail -8</automated>
    Expected: GREEN — publisher tests (now under PAC), command tests, Sync suites all pass; no behaviour change.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint app/Domain/ProductAutoCreate/Services/WooGtinPublisher.php app/Domain/ProductAutoCreate/Services/WooGalleryPublisher.php app/Domain/ProductAutoCreate/Services/WooBrandPublisher.php app/Domain/ProductAutoCreate/Listeners/PushProductFieldsToWoo.php app/Domain/ProductAutoCreate/Commands/ScanSupplierAddCandidatesCommand.php app/Domain/Integrations/Clients/ClaudeClient.php app/Domain/Products/Models/SupplierFreshnessSnapshot.php app/Domain/Sync/Console/Commands/CheckStaleSuppliersCommand.php app/Domain/Products/Models/SupplierOfferSnapshot.php 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - Publishers + PushProductFieldsToWoo in PAC; ClaudeClient in Integrations; ScanSupplierAddCandidates in PAC; CheckStaleSuppliers facade-free; SupplierOfferSnapshot no longer calls the Sync resolver; deptrac ≈8 (Filament-only remaining, listed); all affected tests green; pint clean.
  </done>
</task>

</tasks>

<verification>
1. `deptrac analyse` → ~8, remaining are ONLY the Filament cross-reads (listed)
2. `pest` Products/ProductAutoCreate/Sync → GREEN
3. `pint --test` → PASS

Operator notes (for SUMMARY.md):
- Deploy: push main → `sudo -u stcav /home/stcav/ms.21stcav.com/deploy/deploy.sh` (NO migration — namespace moves +
  facade→Eloquent, identical SQL). Command signatures unchanged (ScanSupplierAddCandidates keeps its name).
- Confirms which classes moved layer + why; lists the ~8 Filament cross-reads left for the follow-up task
  (260709 deptrac Filament refactor) — those need UI-layer relocation / event-routing, handled separately so the
  admin UI isn't put at risk in the same pass.
</verification>

<success_criteria>
- The 16 clean Deptrac violations cleared via namespaced moves + Eloquent swaps + the model inversion (identical behaviour); deptrac ≈8 with only the Filament cross-reads remaining (listed); all affected test suites green; no migration; pint clean.
</success_criteria>

<output>
Create `.planning/quick/260709-m3p-deptrac-refactors-move-woogtin-gallery-b/260709-m3p-SUMMARY.md` documenting each move (from→to layer + why), the CheckStaleSuppliers Eloquent swap, the SupplierOfferSnapshot inversion, deptrac 24→~8, and the exact remaining Filament violations for the follow-up.
</output>
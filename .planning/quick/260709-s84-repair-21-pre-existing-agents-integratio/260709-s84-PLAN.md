---
phase: 260709-s84-repair-21-pre-existing-agents-integratio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Domain/Agents/Services/AgentRunGdprScrubber.php
  - app/Domain/Agents/Console/Commands/AgentsGdprPurgeLangfuseCommand.php
  - tests/Feature/Agents/RunPricingAgentJobTest.php
  - tests/Feature/Agents/ClaudeClientTest.php
  - tests/Feature/Agents/PricingAgentCalibrationTest.php
  - tests/Feature/Agents/AgentRunGdprScrubberTest.php
  - tests/Feature/Integrations/IntegrationCredentialKindEnumTest.php
  - tests/Feature/Integrations/IntegrationHealthWidgetTest.php
must_haves:
  truths:
    - "The 21 pre-existing Agents/Integrations failures go to 0 across 5 root-cause buckets. ONE genuine code fix (a SQLite↔MariaDB driver-portability bug in the GDPR scrubber); the rest are test-rot (missing test-env credential, enum outgrew its assertions, in-test queued-listener serialization). No behaviour change to production beyond the GDPR query becoming driver-portable."
    - "Bucket 1 (7, CODE): AgentRunGdprScrubber (+ AgentsGdprPurgeLangfuseCommand) used `whereRaw('JSON_SEARCH(LOWER(tool_calls), ?, ?) IS NOT NULL', ['one', $email])` — JSON_SEARCH is MariaDB-only, absent on SQLite, so the scrubber query threw on the test DB (and was untested). Replace with the driver-portable `whereRaw('LOWER(tool_calls) LIKE ?', ['%'.$email.'%'])`, keeping the existing `orWhere('agent_reasoning_summary','like',...)`. Same intent (find agent_runs whose tool_calls mention the email); broader-or-equal match (SAFER for GDPR erasure — never misses). Works on both SQLite + MariaDB. (SuggestionResource also uses JSON_SEARCH but its tests pass and it is OUT OF SCOPE here — note it as a latent follow-up.)"
    - "Bucket 2 (7, TEST): tests that invoke ClaudeClient (RunPricingAgentJobTest, ClaudeClientTest, PricingAgentCalibrationTest) threw IntegrationCredentialMissingException('anthropic_api') because neither a DB credential nor the env fallback config('prism.providers.anthropic.api_key') was set in the test env. Provision it in each setup — mirror the PASSING ClaudeClientResolverIntegrationTest pattern (config()->set('prism.providers.anthropic.api_key','test-...') OR IntegrationCredential::factory()->kind(IntegrationCredentialKind::AnthropicApi)->create(['api_key'=>'test-...']))."
    - "Bucket 3 (3, TEST): RunPricingAgentJobTest Fixtures 2 & 3 call the job's handle() WITHOUT faking events, so the real ShouldQueue agent notify-listeners try to serialize in-test → 'Serialization of Closure is not allowed'. Add Queue::fake() (or Event::fake of the agent events, mirroring Fixture 1) so the queued listeners are recorded not serialized. This is a test-env artifact, NOT a prod bug (prod listener deps are real/serializable-via-container)."
    - "Buckets 4+5 (4, TEST): the IntegrationCredentialKind enum has 10 cases now (grew past when the tests were written). IntegrationCredentialKindEnumTest asserted 7 and IntegrationHealthWidgetTest asserted 5 tiles. Update BOTH to derive the expected count from count(IntegrationCredentialKind::cases()) so they never rot again, rather than hard-coding 10."
  artifacts:
    - path: "app/Domain/Agents/Services/AgentRunGdprScrubber.php"
      provides: "driver-portable email-in-tool_calls match (no JSON_SEARCH)"
      contains: "LIKE"
    - path: "tests/Feature/Integrations/IntegrationCredentialKindEnumTest.php"
      provides: "enum-count assertion derived from cases()"
      contains: "cases()"
  key_links: []
---

<objective>
Take the pre-existing Agents/Integrations test rot (21 failures) to 0 so `pest` is trustworthy end-to-end.
One real driver-portability fix in the GDPR scrubber (SQLite↔MariaDB trap — [[meetingstore-sqlite-mariadb-strict-trap]]);
the rest are test-rot (missing test credential, enum outgrew its tests, in-test queued-listener serialization).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-s84-repair-21-pre-existing-agents-integratio/
@CLAUDE.md
@app/Domain/Agents/Services/AgentRunGdprScrubber.php
@app/Domain/Agents/Console/Commands/AgentsGdprPurgeLangfuseCommand.php
@app/Domain/Integrations/Services/IntegrationCredentialResolver.php
@app/Domain/Integrations/Enums/IntegrationCredentialKind.php
@tests/Feature/Agents/RunPricingAgentJobTest.php
@tests/Feature/Agents/ClaudeClientTest.php
@tests/Feature/Agents/PricingAgentCalibrationTest.php
@tests/Feature/Agents/AgentRunGdprScrubberTest.php
@tests/Feature/Integrations/IntegrationCredentialKindEnumTest.php
@tests/Feature/Integrations/IntegrationHealthWidgetTest.php
@tests/Feature/Integrations/ClaudeClientResolverIntegrationTest.php
---
Verified failure buckets (21): JSON_SEARCH-on-SQLite ×7; no anthropic_api credential ×7; closure-serialization ×3;
widget 5≠10 ×3; enum 7≠10 ×1.
- Resolver env fallback for anthropic_api = `config('prism.providers.anthropic.api_key','')` (empty in tests → throws).
- PASSING pattern to copy (ClaudeClientResolverIntegrationTest): `config()->set('prism.providers.anthropic.api_key', ...)`
  and/or `IntegrationCredential::factory()->kind(IntegrationCredentialKind::AnthropicApi)->create([...])`.
- IntegrationCredentialKind::cases() = 10.
- RunPricingAgentJobTest: Fixture 1 uses Event::fake([AgentRunStarted, AgentRunCompleted]); Fixtures 2 & 3 call
  ->handle() directly with no fake (source of the closure serialization via the ShouldQueue Notify* listeners).
</context>

<interfaces>
=== Bucket 1 — driver-portable GDPR match (CODE) ===
In AgentRunGdprScrubber (the class AgentRunGdprScrubberTest exercises) AND AgentsGdprPurgeLangfuseCommand, replace:
`->whereRaw('JSON_SEARCH(LOWER(tool_calls), ?, ?) IS NOT NULL', ['one', $normalised])`
with:
`->whereRaw('LOWER(tool_calls) LIKE ?', ['%'.$normalised.'%'])`
Keep the sibling `->orWhere('agent_reasoning_summary', 'like', '%'.$normalised.'%')` and the surrounding grouping
exactly. (Do NOT touch SuggestionResource's JSON_SEARCH — out of scope; note it as a latent follow-up.)

=== Bucket 2 — provision the test credential (TEST) ===
In RunPricingAgentJobTest, ClaudeClientTest, PricingAgentCalibrationTest setup (beforeEach/at the top), provision
the anthropic credential the resolver needs — mirror ClaudeClientResolverIntegrationTest:
`config()->set('prism.providers.anthropic.api_key', 'test-key');`  (simplest env-fallback), and/or the
IntegrationCredential::factory()->kind(IntegrationCredentialKind::AnthropicApi)->create(['api_key' => 'test-key'])
row if the test asserts the DB-credential path specifically. Use whichever the individual test's intent needs.

=== Bucket 3 — stop in-test queued-listener serialization (TEST) ===
In RunPricingAgentJobTest Fixtures 2 & 3 (and any other fixture calling ->handle() without faking), add
`Queue::fake();` (or `Event::fake([AgentRunStarted::class, AgentRunCompleted::class, /* + any Agent notify events */])`
mirroring Fixture 1) so the ShouldQueue Notify* listeners are recorded, not serialized. Preserve each fixture's
existing assertions (they assert on the AgentRun row / suggestion evidence, not on the listeners).

=== Buckets 4+5 — derive enum counts (TEST) ===
- IntegrationCredentialKindEnumTest: change the hard-coded `7` to `count(IntegrationCredentialKind::cases())`
  (import the enum). If it also asserts a specific SET of cases, update that set to the current 10 (or assert via cases()).
- IntegrationHealthWidgetTest: change the expected tile count `5` (+ the "must render 5 tiles" message) to
  `count(IntegrationCredentialKind::cases())` so it tracks the enum. Keep the "one tile per integration kind" intent.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repair the 21 Agents/Integrations failures</name>
  <files>
    app/Domain/Agents/Services/AgentRunGdprScrubber.php,
    app/Domain/Agents/Console/Commands/AgentsGdprPurgeLangfuseCommand.php,
    tests/Feature/Agents/RunPricingAgentJobTest.php,
    tests/Feature/Agents/ClaudeClientTest.php,
    tests/Feature/Agents/PricingAgentCalibrationTest.php,
    tests/Feature/Agents/AgentRunGdprScrubberTest.php,
    tests/Feature/Integrations/IntegrationCredentialKindEnumTest.php,
    tests/Feature/Integrations/IntegrationHealthWidgetTest.php
  </files>
  <behavior>
    Apply the 5 buckets per <interfaces>. Bucket 1 is a code fix proven by AgentRunGdprScrubberTest (now runs on
    SQLite + still finds the email in tool_calls — verify its existing assertions pass, adjust ONLY if the test
    hard-codes JSON_SEARCH-specific behaviour). Buckets 2-5 are test-side. For each bucket, confirm the specific
    failing cases go green. Do NOT change production behaviour beyond the GDPR query portability.
  </behavior>
  <action>
    Fix the GDPR scrubber + command query; provision the anthropic credential in the 3 test setups; Queue::fake the
    handle()-calling fixtures; derive the enum/widget counts from cases(). Run the whole Agents/Integrations set + pint.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Agents tests/Feature/Integrations tests/Unit/Domain/Agents 2>&1 | tail -8</automated>
    Expected: 0 failed (was 21 failed / 283 passed → ~304 passed). Report the Tests: line.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/Agents/AgentRunGdprScrubberTest.php 2>&1 | tail -6</automated>
    Expected: GREEN — the scrubber now runs on SQLite and matches the email-bearing tool_calls rows.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint app/Domain/Agents/Services/AgentRunGdprScrubber.php app/Domain/Agents/Console/Commands/AgentsGdprPurgeLangfuseCommand.php 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - All 21 failures green; GDPR scrubber query driver-portable (JSON_SEARCH→LIKE); anthropic credential provisioned in the 3 tests; handle()-fixtures Queue::faked; enum/widget counts derived from cases(); no prod behaviour change beyond query portability; pint clean.
  </done>
</task>

</tasks>

<verification>
1. `pest tests/Feature/Agents tests/Feature/Integrations tests/Unit/Domain/Agents` → 0 failed
2. `pest AgentRunGdprScrubberTest` → GREEN
3. `pint --test` → PASS

Operator notes (for SUMMARY.md):
- Deploy: push main → `sudo -u stcav /home/stcav/ms.21stcav.com/deploy/deploy.sh` (NO migration). The only prod
  change is the GDPR scrubber/purge query now uses a driver-portable LOWER(tool_calls) LIKE (was MariaDB-only
  JSON_SEARCH) — same/broader match; GDPR erasure now also testable on SQLite CI.
- Pure test-rot fixed: missing anthropic test credential, IntegrationCredentialKind enum outgrew its count
  assertions (now derived from cases()), and RunPricingAgentJob fixtures faked their queued listeners.
- Note the latent follow-up: SuggestionResource also uses JSON_SEARCH (not failing today — driver-guarded or not
  hit on SQLite) — worth a portability pass someday.
</verification>

<success_criteria>
- All 21 Agents/Integrations failures green; the GDPR scrubber query is driver-portable (real fix); the other 4 buckets fixed test-side (credential provisioned, enum/widget counts derived from cases(), queued listeners faked); no prod behaviour change beyond query portability; pint clean.
</success_criteria>

<output>
Create `.planning/quick/260709-s84-repair-21-pre-existing-agents-integratio/260709-s84-SUMMARY.md` documenting each bucket (root cause + fix, test-rot vs code), the driver-portable GDPR change, before/after counts, and the SuggestionResource JSON_SEARCH latent follow-up.
</output>
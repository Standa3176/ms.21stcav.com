---
phase: 260710-j2m-fix-18-deterministic-test-rot-from-full-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/CRM/BitrixClientExceptionClassificationTest.php
  - tests/Feature/CRM/BitrixClientShadowModeTest.php
  - tests/Feature/CRM/SyncDiffsProviderColumnTest.php
  - tests/Feature/Competitor/NewProductOpportunityApproveActionTest.php
  - tests/Feature/Console/BackfillMerchantFeedCommandTest.php
  - tests/Feature/Dashboard/DashboardRefreshCommandTest.php
  - tests/Feature/Dashboard/GlobalSearchTest.php
  - tests/Feature/Dashboard/NotificationCentrePageTest.php
  - tests/Feature/Dashboard/QueuedCsvExportJobTest.php
  - tests/Feature/Dashboard/WidgetDataSourceTest.php
  - tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php
  - tests/Feature/DryRunModeTest.php
  - tests/Feature/ExampleTest.php
  - tests/Feature/FailedJobAlertTest.php
  - tests/Feature/HorizonSupervisorTest.php
  - tests/Feature/IntegrationLoggerTest.php
  - tests/Feature/Phase02DataModelTest.php
  - tests/Feature/ShadowModeTest.php
  - app/Domain/Cutover/Services/OverridePopulator.php
  - tests/Feature/Cutover/PopulateOverridesCommandTest.php
must_haves:
  truths:
    - "18 DETERMINISTIC test-rot failures (order-independent, fail even run-alone) go green + 1 REAL prod-crash bug is fixed. All 18 are test-side; the 1 real fix is OverridePopulator. Does NOT touch the 8 isolation-only files, or the 2 flagged real bugs still under decision (#11 IntegrationEventPolicy, #22 quote-PDF snapshot)."
    - "The real bug (#13): OverridePopulator creates PIN-ONLY ProductOverride rows WITHOUT margin_basis_points, but that column is NOT NULL (create_product_overrides migration `$t->integer('margin_basis_points')`, no default) → `cutover:populate-overrides --live` crashes in prod (MySQL strict) on any pin-only override. Fix: set `margin_basis_points => 0` in the create attrs (matches the gy0/FieldPinManager pins-only convention). No migration."
    - "The 18 test-rot fixes are each exact + mechanical (constructor-signature drift now needs IntegrationCredentialResolver / WooGtinPublisher; MySQL-only `SHOW INDEX` → driver-agnostic Schema::getIndexes; wrong class FQNs; drifted metric counts 11→15 + supervisor 7→8; toHaveKey 2nd-arg misuse; abstract Livewire\\Component instantiation; Mockery on a final class; a SupplierClient stub missing `extends`; ExampleTest 200→302-redirect; NullNotifiable; a sqlite drop-indexed-column rollback guarded to MySQL). No production code changes except OverridePopulator."
  artifacts:
    - path: "app/Domain/Cutover/Services/OverridePopulator.php"
      provides: "pin-only override create sets margin_basis_points=0 (no NOT-NULL crash)"
      contains: "margin_basis_points"
    - path: "tests/Feature/ExampleTest.php"
      provides: "asserts the /→/admin redirect"
      contains: "assertRedirect"
  key_links: []
---

<objective>
Green the 18 deterministic (order-independent) test-rot failures from the full-suite sweep + fix the one genuine
prod-crash bug they surfaced (OverridePopulator's NOT-NULL margin crash). Leave the 8 isolation-only files and the
2 decision-pending real bugs (#11 IntegrationEventPolicy registration, #22 quote-PDF snapshot) untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260710-j2m-fix-18-deterministic-test-rot-from-full-/
@CLAUDE.md
@app/Domain/Cutover/Services/OverridePopulator.php
@database/migrations/2026_04_19_090100_create_product_overrides_table.php
---
All 18 verdicts + exact fixes are from a per-file isolation run (classifier). Constructor drifts: BitrixClient now
`(…, IntegrationCredentialResolver $resolver)`; BackfillMerchantFeedCommand ctor gained `WooGtinPublisher`; WooClient
`(IntegrationLogger, IntegrationCredentialResolver, ?AutomatticClient)`. Resolver FQN =
`App\Domain\Integrations\Services\IntegrationCredentialResolver`. OverridePopulator pin-only create (~lines 118-126)
omits margin_basis_points (NOT NULL) — set it to 0.
</context>

<interfaces>
Apply each exact fix (test-side unless noted):
1. **CRM/BitrixClientExceptionClassificationTest** (~L37/39/41): add `IntegrationCredentialResolver` — pass `app(\App\Domain\Integrations\Services\IntegrationCredentialResolver::class)` as the new 2nd ctor arg; add the ctor param; `parent::__construct($logger, $resolver);`.
2. **CRM/BitrixClientShadowModeTest** (~L31): anon `extends BitrixClient` — pass `app(...IntegrationCredentialResolver::class)` as the 2nd arg (inherits parent ctor).
3. **CRM/SyncDiffsProviderColumnTest** (~L55-56): replace `DB::select('SHOW INDEX FROM sync_diffs …')` with `collect(\Schema::getIndexes('sync_diffs'))->filter(fn($i)=>in_array('provider',$i['columns']))` then `expect(...->count())->toBeGreaterThanOrEqual(1)`.
4. **Competitor/NewProductOpportunityApproveActionTest** (~L108): FQN `App\Domain\Competitor\Appliers\NewProductOpportunityApplier` → `App\Domain\ProductAutoCreate\Appliers\NewProductOpportunityApplier`.
5. **Console/BackfillMerchantFeedCommandTest** (~L642/651/654): add `use App\Domain\ProductAutoCreate\Services\WooGtinPublisher;`; add `app(WooGtinPublisher::class),` to the `new class(...)` args (after AdCandidateScanner); add ctor param `WooGtinPublisher $gtinPublisher,`; append `, $gtinPublisher` to the `parent::__construct(...)`.
6. **Dashboard/DashboardRefreshCommandTest** (~L30/53 + keys L35-47): `toBe(11)`→`toBe(15)`; expected keys = the 15 sorted metric keys (adds ad_candidates_health, category_audit_health, stock_divergence, supplier_freshness).
7. **Dashboard/GlobalSearchTest** (~L71-72): `toHaveKey(Class, msg)` → `toHaveKey(\App\Filament\Concerns\HasExportableTable::class)` (drop the 2nd arg).
8. **Dashboard/NotificationCentrePageTest** (~L83): `new \Livewire\Component()` → `new class extends \Livewire\Component {}`.
9. **Dashboard/QueuedCsvExportJobTest**: replace all `App\Domain\Dashboard\Services\CsvExportWriter` → `App\Filament\Exports\CsvExportWriter`.
10. **Dashboard/WidgetDataSourceTest** (~L236): `toBe(11)`→`toBe(15)`.
11. **Domain/Quotes/PushQuoteToBitrixDealJobTest** (~L269): `Mockery::mock(EntityDeduper::class)` (final class) → use `app(EntityDeduper::class)` (its BitrixClient dep is already mocked).
12. **DryRunModeTest** (~L28): `new class($supplierFeed)` → `new class($supplierFeed) extends SupplierClient`.
13. **ExampleTest** (~L17): `assertStatus(200)` → `assertRedirect('/admin')`.
14. **FailedJobAlertTest** (~L166): `->toBeNull()` → `->toBe(\App\Domain\Alerting\Notifiables\NullNotifiable::class)`.
15. **HorizonSupervisorTest** (~L11/14/33): supervisor count 7→8 + add the 8th supervisor; add the `agents` queue to the expected list (confirm names vs config/horizon.php).
16. **IntegrationLoggerTest**: replace `SHOW INDEX FROM integration_events` with the `\Schema::getIndexes('integration_events')` driver-agnostic pattern (as #3).
17. **Phase02DataModelTest**: guard the drop-indexed-column rollback test to MySQL — `if (DB::connection()->getDriverName() !== 'mysql') test()->markTestSkipped('sqlite cannot drop an indexed column on rollback');` (prod is MySQL — sqlite-only divergence).
18. **ShadowModeTest** (~L55): `new WooClient(app(IntegrationLogger::class), $mockInner)` → 3-arg with `app(...IntegrationCredentialResolver::class)` as arg 2.
19. **REAL FIX — OverridePopulator** (~L118-126): add `'margin_basis_points' => 0,` to the pin-only `ProductOverride::create([...])` attrs. This greens PopulateOverridesCommandTest's 5 failing cases + prevents the prod `--live` crash. Update the stale "margin_basis_points is nullable by design" comment.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 18 deterministic test-rot + OverridePopulator crash fix</name>
  <files>
    tests/Feature/CRM/BitrixClientExceptionClassificationTest.php, tests/Feature/CRM/BitrixClientShadowModeTest.php,
    tests/Feature/CRM/SyncDiffsProviderColumnTest.php, tests/Feature/Competitor/NewProductOpportunityApproveActionTest.php,
    tests/Feature/Console/BackfillMerchantFeedCommandTest.php, tests/Feature/Dashboard/DashboardRefreshCommandTest.php,
    tests/Feature/Dashboard/GlobalSearchTest.php, tests/Feature/Dashboard/NotificationCentrePageTest.php,
    tests/Feature/Dashboard/QueuedCsvExportJobTest.php, tests/Feature/Dashboard/WidgetDataSourceTest.php,
    tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php, tests/Feature/DryRunModeTest.php,
    tests/Feature/ExampleTest.php, tests/Feature/FailedJobAlertTest.php, tests/Feature/HorizonSupervisorTest.php,
    tests/Feature/IntegrationLoggerTest.php, tests/Feature/Phase02DataModelTest.php, tests/Feature/ShadowModeTest.php,
    app/Domain/Cutover/Services/OverridePopulator.php, tests/Feature/Cutover/PopulateOverridesCommandTest.php
  </files>
  <behavior>
    Apply the 18 test-side fixes + the OverridePopulator margin=0 fix per <interfaces>. Each failing test is the
    oracle. Where a fix depends on a current value (metric keys, supervisor names), confirm against the live
    source (SnapshotAggregator / config/horizon.php) before hard-coding. Do NOT touch the 8 isolation files
    (Quotes Unit observers/services, GoldenFixtureV2Trade, TradeRuleResolverPurity, HomeDashboardPage,
    ImportQuoteAction) or IntegrationEventPolicy / QuotePdfRouteSnapshotTest.
  </behavior>
  <action>
    Fix all 19 files. Run them together + pint on OverridePopulator.
  </action>
  <verify>
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pest tests/Feature/CRM/BitrixClientExceptionClassificationTest.php tests/Feature/CRM/BitrixClientShadowModeTest.php tests/Feature/CRM/SyncDiffsProviderColumnTest.php tests/Feature/Competitor/NewProductOpportunityApproveActionTest.php tests/Feature/Console/BackfillMerchantFeedCommandTest.php tests/Feature/Dashboard/DashboardRefreshCommandTest.php tests/Feature/Dashboard/GlobalSearchTest.php tests/Feature/Dashboard/NotificationCentrePageTest.php tests/Feature/Dashboard/QueuedCsvExportJobTest.php tests/Feature/Dashboard/WidgetDataSourceTest.php tests/Feature/Domain/Quotes/PushQuoteToBitrixDealJobTest.php tests/Feature/DryRunModeTest.php tests/Feature/ExampleTest.php tests/Feature/FailedJobAlertTest.php tests/Feature/HorizonSupervisorTest.php tests/Feature/IntegrationLoggerTest.php tests/Feature/Phase02DataModelTest.php tests/Feature/ShadowModeTest.php tests/Feature/Cutover/PopulateOverridesCommandTest.php 2>&1 | tail -12</automated>
    Expected: all GREEN (0 failed). Report the Tests: line.
    <automated>~/.config/herd/bin/php84/php.exe vendor/bin/pint app/Domain/Cutover/Services/OverridePopulator.php 2>&1 | tail -5</automated>
    Expected: PASS.
  </verify>
  <done>
    - 18 deterministic test-rot files green; OverridePopulator sets margin_basis_points=0 (PopulateOverrides green + prod crash fixed); isolation files + #11/#22 untouched; pint clean.
  </done>
</task>

</tasks>

<verification>
1. The 19 files → GREEN
2. `pint` OverridePopulator → PASS

Operator notes (for SUMMARY.md):
- Deploy: push main → `sudo -u stcav /home/stcav/ms.21stcav.com/deploy/deploy.sh` (NO migration).
- 18 pure test-rot fixes (no prod impact). ONE real fix: OverridePopulator now sets margin_basis_points=0 on
  pin-only overrides — `cutover:populate-overrides --live` would otherwise crash (NOT NULL) when creating any
  pin-only override.
- STILL OWED (deliberately out of scope): 8 test-ISOLATION-fragility files (separate triage doc) + 2 decision-pending
  real bugs — #11 IntegrationEventPolicy (registered in AppServiceProvider; delete-vs-keep decision) and #22 quote-PDF
  snapshot (needs a no-mutation control to confirm money-bug vs PDF non-determinism).
</verification>

<success_criteria>
- 18 deterministic test-rot files green + OverridePopulator margin_basis_points=0 (PopulateOverridesCommandTest green + prod --live crash fixed); the 8 isolation files + #11 + #22 untouched; pint clean; SUMMARY lists what's still owed.
</success_criteria>

<output>
Create `.planning/quick/260710-j2m-fix-18-deterministic-test-rot-from-full-/260710-j2m-SUMMARY.md` documenting the 18 test-rot fixes (grouped by cause), the OverridePopulator real fix, before/after, and the still-owed isolation + #11/#22 items.
</output>